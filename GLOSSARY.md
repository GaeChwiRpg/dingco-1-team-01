# GLOSSARY — 용어집

> `PRD.md` · `DECISIONS.md` · `CLAUDE.md` 를 읽다가 막히는 단어를 찾는 곳.
> **처음 합류했다면 「0. 5분 요약」만 읽고 나머지는 필요할 때 찾아보면 됩니다.**
> 각 용어는 「뜻」과 「이 프로젝트에서 어디에 나오나」를 함께 적었습니다.

## 0. 5분 요약 — 이 시스템이 하는 일

고객이 카테고리를 고르지 않고 문의를 남기면, 그게 어떤 종류인지(배송 문제인지, 환불 요청인지…) **AI 가 자동으로 분류**해줍니다.
문제는 **AI 가 틀릴 수 있다**는 것입니다. 그래서 두 겹으로 거릅니다.

```text
문의 접수 → 고객에게 즉시 접수 확인 (AI 를 기다리지 않음)
  ↓
AI 가 분류 + "얼마나 확신하는지" 점수를 함께 냄
  ↓        ← 똑같은 내용의 문의면 AI 를 다시 부르지 않고 이전 답을 재사용
[1차] 확신 점수가 기준보다 낮으면 → 상담원에게 넘김
  ↓
[2차] 점수가 높아 자동 확정된 것도 → 20건 중 1건은 몰래 뽑아 상담원이 다시 봄
```

**2차가 이 프로젝트의 핵심입니다.** 1차만 있으면 "AI 가 스스로 확신한다고 말한 것"을 그냥 믿는 셈이기 때문입니다. AI 가 **자신 있게 틀린 경우**는 1차로 절대 못 잡습니다.

> 📌 원래는 에러 분류 시스템이었고 **같은 에러 1000건을 하나로 묶어 판정**하는 게 설계의 중심이었습니다. CS 문의로 바꾸면서 그 묶기를 **포기**했습니다 — 문의는 사람마다 사정이 다르니 묶으면 개별 건이 사라지기 때문입니다. 대신 **판정은 문의마다 하되 AI 호출만 재사용**합니다. 자세한 경위는 D-031.

---

## 1. 이 프로젝트에서 만든 말

### 정규화 키 (normalized key)

문의 본문을 비교하기 쉽게 다듬은 **짧은 식별 문자열**입니다. 고객이 쓴 문장에는 매번 달라지는 값(주문번호, 날짜, 금액, 연락처)이 섞여 있어서 그대로 비교하면 같은 내용의 문의도 다 다르게 보입니다. 그래서 소문자로 바꾸고, 공백·문장부호를 정리하고, 숫자·주문번호 같은 건 가린 뒤 문자열로 만듭니다. **이 값이 같으면 AI 를 다시 부르지 않고 이전 답을 재사용**합니다.

⚠️ **fingerprint 와 다릅니다.** fingerprint 는 "이것들은 같은 한 건"이라는 뜻이었지만, 정규화 키는 **"AI 에게 물어볼 내용이 같다"** 는 뜻일 뿐입니다. 문의 자체는 여전히 각각 따로 처리됩니다.

> 나오는 곳: `inquiries.normalized_key`, PRD §5-4, D-031

### 2단 절감 경로

AI 호출을 줄이는 장치입니다. 순서대로 **① 캐시에서 찾아보고 → ② 없으면 DB 에서 같은 키의 이전 답을 찾아보고 → ③ 그래도 없으면 AI 를 부릅니다.**

②를 두는 이유는, 캐시만 쓰면 서버를 재시작할 때 절감이 0 으로 돌아가고 **「캐시 적중률」과 「AI 절감률」이 같은 값이 되어버리기** 때문입니다. 둘은 다른 것을 재는 지표라 구분돼야 합니다.

> 나오는 곳: PRD §5-4, D-014, D-031

### 과도 병합 / 과소 병합

정규화를 얼마나 세게 하느냐에 따라 생기는 **반대 방향의 두 실패**입니다.

- **과도 병합** — 너무 많이 지워서 **서로 다른 문의가 같은 키**가 됩니다. 엉뚱한 분류가 재사용됩니다
- **과소 병합** — 너무 조금 지워서 **같은 내용인데 다른 키**가 됩니다. AI 를 쓸데없이 또 부릅니다

**과도 병합이 더 위험합니다.** 과소 병합은 돈만 더 쓰지만, 과도 병합은 **틀린 답이 조용히 퍼집니다.**

> 나오는 곳: PRD §5-4 · §8 「알고 있는 위험」, D-031

### 임계값 (threshold)

**AI 를 믿어줄 최소 점수**입니다. AI 가 "이거 환불 요청이고, 확신도 0.85" 라고 답했을 때 임계값이 0.8 이면 통과(자동 확정), 0.9 면 상담원에게 넘깁니다.

> 나오는 곳: `application.yml` 의 `classification.threshold`. 카테고리마다 값을 다르게 두는 안(D-006)은 폐기됐습니다 — D-031

### 격리 (isolation)

AI 결과를 그대로 쓰지 않고 **검토 큐에 넣어 사람을 기다리게** 하는 것입니다. "믿을 수 없으니 일단 보류"에 가깝습니다.

### 감사 샘플링 (audit sampling)

자동 확정된 건 중 **일부(기본 5%)를 무작위로 뽑아 상담원이 다시 보게** 하는 것입니다. 학교 시험으로 치면 전수 채점 대신 표본만 다시 채점해 채점 자체가 맞는지 확인하는 것과 같습니다.

> 나오는 곳: `inquiry_review_queue.reason = AUDIT_SAMPLE`, D-005

### blind (블라인드)

상담원에게 **"이건 감사 표본입니다"라고 알려주지 않는 것**입니다. 알려주면 평소보다 신중하게 봐서, 측정된 정확도가 실제 운영보다 좋게 나옵니다.

> 나오는 곳: D-010, D-019, 측정 10

### 역산 (逆算)

응답에 담긴 값들을 조합해 **가려둔 정보를 거꾸로 계산해내는 것**입니다. 예를 들어 감사 표본은 정의상 "확신 점수 ≥ 임계값"인 건들이므로, 점수와 임계값을 둘 다 보여주면 뺄셈 한 번으로 감사 표본을 전부 골라낼 수 있습니다. 그래서 `reason` 만 가려서는 소용이 없습니다.

- **결정적 역산** — 필드 조합으로 100% 확실하게 알아내는 것. **0건이어야 합니다**
- **확률적 추론** — "이건 딱 봐도 명확한 문의인데 왜 내 큐에 있지" 같은 짐작. 완전히 막을 수 없어 목록만 남깁니다

> 나오는 곳: D-019, 측정 10

### 오분류 (misclassification)

AI 가 제안한 카테고리(`category`)와 사람이 최종 확정한 카테고리(`final_category`)가 **다른 경우**입니다. 이 프로젝트의 결론이 되는 수치입니다.

### 정답 레이블 (ground truth)

"이 문의의 진짜 정답은 이것이다"라고 **사람이 미리 정해둔 답**입니다. AI 채점의 기준이 됩니다. 정답이 없으면 AI 가 맞았는지 틀렸는지 말할 수 없습니다.

### 조치 기준 분류

카테고리를 고를 때 **문의의 원인이 아니라 고객이 요구하는 조치**를 보는 규칙입니다. "배송이 늦어서 환불해주세요"는 원인이 배송이어도 요구가 환불이므로 `RETURN_REFUND` 입니다.

이 규칙이 없으면 상담원마다 다르게 넣게 되고, 그러면 오분류율을 재도 **그게 AI 탓인지 사람 탓인지 구분되지 않습니다.**

> 나오는 곳: PRD §7 경계표, 측정 12

### 앵커링 편향 (anchoring bias)

먼저 본 숫자나 답에 **판단이 끌려가는 현상**입니다. 상담원에게 AI 제안 카테고리를 보여주면 "AI 가 이렇게 봤네" 하고 동의 쪽으로 기울게 됩니다. 그래서 이렇게 측정한 오분류율은 실제보다 **작게** 나옵니다.

### 하한값 (lower bound)

"**최소한 이만큼은 된다**"는 값입니다. 앵커링 편향 때문에 측정된 오분류율은 실제보다 작으므로, "오분류율 3%"는 "정확히 3%"가 아니라 "적어도 3% 이상"으로 읽어야 합니다.

### 편향원 (bias source)

측정값을 실제와 어긋나게 만드는 **원인**입니다. 이 프로젝트에는 두 개가 있습니다 — 앵커링(위)과 검토자 불일치(사람마다 판단이 다른 것). 둘을 분리해야 "AI 가 틀린 것"과 "사람이 다르게 본 것"을 구분할 수 있습니다.

> 나오는 곳: 측정 8, 측정 12, D-027

### fail-safe (안전한 쪽으로 실패하기)

무언가 잘못됐을 때 **덜 위험한 쪽으로 넘어지게** 만드는 설계입니다. 이 시스템에서는 항상 **격리 쪽**입니다 — AI 응답이 깨졌으면 대충 통과시키지 말고 사람에게 넘깁니다.

### 대조군 (control group)

비교 기준이 되는 쪽입니다. "AI 오분류율 15%" 라는 수치가 나왔을 때, 그중 얼마가 **사람끼리도 의견이 갈리는 몫**인지 알려면 같은 문의를 상담원 2명이 따로 분류해본 결과와 나란히 놓아야 합니다. 이때 사람끼리 비교한 쪽이 대조군입니다.

> 나오는 곳: 측정 12

---

## 2. 데이터베이스

### 인덱스 (index)

책 뒤의 색인과 같습니다. 없으면 처음부터 끝까지 다 읽어야 하고, 있으면 바로 찾아갑니다. **대신 데이터를 쓸 때마다 색인도 같이 고쳐야 해서 쓰기가 느려집니다.** 이 프로젝트에서 인덱스를 함부로 안 늘리는 이유입니다.

### B-tree

대부분의 DB 인덱스가 쓰는 자료구조입니다. **정렬된 순서로 저장**되기 때문에 "범위로 찾기"와 "정렬"을 동시에 잘합니다. 다만 한 인덱스로 *범위 검색 + 다른 컬럼 정렬*을 동시에 처리하지는 못합니다 — 이건 버그가 아니라 구조상의 한계입니다.

> 나오는 곳: `CLAUDE.md` 인덱스 표의 "커버 불가" 행

### 복합 인덱스 (composite index)

컬럼 여러 개를 묶은 인덱스입니다. `(status, created_at)` 이면 "status 로 먼저 정렬하고, 같은 status 안에서 created_at 순으로 정렬"된 상태입니다. **순서가 중요합니다** — 앞 컬럼부터 써야 인덱스를 탈 수 있습니다.

### EXPLAIN

쿼리를 실제로 돌리기 전에 **DB 가 어떻게 찾아갈 계획인지 물어보는 명령**입니다. 인덱스를 쓰는지 안 쓰는지가 여기 나옵니다.

접근 방식은 좋은 순서대로 이렇습니다.

| 표시 | 뜻 |
| --- | --- |
| `const` | 딱 1행. 가장 빠름 |
| `eq_ref` | 조인할 때 상대 테이블에서 1행씩 정확히 찾음 |
| `ref` | 인덱스로 여러 행을 찾음. 보통 여기까지면 합격 |
| `range` | 인덱스로 범위를 훑음 (`>`, `BETWEEN`) |
| `ALL` | **인덱스 없이 전체 훑기.** 피해야 함 |

### filesort

인덱스만으로 정렬이 안 돼서 **DB 가 결과를 따로 모아 정렬하는 것**입니다. 이름에 file 이 붙었지만 보통 메모리에서 합니다. 느리므로 피하려 합니다.

### 역정규화 (denormalization)

원래는 한 곳에만 있어야 할 값을 **조회를 빠르게 하려고 다른 테이블에 복사해두는 것**입니다. 조인을 안 해도 되니 빨라지지만, **원본이 바뀌면 사본도 같이 바꿔줘야** 해서 어긋날 위험이 생깁니다.

> 나오는 곳: `inquiries.current_category` / `current_confidence`, D-011

### append-only

**추가만 하고 수정·삭제는 하지 않는** 테이블입니다. `inquiry_classification_result` 가 그렇습니다 — 분류를 다시 해도 기존 행을 고치지 않고 새 행을 쌓습니다. 고쳐버리면 **AI 가 처음에 뭐라고 했는지가 사라집니다.**

### upsert

**있으면 수정(update), 없으면 삽입(insert)** 하는 동작입니다. 두 단어를 합친 말입니다.

> 나오는 곳: D-029, 트랜잭션 ①

### UNIQUE 제약

"이 컬럼에 같은 값이 두 번 들어오면 안 된다"고 **DB 에게 시키는 규칙**입니다. 코드로 "먼저 조회해보고 없으면 넣기"만 하면 동시에 두 요청이 들어올 때 둘 다 "없네" 하고 넣어버립니다. DB 제약은 그걸 확실히 막아줍니다.

### seed (시드 데이터)

테이블을 만들 때 **미리 넣어두는 기본 데이터**입니다. 여기서는 카테고리 10종의 임계값 기본값이 해당합니다.

### Flyway

**DB 스키마 변경을 파일로 관리**하는 도구입니다. `V1__init_schema.sql`, `V2__candidate_index.sql` 처럼 번호가 붙은 SQL 파일을 순서대로 적용합니다. 누가 언제 무엇을 바꿨는지가 기록으로 남습니다.

### ddl-auto

JPA 가 **엔티티 코드를 보고 테이블을 자동으로 만들어주는 기능**입니다. 편하지만 인덱스를 붙였다 뗐다 하는 실험이 불가능해서, 이 프로젝트는 `validate`(검증만) 로 두고 실제 스키마는 Flyway 가 관리합니다.

> 나오는 곳: D-023

---

## 3. 동시성 — 여러 요청이 한꺼번에 올 때

### race condition (경합 상태)

**두 요청이 동시에 들어와서 순서에 따라 결과가 달라지는 상황**입니다. 예: 상담원 두 명이 같은 검토 큐 항목을 동시에 열어 확정 버튼을 누르면, 둘 다 "아직 PENDING 이네" 를 보고 각자 확정하려 합니다.

### check-then-act (확인하고 나서 행동하기)

"확인 → 행동" 사이에 **다른 요청이 끼어들 수 있는 틈**이 생기는 패턴입니다. "PENDING 인지 확인 → 확정 처리" 사이에 다른 상담원이 먼저 확정해버릴 수 있습니다. 그래서 상태 확인만으로는 부족합니다.

### 낙관적 락 (optimistic lock)

"**충돌은 드물 것이다**" 라고 보고, 일단 진행한 뒤 저장할 때 확인하는 방식입니다. 행마다 `version` 숫자를 두고, 읽을 때의 version 과 저장할 때의 version 이 다르면 "누가 먼저 바꿨구나" 하고 실패시킵니다. 미리 막지 않으니 빠릅니다.

> 나오는 곳: `inquiry_review_queue.version`, D-021. **도메인 전환 이후 이 프로젝트에 남은 유일한 동시성 장치입니다** (D-031)

### 비관적 락 (pessimistic lock)

"**충돌이 잦을 것이다**" 라고 보고, 아예 **다른 요청을 기다리게 막아두는** 방식입니다. 확실하지만 느립니다. 이 프로젝트에서 접수 경로에 이걸 걸면 전체가 한 줄로 서게 되어 안 씁니다.

### 원자적 UPDATE (atomic update)

`SET count = count + 1` 처럼 **읽기와 쓰기를 DB 가 한 번에 처리**하게 하는 것입니다. 코드에서 "읽어서 +1 하고 저장"하면 그 사이에 끼어들 틈이 생기지만, 이렇게 하면 DB 가 알아서 막아줍니다. 락이 필요 없습니다.

> ⚠️ **이 프로젝트에서는 더 이상 쓰지 않습니다.** 쓰던 자리(`occurrence_count` 증가)가 도메인 전환으로 사라졌기 때문입니다 (D-007 → D-031). 용어는 남겨둡니다 — 무엇을 잃었는지 알아야 하니까요.

### rollback-only 마킹

트랜잭션 도중 DB 제약 위반 같은 오류가 나면 **Hibernate 가 "이 트랜잭션은 이제 무조건 취소한다"고 표시**해버립니다. 이 상태에서는 같은 트랜잭션 안에서 뭘 더 하려 해도 실패합니다. 그래서 재시도는 반드시 **트랜잭션을 완전히 끝낸 뒤 밖에서** 해야 합니다.

> 나오는 곳: D-016 (이걸 몰라서 D-007 의 초안이 틀렸습니다). D-016 자체는 D-031 으로 폐기됐지만 **이 지식은 유효합니다** — 어떤 제약 위반이든 똑같이 적용됩니다

---

## 4. Spring

### `@Transactional`

이 메서드 안의 DB 작업들을 **전부 성공하거나 전부 취소되게** 묶습니다. "분류 결과는 저장됐는데 검토 큐 삽입은 실패" 같은 어중간한 상태를 막습니다.

### `@Async`

메서드를 **별도 스레드에서 돌려** 호출한 쪽이 기다리지 않게 합니다. AI 호출이 3초 걸려도 문의 접수 API 는 바로 응답할 수 있습니다.

### `@Retryable` / `@Recover`

실패하면 **자동으로 다시 시도**하고(`@Retryable`), 정해진 횟수를 다 쓰면 **마지막 처리**로 넘어갑니다(`@Recover`). 여기서는 3회 재시도 후 검토 큐에 넣습니다.

### `@Cacheable` / `@CacheEvict`

결과를 **저장해뒀다 재사용**하고(`@Cacheable`), 데이터가 바뀌면 **버립니다**(`@CacheEvict`).

### AFTER_COMMIT

**트랜잭션이 실제로 커밋된 뒤에** 실행하라는 뜻입니다. 캐시 갱신을 커밋 전에 하면, 트랜잭션이 취소됐는데 캐시에는 남아버립니다.

### 프록시 (proxy)

Spring 이 `@Transactional` 같은 기능을 붙이려고 **원래 객체를 감싸 만든 대리 객체**입니다. 중요한 함정이 있습니다 — **같은 클래스 안에서 메서드를 직접 호출하면 이 대리 객체를 거치지 않아 기능이 안 먹습니다.** 그래서 재시도 로직을 별도 빈으로 분리합니다.

### N+1 문제

목록 1번 조회(1) 후, 각 항목의 연관 데이터를 꺼내려고 **건마다 쿼리가 또 나가는(N)** 현상입니다. 100건이면 101번 쿼리가 나갑니다. `@EntityGraph` 로 한 번에 가져와 해결합니다.

### LAZY 로딩

연관된 데이터를 **미리 안 가져오고, 실제로 쓸 때 가져오는** 방식입니다. 기본값으로 씁니다. 대신 N+1 이 생기기 쉬워 목록 조회에서는 주의해야 합니다.

### Actuator

Spring 이 제공하는 **상태 확인·지표 노출 기능**입니다. `/actuator/health` 로 앱이 살아있는지 보고, 커스텀 지표도 여기로 내보냅니다.

### gauge (게이지)

**지금 이 순간의 값**을 나타내는 지표입니다. 온도계처럼 오르내립니다. (반대로 counter 는 계속 쌓이기만 합니다.)

### 스크랩 (scrape)

모니터링 도구가 **주기적으로 지표를 긁어가는 것**입니다. 매번 긁어갈 때마다 전체 건수를 세는 쿼리가 돌면 부담이 되므로 캐시를 씌웁니다.

### 정적 팩토리 메서드 (static factory method)

`new` 대신 `Inquiry.receive(...)` 같은 **이름 있는 생성 메서드**를 쓰는 방식입니다. 생성자와 달리 이름을 붙일 수 있고, **잘못된 조합으로는 아예 만들 수 없게** 강제할 수 있습니다.

> 나오는 곳: D-025

---

## 5. 성능 측정

### QPS (Queries Per Second)

**초당 요청 수**입니다. 이 시스템에서 가장 QPS 가 높은 곳은 문의 접수(`POST /api/inquiries`) 경로입니다.

### p95 (95 퍼센타일)

요청 100건을 응답 시간 순으로 줄 세웠을 때 **95번째 값**입니다. 평균은 느린 몇 건에 가려지기 쉬워서, 보통 p95·p99 로 봅니다. "p95 < 100ms" 는 "100건 중 95건은 100ms 안에 끝난다"는 뜻입니다.

### throughput (처리량)

**단위 시간에 처리한 총량**입니다.

### 캐시 hit rate (적중률)

캐시를 찾아봤을 때 **있었던 비율**입니다. `hit / (hit + miss)`.

> ⚠️ 이 프로젝트에서 hit rate 와 **AI 절감률은 다른 지표**입니다. 캐시에 없어도 DB 에 그룹이 있으면 AI 는 안 부릅니다. AI 절감을 만드는 건 캐시가 아니라 **그룹핑**입니다 (D-014).

### TTL (Time To Live)

캐시에 넣은 값이 **살아있는 시간**입니다. 지나면 자동으로 사라집니다.

### hey / wrk

**부하를 주는 도구**입니다. 요청을 대량으로 쏴서 응답 시간을 잽니다. 이 프로젝트는 AI 가 추정한 숫자를 근거로 쓰지 않고 이 도구의 실측값만 씁니다.

---

## 6. 프로젝트 운영 용어

### Phase 2 / Phase 3

**Phase 2** 는 이번 주(Week 9) 안에 만드는 범위, **Phase 3** 는 그 뒤로 미룬 것입니다. 미룬 항목에는 D~J 알파벳이 붙어 있습니다.

### P1 / P2 / P3

코딩 작업을 3명이 나눈 **작업 패키지**입니다. P1 접수·절감 경로(이용택) / P2 분류·검증(김준현) / P3 검토·관측(김은빈).

### 계약 A / B / C

세 사람이 **동시에 작업하려고 미리 고정해둔 경계**입니다. A 는 이벤트 형태, B 는 큐에 넣을 때 필수 컬럼, C 는 캐시 값 구조입니다.

### 트랜잭션 ① ② ③

이 프로젝트에서 `@Transactional` 을 붙이는 **딱 세 곳**입니다. ① 문의 접수 ② 분류 결과 저장 ③ 검토 확정.

⚠️ **①과 ②는 반드시 따로입니다.** ②가 실패해도 ①은 취소되면 안 됩니다 — 고객이 이미 받은 접수 확인이 거짓말이 되니까요 (D-031).

### hallucination (환각)

AI 가 **사실이 아닌 것을 자신 있게 말하는 것**입니다. 없는 함수를 있다고 하거나, 확인하지 않은 수치를 단정하는 식입니다. 잡을 때마다 `evidence/failure-cases.md` 에 기록합니다.

### 페·목·형·제

프롬프트를 쓸 때 반드시 넣는 4요소입니다 — **페**르소나 / **목**표 / **형**식 / **제**약.

### stub (스텁)

**나중에 제대로 만들 것을 임시로 흉내만 내둔 것**입니다. Phase 2 의 인증이 그렇습니다 — 진짜 JWT 대신 `X-User-Role` 헤더를 그냥 믿습니다.

### 멱등 (idempotent)

**같은 요청을 여러 번 보내도 결과가 한 번 보낸 것과 같은** 성질입니다.

### circuit breaker (서킷 브레이커)

외부 시스템이 계속 실패하면 **잠시 호출 자체를 멈추는** 장치입니다. 두꺼비집과 같은 원리입니다. Phase 3 항목입니다.

### 백오프 (backoff)

재시도할 때 **간격을 점점 늘리는** 방식입니다. 1초 → 2초 → 4초. 실패한 상대를 계속 두드리지 않기 위해서입니다.

---

## 7. 더 읽을 것

| 궁금한 것 | 볼 곳 |
| --- | --- |
| 이 시스템이 무엇을 하는가 | `PRD.md` §1, §3 |
| 왜 이렇게 만들었는가 | `DECISIONS.md` 「한눈에 보기」 |
| 코드 짤 때 지켜야 할 규칙 | `CLAUDE.md` |
| API 요청·응답 형태 | `API-CONTRACT.md` |
| AI 가 틀렸던 사례 | `evidence/failure-cases.md` |
| 누가 무엇을 맡았는가 | `LIFECYCLE-COVERAGE.md` |

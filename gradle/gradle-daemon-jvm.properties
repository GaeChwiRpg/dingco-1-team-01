#
# Gradle 데몬이 어느 JDK 위에서 뜰지를 고정한다.
#
# Gradle 8.x 는 JDK 25 위에서 데몬을 띄우지 못한다. 팀원마다 기본 JAVA_HOME 이 다르므로
# (예: 25 가 기본인 머신) 각자 환경변수를 맞추게 하는 대신 이 파일을 커밋해
# 설치된 JDK 21 을 auto-detect 로 찾아 쓰게 한다.
#
# 컴파일 타깃(java.toolchain)과는 별개 설정이다 — 이쪽은 "Gradle 자신이 도는 JVM".
#
toolchainVersion=21

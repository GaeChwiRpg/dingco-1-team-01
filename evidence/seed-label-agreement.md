# 정답 레이블 2인 일치율 — 측정 12 예비값

> D-046 ⓓ 의 산출물이다. 작성 절차는 `src/test/resources/seed/README.md` 「다 채운 뒤」 참조.
> **2026-08-10 측정 완료.** 아래 숫자는 두 사람이 실제로 붙인 것이다.

## 측정 이유

측정 8ⓐ-1 은 "AI 답과 정답이 다른 비율"이다. 그런데 **정답 자체가 사람마다 갈리면** 그 비율에 "AI 가 틀린 것"과 "사람이 다르게 본 것"이 섞여서 읽을 수 없다 (D-027 기준 2 — 정답 단일성).

이 일치율이 그 몫을 떼어낸다. 그리고 Day 1 에 나오므로, **낮으면 Day 5 이전에 `PRD.md` §7 경계표를 고칠 시간이 생긴다.**

> 개념 설명(왜 "독립"이어야 하는지, 갈렸을 때 정답을 왜 임의로 정하면 안 되는지)은 `GLOSSARY.md` 「2인 독립 레이블」. 절차는 `PRD.md` §8 0단계 (4) 와 D-046 ⓓ.

## 측정 결과

```text
측정 일시:        2026-08-10
1차 레이블 작성자: 김준현  (inquiries-50-label-1st-junhyun.csv)
2차 레이블 작성자: 이용택  (inquiries-50-label-2nd-yongtaek.csv, 커밋 b7e7543)
전체 건수:        50
일치 건수:        49
일치율:           49/50 = 98.0%
```

**재현 방법** — 두 답안지를 `ref` 로 맞춰 마지막 칸만 비교한다.

⚠️ **세는 것보다 먼저 「셀 수 있는 상태인지」를 확인한다.** `join` 은 한쪽에만 있는 `ref` 를
**조용히 버리고**, 중복된 `ref` 는 **곱해서 늘린다.** 그러면 분모가 50 이 아닌데도 숫자는 멀쩡히
나온다 — 일치율이 실제보다 높게도 낮게도 될 수 있고, **어느 쪽인지 알 방법이 없다.**
그래서 아래 1단계가 실패하면 거기서 멈춘다.

```bash
cd src/test/resources/seed

# 1) 셀 수 있는 상태인지 — 각 파일이 정확히 50행이고 ref 1~50 이 한 번씩만 있는가
for f in inquiries-50-label-1st-junhyun.csv inquiries-50-label-2nd-yongtaek.csv; do
  n=$(tail -n +2 "$f" | wc -l | tr -d ' ')
  u=$(tail -n +2 "$f" | cut -d, -f1 | sort -n | uniq | wc -l | tr -d ' ')
  s=$(tail -n +2 "$f" | cut -d, -f1 | sort -n | tr '\n' ' ')
  [ "$n" = 50 ] && [ "$u" = 50 ] && [ "$s" = "$(seq 1 50 | tr '\n' ' ')" ] \
    || { echo "❌ $f — 행 $n / 고유 ref $u. 여기서 멈춘다"; exit 1; }
  echo "✅ $f — 50행, ref 1~50 각 1회"
done

# 2) 그다음에 센다
tail -n +2 inquiries-50-label-1st-junhyun.csv  | awk -F, '{print $1"\t"$NF}' | sort -n > /tmp/l1.tsv
tail -n +2 inquiries-50-label-2nd-yongtaek.csv | awk -F, '{print $1"\t"$NF}' | sort -n > /tmp/l2.tsv
joined=$(join -t$'\t' /tmp/l1.tsv /tmp/l2.tsv | wc -l | tr -d ' ')
[ "$joined" = 50 ] || { echo "❌ 맞춰진 행이 $joined 건 — 50 이 아니다"; exit 1; }
join -t$'\t' /tmp/l1.tsv /tmp/l2.tsv | awk -F'\t' '$2==$3' | wc -l
```

### 갈린 건 목록

`id` 는 `inquiries-50.csv` 의 원본 id, `ref` 는 blind 서식의 번호다 (`inquiries-50-blind-map.csv` 로 맞춘다).

| id (ref) | 1차 답 | 2차 답 | §7 표로 판정됨? | 조정 결과 | §7 표를 고쳤나 |
| --- | --- | --- | --- | --- | --- |
| 24 (ref 45) | `ACCOUNT` | `SERVICE_USAGE` | **예** — §7 표의 `ACCOUNT` 항목에 「탈퇴」가 명시돼 있다 | **`ACCOUNT`** ✅ 2026-08-10 확정 | 아니오 (표로 판정되므로 불필요) |

**본문**: *"회원 탈퇴하고 싶습니다. 어디서 하는지 알려주세요."*

갈린 이유는 **한 문장이 두 항목에 동시에 걸리기 때문**이다 — 「탈퇴」는 `ACCOUNT`, 「어디서 하는지」는 `SERVICE_USAGE`(앱·웹 사용법)로 읽힌다. §7 표는 `ACCOUNT` 쪽에 「탈퇴」를 적어두었으므로 표로는 판정되지만, **두 항목이 겹칠 때 무엇이 이기는지는 표에 없다.**

**확정 근거 (2026-08-10)** — 한때 `SERVICE_USAGE` 로 뒤집는 안을 검토했으나, **50건 전체에 파급을 대보고 접었다.**

같은 구조의 문장이 이미 여럿 있고, 그 건들은 **두 사람이 독립적으로 합의**한 상태였다.

| ref | 본문 | 합의된 답 | 「방법 묻는 건 `SERVICE_USAGE`」 를 넣으면 |
| --- | --- | --- | --- |
| 26 | 반품 신청하려고 합니다. **어떻게 하면 되나요?** | `RETURN_REFUND` | → `SERVICE_USAGE` ⚠️ |
| 27 | 돈 돌려받고 싶은데 **어떻게 하나요** | `RETURN_REFUND` | → `SERVICE_USAGE` ⚠️ |
| 7 | 교환하고 싶습니다. 43000원은 **어떻게 되나요?** | `RETURN_REFUND` | → `SERVICE_USAGE` ⚠️ |
| 37 | 결제 수단 변경이 **되나요?** | `PAYMENT` | → `SERVICE_USAGE` ⚠️ |
| **45** | 회원 탈퇴하고 싶습니다. **어디서 하는지** 알려주세요 | (갈림) | → `SERVICE_USAGE` |

**ref 45 와 ref 26 은 문장 구조가 같다** — 「조치 요청」 + 「방법 질문」. 45 를 `SERVICE_USAGE` 로
하면 26·27 도 그래야 일관되는데, 그건 **명백히 틀린 분류**이고 이미 합의된 49건을 무너뜨린다.

헌법의 판단 기준도 같은 답을 가리킨다 — **"원인이 아니라 고객이 요구하는 조치"**. ref 45 가
요구하는 조치는 **탈퇴**이고, 「어디서 하는지」는 그 조치를 위한 부가 질문이다.

> **그래서 §7 표를 고치지 않았다.** 표·헌법 기준·49건 합의가 전부 `ACCOUNT` 를 가리킨다.
> 한때 적어둔 권고(*"`ACCOUNT` 대상에 대한 사용법 문의도 `ACCOUNT`"* 한 줄 추가)는 **표를 바꾸는
> 게 아니라 이미 그런 뜻임을 분명히 하는 것**이라, 넣어도 판정은 안 바뀐다. 넣을지는 별도 PR 사안.

> **`§7 표로 판정됨?` 이 「아니오」인 건은 `§7 표를 고쳤나` 가 반드시 「예」여야 한다.**
> 둘 다 아니오면 정답을 임의로 정했다는 뜻이고, 그러면 측정 8ⓐ-1 의 "틀림"이 **AI 가 틀린 것인지 정답이 자의적인 것인지** 구분되지 않는다. 정답을 억지로 맞추지 않는다.

## ⚠️ 이 값을 읽을 때 알아야 할 것 — 조건이 완전히 깨끗하지 않다

**세 가지를 숨기지 않고 적는다.** 숨기면 나중에 "일치율이 왜 이렇게 높지"를 설명할 수 없게 되고, 그러면 이 숫자로 측정 8ⓐ-1 에서 검토자 불일치 몫을 떼어낼 수 없다.

### ⓐ 1차는 두 번 매겼다 — 90.0% → 98.0%

| | 조건 | 일치율 |
| --- | --- | --- |
| **첫 판** | 갈린 자리를 모르는 상태 | **45/50 = 90.0%** |
| **재작성** (채택) | **갈린 5건의 `ref` 번호를 아는 상태.** 2차 답은 보지 않음 | **49/50 = 98.0%** |

첫 판 직후 대조를 돌렸고, 그 과정에서 **어느 건이 갈렸는지가 1차 작성자에게 알려졌다.** 답 자체는 보지 않고 다시 매겼다(작성자 확인). 다만 *"이 5건을 다시 보라"* 는 정보는 이미 전달된 뒤였다.

바뀐 4건은 다음과 같다. **첫 판은 커밋되지 않았으므로 이 기록이 유일한 근거다.**

| ref | 첫 판 | 재작성 | 2차 |
| --- | --- | --- | --- |
| 14 | `PROMOTION` | `SERVICE_USAGE` | `SERVICE_USAGE` |
| 16 | `PAYMENT` | `SERVICE_USAGE` | `SERVICE_USAGE` |
| 28 | `PROMOTION` | `SERVICE_USAGE` | `SERVICE_USAGE` |
| 41 | `SERVICE_USAGE` | `COMPLAINT` | `COMPLAINT` |
| 45 | `ACCOUNT` | `ACCOUNT` (유지) | `SERVICE_USAGE` |

> **보수적으로 읽는다: 90.0% 를 하한, 98.0% 를 상한으로 본다.** 측정 12 가 재려는 것은 *"두 사람이 서로 안 보고 매기면 얼마나 일치하나"* 이고, 그 조건에 더 가까운 것은 **첫 판(90.0%)** 이다.
>
> ⚠️ **단 아래 ⓑ 의 노출 3건까지 빼면 89.4% 로 기준을 밑돈다.** 네 값을 한 표로 정리한 것은 「어느 값을 쓰나」 절에 있다 — **"어느 쪽이든 90% 를 넘는다"고 읽지 않는다.**

**첫 판에서 갈린 5건 중 4건이 `SERVICE_USAGE` 와 얽혀 있었다** (적립금 사용처 · 쿠폰 미적용 · 쿠폰 미표시 · 앱 튕김). 우연으로 보기 어렵고, **§7 표에서 `SERVICE_USAGE` 와 다른 항목의 경계가 얇다**는 신호다. 재작성으로 수치는 올랐지만 **경계가 얇다는 사실 자체는 그대로 남아 있다.**

### ⓑ `ref 1·2·3` 의 2차 답이 1차 작성 전에 노출됐다

AI 보조 도구(Claude Code)가 작업 상태를 확인하는 과정에서 2차 답안지의 앞 3줄을 화면에 띄웠다. 나머지 47건은 노출되지 않았다.

- 세 건 모두 두 사람의 답이 **일치**했다 (`SERVICE_USAGE` · `ACCOUNT` · `ORDER_CHANGE`)
- **제외하고 계산하면 46/47 = 97.9%** — 노출이 결과를 크게 움직이지는 않았다
- 다만 첫 판 기준으로는 42/47 = **89.4%** 로 90% 를 밑돈다

## 어느 값을 쓰나 — 네 값이 나오는 이유

보정이 둘(ⓐ 첫 판 / ⓑ 노출 제외)이라 조합이 넷이다. **골라 쓰지 말고 넷을 다 적는다.**

| | 전체 50건 | 노출 3건 제외 (47건) |
| --- | --- | --- |
| **첫 판** (조건에 더 가까움) | 45/50 = **90.0%** | 42/47 = **89.4%** ← 가장 보수적 |
| **재작성** (채택) | 49/50 = **98.0%** | 46/47 = **97.9%** |

**⚠️ 가장 보수적인 값 89.4% 는 판정 기준 90% 를 0.6%p 밑돈다.** 이걸 숨기지 않는다.

**다만 89.4% 가 「노출 때문에 부풀었다」는 증거는 아니다.** 뺀 3건이 **전부 일치**한 건이라,
빼면 분자에서 3, 분모에서 3 이 함께 빠진다 — 원래 비율이 90% 인 표본에서 100% 일치인 3건을
덜어내면 **산술적으로 반드시 내려간다.** 즉 이 하락은 노출의 영향이 아니라 **표본이 47건으로
작아서 생긴 흔들림**이다. 1건이 0.6%p 를 좌우하는 크기다.

**그래서 이렇게 읽는다.**

- **판정에 쓰는 값**: 90.0% (첫 판, 전체 50건) — 노출된 3건도 **두 사람이 독립적으로 같은 답을
  낸 건**이라 표본에서 뺄 이유가 없다. 뺀 계산은 **민감도 확인용**이다
- **89.4% 가 말해주는 것**: 이 표본에서는 **1건이 판정을 뒤집을 수 있다.** 50건은 90% 선을
  가르기엔 작은 표본이고, 그 사실이 결론보다 중요하다

### ⓒ 1차 작성자는 AI 가 붙인 `expected_category` 를 이미 본 상태다

`inquiries-50.csv` 의 `expected_category` 는 AI 가 붙인 값이고(`seed-draft-provenance.md`), 1차 작성자는 그 파일을 만든 사람이라 **앵커링이 남는다.** 2차 작성자는 blind 서식만 받아서 이 영향이 없다. `README.md` 가 미리 적어둔 한계 그대로다.

## 판정

**98.0% (판정에 쓰는 하한 90.0%) → 「90% 이상」에 해당하므로 그대로 진행한다.** §7 경계표를 조이고 **다시 레이블하는 절차**는 발동하지 않는다.

**⚠️ 그러나 「경계표를 안 고쳐도 된다」로 읽지 않는다.** 세 가지가 같은 곳을 가리키고 있다.

1. 가장 보수적인 값 **89.4% 는 기준을 밑돈다** — 1건이 판정을 뒤집는 표본 크기다
2. 첫 판에서 갈린 5건 중 **4건이 `SERVICE_USAGE` 와 얽혀 있었다**
3. 아직 안 갈린 1건(`id 24`)도 **`ACCOUNT` × `SERVICE_USAGE` 겹침**이다

**절차가 발동하지 않는 것과 표가 멀쩡한 것은 다른 말이다.** 재레이블은 안 하되, 위 「남은 일」 2번(§7 표에 겹침 규칙 추가)은 **그대로 남는다.**

| 일치율 | 무엇을 하나 |
| --- | --- |
| **90% 이상** | **그대로 진행** ← 여기 |
| 90% 미만 | **§7 경계표를 조인 뒤 다시 레이블한다** |
| 조여도 안 오름 | D-046 재평가 조항 발동 — 카테고리 10종 → 5종 축소를 앞당긴다. 사람에게도 안 갈라지는 카테고리는 AI 에게 물어보는 것 자체가 성립하지 않는다 |

## 정답 50건이 완성됐다 (2026-08-10)

`id 24` 확정과 함께 **`inquiries-50.csv` 의 `expected_category` 를 사람 확정답으로 교체**했다.
이제 그 칸은 **AI 가 붙인 값이 아니라 사람이 붙인 값**이다 (「꼭 지킬 것」 1번 충족).

두 단계를 거쳤다. **① 2인 레이블 결과로 교체 → ② 그중 5건을 §7 표로 재판정.**

### ① 2인 레이블 결과로 교체 — 7건

| id | AI 초안 | 2인 일치 답 |
| --- | --- | --- |
| 26 | `ORDER_CHANGE` | `RETURN_REFUND` |
| 27 · 28 | `ORDER_CHANGE` | `DELIVERY` |
| 31 · 33 · 34 | `PROMOTION` | `SERVICE_USAGE` |
| 32 | `PROMOTION` | `PAYMENT` |

### ② 그 7건을 §7 표에 다시 대봤다 — 5건이 표와 어긋나 있었다

**분포가 「10종 × 5건」에서 크게 벗어난 것이 계기였다.** 맞추려고 손댄 것이 아니라,
**왜 이렇게까지 벗어났는지**를 §7 표로 되짚는 과정에서 어긋남이 드러났다.

| id | 본문 | 2인 답 | 재판정 | §7 표의 어느 줄로 판정했나 |
| --- | --- | --- | --- | --- |
| **26** | 아직 **상품 준비중**…주문 취소하고 환불받을 수 있나요 | `RETURN_REFUND` | **`ORDER_CHANGE`** | `RETURN_REFUND` 항목의 예외 — *"발송 **전** 취소는 `ORDER_CHANGE`"* |
| **32** | 쿠폰을 넣었는데 **할인이 적용이 안 돼요** | `PAYMENT` | **`PROMOTION`** | 예외는 *"쿠폰 쓴 결제가 **실패**하면 `PAYMENT`"* — 이건 결제 실패가 아니다 |
| **31** | 받은 쿠폰이 목록에 **아예 안 보여요** | `SERVICE_USAGE` | **`PROMOTION`** | `PROMOTION` = 쿠폰·적립금·할인. 「사용법 질문」이 아니라 **쿠폰이 동작 안 함** |
| **33** | 쿠폰 적용 버튼을 눌러도 **금액이 그대로** | `SERVICE_USAGE` | **`PROMOTION`** | 〃 |
| **34** | 적립금 5000원이 있는데 **어디서 쓰는지** 안 보여요 | `SERVICE_USAGE` | **`PROMOTION`** | `PROMOTION` 에 「적립금」 명시. **`id 24` 를 `ACCOUNT` 로 확정한 것과 같은 논리** — 대상이 이기고 방법 질문은 부가 |

**같은 부류끼리 대조해도 재판정 쪽이 일관된다.**

```text
id 29  "방금 주문한 건 취소... 아직 발송 안 됐어요"   → ORDER_CHANGE   ✅ 표대로
id 26  "상품 준비중... 취소하고 환불받을 수"          → RETURN_REFUND  ❌ 같은 부류인데 달랐다
id  6  "어제 받았는데... 취소하고 돈 돌려받고"        → RETURN_REFUND  ✅ 발송 후라 맞다
```

### ⚠️ `27 · 28` 은 손대지 않았다 — 표가 갈리기 때문이다

```text
본문: "아직 발송 전인데 배송지 주소를 바꾸고 싶어요" (27) / "출고 전이면 받는 주소 변경 가능한가요?" (28)

DELIVERY      배송 상태·지연·분실·주소 변경     ← 「주소 변경」이 여기 있고
ORDER_CHANGE  발송 전 주문 변경·취소            ← 「발송 전 변경」도 여기 있다
```

**두 항목에 동시에 걸린다.** 표로 한쪽이 이기지 않으므로 **2인이 합의한 `DELIVERY` 를 유지**했다.
표를 고쳐 한쪽으로 몰 수도 있었지만, **분포를 맞추려고 표를 고치는 모양**이 되므로 하지 않았다
(`id 24` 때 §7 표를 고치지 않은 것과 같은 판단).

> **결과적으로 이 2건이 정답지가 AI 초안의 사본이 되는 것을 막았다.** 아래 참조.

### 최종 결과 — 분포는 여전히 5×10 이 아니다

```text
DELIVERY       7   ← 5 아님
ORDER_CHANGE   3   ← 5 아님
나머지 8종      5
```

**AI 초안과 다른 건이 2건 남았다 (`id 27 · 28`).** 이것이 이 데이터가 **AI 답의 사본이 아니라는
유일한 구조적 증거**다.

> ⚠️ **그럼에도 다음 사람이 반드시 알아야 할 것** — 재판정 5건의 결과가 **공교롭게도 AI 초안과 같다.**
> 「사람이 §7 표로 재판정했더니 AI 와 같은 결론이 나왔다」와 「AI 답을 베꼈다」는 전혀 다르지만,
> **데이터만 봐서는 구분되지 않는다.** 그래서 위 표에 **건별로 §7 표의 어느 줄을 근거로 삼았는지**를
> 남겼다. 판정이 미심쩍으면 그 줄을 직접 확인하면 된다.
>
> **측정 8ⓐ-1 을 읽을 때 이 사실을 함께 본다.** 오분류율이 낮게 나와도 그것이 「AI 가 실제로 안
> 틀렸다」인지 「채점 기준의 48/50 이 AI 답과 같아서」인지는 **이 숫자만으로 못 가린다.**
> 갈리는 자리는 `27 · 28` 두 건과, AI 가 애초에 틀릴 **경계 9건**이다.
>
> ✅ **가려낼 방법은 정해뒀다 — [`measurement-8a-design.md`](./measurement-8a-design.md) 3장.**
> 셋 다 추가 데이터 수집 없이 된다: ① 실제 AI 답과 시드 초안을 대조해 **우려의 실체부터 확인**
> ② 8ⓐ-1 을 **두 기준**(최종 정답 / **2차 답 단독**)으로 계산해 **차이만큼을 앵커링 몫으로 읽는다**
> ③ **경계 9건만 따로** 집계한다.
> **2차 답(이용택)은 blind 서식만 받아 앵커링이 구조적으로 불가능하다** — 그것이 이 대조를
> 가능하게 하는 자산이다 (AI 초안과 **8건** 다르다).

### 이 재판정이 드러낸 것 — 일치율 98% 가 못 보는 층위

**두 사람이 일치했다고 해서 §7 표와 맞는 것은 아니었다.** 위 5건은 갈린 것이 아니라
**둘 다 같은 방식으로 표를 벗어난** 건들이다.

`README.md` 는 *"판단이 **갈리면** `PRD.md` §7 경계표를 본다"* 고 안내한다. 뒤집으면
**안 갈리면 표를 안 보게 된다.** 두 사람이 같은 직관을 공유하면 표를 펼칠 계기 자체가 없다.

> **일치율은 「두 사람이 같게 읽나」만 재고, 「둘 다 기준대로 읽나」는 재지 않는다.**
> 측정 12(검토자 간 일치도)를 읽을 때도 같은 한계가 적용된다 — 높은 일치율이
> **기준 준수의 근거는 아니다.**

**다음 레이블 작업에 반영할 것**: 갈린 건만 §7 표를 보지 말고, **끝난 뒤 전수를 표에 한 번 대본다.**
이번에는 분포가 크게 벗어난 것이 그 계기가 됐지만, **분포가 우연히 고르면 그 계기조차 없다.**

**남은 일** (이 문서 밖)

1. ~~`id 24` 정답 확정~~ → **2026-08-10 완료** (`ACCOUNT`)
2. ~~`expected_category` 교체~~ → **2026-08-10 완료** (7건)
3. **§7 표에 `ACCOUNT` × `SERVICE_USAGE` 겹침 규칙 추가 검토** — 판정은 안 바뀌지만 다음 사람이
   같은 곳에서 멈추지 않게 하는 일이다. `PRD.md` 수정이라 별도 PR
4. **TRI-77(측정 1·8ⓐ) 착수 가능** — 이 문서가 그 관문이었다. 단 **재사용을 끈 상태**에서 재야
   하므로 TRI-90(스위치)이 먼저다 (D-043 ⓒ)

## 함께 볼 것

문장을 누가 썼는지는 [`seed-draft-provenance.md`](./seed-draft-provenance.md) 에 있다. **41건이 AI 초안 상태인 채로 잰 일치율은 예비값**이다 — AI 가 쓴 문장은 사람이 쓴 문장보다 애매함이 적어서 일치율이 실제보다 높게 나온다.

## TRI-78 (측정 12) — 세 번째 라벨러로 재확인 (2026-08-13)

TRI-78 은 "20건을 새로 뽑아 두 사람이 서로 안 보고 라벨링"하라고 요구했다. **두 사람을 새로
구하지 않고, 김은빈이 20건을 블라인드로 라벨링해서 이용택의 기존 2차 답(위 「측정 결과」,
`inquiries-50-label-2nd-yongtaek.csv`)과 대조했다.**

**왜 이렇게 해도 되나** — 이용택의 2차 답안은 작성 당시 **blind 서식만 받아 독립적으로 작성됐기 때문에, 작성 과정에서 다른 라벨러의 답을 보고 영향을 받을 수 있는 구조가 아니었다.** 김은빈 역시 이용택의 답안을 보지 않은 상태에서 별도로 라벨링했다. 따라서 **이번 대조는 서로의 라벨을 보지 않고 작성된 두 라벨러의 결과를 비교한 추가 관찰**로 취급한다.

다만 TRI-78은 **신규 라벨러 2명을 동시에 모집해 처음부터 독립적으로 측정한 방식이 아니므로,** 기존 측정 12와 **동일한 절차적 증거 수준이라고 보지는 않는다.** 따라서 이번 결과는 모집단 일치율의 재판정값이 아니라 **경계 사례에서 사람 간 불일치가 실제로 발생하는지를 확인한 보강 증거**로 사용한다.

### 표본

20건 — 카테고리(10종) 당 2건씩, 그중 1건은 `is_boundary=Y`(경계 사례, 카테고리별 1건씩
설계됨) 문항을 **의도적으로 전부 포함**했다. 사람 간 불일치는 애매한 문항에서 나올 확률이
높아서, 무작위보다 이쪽이 측정의 목적(어디서 갈리는지 확인)에 맞다.

⚠️ **그래서 이 표본은 대표 표본이 아니다.** 경계 사례 비중이 표본에서는 45%(9/20)인데
전체 50건에서는 18%(9/50)뿐이다. 아래 raw 일치율을 모집단 수준의 90% 문턱과 **직접
비교하지 않는다** — 그 비교는 대표 표본에서만 유효하다.

### 실측 결과 (2026-08-13)

```text
1차: 이용택 (2차 라벨러, 기존 blind 답 재사용)
2차: 김은빈 (신규, `inquiries-50-label-3rd-eunbin.csv`)
건수: 20 (전체 50건 중 카테고리별 2건, 경계 사례 9건 포함)
일치: 16
일치율: 16/20 = 80.0%
```

### 갈린 건 4건 — 전부 §7 표로 판정됨

| ref | id | 본문 | 이용택 | 김은빈 | §7 판정 |
| --- | --- | --- | --- | --- | --- |
| 14 | 34 | "적립금 5000원이 있는데 어디서 쓰는지 안 보여요" | `SERVICE_USAGE` | **`PROMOTION`** | TRI-76 이 이미 이 문항을 §7 로 재판정해 `PROMOTION` 으로 확정함(대상이 이기고 방법 질문은 부가) — 김은빈 답이 확정답과 일치 |
| 28 | 31 | "쿠폰이 목록에 아예 안 보여요" | `SERVICE_USAGE` | **`PROMOTION`** | 위와 같은 판정으로 TRI-76 이 `PROMOTION` 확정 — 김은빈 답이 확정답과 일치 |
| 41 | 44 | "앱이 자꾸 튕겨서 짜증나네요" | **`COMPLAINT`** | `SERVICE_USAGE` | §7 원칙("요구사항 없이 불만만 있으면 `COMPLAINT`") — 이용택 답이 원칙과 일치 |
| 45 | 24 | "회원 탈퇴… 어디서 하는지" | `SERVICE_USAGE` | **`ACCOUNT`** | TRI-76 의 그 갈린 건(§7 표로 `ACCOUNT` 확정) — 김은빈 답이 확정답과 일치 |

**4건 모두 §7 표로 판정된다** — 표를 못 가르는 새 경계는 안 나왔다. 그리고 4건 중 3건은
**TRI-76 이 이미 확정한 정답과 김은빈의 신규 블라인드 답이 일치**한다 — 이용택의 답은
그 확정 *이전*의 원래 블라인드 값이었다. 나머지 1건(`id 44`)만 반대로 이용택 답이 §7 원칙과
맞고 김은빈 답이 갈린다. 이 1건은 이전에 아무도 안 다룬 **새로 드러난 불일치**다.

### 판정

- 이 표본(경계 사례 과대표집) 안에서 raw 일치율은 **80.0%**다
- 경계 사례(9건)만 보면 8/9 = 88.9% 일치, 비경계(11건)만 보면 8/11 = 72.7% 일치 — 표본이
  작아 이 세부 격차를 일반화하지 않는다
- **모집단 수준 일치율(측정 8ⓐ 인용용)은 여전히 TRI-76 의 값(90.0%, 전체 50건, 대표적)을
  쓴다.** 이번 20건은 "경계에서 사람이 실제로 갈리는가"를 보여주는 **보강 증거**이지,
  90% 문턱을 다시 판정하는 자리가 아니다
- 이번 측정이 보여준 것: 경계 사례에서 사람 간 불일치가 실재하고(TRI-76 의 "첫 판에서 갈린
  5건 중 4건이 `SERVICE_USAGE` 와 얽혔다"는 관찰과 같은 방향), 이번 4건은 모두 기존 §7 표 또는 §7 원칙으로 사후 판정할 수 있었다. 새로운 미해결 경계는 발견되지 않았다.

### TRI-78 완료 조건 대조

| 완료 조건 | 충족 여부 |
| --- | --- |
| 일치율과 갈린 건 목록이 `evidence/` 에 있다 | ✅ 위 「실측 결과」·「갈린 건 4건」 |
| 측정 8ⓐ 결과를 적을 때 이 값을 함께 인용한다 | 인용할 값: **90.0%**(TRI-76, 모집단 대표) — 이번 80.0%는 "경계 사례 국소 일치율" 참고값으로 함께 적음 |

### 한계

- 표본이 20건, 대표 표본이 아니라 이 raw 80.0%로 §7 재평가 절차(90% 미만 시 표 조이고
  재레이블)를 발동하지 않는다 — 발동 기준은 대표 표본에 대해서만 의미가 있다
- `id 44`(§7 원칙상 `COMPLAINT`, 김은빈은 `SERVICE_USAGE`로 답함) 하나는 이번에 처음
  드러난 불일치이지만 표본 1건이라 일반화하지 않는다. 다음에 유사 문항이 또 갈리면 그때
  §7 표에 「액션 없는 불만 vs 사용법 문의」 경계를 명시하는 것을 검토한다

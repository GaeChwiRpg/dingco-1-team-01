# PAAR-CARDS — 1기 team-01 대표 문제 증명

> PAAR는 `Problem → Analyze → Action → Result`다. 아래 카드는 할 일 목록이 아니라 **학생별로 끝까지 증명할 문제 1개**다.
> 측정 전 수치는 결과처럼 쓰지 않는다. PR 수·커밋 수·테스트 수·CI Green만으로 Result를 대신하지 않는다.

## 공통 통과 조건

- 구현 전에 실패 재현 또는 baseline을 남긴다.
- 현재 방식 포함 최소 2개 대안과 선택 기준을 기록한다.
- 본인 PR과 `evidence/` 파일을 Action에 연결한다.
- 같은 입력·환경으로 Result를 다시 측정하고 한계와 다음 행동을 적는다.

## @whitejh — 5% 감사가 놓치는 오분류

> **한 문장으로**: AI 가 「확신한다」며 스스로 통과시킨 문의 중 **20건에 1건만 뽑아 사람이 다시 보는데**, 그렇게 해서 **실제 오답 3건 중 1건만 잡고 2건을 놓쳤다.**

### 배경을 모르는 사람을 위해 — 30초 설명

이 시스템은 고객 문의를 AI 가 분류하고, **AI 가 확신하는 건은 사람을 안 거치고 그대로 확정**합니다. 빠르지만 위험합니다 — **AI 가 자신 있게 틀리면 아무도 모릅니다.**

그래서 자동으로 확정된 것 중 **20건에 1건(5%)을 몰래 뽑아 상담원이 다시 봅니다.** 이 카드가 던지는 질문은 하나입니다.

> **그 5% 로 실제 오답을 얼마나 찾아낼 수 있나?**

**이 카드에 나오는 말**

| 말 | 뜻 |
| --- | --- |
| **자동 확정** | AI 가 확신도 `0.8` 이상으로 답해서 **사람 손을 안 거치고 끝난 것** |
| **감사(5%)** | 그렇게 끝난 것 중 20건에 1건을 몰래 뽑아 **사람이 다시 보는 것** |
| **8ⓐ-1** | 자동 확정된 건이 **실제로** 틀린 비율 — 정답지와 **전부** 대조해서 구한 참값 |
| **8ⓐ-2** | 그 오답 중 **감사가 실제로 집어낸** 비율 |
| **8ⓑ** | 같은 문의가 또 왔을 때 **지난 답을 다시 쓴 건**이 틀린 비율 |
| **②** | 분류 결과를 저장하는 묶음(트랜잭션). 이 안에서 감사 표본도 함께 넣는다 |
| **§7** | `PRD.md` 7장 — 문의 종류 10가지의 **경계를 정한 표** |
| **D-005 · TRI-66** | 결정 기록 번호 · Jira 티켓 번호 |

---

- **Problem**: 자동 확정 40건의 5%는 2건뿐이라 감사 결과만으로 실제 오분류를 판단할 수 없다.
  - *쉽게 말하면* — **뽑히는 게 2건뿐인데 그걸로 "AI 가 얼마나 틀리나"를 말할 수 있겠나?*
- **Analyze**: 전수 사람 검토 / 임계값+검토 큐 / 임계값+5% 감사. 기준은 사람 검토량과 오분류 검출력이다.
- **Baseline gate**: 정답 문의 50건을 2명이 독립 레이블링한다. 일치율이 90% 미만이면 구현보다 카테고리 경계를 먼저 고친다.
- **Action evidence**: PR #12 이후 분류·감사 구현 PR과 `evidence/seed-label-agreement.md`를 연결한다.
- **Result gate**: 자동 확정 전수 오분류와 5% 감사 검출 건수를 분리하고, 표본 2건의 한계를 함께 적는다.
- **상태**: [x] Baseline [x] Analyze [x] Action [x] **Result** *(2026-08-13 — 아래 「Result」)*

### 진행 (Baseline 2026-08-10 · Action 2026-08-12)

**✅ Analyze — `DECISIONS.md` D-005 에 3개 대안과 선택 기준이 기록돼 있다**

| 선택지 | 왜 안 됐나 |
| --- | --- |
| ① 사전 실험(50건)으로만 측정 | 운영 중에는 측정이 불가능하다 |
| ② 자동 승인 건 전수 검토 | 자동화의 의미가 사라진다 |
| ③ **자동 승인 건의 5% 무작위 추출 → 검토 큐 (채택)** | 적체가 소폭 느는 대가로 **운영 중 상시 측정**이 된다 |

**표본이 2건이라는 문제는 D-043 에서 한 번 더 다뤘다** — 측정 8ⓐ 를 **8ⓐ-1(자동 확정 전수 오분류)** 과 **8ⓐ-2(5% 감사가 그중 몇 건을 잡았나)** 로 쪼갰다. 새 측정을 늘린 게 아니라 **하나로는 못 읽던 것을 둘로 나눈 것**이고, 두 값의 차이가 곧 **샘플링의 검출 한계**다.

**✅ Baseline — 통과했다 (2026-08-10)**

| | 값 | 근거 |
| --- | --- | --- |
| 2인 독립 레이블 일치율 | **49/50 = 98.0%** | `evidence/seed-label-agreement.md` (PR #47) |
| 보수적 하한 | **90.0%** (첫 판 기준) | 같은 문서 「어느 값을 쓰나」 |
| 판정 | **90% 이상 → 그대로 진행.** 경계표 재작성 절차 미발동 | |

**⚠️ 통과했지만 「경계표가 멀쩡하다」로 읽지 않는다.** 가장 보수적인 값(노출 3건 제외 + 첫 판)은 **89.4% 로 기준을 밑돌고**, 첫 판에서 갈린 5건 중 **4건이 `SERVICE_USAGE` 와 얽혀** 있었다. **1건이 0.6%p 를 좌우하는 표본 크기**라는 사실을 결론보다 앞에 적었다.

**정답 50건이 완성됐다 (PR #48)** — 2단계를 거쳤다. **① 2인 레이블 결과로 교체(7건) → ② 그중 5건을 §7 표로 재판정.** 갈렸던 `id 24` 는 표·헌법 기준·같은 구조 4건의 합의가 모두 가리키는 `ACCOUNT` 로 확정했다.

> **② 가 드러낸 것 — 일치율 98% 가 못 보는 층위가 있다.** 재판정한 5건은 **갈린 게 아니라 둘 다 같은 방식으로 §7 표를 벗어난** 건들이었다. `README` 가 *"판단이 **갈리면** 표를 본다"* 고 안내하니, **안 갈리면 표를 펼칠 계기가 없다.**
> **일치율은 「두 사람이 같게 읽나」만 재고 「둘 다 기준대로 읽나」는 재지 않는다** — 측정 12 를 읽을 때도 같은 한계가 걸린다.
>
> **분포는 여전히 5×10 이 아니다** (`DELIVERY` 7 · `ORDER_CHANGE` 3). `id 27·28`(발송 전 주소 변경)은 §7 표가 `DELIVERY`·`ORDER_CHANGE` 양쪽에 걸려 **표로 판정되지 않으므로 2인 합의를 유지**했다. 결과적으로 **이 2건이 정답지가 AI 초안의 사본이 되는 것을 막았다.**
>
> ⚠️ **그럼에도 48/50 은 AI 초안과 같다.** 「사람이 표로 재판정했더니 AI 와 같았다」와 「AI 답을 베꼈다」는 데이터만 봐서는 구분되지 않아, evidence 에 **건별로 §7 표의 어느 줄을 근거로 삼았는지** 남겼다. **측정 8ⓐ-1 의 오분류율이 낮게 나와도 그 두 가지를 이 숫자만으로는 못 가린다.**

**✅ Action — 분류 경로에 이어 감사 경로까지 섰다 (2026-08-12 완료)**

| 무엇 | 상태 | 근거 |
| --- | --- | --- |
| AI 호출 + 값 검증 4종 | ✅ | PR #16 · #23 (TRI-46·48·49·50) |
| 트랜잭션 ② (판정·저장·큐 삽입) | ✅ | PR #26 (TRI-51·52·82) |
| 비동기 분류 + 자원 설정 | ✅ | PR #30 (TRI-47·83) |
| 재시도 + 최종 실패 복구 | ✅ | PR #35 (TRI-54) |
| **재시도 실측 (측정 4)** | ✅ | PR #44 · `evidence/retry-recover-measure4.md` |
| ↳ *이 측정은 `@techietaek` 카드의 Baseline 도 함께 채운다* | | 아래 그 카드의 📌 참조 |
| **감사 표본 추출 정책** | ✅ | PR #50 (TRI-64) |
| 감사 삽입을 ② 안에 (누락 0 보장) | ✅ | PR #50 (TRI-65) — **D-012 가 요구하는 자리** |
| 감사 장치 자체를 감사하는 필드 | ✅ | PR #61 (TRI-66) — `actualSampleRate` vs `configuredSampleRate` |
| 측정할 때 재사용을 끄는 스위치 | ✅ | PR #58 (TRI-90) — 관문 1 |
| **밖으로 빼면 표본이 몇 건 새나 (대조 실험)** | ✅ | PR #66 (TRI-86) — D-012 를 주장에서 근거로 |

---

### ✅ Result (2026-08-13) — 잰 값

> **측정 조건** — 이 줄이 없으면 아래 숫자는 어느 조건의 것인지 알 수 없다
>
> 재사용 **끔** · `claude-sonnet-5` · 기준값 `0.8` · 감사 비율 `0.05` · 입력 정답 50건 · 커밋 `38b2f25`
> 근거: `evidence/measurement-1-8a.md` (PR #64) · 재현 절차는 그 문서 8절

#### 세 줄로 먼저

```text
정답 50건을 실제로 돌렸더니 → 32건이 자동 확정됐고 → 그중 3건이 오답이었다
감사는 그 32건에서 5건을 뽑았고 → 오답 3건 중 1건을 찾아냈다
남은 2건은 아무도 안 봤다
```

#### 카드가 요구한 대로 두 숫자를 분리했다

| | 무엇을 재나 | 값 |
| --- | --- | --- |
| **8ⓐ-1** | 자동 확정된 건이 **실제로** 틀린 비율 (전부 대조한 참값) | 32건 중 **3건 = 0.094** |
| **8ⓐ-2** | 그 오답 중 **5% 감사가 집어낸** 비율 | 3건 중 **1건 = 0.333** |

**두 값의 차이가 이 카드의 답이다 — 3건 중 2건을 놓쳤다.**

> **왜 두 숫자를 굳이 나눴나** — 감사가 잡은 것만 보면 *"오답이 1건이구나"* 로 읽힌다. 실제로는 3건이었다. **감사만 보고 있으면 놓친 2건은 존재조차 모른다.**

#### Problem 이 실재했다

카드의 Problem 은 *"5%는 2건뿐이라 감사 결과만으로 실제 오분류를 판단할 수 없다"* 였다. **판단할 수 없는 정도가 아니라 3분의 1만 보인다.**

| seed | AI 답 | 정답 | 확신도 | 감사에 뽑혔나 |
| --- | --- | --- | --- | --- |
| **27** | `ORDER_CHANGE` | `DELIVERY` | **0.95** | ✅ 잡음 |
| 26 | `RETURN_REFUND` | `ORDER_CHANGE` | 0.85 | ❌ 놓침 |
| 34 | `SERVICE_USAGE` | `PROMOTION` | 0.80 | ❌ 놓침 |

**확신도 0.95 에서 틀린 건이 나왔다.** 「확신도가 낮으면 사람에게 넘긴다」는 1차 장치로는 이 3건 중 **어느 것도** 못 잡는다 — 셋 다 기준값(0.8)을 넘겼기 때문이다. **감사가 없었다면 오분류율은 관측값 0 이었을 것이다.**

#### 놓친 2건이 실제 피해로 이어지는 것까지 봤다 (측정 8ⓑ)

이 시스템은 **같은 내용의 문의가 또 오면 AI 를 다시 부르지 않고 지난 답을 그대로 씁니다.** 그러니 틀린 답이 한 번 확정되면 **그 뒤로 계속 나갑니다.**

그래서 같은 50건을 **재사용을 켜고** 다시 돌려 오답 3건이 어떻게 되는지 따라가 봤다 (`evidence/measurement-8b.md`, PR #65).

| seed | 감사에 뽑혔나 | 그 다음 |
| --- | --- | --- |
| 27 | ✅ 뽑힘 → 사람이 정정 | **다음 문의부터 올바른 답이 나갔다** |
| 26 · 34 | ❌ 안 뽑힘 | **틀린 채로 재사용돼 퍼졌다** |

**한 번의 실행 안에서 대조군이 저절로 생겼다.** 감사에 뽑힌 1건과 안 뽑힌 2건이 갈렸기 때문이다.

**8ⓐ-2 의 0.333 은 종이 위의 비율이 아니다.** 감사가 놓친 만큼이 **다음 문의의 오답**으로 그대로 이어진다.

#### ⚠️ 표본 한계 — 숫자 옆에 둔다 (Result gate 가 요구한 것)

**이 카드가 막으려던 일이 정확히 여기서 일어날 뻔했다.**

| 한계 | 내용 |
| --- | --- |
| **감사 표본이 5건뿐** | **「비율」로 말할 수 없다.** 0.333 은 「3분의 1을 잡는다」가 아니라 「**이번에** 3건 중 1건을 잡았다」이다 |
| **그 5건도 설정보다 3배 뽑힌 값** | 설정은 5% 인데 실제로는 **15.6%** 가 뽑혔다(32건 중 5건 — 우연히 이렇게 될 확률 약 2%). **정상대로 1~2건만 뽑혔다면 오답을 잡을 확률은 훨씬 낮았다** — 0.333 은 「5% 감사의 실력」이 아니라 **「우연히 3배 뽑혔을 때의 실력」이라 실제보다 좋게 나온 값이다** |
| **오분류 3건의 성격이 다르다** | 2건은 **사람 둘도 AI 와 똑같이 봤고**, 나중에 §7 경계표로 다시 판정하면서 정답이 갈린 건이다. **「AI 가 못 맞힌 몫」과 「경계 규칙이 사람의 직관과 어긋나는 몫」이 한 숫자에 섞여 있다** — 뒤쪽은 모델을 바꿔도 안 줄어든다<br>✅ **측정 12(2026-08-13)가 이 몫이 실재함을 확인했다** — 두 사람이 같은 20건에서 4건 갈렸다 |
| **자동 확정 32건에 오분류 3건** | **1건이 늘고 줄면 비율이 3%p 씩 움직인다.** 통계적으로는 0.094 가 아니라 **「3.2% ~ 24.2% 사이」**라고 말하는 편이 정확하다(95% 신뢰구간) |
| **검토 화면의 쏠림은 이번 값에 안 걸린다** | 상담원이 AI 제안을 먼저 보고 그쪽으로 기우는 문제(D-010)는 **이번 측정에 개입하지 않았다.** 8ⓐ-1 은 AI 답과 정답지를 **전수 대조**한 값이고, 감사 확정도 사람이 화면을 보고 한 게 아니라 **정답지를 그대로 넣었다.** 운영에서 상담원이 확정한 값으로 오분류를 셀 때 걸리는 한계다 |
| **모델 한정 · 1회 실행** | `claude-sonnet-5` 에 한정되고(D-024), 같은 조건으로 또 재면 같은 값이 나오는지는 확인하지 않았다 |

> **설정보다 3배 뽑힌 것을 우리가 알 수 있었던 이유가 TRI-66 이다.** 그 필드가 없었으면 *"감사가 3건 중 1건을 잡았다"* 만 보고 넘어갔을 것이고, **8ⓐ-2 가 낙관 편향됐다는 사실 자체를 몰랐다.** 재는 장치를 재는 장치가 결론을 바꾼 자리다.

#### 채점 기준이 AI 답과 가까웠던 문제는 세 가지로 가렸다

Baseline 이 남긴 *"48/50 이 AI 초안과 같다"* 는 우려에 **추가 수집 없이** 답했다.

*(「채점 기준이 AI 답과 가깝다」가 왜 문제인가 — 정답지를 만들 때 AI 가 먼저 붙여둔 값을 사람이 보고 고쳤다. 그러면 **AI 는 자기가 만든 답안지로 시험을 본 셈**이라 점수가 좋게 나오는 게 당연하다.)*

| 방법 | 결과 |
| --- | --- |
| ① **실제 AI 답**을 시드 초안과 대조 | **4건 갈렸다** — 채점 기준이 「지금 이 AI 가 내는 답」과 같지 않다 |
| ② **AI 값을 본 적 없는 사람의 답**으로 다시 채점 | 오분류율 **0.062 로 오히려 낮다**(−1건). *"AI 답이 기준이라 좋게 나왔다"* 가 힘을 잃는다 |
| ③ **경계 9건만** 따로 | 오분류율 **0.250 — 전체의 2.7배.** 「경계가 AI 가 틀리는 자리」라는 예측이 맞았다 |

> ②가 이 프로젝트의 자산이다. 2차 작성자(이용택)는 **AI 값이 지워진 서식만 받았기 때문에** 그쪽으로 끌릴 수가 없다. 그 답으로 채점해도 결과가 나빠지지 않았다는 것이 **가장 강한 반증**이다.

#### 이 숫자를 믿어도 되는 이유 — 「몇 건 뽑혔나」를 지켰다

위 숫자들은 전부 **감사가 몇 건 뽑았는지**를 밑바닥에 깔고 있다. 뽑힌 건수가 조용히 줄면 **결론 전체가 흔들리는데 아무도 모른다.** 그래서 그것을 지키는 장치를 두 개 만들고 각각 확인했다.

| 무엇 | 확인한 것 |
| --- | --- |
| **TRI-66** — 설정 비율과 실제 비율을 나란히 보여주는 값 | **이번에 3배 과다 추출을 잡아냈다.** 이 값이 없었으면 *"감사가 3건 중 1건을 잡았다"* 만 보고 넘어갔다 |
| **TRI-86** — 감사 표본 넣기를 저장 묶음 **밖으로 빼보는 대조 실험** | **표본 10건이 오류 하나 없이 전부 사라졌다.** 예외도 로그도 안 남아서 앱은 잃은 줄도 모른다 — *"측정 장치를 저장 묶음 밖에 두지 않는다"*(D-012)가 **주장에서 근거로** 바뀌었다 |

#### 한계를 알고 나서 남는 결론

**"5% 감사는 오분류를 다 잡는 장치가 아니라, 오분류가 존재한다는 사실을 알려주는 최소한의 장치다."**

감사를 안 넣었다면 오분류율은 **관측값 0** 이었을 것이다. 그리고 **「0 건 관측」과 「AI 가 안 틀린다」는 화면상 구별되지 않는다.**

```text
감사 없음   →  오답 3건이 그대로 확정되고 관측값은 0        →  "잘 돌아간다"
감사 5%     →  오답 3건 중 1건이 드러난다                  →  "틀리는 일이 있다" ✅
전수 검토   →  3건 다 드러나지만 자동화의 의미가 사라진다
```

**가운데를 고른 것이 이 프로젝트이고, 그 대가가 「놓친 2건」이다.** 대가를 숨기지 않고 숫자로 적는 것까지가 이 카드의 답이다.

#### 다음 행동

| 무엇 | 왜 | 어디서 |
| --- | --- | --- |
| ~~**측정 12(검토자 간 일치도)**~~ → **2026-08-13 완료** | 0.094 안에 섞인 「사람끼리도 갈리는 몫」을 떼어냈다. **두 사람이 서로 안 보고 같은 20건을 분류해 16/20 일치.** 갈린 4건은 **전부 §7 표로 사후 판정**됐다 — 표가 못 가르는 새 경계는 안 나왔다<br>⚠️ **세 숫자를 섞지 않는다** — 아래 표 참조. 이 20건은 **경계 사례를 일부러 많이 넣은 표본**이라 그 일치율(**80.0%**)을 모집단 수치로 쓰지 않는다<br>🔁 **재현**: 두 사람의 답안지(`inquiries-50-label-2nd-yongtaek.csv` · `-3rd-eunbin.csv`)를 `ref` 로 조인해 `label` 을 비교하면 16/20 이 그대로 나온다. 갈린 4건의 §7 판정 근거도 같은 문서에 있다 | TRI-78 · `evidence/seed-label-agreement.md` |
| **§7 경계표 보강 — `SERVICE_USAGE`** | 같은 신호가 **네 번** 나왔다 (2인 레이블 갈린 5건 중 4건 · 오분류 seed 34 · AI 와 시드가 갈린 4건 중 3건 · **측정 12 에서 갈린 4건 중 3건**). **AI 를 바꿔도 안 줄어드는 몫**이다 | PRD §7 |
| **`id 44` 부류를 지켜본다** | *"앱이 자꾸 튕겨서 짜증나네요"* — 측정 12 에서 **처음 드러난 불일치**다(요구 없는 불만 vs 사용법 문의). 표본 1건이라 일반화하지 않되, 같은 부류가 또 갈리면 §7 에 그 경계를 명시한다 | PRD §7 |

**⚠️ 사람 일치율이 세 개다. 골라 쓰면 틀린다** (`evidence/seed-label-agreement.md` 「어느 값을 쓰나」)

| 값 | 무엇 | 어디에 쓰나 |
| --- | --- | --- |
| **98.0%** (49/50) | 정답지를 확정한 **최종판** 2인 일치율 | 정답지가 얼마나 합의됐나 |
| **90.0%** (45/50) | **첫 판** 기준 — **보수적 하한** (D-046) | **모집단 인용값.** 측정 12 가 재려는 조건에 더 가깝다 |
| **80.0%** (16/20) | 측정 12 의 **경계 사례 표본** | *"경계에서 사람이 갈리는가"* 의 보강 증거. **모집단 수치가 아니다** |

> 가장 보수적인 값은 **89.4%** 로 판정 기준 90% 를 밑돈다(노출 3건까지 제외). **"어느 쪽이든 90% 를 넘는다"고 읽지 않는다.**
| **감사 비율 재관측** | 1차 0.156 · 2차 0.086. 두 번 다 설정(0.05)보다 많다. **두 번으로 단정하지 않고** 다음 측정에서 또 나오는지 본다 | TRI-66 필드 |
| 재현 | 같은 조건으로 다시 재는 절차와 원자료를 남겼다 — **AI 를 다시 부르지 않고** 집계만 고칠 수 있다 | `evidence/measurement-1-8a/` |

## @techietaek — 비동기 분류 실패 시 문의 유실 방지

- **Problem**: AI 호출과 큐 삽입이 실패하면 고객은 접수 응답을 받았지만 문의가 처리에서 사라질 수 있다.
- **Analyze**: 동기 처리 / 비동기 재시도 / 비동기 재시도 후 사람 큐. 기준은 접수 지연과 유실 건수다.
- **Baseline gate**: AI 호출을 연속 3회 실패시키고 현재 남는 문의·상태·로그를 기록한다.
- **Action evidence**: 재시도·복구·관측 구현 PR과 실패 주입 테스트를 연결한다.
- **Result gate**: 문의 원문 유실 0건, 실패 건의 검토 가능 상태 전환, `stuckReceived` 관측 여부를 같은 실패 입력으로 확인한다.
- **상태**: [ ] Baseline [ ] Analyze [ ] Action [ ] Result

> **📌 이미 재어둔 값이 있다 — 다시 재기 전에 읽어보세요 (김준현이 남김, 2026-08-10).**
> 체크는 카드 주인이 판단할 몫이라 손대지 않았고, **어느 항목에 무엇이 쓰이는지만** 적는다.
>
> | 이 카드의 항목 | 이미 있는 근거 | 누가·어디 |
> | --- | --- | --- |
> | **Baseline** — AI 호출 3회 실패 후 남는 문의·상태·로그 | **측정 4** 가 그대로 수행 — 실제 호출 3회, `verdict=FAILED`, `attempt_count=3`, 원문 잔존, 로그 `classify_failed reason=API_ERROR attempts=3` | 김준현 · PR #44 · `evidence/retry-recover-measure4.md` |
> | **Result** — 원문 유실 0건 | 같은 문서 6-5 — `inquiries` 원문 그대로, `status=UNCLASSIFIED` | 〃 |
> | **Result** — 실패 건의 검토 가능 상태 전환 | 같은 문서 5-5 — 큐에 `CLASSIFY_FAILED` 1건, `classification_result_id` 채워짐 | 〃 |
> | **Result** — `stuckReceived` 관측 | **측정 3** — 임계 뒤 0 → 1 확인 | 이용택 · PR #39 · `evidence/measurement-3-rollback.md` |
>
> ⚠️ **두 측정은 서로 다른 실패를 다룬다. 하나로 합쳐 읽으면 안 된다.**
>
> ```text
> 측정 4  AI 호출이 실패  → ②가 정상 완주해 FAILED 로 기록 → stuckReceived 안 오른다 (정상)
> 측정 3  ②의 저장이 실패 → RECEIVED 에 방치            → stuckReceived 오른다 (유실 신호)
> ```
>
> 즉 **Baseline 과 Result 앞 두 줄은 측정 4, `stuckReceived` 줄은 측정 3** 이 답한다.
> **"AI 분류 실패"는 stuck 이 아니다** — ②가 완주하면 `UNCLASSIFIED` 로 전이되고 큐에 들어간다.
> 남은 것은 **같은 실패 입력으로 셋을 한 번에** 확인하는 일이고, 그건 카드 주인의 판단이다.

## @agbink — 검토 중복 확정 차단

- **Problem**: 상담원 두 명이 같은 검토 건을 동시에 확정하면 최종 분류와 통계가 중복 반영될 수 있다.
- **Analyze**: 애플리케이션 선확인 / 낙관적 락 / 상태 조건부 UPDATE. 기준은 중복 확정 0건과 충돌 응답의 설명 가능성이다.
- **Baseline gate**: 같은 항목에 동시 확정 2건을 보내 현재 결과를 재현한다.
- **Action evidence**: 확정 API PR, 동시성 테스트, 결정 로그를 연결한다.
- **Result gate**: 성공 1건·충돌 1건(409), 최종 기록 1건, 통계 중복 0건을 확인한다.
- **상태**: [ ] Baseline [ ] Analyze [ ] Action [ ] Result

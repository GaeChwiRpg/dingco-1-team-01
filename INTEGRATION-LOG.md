# INTEGRATION LOG — 일자별 + 담당자별 진행

> 팀 통합 진행 로그. 그날 한 일 요약 + blocker + 다음 액션.
> 결정의 근거는 `DECISIONS.md`, 단계별 커버리지는 `LIFECYCLE-COVERAGE.md`, AI 오류는 `evidence/failure-cases.md`.
> 참고용 sample: [`examples/week9-team-integration-log.md`](./examples/week9-team-integration-log.md).

## 팀 정보

- 팀 이름: GaeChwiRpg / `dingco-1-team-01`
- 인원: 3명 — 이용택 / 김준현 / 김은빈
- 기간: 2026-07-30 ~ 진행 중 (기준 시점 **2026-08-12**)
- 도메인: **AI 고객 문의 분류 검증 파이프라인** (`PRD.md`) — 2026-08-05 에 에러 분류에서 전환했다 (D-031)
- 책임자 분담: `LIFECYCLE-COVERAGE.md` 매트릭스 (D-002 → **D-020 확정**)
- 코드 작업 패키지: **P1 접수·절감 경로**(이용택) / P2 분류·검증(김준현) / P3 검토·관측(김은빈) — D-015, **재배정은 D-031**

> **git 계정 ↔ 담당자 매핑**: `whitejh` = 김준현 / `techietaek` = 이용택 / `agbink`·`김은빈` = 김은빈 / `dingcodingco`·`dingcodingco-bot`·운영진 계정 = 부트스트랩·공용 워크플로 관리.
> 아래 귀속은 이 매핑을 전제로 한 것이다. 틀린 항목이 있으면 여기 한 줄만 고치면 전체가 따라 맞는다.

---

## 2026-07-30 (목) — 기획 확정 + 헌법 + API 계약

### 팀 공통

- 자율 주제를 **AI 에러 분류 검증 파이프라인**으로 확정 (D-001). 템플릿의 ticket 도메인 예시 전면 폐기
- 라이프사이클 5단계 책임자 분담 합의 (D-002 → 이날 저녁 D-020 으로 확정 대체)
- 기술 스택 합의 — Spring Boot 3.3 / Java 21 / JPA / MySQL 8 / Redis (D-003)
- **PR #1 하나에 AI 코드리뷰를 5회 돌렸다.** 코드 착수 전에 설계 결함을 최대한 뽑아내려는 의도였고, 실제로 회차마다 다른 층위가 나왔다

### 개인

- **김준현 (기획·P2)**: PR #1 전체 작성 — `PRD.md`(9절·US 13개), `CLAUDE.md` 팀 헌법, `API-CONTRACT.md` v0.1→v0.6, `DECISIONS.md` D-001~D-021, `evidence/failure-cases.md` 사례 1~12
- **이용택 (테스트·배포운영·P1)**
  - PR #1 리뷰 참여
- **김은빈 (리뷰·P3)**: PR #1 리뷰 참여 + AI 리뷰 workflow 확인

### AI 리뷰가 잡은 것 (5회 / 지적 21건)

| 회차 | 층위 | 잡은 것 | 결과 |
| --- | --- | --- | --- |
| 1차 | 구조 | blind 규칙이 `confidence`·`threshold` 로 역산 가능 / 인덱스 표가 다른 테이블 컬럼을 가리킴 | D-010, D-011 |
| 2차 | 원자성 | 감사 표본 삽입을 트랜잭션 밖에 둬 **측정 장치를 스스로 훼손** | D-012, D-013 |
| 3차 | 논리 | "캐시 hit rate = AI 절감률" 등식이 틀림 / 캐시 값 구조 미정의 상태로 병렬 계약 확정 | D-014 (+ 계약 C) |
| 4차 | 실행 가능성 | UNIQUE 충돌 재시도를 같은 트랜잭션에서 하면 **반드시 실패** / 롤백된 그룹의 복구 경로가 실재하지 않음 | D-016, D-017 |
| 5차 | 검증 가능성 | 409 를 뭉뚱그려 락 동작을 증명할 수 없음 / 인덱스 쓰기 비용 미고려 | D-018, D-021 |

### 진행 메모

- **`CLAUDE.md` 작업 경계 규칙이 자기 첫 커밋을 차단**했다 (문서 단독 PR 금지). 규칙을 삭제하지 않고 적용 대상을 `API-CONTRACT.md` 로 좁혀 해결 (D-009) — 규칙의 원래 의도가 "계약과 구현의 괴리 방지"였기 때문
- `.mcp.json` 의 Atlassian MCP 가 **지원 종료된 SSE 엔드포인트**를 쓰고 있어 교체 (커밋 `3c2c782`, evidence 3)
- 관찰: **리뷰가 "잘한 점"으로 칭찬한 항목에서 결함이 나왔다.** D-007 락 전략은 1·2·3차가 모두 칭찬했고 4차에서야 실행 불가임이 드러났다 (evidence 관찰 4)

---

## 2026-07-31 (금) — PR #1 머지

- **PR #1 머지** (14:53 KST). 기획·헌법·API 계약 확정
- `API-CONTRACT.md` v0.7 — 파싱 실패 규격 확정. `confidence` 에 `0` 을 쓰지 않고 `verdict=FAILED` + `category`·`confidence` 모두 `null` (D-022). `0` 을 쓰면 측정 8 의 최하위 신뢰도 구간에 "AI 가 0 이라 신고한 건"과 "응답이 깨진 건"이 섞여 오염된다

### blocker

- 코드가 한 줄도 없다. 문서 5회 리뷰로 잡을 수 있는 층위는 소진됐고, **실행 가능성·검증 가능성은 문서만으로 자기 검증이 불가능**하다 (evidence 관찰 5) → 주말 이후 baseline 최우선

---

## 2026-08-03 (월) — baseline 5연속 PR (#2~#6)

### 팀 공통

- **baseline 담당을 이용택으로 배정**했다가 실제로는 김준현이 처리했다. 착수 조건이 없는 유일한 담당이라는 것이 원래 근거였는데, 이날 P1 이 다른 일정으로 붙지 못해 기획 담당이 이어받았다
- **`settings.gradle` + `build.gradle` + wrapper 를 먼저 분리 push** (PR #2) — 김준현의 `SecurityConfig` 는 도메인 의존이 0이라 이것만으로 착수 가능한데, baseline 전체를 완성한 뒤 push 하면 반나절을 통째로 대기시키기 때문
- 측정 11개(당시) 실행 책임자 배정 — "측정은 누구든 돌릴 수 있어서 아무도 안 돌리기 쉽다"는 판단으로 **자기 패키지가 만든 장치를 자기가 재게** 했다

### 개인

- **김준현**: PR #2~#6 전부
  - **#2** Gradle 골격 + Spring Boot 3.3 부트스트랩 (D-003, D-023)
  - **#3** baseline 스키마 + 공유 엔티티 5종 + 시연 스택 — Flyway `V1__init_schema.sql`(확정분) / `V2__candidate_index.sql`(후보 인덱스 2개) 분리 (D-023)
  - **#4** `e2e.yml` + DB 스키마명 `triage_db` 통일 + AI 프로바이더 Anthropic 확정 (D-024)
  - **#5** 엔티티 생성 정적 팩토리 — 계약 B·C 와 D-022 를 **컴파일러가 지키게** (D-025). Lombok 미도입
  - **#6** 라이프사이클 현황 동기화 + **모델 ID 실물 검증**
- **이용택 (테스트·배포운영·P1)**
  - 로컬 환경 세팅 중 Testcontainers 실패 발견 (다음 날 **D-026** 으로 이어짐)
- **김은빈**: PR #2~#6 AI 리뷰 반영 확인. **P3 코드 미착수**

### AI 리뷰 반영

| PR | 지적 | 커밋 |
| --- | --- | --- |
| #2 / #3 | `@EnableRetry` 누락 — **없으면 재시도가 조용히 사라진다** (D-016·D-022 의 전제가 통째로 무효화) | `e1c9942` |
| #3 | JPA Auditing 미설정 + setter 금지 명시 + A/B 절차 정정 | `83869c6` |
| #4 | redis 로그 수집 + artifact 저장 + `BASE_URL` 중복 제거 | `812bc2a` |
| #4 | e2e 에서 chromium 설치 제거 — `request` fixture 는 브라우저를 안 쓴다 | `a9ff684` |

### 진행 메모 — 근거 사슬이 순환하고 있었다

모델 ID `claude-sonnet-5` 의 출처를 되짚어보니 **어디에도 실물 확인이 없었다.**

```text
API-CONTRACT.md §3 의 AI 생성 예시  →  D-024 가 "기존 값"이라며 채택 근거로 인용
                                    →  .env.example · 테스트 · workflow 로 전파
```

`GET https://api.anthropic.com/v1/models` 로 실제 조회해 존재를 확인했다 (2026-08-03). **결과적으로 값은 맞았지만, 맞은 것과 확인한 것은 다르다.** `curl` 한 번으로 닫을 수 있는 항목을 문서 간 상호 인용만으로 유지하는 것이 이 프로젝트가 경계하는 "조용히 틀리기"의 전형이라 먼저 닫았다.

---

## 2026-08-04 (화) — Hooks + 서브에이전트 + 문서 정합 점검

### 팀 공통

- **Claude Code hooks 도입** (PR #7 머지 / PR #8 리뷰 중). 훅과 서브에이전트의 역할을 갈랐다 — 훅은 **조건이 맞으면 무조건** 돌고 정적으로 확실한 것만 보고, 판단이 필요한 규칙은 `constitution-auditor` 서브에이전트가 본다
  - 판단 기준: **오탐이 거의 없는 규칙만 훅에 넣는다.** 정상 작업을 막는 훅은 곧 꺼지고, 꺼진 훅은 통과가 보증처럼 보여서 없는 것만 못하다
- 도메인 교체 제안이 나와 검토 → **현행 유지 + 판별 기준 3개 명문화** (D-027)

### 개인

- **이용택 (테스트·배포운영·P1)**
  - **PR #7** — Claude Code hooks dispatcher (push 전 전체 테스트 검증)
    - `.claude/hooks/dispatcher.sh` + `handlers/verify-before-push.sh` — push 전 `./gradlew test` 강제
    - AI 리뷰 반영 3건 — **jq 부재 시 fail-closed** · heredoc 이후 push 누락 · 실패 요약 (`b424fa3`)
    - 부산물 **D-026** — 로컬 Docker Desktop 을 4.44.2(build 202017) 이하로 고정 (evidence 13)
- **김준현 (기획·P2)**: **PR #8**(리뷰 중) — 서브에이전트 5종 + 편집 시점 헌법 가드 훅 + **D-027**. 이어서 문서 정합 점검(아래)
- **김은빈 (리뷰·P3)**: PR #7 리뷰. **P3 코드 여전히 미착수**

### D-026 — 로컬만 깨지고 CI 는 green 인 상태

`verify-before-push.sh` 를 만들며 `./gradlew test` 를 처음 실 Docker 환경에서 돌리자 Testcontainers 기반 통합 테스트 7건이 전부 `"Could not find a valid Docker environment"` 로 실패했다.

- 원인: Spring Boot 3.3.13 BOM 이 고정하는 **Testcontainers 1.19.8** 이 Docker API **1.24** 를 하드코딩 요청하는데, 최신 Docker Desktop 데몬은 `MinAPIVersion=1.40` 이라 요청 자체를 거부한다 (`/v1.24/_ping` 직접 호출로 실측 확인)
- **개인 머신 문제가 아니다** — 구버전 쓰는 팀원은 통과하고 최신 버전 쓰는 사람만 실패했다. 즉 Docker Desktop 을 최신으로 유지하는 것 자체가 전원의 로컬 통합 테스트를 막는 요인
- Testcontainers 2.x 는 `mysql`·`junit-jupiter` 좌표가 Maven Central 에서 사라져 단순 버전 교체가 아니었다 → **버전 고정** 선택
- 이 결함은 **CI 에서는 안 보인다.** `e2e.yml` 은 Testcontainers 가 아니라 실제 `docker compose` 스택을 쓰기 때문 — 로컬 실패를 CI green 으로 덮지 않기로 함

### 문서 정합 점검 — PRD / DECISIONS / LIFECYCLE / API-CONTRACT 교차 대조

`service/`·`api/` 착수 직전에 문서 5종을 나란히 놓고 대조했다. 코드가 없는 지금이 계약을 고치는 비용이 가장 싼 시점이다.

**찾은 결함 3건**

| # | 결함 | 성격 | 처리 |
| --- | --- | --- | --- |
| 1 | `API-CONTRACT.md` §2 가 `sort` 파라미터 근거를 **D-013 이 아닌 D-012 로 인용** | 내부 불일치 (기계적 검출 가능) | v0.8 정정 / evidence 14 |
| 2 | 계약의 `404 (미정의 카테고리)` 가 헌법의 `미등록 = 0.9 fallback` 과 **모순** — 그 카테고리 임계값을 영원히 설정 불가 | 설계 결함 | **D-029** upsert / evidence 15 |
| 3 | PRD 관리자 페르소나의 **"감사 비율 설정"이 계약 8 endpoint 어디에도 없음** | 소유자 부재 | **D-028** |

- 결함 2 가 지금 안 터지는 이유는 설계가 맞아서가 아니라 **V1 seed 가 10종을 다 넣어줘서**다. 카테고리 축소·확장이 발동하는 순간 바로 걸린다
- 결함 3 은 문서 품질 문제가 아니라 **P2/P3 중 누가 만드는지가 정해지지 않는 문제**였다 — D-015 가 막으려던 구멍. 감사 비율은 운영 파라미터가 아니라 **측정 조건**이므로 런타임 API 를 두지 않고 설정 고정 + 조회만 노출하는 쪽으로 닫았다

**메운 계약 공백 (v0.8)**

- 공통 **오류 응답 바디 형식** + `code` 상수 — 3인이 병렬로 짜면서 각자 다른 오류 모양을 내보내는 것을 차단
- §1 **요청 필드 표** — 길이 상한을 `V1` 컬럼 정의와 일치시킴. 어긋나면 400 이어야 할 요청이 500 으로 나가 **클라이언트 잘못과 서버 잘못이 로그에서 섞인다**
- §6 에 `mode`·`globalThreshold`·`audit.sampleRate` 를 읽기 전용으로 노출 — 측정 9 의 처리군을 사후에 식별 가능하게

**문서 갱신**: `LIFECYCLE-COVERAGE.md`(단계별 흐름 3곳의 템플릿 잔재 제거 + "Phase" 이중 의미 분리), `INTEGRATION-LOG.md`(이 문서 신규 작성), `PRD.md`(§2·§7·§8), `evidence/failure-cases.md`(사례 14·15)

### blocker

1. **`service/`·`api/` 가 여전히 0줄이다.** baseline·훅·문서는 갖춰졌고 P1/P2/P3 착수 조건은 전부 해제된 상태 — 남은 것은 착수 자체
2. **P3(김은빈) 코드 미착수.** 커밋 이력상 코드 기여가 아직 없다. P3 는 endpoint 수 기준 범위가 가장 크므로 가장 먼저 붙어야 하는 쪽이다
3. 측정 12개 전부 미실측. 측정 3(롤백)·7ⓐ(동시 POST)·7ⓑ(동시 PATCH)는 **Day 2~3 시점 예정이었으나 코드 부재로 밀렸다**
4. `API-CONTRACT.md` v0.8 은 문서 단독 머지가 헌법상 불가 → **P2/P3 코드 PR 에 동반**시켜야 한다

---

## 2026-08-05 (수) — 도메인 전환 (D-031)

### 팀 공통

- **도메인을 CS 문의 자동 분류로 교체**하기로 팀 협의 확정. 설계 골격(비동기 → 재시도 → 임계값 검증 → 격리 → 사람 확정 → 관측)은 그대로 두고 판정 대상만 에러 → 문의로 이동
- 팀이 가져온 스코프에서 **그룹핑(`topic_key`)·카테고리별 임계값 정책 테이블·임계값 변경 API 를 명시적 제외**

### 전환 결정이 D-027 과 충돌한 지점

전날(08-04) 채택한 D-027 이 판별 기준 3개를 헌법에 올리고 *"교체 제안은 기준 통과를 먼저 보인다"* 고 못박은 상태였다. CS 문의를 그 기준에 넣으니 **기준 1(묶음 동질성)에서 탈락**했다 — 문의는 개인 건이라 묶으면 각자의 사정이 사라진다. D-027 이 후보 ①(이상거래)을 탈락시킨 것과 같은 사유다.

**기준을 세워둔 값어치가 여기서 나왔다.** 기준이 없었다면 "도메인만 갈아끼우면 된다"로 넘어갔을 텐데, 기준이 있으니 이 전환의 진짜 쟁점이 **"그룹핑을 포기하는 것"** 임을 처음부터 볼 수 있었다. 그룹핑은 D-004 가 "시스템의 척추"라고 부른 것이고 캐시·AI 절감률·인덱스 A/B·UNIQUE 재시도가 전부 거기 매달려 있었다.

### 결정 — D-031

| 쟁점 | 결정 |
| --- | --- |
| 기준 1 탈락 | **인정하고 그룹핑을 포기.** 기준 1 의 적용 조건을 "그룹핑을 하는 설계의 전제"로 정정 |
| 빈 「캐시」 자리 | **정규화 키 2단 절감 경로** — 캐시(1단) → DB(2단) → AI. 판정은 문의마다 하고 **AI 호출만 재사용** |
| 감사 샘플링 | 팀 US 9개에 없었으나 **US-10 으로 추가.** 프로젝트 차별점이자 성공 기준 2·3 의 근거 |
| 기존 결정 로그 | **덮어쓰지 않고 상태 필드만 갱신.** 폐기 6 / 부분 개정 8 |

### 문서 전환 작업

| 문서 | 변경 |
| --- | --- |
| `DECISIONS.md` | **D-031 신설** + 폐기 6건(D-004·006·013·016·018·029) + 부분 개정 8건(D-001·007·011·014·015·023·027·028) + 「한눈에 보기」 표 갱신 |
| `CLAUDE.md` | 「도메인 컨텍스트」 전면 교체 · 불변 규칙 4→3 · 락 전략 3분할→1 · 캐시 전략에 2단 경로 · 계약 A/B/C 재정의 |
| `PRD.md` | 전면 재작성. 5테이블→3테이블, **카테고리 10종 경계표 신설**("원인이 아니라 조치" 규칙) |
| `API-CONTRACT.md` | v0.8 → **v1.0**. 8 endpoint → 7 + Actuator, 역할 3종 교체, `PATCH /api/policies/{category}` 삭제 |
| `GLOSSARY.md` | fingerprint·에러 그룹 삭제 → 정규화 키·2단 절감 경로·과도/과소 병합·조치 기준 분류 추가 |
| `LIFECYCLE-COVERAGE.md` | 측정 12개 → **11개**, P1 범위 재배정 |

### 이번 전환에서 잃은 것 (감추지 않고 기록)

1. **동시성 학습 자산** — D-007 의 3분할 중 원자적 UPDATE 와 UNIQUE 충돌 재시도가 함께 소멸. 낙관적 락 1개만 남았다
2. **측정 9** (카테고리별 임계값 대조군) — D-006 폐기와 함께
3. **인덱스 쓰기 비용 A/B** (측정 5ⓔ) — 대상 컬럼 `occurrence_count` 소멸

### 새로 생긴 최대 리스크

**정규화 키의 hit rate 가 0 에 수렴할 가능성.** CS 문의는 자연어라 완전일치가 드물다. 측정 6 에서 절감률이 유의미하지 않으면 「캐시」 필수 기능의 정당화가 무너진다 — D-031 재평가 조항의 발동 조건이다. 정규화를 조이면 이번엔 **과도 병합**(다른 문의가 한 키로 묶여 틀린 분류가 재사용됨)이 생기므로 양쪽을 다 테스트한다.

### 진행 메모 — US-9 가 자기모순이었다

팀 원안 US-9 는 *"큐 삽입이 실패하면 `inquiries` 원문 저장까지 롤백되는지 확인"* 이었는데, US-2 가 이미 *"`@Async` 로 문의 저장 트랜잭션과 분리"* 라고 했다. 분리했으면 접수는 그 시점에 커밋이 끝나 있으므로 **원안대로 롤백되면 고객이 이미 받은 접수 확인이 거짓말이 된다.**

AC 를 **"롤백 경계가 ②에서 끊기는지"** 로 뒤집었다 — ②는 롤백되고 ①은 생존해야 한다. 그러면 문의가 `RECEIVED` 로 방치되므로 **D-017(stuck 지표)이 다시 필요해졌다.** 폐기될 뻔한 결정이 도메인 전환으로 오히려 중요해진 경우다.

### 오후 — 병렬 PR 3건이 동시에 착지하며 생긴 일

| PR | 담당 | 결과 |
| --- | --- | --- |
| #8 서브에이전트 5종 + 헌법 가드 훅 | 김준현 | 머지 |
| #9 Sentry SDK + Source Context + MCP 연동 (실측) | 이용택 | 머지 |
| #10 계약 v0.8 + D-028·D-029 + 이 로그 신설 | 김준현 | 머지 |
| #11 도메인 전환 (문서 + 코드) | 김준현 | 진행 중 |

**이용택 (테스트·배포운영·P1)**

- **PR #9** — Sentry SDK + Source Context + MCP 연동 (실측 검증, 커밋 `05dba57`)
  - Sentry SDK 연동 — 에러 자동 캡처 / Source Context — 스택트레이스에 실제 소스 코드 표시 / Sentry MCP 등록
  - **D-030** — 예외 처리 시 캡처 규칙을 헌법(`CLAUDE.md`)에 명문화 + `SENTRY-GUIDE.md` 신설
  - *catch 해서 로그만 찍으면 Sentry 가 영구히 모른다* 는 것을 회귀 테스트(`BaselineSmokeTest`)로 고정
  - AI 리뷰 5차까지 반영

**결정 번호가 충돌했다.** PR #9 와 도메인 전환 작업이 **동시에 `D-030` 을 썼다.** 병렬 작업에서 결정 번호는 **선착순 자원**인데 잠금 장치가 없다 — `DECISIONS.md` 꼬리의 `<!-- 다음 결정 추가 시 D-NNN 부터 -->` 마커는 **자기 브랜치에서만 최신**이다.

해소 원칙: **먼저 트렁크에 들어간 번호가 이긴다.** develop 의 D-030(Sentry)은 이미 `CLAUDE.md` 예외 처리 규칙이 인용하고 있어 움직이면 머지된 기록을 고쳐야 한다. 아직 PR 상태인 도메인 전환 계열 4건이 비켰다.

```text
D-030 도메인 전환      → D-031      D-032 확정 답 재사용 → D-033
D-031 검토 항목 선점   → D-032      D-033 AI 값 검증     → D-034
```

넷을 통째로 민 이유는 도메인 전환만 뒤로 보내면 **그것을 원인으로 삼는 세 결정이 원인보다 앞 번호**가 되기 때문이다. 인용 146건을 문서 8종 + 서브에이전트 5종 + `constitution-guard.py` + `src/` 에서 일괄 치환했다. **develop 병합 전에** 밀었다 — 병합 후엔 두 개의 `D-030` 을 문맥으로 구분해야 해서 오치환 위험이 생긴다.

### 문서 PR 과 코드 PR 을 합쳤다

도메인 전환 문서와 `src/` 재작성이 **서로 다른 브랜치에서 병렬로** 진행됐다. 파일 겹침은 0건이었지만 **나눠서 머지하면 develop 이 "문서는 문의 / 코드는 에러"인 모순 상태**가 되고, 그 상태에서 **CI 는 green 이다** — src 끼리는 일관되기 때문이다. 모순을 잡을 장치가 없는 채로 트렁크가 굴러간다.

합쳐야만 `e2e.yml` 이 검증력을 갖는다 — 계약 v1.0 ↔ `V2` 스키마 ↔ 엔티티가 한 PR 에 있어야 `ddl-auto=validate` 부팅 검증이 *"전환이 성립하는가"* 를 증명한다. 덤으로 헌법 「작업 경계」의 "계약은 코드 PR 동반" 조건도 예외 없이 충족됐다 (PR #10 에서 한 번 쓴 예외를 두 번 연속 쓰지 않는다).

- `./gradlew test` **exit 0** — Flyway `V1` → `V2__domain_switch.sql` 적용 후 실제 MySQL 에서 엔티티 3종이 새 스키마와 일치함을 확인. **이 통과가 곧 "전환이 성립한다"는 증거다**
- 코드 브랜치는 8/3 기준이라 develop 보다 34커밋 뒤처져 있었다. cherry-pick 으로 얹으며 **PR #9 의 Sentry 자산 3곳(플러그인·설정·회귀 테스트)이 살아남았는지 확인**했다

### AI 리뷰가 처음으로 "틀렸다" — 사례 16

리뷰가 *"이 PR 에 V2 마이그레이션이 안 보인다 / 코드 0줄이니 다음 PR 에서 `ddl-auto=validate` 로 검증하라"* 고 했는데 **둘 다 이미 이 PR 에 있었고 검증도 통과한 뒤**였다.

원인은 hallucination 이 아니라 **입력 절단**이었다 — `ai-review.yml` 이 `head -c 200000` 으로 자르는데 이 PR 은 41파일/±3,300줄이고 `git diff` 는 경로 알파벳 순이라 **`src/` 가 뒤쪽에 몰려 통째로 잘려 나갔다.**

기록한 이유: 리뷰가 **못 본 것**을 **없는 것**으로 바꿔 말한 데 그치지 않고 그 위에 **행동 지시**까지 얹었다. 그대로 따랐다면 끝난 일을 다시 하거나 "아직 검증 안 됐다"고 믿고 머지를 미뤘을 것이다. **틀린 사실보다 그 사실에 근거한 지시가 더 멀리 간다.**

> 이 발견으로 **큰 PR 에서는 리뷰가 부분만 본다**는 것이 확정됐다. 다만 이번처럼 나누면 트렁크가 모순 상태가 되는 경우는 예외다 — **리뷰 품질과 트렁크 정합성이 상충하면 정합성이 우선**이다.

리뷰가 맞은 지적 2건은 반영했다: 계약 변경 이력에서 v1.1 이 v1.0 위에 있던 순서 붕괴, `final_category IS NOT NULL` 이 인덱스로 안 걸린다는 점(측정 5 케이스로 편입, 인덱스는 **측정 전까지 추가하지 않는다** — D-018 이 남긴 논거).

### 문서가 자기 잔여 목록을 과다 계상하고 있었다

`LIFECYCLE-COVERAGE.md` 가 `MONITORING.md` 를 "템플릿 상태"로 적고 있었는데 **PR #9 에서 이미 실측까지 채워진 뒤**였다. 잔여가 4건이 아니라 3건이었다. 남은 것을 세는 문서가 스스로 낡으면 **끝난 일이 계속 할 일 목록에 남는다** — 이번 정합 점검에서 정정했다.

### API-CONTRACT 재대조 — PRD·결정과 어긋난 6건

PAAR 정렬 작업(D-043~D-050)으로 PRD 가 크게 바뀌어서, **계약이 따라오지 못한 곳이 있는지** 다시 대조했다.

| # | 어긋난 곳 | 무엇이 | 언제 고치나 |
| --- | --- | --- | --- |
| **1** | §3 응답 예시의 `"model": "claude-haiku-4-5-20251001"` | **D-024 가 확정한 모델은 `claude-sonnet-5`** 다. `.env.example` 도 sonnet 이고, `build.gradle` 주석은 *"API-CONTRACT §3 예시는 claude-sonnet-5"* 라고 적고 있다 — **주석이 가리키는 값과 실제 계약 파일이 다르다.** v1.0 전면 개정 때 예시가 바뀐 것을 아무도 못 봤다 | `TRI-14`(AI 호출) PR |
| 2 | 변경 이력 v1.2·v1.3 의 PR 번호 `#12` | 실제 머지는 **PR #11**(`42d5d0d`) 이다. `#12` 는 그 시점에 없던 번호였고, 지금 #12 는 전혀 다른 문서 PR 이 차지했다 | 다음 계약 변경 PR |
| 3 | §4 의 「결정적 역산 / 확률적 추론」 | 용어를 **「계산으로 확실히 알아내기 / 짐작으로 알아채기」** 로 통일했는데 계약만 옛 말로 남았다 | `api/` PR |
| 4 | §7 `audit.autoAccepted.byConfidenceBucket` 의 *"이 프로젝트의 결론이 나오는 자리"* | **D-043 으로 결론은 8ⓐ-1(자동 확정 전수를 정답 레이블과 대조)** 이 됐다. 정답 레이블은 운영 DB 에 없으므로 **stats 가 보여주는 것은 8ⓐ-2(감사가 그중 몇 건을 잡았나)** 다. 둘은 다른 숫자다 | `api/` PR |
| 5 | §6 `GET /api/policies` 에 `classification.reuse.enabled` 가 없음 | D-043 ⓒ 로 기동 시 고정 설정값이 하나 늘었다. **이걸 껐다 켜면 `aiCallSavings` 와 `cache` 수치가 통째로 달라지는데, 어느 상태에서 나온 값인지가 응답 어디에도 없다.** D-028 이 *"`configuredSampleRate` 를 내보내면서 그 값을 어디서 정하는지가 계약에 없는 상태가 더 나쁘다"* 고 한 것과 같은 논리다 | **제안 — 새 결정 항목 먼저** |
| 6 | `ONBOARDING.md`·`LIFECYCLE-COVERAGE.md` 가 계약을 **v1.1** 로 표기 | 실제는 **v1.3** | **이 PR 에서 정정** ✅ |

**1번이 가장 위험하다.** 나머지는 읽는 사람이 헷갈리는 수준이지만, 이건 **계약대로 구현하면 D-024 의 모델 선정 근거가 무너진다.** D-024 가 sonnet 을 고른 이유는 *"상위 모델은 측정 8 이 재려는 「AI 가 자신 있게 틀리는 빈도」의 표본이 귀해진다"* 였고, 하위 티어로 내려가면 그 판단을 아무도 다시 하지 않은 채 정확도만 달라진다. **모델 ID 는 측정 결과에 붙는 조건**이라 evidence 해석이 통째로 흔들린다.

**1~5 를 이 PR 에서 고치지 않은 이유**: 헌법 「작업 경계」가 *"`API-CONTRACT.md` 는 코드 PR 에 동반 변경만 허용"* 이다. 이 문서 위쪽에 이미 적어둔 대로 **PR #10 에서 한 번 쓴 예외를 두 번 연속 쓰지 않는다.** 게다가 1·3·4 는 `api/` 착수 PR 이 어차피 그 절을 건드리므로, **그때 고치는 편이 "계약과 구현이 같은 PR 에서 맞춰진다"는 규칙의 원래 목적에도 맞다.**

⚠️ **6번만 이 PR 에서 고쳤다.** 그 둘은 계약 파일이 아니라 **계약을 가리키는 다른 문서**라 작업 경계에 걸리지 않는다.

---

## 2026-08-06 (목) — `service/` 가 처음 열렸다

### 팀 공통

- **8/4 blocker 1번이 해소됐다** — *"`service/`·`api/` 가 여전히 0줄"* 이던 상태에서 셋이 같은 날 각자 첫 코드를 올렸다. P1 은 정규화·마스킹, P2 는 AI 호출 뼈대, P3 는 리뷰 도구 문서
- 운영진이 팀별 PAAR 결승선 문서를 추가 (`c96b30e`, PR #13)

### 개인

- **김준현 (기획·P2)**
  - **PR #16** — AI 분류 호출 뼈대 (TRI-75, TRI-46, TRI-48)
    - `build.gradle` 에 빠져 있던 의존성 2개(Spring Security, Anthropic SDK) + 부팅용 `SecurityConfig` 골격
    - `service/AiClassificationService` + 프롬프트 — 페르소나·목표·형식·제약 4요소를 프롬프트에 명시
    - API key 는 환경변수로만 (`AnthropicProperties`), 코드·설정 파일 하드코딩 0
    - **D-051 신설** — AI 에게 「생각하기」를 끈다
  - **PR #15** — 정답 50건에 남은 일을 「2차 레이블 하나」로 좁혀 적음. `GLOSSARY.md` 에 **「2인 독립 레이블」** 용어 신설
- **이용택 (테스트·배포운영·P1)**
  - **PR #18** — 개인정보 마스킹·정규화 키 (TRI-37, TRI-39, TRI-38)
    - `ContentMasker`(TRI-37) — AI 전송·응답 출력이 같이 쓰는 단 하나의 마스킹 구현 (D-040)
    - `NormalizedKeyGenerator`(TRI-39, **D-052**) — AI 호출 절감용 조회 키. 마스킹 문자열의 SHA-256
    - 마스킹 단위 테스트(TRI-38) + AI·CodeRabbit 리뷰 3회 반영 → **D-053**(키 전용 토큰 + 유니코드 접기)
  - **PR #17** — 측정 3·11·속도 b 책임자를 Jira 배정 기준으로 정정
- **김은빈 (리뷰·P3)**: **PR #14** — CodeRabbit 병행 도입(**D-054**) 문서 반영 + GitHub App 설치. **P3 코드는 이날도 미착수**(다음 날 새벽 착수)

### D-051 이 왜 결정 항목이 됐나 — 기본값에 맡기면 아무도 고른 적 없는 조건으로 측정이 나온다

`AiClassificationService` 를 만들며 요청에 한 줄을 더 넣어야 했다. 요즘 모델은 답하기 전에 **혼자 생각하는 단계**를 켜고 끌 수 있는데, D-024 의 확정값 표(SDK·모델·키 이름·JSON 강제 방식)에 이 항목이 **없었다.**

껐다. 이유 두 개 중 **두 번째가 결정적**이다.

1. 답이 `{"category": ..., "confidence": ...}` 한 줄이라 길게 생각할 것이 없다. 생각하는 동안 분류 담당 스레드가 묶인다 (D-047)
2. **생각을 시키면 이 프로젝트가 재려는 표본이 사라진다.** 측정 8ⓐ 는 *"AI 가 확신하면서 틀린 비율"* 인데, 정확해질수록 그 사례가 귀해진다

> 실패를 관찰하겠다고 선언한 시스템이 실패를 줄이는 장치를 먼저 켜면 앞뒤가 맞지 않는다. structured outputs 를 쓰지 않기로 한 D-024 와 **정확히 같은 논리**다. 다만 이건 "분류를 잘하게 만들지 않겠다"가 아니라 **"정확도를 올리는 선택은 표본을 깎는 대가와 함께 기록한다"** 는 뜻이다.

### 프로세스 구멍이 하루 만에 재현됐다 — 결정 번호 충돌 2회차

8/5 기록의 마지막 줄에 *"프로세스 구멍 1건 — 결정 번호에 잠금이 없다"* 고 적어뒀는데, **바로 다음 날 같은 일이 났다.**

```text
11:49  김은빈  34440b6  CodeRabbit 도입      → D-051
11:54  김준현  e1a29ad  AI 생각하기 끄기      → D-051     ← 5분 차이
```

이번엔 **CodeRabbit 쪽이 D-054 로 비켰다** (`1191c84`). 8/5 의 원칙("먼저 트렁크에 들어간 번호가 이긴다")과 방향이 반대인데, 이날은 둘 다 아직 머지 전이라 **선착 기준이 적용될 상태가 아니었다.** 인용이 적은 쪽이 비키는 것으로 그때그때 처리했다.

> **두 번 났다는 것은 우연이 아니라는 뜻이다.** 그런데도 이날 절차를 못 고쳤다 — 다음 날 세 번째가 난다.

---

## 2026-08-07 (금) — P2 값 검증 완성 · 이름과 경계 정리

### 팀 공통

- **P3 가 드디어 코드에 붙었다** (04:41). 8/4·8/5 두 번 기록한 blocker "김은빈 코드 기여 0" 이 해소
- **하루에 PR 6건 머지(#19~#24).** `config/`·`api/`·`service/` 가 같은 날 함께 움직였다
- **Lombok 도입** (D-057, 이용택) — 허용하되 `@Data`·`@Setter` 는 `lombok.config` 가 **컴파일 에러로** 막는다

### 개인 — 김준현 (P2)

| PR | 내용 | 티켓 |
| --- | --- | --- |
| **#21** | 분류 담당 클래스명을 `AiClassifyWorker` → `service/event/InquiryReceivedEventListener` 로 개명 (**D-058**) + 용어집에 「분류」 항목 신설 | TRI-47 준비 |
| **#22** | 2차 레이블용 blind 시드 + 사람별 답안지 분리 — 정답이 새는 경로 4개 차단 | TRI-80 |
| **#23** | **AI 응답 파싱 + 값 검증 4종 + 실패 사유 5종 구조화 로그** + 계약 **v1.5** | TRI-49, TRI-50 |
| **#24** | AI 관련 6개를 `service/ai/` 로 이동 — 패키지는 개수가 아니라 경계로 나눈다 (**D-059**) | — |

- **이용택 (테스트·배포운영·P1)**
  - **PR #20** — 문의 접수 경로: 트랜잭션 ① + `POST /api/inquiries` (TRI-31, TRI-32, TRI-33)
    - 트랜잭션 ①(`InquiryIngestService.receive`) — 저장 후 `AFTER_COMMIT` 이벤트 발행, ②와 분리
    - `POST /api/inquiries` — 202 Accepted, `normalizedKey` 미노출 + ①②분리 통합 테스트
    - Lombok 도입(**D-057**) — `@Data`·`@Setter` 는 `lombok.config` 가 컴파일 에러로 차단
  - **PR #27** — 2차 독립 레이블 정답 50건 (TRI-80). 아래 「2차 독립 레이블」 참조
- **김은빈 (P3)**: **PR #19** — 역할 기반 권한 필터 + 공용 예외 처리 지점 (TRI-25~30, **D-055**·**D-056**)

### TRI-49·50 — 값 검증을 「기준값 비교 앞」에 둔다는 것을 코드 순서로 드러냈다

**문제**: AI 가 형식은 지키면서 값을 틀릴 수 있다. `{"category": "DELIVERY", "confidence": 1.5}` 는 JSON 으로 멀쩡하다.

**왜 걸러야 하나**: 그냥 두면 `1.5 >= 0.8` 이라 자동 확정되는데, 신뢰도 구간별로 셀 때 `1.5` 는 어느 구간에도 안 들어간다. **자동 확정되면서 동시에 집계에서 사라져** 측정 8ⓐ 의 분모가 줄어든다.

**어떻게 확인되게 만들었나** — 검사 4종을 파서에 넣고, **파서가 기준값을 아예 모르게** 했다. 임계값 비교는 트랜잭션 ②(TRI-51)에만 있으므로 *"검증이 비교보다 앞"* 이 문서 약속이 아니라 **import 관계**로 고정된다.

- `AiParsedClassification` 은 record 생성자에서 **통과한 결과와 실패를 동시에 들 수 없게** 막는다. `FAILED` 는 `category`·`confidence` 가 둘 다 null (D-022)
- 범위 밖 값을 clamp 하지 않는다 — `1.5 → 1.0` 으로 자르면 최상위 구간에 "AI 가 1.0 이라 신고한 건"과 "잘라낸 건"이 섞인다
- 실패 사유 5종은 **구조화 로그로만** 남긴다. 컬럼도 enum 도 늘리지 않는다 (계약 B·D-022)
- 계약 **v1.5** 를 같은 PR 에 동반 — 8/5 기록의 「API-CONTRACT 재대조」 1번(§3 모델 id 가 `claude-haiku-4-5-20251001` 로 적혀 있어 D-024 확정값 `claude-sonnet-5` 와 어긋남)을 여기서 닫았다

### 리뷰가 잡은 것 3건 — 전부 「막았다고 생각한 것이 안 막혀 있었다」

| 잡힌 것 | 무엇이 뚫려 있었나 | 커밋 |
| --- | --- | --- |
| **절반만 채워진 실패가 생성자를 통과** | 가드가 「종류·확신도가 둘 다 있는가」와 「사유가 있는가」를 비교하는 방식이라, 「둘 다 있지는 않다」와 「둘 다 없다」를 같은 것으로 셌다. `(DELIVERY, null, OUT_OF_RANGE)` 조합이 통과해 **`FAILED` 인 건이 종류를 들고 ②로 넘어간다** — D-022 가 막으려던 바로 그 상태 | `46b1c17` |
| **blind 시드에 원본 id 가 남음** | 2차 레이블용 파일에서 정답만 지웠는데, **id 만으로 정답 50/50 이 역산**됐다 | `5a42957` |
| **한 파일에 둘이 적으면 독립이 깨짐** | 2인 독립 레이블인데 답안지가 하나였다 | `55b83d9` |

> 셋 다 성격이 같다 — **차단 장치를 만들어놓고 그 장치가 실제로 차단하는지 확인하지 않았다.** blind 규칙(D-010)이 계약 검토에서 두 번 걸렀던 항목인데 시드 파일에서 다시 뚫린 것은, 규칙이 적용되는 자리가 문서마다 새로 생긴다는 뜻이다.

### 2차 독립 레이블 — 정답이 한 사람 손에서 나오지 않게 (TRI-80, 이용택)

**문제**: 정답 세트가 한 사람 판단이면, 측정 8ⓐ-1(자동 확정을 정답 레이블과 대조)에서 *"AI 가 틀린 것"* 과 *"레이블러가 다르게 본 것"* 이 안 갈린다. 정답이 흔들리면 오분류율에 검토자 불일치가 섞인다.

**어떻게**: PR #22 가 만든 blind 답안지(1차 초안이 안 보이는 시드)에, 두 번째 사람이 **본문만 보고** 정답을 따로 붙였다.

- **PR #27** (커밋 `b7e7543`) — `inquiries-50-label-2nd-yongtaek.csv` 50행의 `label` 칸을 채웠다 (`+50/−50`). `InquiryCategory` 10종 안, 빈 칸 0. 1차 파일(`...-1st-junhyun.csv`)과 `content`·`channel` 은 완전히 같고 **라벨만 독립적으로 다르다** — 같은 본문에 두 사람이 각자 붙였음이 확인된다

**아직 안 끝났다 — TRI-80 은 진행 중**: 두 사람 일치율을 `evidence/` 에 적고(측정 12 예비값), 갈린 건을 §7 경계표로 조정한 뒤 확정본 `inquiries-50.csv` 를 만드는 단계가 남았다. 셋이 함께 하는 자리라 이 PR 은 그 앞 단계인 **2차 레이블링까지**다. 일치율이 낮게 나오면 Day 5 이전에 §7 을 고칠 시간이 생긴다는 게 이 순서의 목적이다.

### 결정 번호 충돌 3회차 — 이번엔 이름이 겹쳤다

```text
12:41  김준현  202d8f5  분류 담당 개명       → D-057
12:48  이용택  74ac6d9  Lombok 규칙         → D-057     ← 7분 차이
```

PR #20 이 먼저 머지되며 **Lombok 이 D-057 을 가져갔고**, 개명 건이 **D-058** 로 비켰다 (`b6ad081`). 8/5 의 원칙("먼저 트렁크에 들어간 번호가 이긴다")이 이번엔 그대로 적용됐다.

**사흘 동안 세 번이다.** 8/5 에 후보로 적어둔 해법(번호를 PR 번호에 종속시키기 / 결정 신설 전 `origin/develop` 확인)을 아직 아무도 안 넣었고, 그 대가가 매번 renumber 다. **기록만 하고 절차를 안 고치면 같은 일이 계속 난다** — 이것이 이 로그가 스스로 증명한 것 중 가장 뼈아픈 항목이다.

### D-058·D-059 — 이름과 패키지를 코드 짜기 전에 정한 이유

둘 다 **아직 안 짠 코드**에 대한 결정이다. 짜고 나면 코드 + 테스트까지 따라오므로 지금이 가장 싼 시점이다.

- **D-058** — `Worker` 로 끝나는 파일 하나가 계층에 안 잡힌다는 지적에서 시작했는데, `Service` 로 통일하면 `AiClassificationService` · `AiClassifyService` · `ClassificationService` **셋이 한 글자 차이로** 늘어선다. 자동완성에서 잘못 고르면 **비동기 진입점을 직접 호출**하게 되고, 그러면 트랜잭션 ①②가 한 덩어리로 붙어 D-031 이 금지한 상태가 된다. **이 클래스를 잘못 쓰는 방법은 하나뿐이고, 그 하나를 이름이 막는다**
- **D-059** — `service/` 가 9개가 됐고 최종 17개가 된다. **개수를 기준으로 삼으면 규칙이 서지 않아서** 「무엇을 다루는가」로 나눴다. `service/ai/` 안쪽은 전부 **외부가 준 값**을 다루고, 밖으로 나가는 것은 `AiParsedClassification` 하나다 — *"AI 가 스스로 신고한 신뢰도를 무조건 믿지 않는다"* 는 뿌리 규칙이 패키지 경계로 드러난다
  - ⚠️ **이 경계에는 강제력이 없다.** `service/event/` 는 리스너를 package-private 으로 만들어 컴파일러가 막지만, `ai/` 는 전부 public 이라 못 막는다. 얻는 것은 **찾기 쉬움과 소유가 보이는 것뿐**이고 그 한계를 `package-info.java` 에 적어뒀다

### AI 리뷰가 두 번째로 "틀렸다" — 사례 17

반올림 방식을 고정하는 테스트에 **`0.9315` 를 쓰라**는 제안이 왔는데, 그 값으로는 `HALF_UP` 과 `HALF_EVEN` 이 **똑같이 `0.932`** 가 나와 방식을 구분할 수 없다. `0.9325` 로 바꿔야 셋(`HALF_UP` 0.933 / `HALF_EVEN` 0.932 / 버림 0.932)이 갈린다.

**사례 16 과 성격이 다르다.** 16 은 입력이 잘려서 못 본 것을 없다고 했고, 이번엔 **입력을 다 보고도 계산이 틀렸다.** 지적의 방향("반올림 방식을 테스트로 고정하라")은 맞았고 제시한 값만 틀려서, 그대로 받았으면 **테스트가 통과하면서 아무것도 보장하지 않는 상태**가 됐다 — 가장 알아채기 어려운 실패다.

---

## 2026-08-08 (토) — 조회 경로·마스킹 일치 머지 (P1 스토리 2건 완료)

### 팀 공통

- develop 이 P2 계열(비동기 분류 리스너 PR #30 등)로 20커밋 전진한 뒤, 그 위에서 **P1 조회 경로(#34)** 와 **마스킹 두 경로 일치 테스트(#36)** 가 머지됐다
- **완료된 계층**: 에픽 **TRI-2**(문의 접수) · 스토리 **TRI-10**(자기 문의 조회) · 스토리 **TRI-11**(개인정보 가리기). 하위 5건(TRI-34·35·36·88·38)
- **오늘은 새 `D-NNN` 결정이 없다.** 사흘간 세 번(D-030·051·057) 났던 결정 번호 충돌이 이날은 없었다 — 작업이 전부 구현·테스트였고, 마주친 설계 판단(아래 저장소 충돌)이 **이미 헌법에 정해져 있어** 새 항목을 안 만들었다

### 개인 — 이용택 (P1)

- **PR #34** — 문의 조회 경로 (TRI-10·34·35·36·88 일괄)
  - `GET /api/inquiries` 목록 + `GET /api/inquiries/{id}` 상세. **조회 범위를 서버가 강제**(D-038 — 저장소 메서드가 소유자를 필수 인자로 받아 "안 넣으면 전체 조회"라는 실패 방향을 없앰), **역할별 `confidence` 표기**(D-039 — 필드 부재 vs `null` 을 sealed 타입으로 구분), 본문 마스킹(D-040)
  - **TRI-88** — 저장소를 `JpaRepository` → `Repository` 마커로 바꿔 `findById`·`findAll` 등 소유자 없는 조회를 상속에서 제거. 가드 테스트로 회귀 고정
  - **TRI-36** — 목록 인덱스는 `V2` 에 이미 있어 V3 를 만들지 않고 EXPLAIN 실측만(측정 5). 발견 F(바인드 인덱스)·G(고객 목록 무인덱스)는 측정 b(TRI-74)로 넘김
  - 리뷰 재검증 중 **`raw_response`(프롬프트·PII) 미노출 회귀 테스트** 추가, CodeRabbit 반영으로 조회 테스트 격리 2건(전역 상태 의존 제거)
- **PR #36** — 마스킹 두 경로 일치 통합 테스트 (TRI-38)
  - `MaskingConsistencyIT` — 같은 원문에 대해 ① AI 로 가는 문자열(리스너의 `classify()` 인자)과 ② 응답 본문이 **같은 `ContentMasker` 빈**으로 가려지고 **동일한지** + 실제로 가려졌는지를 함께 단언. (1) 동일성만 보면 "둘 다 깜빡한 경우"도 통과하므로 (2) 실제 가림을 함께 본다
  - 이 검증은 **두 경로 호출부가 develop 에 다 온 뒤**에야 가능했다 — ① PR #30(리스너), ② PR #34(응답). 8/6 코멘트에 "두 경로 부재로 추후 완료"로 남겨둔 것을 여기서 닫았다
  - 부수 효과: `evidence/failure-cases.md` 「미검출 위험」의 *"개인정보 마스킹이 AI 호출 전에 실제로 걸리는지"* 행이 이 IT 로 닫혔다

### develop 병합 충돌 — 헌법이 미리 정한 해법을 처음 적용했다

PR #34 를 올린 뒤 develop 이 **PR #30(비동기 리스너)** 으로 전진하며 저장소 2개가 충돌했다.

- **`InquiryRepository`** — PR #30 의 `ClassificationService`(트랜잭션 ②)가 `inquiryRepository.findById(inquiryId)` 로 **소유자 없는 로드**에 의존하는데, TRI-88 이 바로 그 `findById` 를 마커로 제거했다. 정면 충돌처럼 보이지만 **이 상황은 TRI-88 이 애초에 예상하고 해법까지 적어둔 것**이다 (`CLAUDE.md`·저장소 javadoc: *"P2/P3 가 소유자 없는 로드가 필요하면 `JpaRepository` 복귀 대신 용도를 드러낸 이름으로 새 메서드를 더한다"*)
  - → **마커 유지** + `findByIdForClassification`(용도가 이름에 드러난 owner-less 로드) 신설 + 호출부(`ClassificationService` 1곳 + 그 테스트 9곳)를 새 이름으로 이관. develop 의 `transitionFromReceived`(쓰기/전이)는 그대로 수용
  - **새 결정 항목을 안 만든 이유**: D-045(1) 이 이미 그 규칙을 명문화했다. 이 충돌 해소가 그 규칙의 **첫 실적**이다
- **`InquiryClassificationResultRepository`** — 같은 조회를 양쪽이 이름만 다르게 추가(`findByInquiry_Id…` vs `findByInquiryId…`). develop 것을 채택하고 중복 제거
- **머지 전 사전 확인**: PR #34 를 먼저 넣으면 **PR #35**(feat/p2-classify-retry, 김준현 TRI-54)가 충돌하는지 물어와, 두 PR 의 변경 파일이 disjoint 임을 확인하고 `merge-tree` + 실제 병합·컴파일·206 테스트로 "충돌 없음"을 증명해 보고했다

### AI 리뷰가 세 번째로 "틀렸다" — 사례 19 (두 봇이 **같은 허구**를 만들었다)

PR #36 에서 Claude 리뷰와 데브캠프 봇이 **함께** *"`contains("[이메일]")` 단언을 추가하라"* 고 제안했다. 그런데 `ContentMasker` 에 **`[이메일]` 토큰은 없다** — 이메일은 전화와 같은 `[연락처]` 로 가려진다. 그대로 넣었으면 그 토큰이 응답에 없어 **테스트가 빌드째 깨진다.**

- 밑에 깔린 지적(이메일·전화가 같은 `[연락처]` 라 이메일 마스킹이 **독립적으로** 검증되진 않는다)은 절반 맞지만, 그 커버는 `ContentMaskerTest` 가 이미 한다. **방향은 맞고 해법이 허구다** (사례 17 과 같은 함정)
- 사례 16(입력 절단)·17(계산 오산)과 다른 점: 이번엔 **코드베이스의 사실(토큰 목록)을 확인하지 않고 지어냈고**, **독립적인 두 봇이 같은 오답**을 냈다 — "여러 봇이 동의함"이 정답의 근거가 못 된다 (`failure-cases.md` 사례 19·관찰 8)
- 미반영하고 머지. 봇이 잘 본 것(2겹 단언 설계·동기 직접구성·같은 빈 검증)은 세 봇 모두 칭찬했다

### Jira 반영 (MCP)

- **완료 전이 8건**: 에픽 TRI-2 / 스토리 TRI-10·TRI-11 / 하위 TRI-34·35·36·88·38. 각 티켓에 완료 근거 코멘트(TRI-34·35 엔 재검증·`JdbcTemplate` 시드 근거도)
- **계층 규칙**: 하위가 모두 끝난 스토리만 닫고, **미완 하위가 남은 상위는 건드리지 않았다** — 에픽 TRI-3(AI 호출 절감)은 캐시·재사용(TRI-40·41·71 등)이 남아 `할 일` 유지. 사례 18 이 경고한 "끝난 것처럼 보이게 만들지 않기"를 그대로 적용

### 8/12 마감 캐시·측정 업무를 10~12일로 분배 (의존 우선)

여유 시간이 생겨 P1 의 8/12 마감 하위작업 10개를 3일로 나눴다 — **우선순위보다 의존관계를 먼저** 봤다. 내부 체인은 `캐시(40) → 재사용(41) → (인덱스·테스트·지표 42·43·70) → 측정(71)` 이다.

- **8/10**: TRI-40(1단 캐시·계약 C) · TRI-41(2단 DB 조회) · TRI-84(원자적 put·메모리 상한)
- **8/11**: TRI-85(키버전·장애내성) · TRI-42(인덱스·1순위 EXPLAIN 5ⓓ) · TRI-43(과도/과소 병합 테스트) · TRI-70(지표 배선)
- **8/12**: TRI-71(측정 6·11) · TRI-45(정정 반영 측정 11) · TRI-74(E2E·속도 a·b)

> 에픽 TRI-3·스토리 TRI-12·13·23 은 컨테이너라 8/12 유지(하위 완료 시 닫힘).

**사람 간 조율 — 이게 이 로그에 남기는 이유 (Jira 날짜만 봐선 안 보인다)**

1. **TRI-40(계약 C 값 구조)을 8/10 에 끝내는 것이 김준현 TRI-53(②커밋 후 캐시 저장)·김은빈 TRI-44(③확정 후 캐시 덮어쓰기)의 전제다** — 둘 다 8/12 이고 이 캐시 값 구조·put 규격 위에 올라탄다. 계약 C 는 세 담당자 합의값이라 **P1 이 먼저 확정해야 둘이 안 막힌다**(TRI-84 원자적 put 규격도 여기 포함). 8/10 완료 시 두 사람에게 공유한다.
2. **TRI-45(내 8/12)는 김은빈 TRI-44(같은 8/12)에 same-day 의존** — 가장 리스크. TRI-44 가 돼야 "정정이 다음 문의에 반영되는지"를 시나리오로 검증 가능하므로 **8/12 오후, TRI-44 완료 후**로 순서를 잡는다.
3. TRI-74(E2E)는 김은빈 ③(TRI-59, 8/10) 필요 — 8/10 완료라 8/12 엔 안전. 속도 a(접수)·b(목록)는 이미 가능하고 E2E 전체 시나리오(투입→격리→확정)만 ③ 대기.

### 다음

- **측정 b (TRI-74)** — 고객 목록 p95 + `(customer_id, received_at)` 인덱스 추가 여부 + `(:param IS NULL OR …)` 래퍼의 바인드 인덱스 실측(발견 F·G). PR #34 evidence 에서 넘어온 잔여
- (선택) 서비스 계층이 응답 DTO 를 조립하는 것이 3계층 *"api/ — DTO 변환까지"* 문구와 표면상 충돌 — CodeRabbit 지적. 코드 문제는 아니고 `DECISIONS.md` 한 줄로 예외를 못박을지 팀 확인 대기

---

## 2026-08-09 (일) — stuckReceived 지표 + ②롤백 측정 3 (P1, 스토리 TRI-24 완료)

### 팀 공통

- develop 최신화 후 P1 이 관측 지표 `stuckReceived` 를 구현하고 **측정 3(②롤백)을 실측으로 닫았다.** 부모 스토리 **TRI-24(US-9)** 완료 — 하위 **TRI-72**(구현)·**TRI-73**(측정).
- **새 `D-NNN` 결정 없음** — 마주친 판단(설정 주입 방식·gauge 단일 출처·트랜잭션 경계)이 전부 이미 헌법/DECISIONS 에 정해져 있었다. 8/8 에 이어 이틀 연속 결정 번호 충돌이 없다.

### 개인 — 이용택 (P1)

- **PR #39** — stuckReceived 지표 + 측정 3 (TRI-72·73)
  - **TRI-72** — 접수 후 `monitoring.stuck-received-threshold`(기본 10분) 이상 `RECEIVED` 에 머문 문의 수를 `GET /api/stats` 의 `classification.stuckReceived` + Actuator gauge `triage.inquiries.stuck_received` 로 노출. **gauge 와 stats 는 같은 `StatsService` 를 읽어** 두 창구가 다른 값을 못 낸다
  - 설정은 `@Value` 가 아니라 `MonitoringProperties` record(`@Validated`)로 받아 **0 이하 임계값은 기동 차단** — `ClassificationProperties` 관례. 잘못된 값을 문의가 들어온 뒤가 아니라 기동 시점에 막는다
  - **stuck 판별은 저장된 플래그가 아니라** `status=RECEIVED` + `received_at` 두 컬럼 + 현재 시각으로 매 조회 COUNT 한다 — stuck 여부가 "시간에 상대적"이라 저장하면 곧 낡는다(D-048 계보)
  - **TRI-73(측정 3)** — 검토 큐 저장소를 `@MockBean` 으로 실패시켜 ② 트랜잭션 전체를 롤백. 실 MySQL 로 ①결과 롤백·②원문 잔존·③`RECEIVED` 유지·④`Clock` 목으로 임계 뒤 `stuckReceived` 0→1 확인. `evidence/measurement-3-rollback.md`

### 왜 stuckReceived 인가 — ①/② 분리의 대가를 계기판에 띄운다

- ①(접수)과 ②(분류 저장)는 **분리된 트랜잭션**이라 ②가 실패해도 문의 원문은 산다(D-031). 그 대가로 문의가 `RECEIVED` 로 방치되는데 **아무도 다시 분류하지 않는다** — 이벤트 신호가 메모리에만 있어 ②가 완주 못 하면(롤백·프로세스 死) 재시도의 근거가 사라지기 때문. 못 고쳐도(「나중에 할 것」 E) 보이게는 만든다(D-017).
- 측정 3 이 증명한 것 둘: (1) **②롤백에도 ① 생존** = 트랜잭션 분리가 실제로 지켜짐, (2) **방치된 문의가 임계 뒤 stuck 으로 잡힘** = 지표가 실제로 유실을 포착.
- ⚠️ **"AI 분류 실패"는 stuck 이 아니다** — 파싱 깨짐·저확신은 ②가 정상 완주해 `UNCLASSIFIED` 로 전이 + 큐 삽입된다. stuck 은 오직 ②가 **완주 못 한** 경우다.

### 셀프리뷰에서 남긴 관찰 (범위 밖, 기록만)

- `/actuator/metrics/**` 가 인증된 **아무 역할이면 접근**된다(CUSTOMER 포함) — 기존 actuator 보안 태세이고 TRI-72 가 만든 게 아니다. 집계값이라 민감도는 낮지만, MANAGER 로 조이려면 별도 `DECISIONS` 항목 사안.
- **측정 3 의 나머지 절반**(감사 삽입을 `REQUIRES_NEW` 로 뺐을 때 표본이 몇 건 새는지 대조, D-045②)은 감사 샘플링 경로(TRI-64·65)가 미착수라 그때 잰다.

### Jira 반영 (MCP)

- 상태 전이: **TRI-72·73·24** 각각 `진행 중 → 검토 중`. 완료 근거 + 커밋 링크 코멘트.
- **계층 규칙**: 스토리 TRI-24 는 하위 둘이 다 끝나 `검토 중` 으로 올렸으나, 위 감사 대조 잔여가 있어 **완료(Done)로는 닫지 않았다** — 사례 18("끝난 것처럼 보이게 만들지 않기")을 그대로 적용.

### 다음

- **docs PR (`docs/stuck-received-coverage`)** — `LIFECYCLE-COVERAGE` 관측 「지금 상태」 갱신 + 이 로그 항목. ⚠️ 코드 PR **#39 뒤에(또는 함께)** 머지 — 먼저 머지되면 develop 이 코드 없이 "구현됨"을 주장하게 된다.
- 측정 3 의 감사 대조는 김준현 TRI-64·65(감사 샘플링) 착수와 물린다 — 그 경로가 오면 이어서 잰다.

---

## 2026-08-10 (월) — 확정 트랜잭션 마무리·동시성 실측 (P3) + 측정 4 실측·develop 복구 (P2) + 2단 절감 경로 공급 측 (P1, PR #42)

### 팀 공통


- **develop 이 컴파일되지 않는 상태였다가 복구됐다** (PR #43). 각각 통과한 PR #35·#36 이 합쳐지면서 깨진 것이라 **세 사람 모두 push 가 막혀 있었다.** 자세한 것은 아래 개인 항목과 `evidence/failure-cases.md` 사례 20.
- **새 `D-NNN` 결정 없음** — 사흘 연속. 오늘 마주친 판단(측정 방식·캡처 지점·복구 범위)이 전부 D-030·D-047·D-022 에 이미 정해져 있었다.

### 개인 — 김은빈 (P3)


- **PR #41** — `ReviewService.confirm`(TRI-59) + `PATCH /api/inquiry-review-queue/{id}`(TRI-60) + 불변 규칙 1 테스트(TRI-61) + 409 두 종류(TRI-62) + 동시 확정 경합 실측(TRI-63)
  - **TRI-59** — 큐 상태 검사 + `final_category` 기록(`category` 는 안 덮음) + 큐 `RESOLVED` 전이 + `Inquiry` `CLASSIFIED` 전이를 한 트랜잭션으로. 불변 규칙 1·2 를 도메인 메서드(`resolve`·`recordFinalCategory`·`confirmByAgent`)로 강제
  - **TRI-60** — 컨트롤러는 인증 정보 추출 + 위임 + DTO 변환까지만. blind 규칙(D-010) 준수, `matched` 는 `suggestedCategory` 가 없으면 `false` 가 아니라 `null`(D-022)
  - **TRI-61** — `InquiryDetailControllerTest`(TRI-35, 이용택 소유 파일)에 케이스 1개 추가해 상세 조회에서 `category`·`finalCategory` 가 함께 노출되는지 실제 응답 로그로 확인
  - **TRI-62** — 별도 코드 없이 TRI-59 코드 + TRI-63 테스트로 충족. 컨트롤러 레벨에서도 두 코드(`ALREADY_RESOLVED`/`CONCURRENT_UPDATE`) 배선을 각각 테스트로 고정
  - **TRI-63(측정 7ⓑ)** — `CyclicBarrier(2)` 로 두 상담원 스레드를 `confirm()` 호출 직전까지 동기화, 매 트라이얼 새 `PENDING` 항목으로 40회 반복. **실측: Windows 3회 + WSL2 Ubuntu(Linux 커널) 1회, 총 4회 실행 모두 `CONCURRENT_UPDATE` 40/40 재현**, `ALREADY_RESOLVED`·예상 밖 결과 0건. `CONCURRENT_UPDATE` 가 0건이면 그 자체로 테스트가 실패하도록 assertion 을 걸어 무효 재현을 차단. `evidence/concurrent-review-confirm.md`

#### 리뷰 반영 (직접 리뷰 + CodeRabbit, 9건 검증)

- **반영 6건**: `attemptConfirm` 의 `InterruptedException` 처리 분리(인터럽트 상태 복원) · 깨진 `{@link #TRIALS}` javadoc 참조 수정 · `SUCCESS+CONCURRENT_UPDATE==trials*2` 과도한 assertion 제거(TRI-63 완료 조건인 "0 이 아니다"보다 더 엄격했음) · `ReviewQueueControllerTest` 에 `CONCURRENT_UPDATE` 컨트롤러 전파 테스트 추가 · `ReviewQueueConfirmIT` 신규(목 없이 실제 DB로 PATCH 를 끝까지 태워 `ReviewConfirmResponse.from()` 의 LAZY 로딩 안전성 주장을 실증 — 작성 중 이 테스트 자체의 버그(`LazyInitializationException`)를 발견해 수정) · evidence 에 `Inquiry` 가 `@Version` 없이도 지금은 안전한 이유 + TRI-64·65 이후 전제가 깨질 수 있다는 한계 기록
- **오탐 3건(미반영)**: `ReviewQueueController` 의 `Long.parseLong` 방어 제안 — `HeaderAuthenticationFilter` 가 이미 숫자 검증을 하고 있어 `InquiryController` 와 같은 기존 관례에 부합. API-CONTRACT.md 갱신 요청 2건 — §5 에 PATCH 계약(요청·응답·`matched` nullable·404·409 두 종류)이 이미 문서화돼 있었고 이번 PR 은 그 파일을 안 건드렸을 뿐

#### Jira 반영 (MCP)

- 상태 전이: **TRI-60·61·62·63** 각각 `진행 중`/`할 일` → `검토 중`(PR 머지 전) → PR #41 머지 확인 후 **완료**로 재전이. 각 티켓에 구현·테스트 근거 코멘트

#### 다음

- **TRI-67(`StatsService.summary`)** — #39 머지로 `StatsService.java` 가 이미 확장 가능한 형태로 develop 에 있어, 새 파일이 아니라 기존 파일에 메서드를 추가하는 형태로 착수
- **TRI-44(확정 커밋 후 캐시 덮어쓰기, D-036)** — PR #42(TRI-40·41·84, 1단 캐시)가 머지돼야 착수 가능. 아직 미머지

### 개인 — 김준현 (P2)


- **PR #43** — 머지되면서 깨진 마스킹 테스트 컴파일 복구
  - `origin/develop` 을 그대로 꺼내 `compileTestJava` 를 돌려 **내 변경 없이도 실패함**을 먼저 확인했다. 원인은 **#35(TRI-54)가 리스너 생성자를 3개 인자로 바꿨고, #36(TRI-38)이 옛 4개 인자로 부르는 테스트를 넣은 것.** 각 브랜치에서는 둘 다 통과했다 — GitHub 은 텍스트 충돌만 보고 컴파일은 보지 않는다
  - **P1 소유 파일이지만 파손 원인이 내 PR #35 라 내가 고쳤다.** 리스너가 이제 AI 를 직접 부르지 않으므로 캡처 지점을 `RetryingAiClassifier` 로 옮겼고, **테스트가 검증하는 내용(리스너가 원문이 아니라 가린 문자열을 넘기는가)은 한 줄도 바꾸지 않았다.** mock 반환값을 채워둔 이유(비우면 ②로 넘기다 NPE 로 끊겨 마스킹을 보기도 전에 죽는다)는 코드 주석에 남겼다
- **PR #44** — 측정 4: 일부러 실패를 내서 재시도가 실제로 도는지 확인 (TRI-55)
  - **왜 또 재나** — `RetryingAiClassifierTest` 는 그 클래스만 떼어 부르므로 `@Async`·`@Retryable`·`@TransactionalEventListener` 배선이 어긋나도 초록불이 뜬다. 스프링이 감싸주는 방식이라 조건이 어긋나면 **예외도 로그도 없이 무시**된다. `InquiryReceivedEventListenerWiringIT` 가 "이건 측정 4 의 몫"이라 남겨둔 자리다
  - **통합 테스트 3경우** — ①답 못 받음 ②받았는데 못 읽음 ③두 번째에 성공. **실제 호출 횟수와 `attempt_count` 가 세 경우 다 일치**(3·3·2). 호출 횟수는 로그나 DB 가 아니라 **주입한 mock 이 받은 횟수**라 컬럼을 대조하는 잣대가 된다
  - **재시도 간격 2.006초 / 4.001초** — 설정(2s, ×2.0)과 맞는다. **백오프를 줄이지 않았다** — 줄이면 실제 설정이 아니라 테스트용 설정의 동작을 재게 된다(D-047 ⓔ)
  - **회수되면 검토 큐에 안 들어간다(0건).** 재시도를 붙인 이유 그 자체다
  - **실제 앱** — 틀린 키로 띄워 **진짜 401** 을 받는 경로도 3회 확인. 접수 응답 0.160초 / 분류 종료는 7.3초 뒤 → **①과 ②가 실제로 분리돼 있다**(D-031). 기동 로그로 `worstCase 96초 < 종료 대기 120초`, `스레드 8 < 커넥션 20` 관계도 실제 값으로 성립 확인
  - **Sentry 실제 도착 확인** — 이벤트 `379a63fbd30e47c3812e35cfe93d855c`, 앱 로그와 **5ms 차이**. **3회 시도인데 이벤트 1건** = 최종 실패 지점에서만 캡처한다는 D-030 이 지켜졌다는 증거. 호출 지점마다 캡처했다면 3건이 됐다
  - **정상 경로도 1건 확인** — 실패만 재고 끝내면 "성공은 확인 안 한" 상태가 남는다. 정상 키로 문의 1건: `attempt=1`·`DELIVERY/0.950`·`model=claude-sonnet-5`·큐 0건·`CLASSIFIED`. **`model` 칸이 채워지는 것은 여기서 처음** 봤다(실패 경로는 계속 비어 있었다)
  - `evidence/retry-recover-measure4.md`

#### 흉내로 대신하지 않은 자리 — ④ Sentry 도착

- `Sentry.captureException` 은 **정적 호출**이라 테스트에서 가로채면 "불렸다"까지만 알 수 있다. 티켓이 물은 것은 **실제로 도착했는지**다. `SENTRY-GUIDE.md` 2-3 이 "catch 하고 아무것도 안 하면 Sentry 가 영구히 모른다"를 실측으로 남긴 것과 같은 층위라, **호출 여부로 도착을 대신하면 그 교훈을 정면으로 어긴다.** 그래서 실 DSN 으로 앱을 띄워 Sentry MCP 로 조회했다.
- ⚠️ **스택트레이스에 `recoverFromCallFailure` 프레임은 없다** — 캡처한 자리가 아니라 예외가 만들어진 자리의 스택이 담기기 때문. 프레임 이름으로 찾다 못 찾으면 **"알림이 안 갔다"로 잘못 읽는다.** 근거는 스택이 아니라 「이벤트 1건」이다. `SENTRY-GUIDE.md` 3-0-2 에 남겼다

#### 문서 — 「신입이 읽어도 막히지 않는가」로 두 번 고쳤다

- 1차: evidence 를 문제→왜→어떻게→확인 순서로 재작성 + 흐름도·용어 풀이표 추가
- 2차: **실제로 읽는 사람이 막힌 세 곳**을 풀었다 — 「역정규화 사본」(→ 복사본이 뭔지 절로 설명, 도서관 대출대장/스티커 비유) · 「진짜 서버까지 갔다」(→ 401 이 왜 그 증거인지, `request_id` 근거) · **그 401 을 어디서 다시 볼 수 있는지**(Sentry 에만 남는다 — 앱 로그는 컨테이너 교체로 사라지고 DB 는 설계상 저장 안 함)
- **막힌 자리가 곧 문서의 구멍이었다.** 질문이 나온 세 곳 전부 설명이 빠져 있었다

#### ⚠️ 덤으로 드러난 것 — 문서마다 용어가 갈려 있다 (미해결, 범위 밖)

「신뢰도 = 확신도」·「임계값 = 기준값」이 같은 말이냐는 질문을 받고 세어보니, **같은 개념을 문서마다 다른 말로 쓰고 있었다.**

| 문서 | 신뢰도 | 확신도 | 임계값 | 기준값 |
| --- | --- | --- | --- | --- |
| `GLOSSARY.md` (용어집 = 기준) | 0 | **12** | **9** | 4 |
| `PRD.md` | 0 | 37 | 1 | **20** |
| `CLAUDE.md` (헌법) | **7** | 1 | 6 | 0 |
| `API-CONTRACT.md` | **4** | 1 | 1 | 1 |

- 용어집이 정한 공식 항목은 **「확신도 (confidence)」·「임계값 (threshold)」** 인데, **헌법은 아직 「신뢰도」를 쓰고**(첫 줄부터 *"신뢰도가 임계값 미만이면 격리"*), **PRD 는 「기준값」을 쓴다.**
- **용어집을 만든 PR #31 이 정의만 하고 다른 문서를 안 맞췄다.** 심지어 용어집 자신도 임계값 항목 본문에서 "기준값"을 한 번 섞어 쓴다.
- **오늘은 `SENTRY-GUIDE.md` 만 용어집 기준으로 맞췄다** (같은 파일 안에서 절반만 고쳐두면 더 헷갈리므로). 2-4 표의 `신뢰도`·`ErrorGroup`(D-031 로 폐기된 개념) 도 함께 정리.
- **나머지는 남겨둔다** — `CLAUDE.md` 는 헌법이라 바꾸려면 `DECISIONS.md` 새 항목이 먼저다(「변경 절차」). D-번호 충돌이 사흘간 세 번 났으므로 별도 PR 에서 번호를 확인하고 진행한다.

> 용어가 흔들리는 것 자체가 위험한 이유: 측정 8 을 읽을 때 **"신뢰도 구간별 오분류율"과 "확신도 구간별 오분류율"이 같은 표를 가리키는지** 확신할 수 없게 된다. 지금은 사람이 알아서 같은 뜻으로 읽고 있을 뿐이다.

### 개인 — 이용택 (P1)

- **PR #42 (머지)** — 2단 절감 경로의 **공급 측** 부품 3개. 같은 내용의 문의가 또 오면 AI 를 다시 부르지 않기 위한 장치다.
  - **TRI-40** — 1단 캐시(Redis) + 계약 C 값 구조(`CachedClassification`: 분류·확신도·출처(HUMAN/AI)·원본 결과 id). 셋이 공유하는 값이라 `service/cache/` 전용 패키지(D-059 예약 자리)에 두고, 값 조합 오류(HUMAN 인데 confidence 있음 등)는 정적 팩토리·생성자로 차단
  - **TRI-41** — 2단 DB 조회. 사람이 확정한 답(1순위 — `final_category IS NOT NULL`, 감사로 정정된 REUSED 포함) → 없으면 AI 자동확정(2순위 — `verdict=AUTO_ACCEPTED` 등치로 체인 차단). 쿼리 두 번, 정렬 축은 `inquiries.created_at`(D-041)
  - **TRI-84** — ②의 넣기를 Lua 1개로 원자화(기존이 사람 답이면 안 덮음, D-048) + `docker-compose` redis `maxmemory 256mb`·`allkeys-lru`
- **부품까지다 — 배선(리스너/②/③ 연결)은 안 했다.** 그건 소비 티켓 소관 — **TRI-53(②커밋 후 캐시 저장, 김준현) · TRI-44(③확정 후 덮어쓰기, 김은빈)**. 기존 코드 0줄 수정, 새 파일만 추가. **이로써 두 사람의 전제(계약 C 값 구조·put 규격)가 해소됐다.**
- **CodeRabbit 리뷰 반영(`bbcf87b`)** — ① 동률 정렬 결정성(`r.id DESC` tie-breaker) ② Lua 타입가드(캐시에 스칼라가 있어도 런타임 에러 대신 자가 치유) + 회귀 테스트 ③ 테스트 컨테이너 싱글턴화. `get` 의 Redis 장애 fail-open 은 TRI-85 위임 — **그 전까지 현재 동작은 예외 전파(캐시가 죽으면 호출자까지 실패)** 이고, 이 판의 결함이 아니라 다음 티켓 범위다
- **테스트 222건 통과**(로컬 `./gradlew test`). 신규 21건(캐시 왕복·사람 답 보호·③·② 경합·eviction·1순위/2순위 필터). curl 은 아직 HTTP 경로에 연결 전이라 Testcontainers 로 대체
  - **경합 실측**: ③(사람 답)·②(AI 답)을 같은 키에 동시 실행 **300회 반복 — 전 회차 최종값이 사람 답(HUMAN)으로 유지, AI 답이 사람 답을 덮은 경우 0건.** Lua 원자성이 지켜짐을 이 수치로 확인
- 개발 중 버그 1건 — 공유 MySQL 컨테이너 FK 정리 순서(격리 실행은 통과, 전체 스위트에서만 드러남). **원인**: 다른 테스트가 남긴 `inquiry_review_queue` 행이 FK 로 결과 삭제를 막음. **조치**: `@BeforeEach` 정리를 FK 순서(큐 → 결과 → 문의)대로 삭제하도록 고정

#### Jira 반영 (MCP)

- **완료 전이** — TRI-47·83(PR #30) · TRI-54(PR #35) · **TRI-24·72·73**(이용택 구역, PR #39·#40 머지 반영)
- **검토 중 전이** — TRI-55(PR #44) · **TRI-40·41·84**(PR #42 열렸을 때)
- **손대지 않은 것 → 이후 갱신** — TRI-81(검토 중인데 연결 PR 없음)은 그대로. **TRI-40·41·84 는 동기화 당시 PR #42 가 열려 있어 `검토 중` 이 맞았고, 그 뒤 #42 머지로 `완료` 전이했다**
- `/ticket 동기화` 를 쓰면서 **규칙만 따르면 잘못 옮길 자리 2건**을 발견해 커맨드에 예외로 못박았다 (사례 18) — ① 같은 티켓을 담은 PR 이 여럿이면 가장 나중 것이 이긴다 ② 한 PR 의 형제 중 혼자만 안 옮겨져 있으면 담당자가 일부러 남긴 것으로 본다

#### 다음

- **TRI-64 → 65 → 66** (감사 표본 추출 → ② 안에 삽입 → 감사 자체를 감사하는 필드). **세 작업이** `ClassificationService` 를 이어서 건드리는 **직렬 사슬**이라 순서대로 친다. 뒤이은 **TRI-86**(감사 삽입을 트랜잭션 밖으로 뺀 대조 실험)은 TRI-66 의 필드를 그대로 쓰므로 이 셋 다음이다
- **TRI-90**(재사용 끄는 스위치)은 PR #42 머지 후 — 같은 파일(`InquiryReceivedEventListener`)을 건드린다. TRI-77(측정 1·8ⓐ)의 선행이라 측정 전에 끝나야 한다
- **TRI-53**(②커밋 후 캐시 저장)은 계약 C 라 **3인 전원 합의** 대상. PR #42·#41 머지 직후 셋이 한 번 맞추는 게 안전하다

---

## 2026-08-11 (화) — 측정 6 발판: 1000건 파생 + 재사용 조회 EXPLAIN 실측 + 병합 테스트 (P1)

### 개인 — 이용택 (P1)

- **PR #51 (열림) — 측정 6용 1000건 파생 (TRI-81).** 정답 50건을 **표현만 바꿔** 20배로 불렸다(새로 쓰면 "사람이 보기에 중복인 비율"을 다시 세어야 한다, D-043 ⓑ). 변형을 두 갈래로 나눠 측정 6 이 두 숫자를 갈라 읽게 함 — **COLLAPSE 12**(공백·문장부호·개인정보 값 → 정규화 키 **동일**, 캐시/DB hit 자리) / **WORDING 8**(단어·어미 → 키 **상이**, 사람은 중복이나 AI 재호출).
  - **실측(제품 코드 `NormalizedKeyGenerator` 로 대조 — 재구현 아님)**: 사람 기준 중복 **956** · 서로 다른 키 **450** · 시스템 절감 상한(키 충돌) **550** · 원본 50건 키 **전부 상이**(과도 병합 없음). D-014 부등식(절감 ≤ 사람 중복)을 데이터가 담는다.
  - 완전 결정론 생성기(난수 없음) + 검산 테스트 2건. **리뷰 반영으로 「파생본이 현재 50건 원본과 일치하는지」 드리프트 가드 추가**(원본 고치고 재생성 안 하면 깨진다).
- **PR #52 (열림) — 재사용 조회 EXPLAIN 실측(TRI-42) + 과도/과소 병합 테스트(TRI-43).**
  - **TRI-42 (측정 5ⓓ 중 1순위)** — 1순위 조인 쿼리 `EXPLAIN` 실측(MySQL 8.0, inquiries 10만). 두 인덱스 **설계대로 사용**(구동 ref·조인 ref, ANALYZE 확인), `final_category IS NOT NULL` 은 **서버 필터**라 훑는 양 = 키당 문의 수(보통 4행 0.107ms / hot 200행 0.363ms). 구동 인덱스 DROP 시 10만 풀스캔, **조인 인덱스는 FK 강제로 드롭 불가(error 1553).** 판단: **추가 인덱스 불필요**(한 자릿수면 서버 필터가 무의미, D-018 계보) — 인덱스는 이미 `V2` 에 있음. `evidence/measurement-5d-…`.
  - **TRI-43** — 근접 과도 병합(`환불해주세요` vs `환불하지 마세요`) + **띄어쓰기 과소 병합 트립와이어**(`환불해주세요!!` vs `환불해 주세요` → 현재 다른 키). **구현 불변**, 유보 근거를 **D-061** 로 기록.
- **⚠️ 크리티컬 패스 — 오늘 것은 계측 발판이지 측정 결과가 아니다.** 측정 6(AI 절감)을 **잴 준비**는 끝났지만 **실제 측정(TRI-71)은 못 돈다** — 리스너의 재사용 분기(**TRI-47 의 미뤄진 분기 · TRI-53**)가 없어 라이브 파이프라인이 재사용을 안 하기 때문. 산출물은 그게 붙는 순간 답을 낼 수 있게 대기 상태다.
- **개발 중 발견 2건** — ① `~`(물결)은 유니코드 범주가 `Sm`(수학기호)라 정리 정규식 `[\p{P}\p{Z}\s]+` 에 안 걸려 접힘 변형 키가 어긋남 → `…`·`。`(`\p{P}`)로 교체(테스트가 잡음). ② 티켓의 과소 병합 예시가 현재 구현상 다른 키(단어 사이 띄어쓰기 미접기) → **D-061**.

#### Jira 반영 (MCP)

- **작업 코멘트 등록** — TRI-81 · TRI-42 · TRI-43 (산출물·실측·완료 조건 대조). **상태 전이는 PR 머지 후.**
- **드러낼 것** — **TRI-47 이 `완료` 인데 재사용 분기(DoD)가 미구현**이다(리스너 javadoc 이 스스로 고지). 재오픈/코멘트로 노출 예정 — 프로세스 액션이라 별도.

#### 다음

- **docs 동기화(본 항목·`LIFECYCLE-COVERAGE`·`D-061`)** — 본 docs PR.
- **TRI-71(측정 6·11 실측)** 은 재사용 배선(TRI-47 미뤄진 분기·TRI-53) 선행. 그게 붙으면 오늘 1000건으로 바로 잰다.

---

## 2026-08-12 (수) — 재사용 경로가 처음으로 끝까지 이어졌다 (P2 배선·감사 표본 + P3 통계·확정 후 덮어쓰기 + P1 캐시 fail-open)

### 팀 공통

- **2단 절감 경로가 처음으로 끝까지 이어졌다.** 8/10·8/11 로그가 두 번 *"부품은 있는데 배선이 없어 재사용이 한 번도 일어나지 않는다"* 고 적어둔 자리다. 오늘 **P2 가 소비 측(TRI-47 잔여·TRI-53)**, **P3 가 확정 후 덮어쓰기(TRI-44)** 를 붙이면서 **공급(P1) → 소비(P2) → 정정(P3)** 이 한 줄로 연결됐다
- **계약 C(캐시 값 구조)를 세 사람이 모두 쓰는 상태가 됐다** — P1 이 읽고 쓰고, P2 가 ② 커밋 후, P3 가 ③ 커밋 후 넣는다. 설계에만 있던 3자 공유가 실제 코드로 다 붙은 첫날이다
- **새 결정 2건 — D-062(재사용 스위치) · D-063(트랜잭션 ②의 두 번째 입구).** 나흘 만에 결정이 다시 늘었고 둘 다 「코드를 짜다가 정해야 했던 것」이다. **번호 충돌은 없었다** — 신설 전에 `origin/develop` 의 `DECISIONS.md` 를 먼저 확인했다. 절차는 여전히 사람의 습관이지 장치가 아니다 (「보강 항목」의 미해결 1건)
- **PR 6건 머지** (#50 · #54 · #55 · #56 · **#58 · #59**) + **PR #57(TRI-85) 열림**
  - 마지막 두 건이 저녁에 들어왔다 — **#58 이 재사용 끄는 스위치(TRI-90)**, **#59 가 오늘 진행 기록과 실패 사례 21~24**. 그래서 **#59 의 본문 일부는 #58 머지 14분 전 상태**를 담고 있었고, **#60(이 기록)** 이 그 어긋남을 맞춘다
  - **#60 의 AI 리뷰 4건은 전부 반영했다 — 오늘 처음으로 틀린 지적이 하나도 없었다.** 넷 다 **문서 안에서 대조 가능한 것**이었다: 기준 시점 문구가 `#57`(열림)을 머지 범위에 넣은 자기모순 · 매트릭스 셀에 로그와 같은 설명이 두 벌 · evidence 기록 형식이 「한 줄로 적는다」로만 적혀 실행 때 빠질 여지 · **「정말 꺼졌는지 무엇으로 확인했나」가 없음**. 사례 21~24 가 전부 **코드 흐름을 추론해야 하는 지적**에서 틀렸던 것과 대비된다 — **대조할 대상이 문서 안에 있으면 리뷰가 잘 맞는다**
- ⚠️ **운영진이 AI 리뷰 워크플로를 opt-in 으로 바꿨다** (커밋 `6a5d35b`, 16:58). 저장소 변수 `ENABLE_AI_REVIEW` 가 `true` 가 아니면 **러너를 아예 배정하지 않는다.**
  - **실측**: 그 커밋 이후 `ai-review` 실행 **0건**(`gh run list` 확인). 같은 시간대에 PR #56 으로 커밋이 계속 올라갔는데도 안 돌았다. 마지막 실행은 16:55 의 PR #57 이다
  - **CodeRabbit 은 별도 앱이라 계속 동작한다.** 변수를 켜기 전까지는 **리뷰 도구가 두 겹에서 한 겹으로 줄어든 상태**이고, 이 상태로 머지된 PR 이 생기면 D-054 의 병행 비교(중복/고유 지적 집계)도 그만큼 표본을 잃는다
  - `team-pr-guard` 도 함께 바뀌었다 — 새 커밋만 올라오는 경우와 봇의 본문 편집에는 러너를 안 쓴다

### 개인 — 김준현 (P2)

- **PR #50 (머지) — 자동 확정된 건도 20건에 1건은 사람이 다시 본다 (TRI-64·65)**
  - 지금까지 검증은 **한 겹**이었다. 확신도가 기준값 미만이면 사람에게 보냈고 넘긴 건은 그대로 확정됐다 — **확신하면서 틀린 건은 아무도 보지 않았다.** 이 PR 이 두 번째 겹을 붙인다
  - `AuditSamplingPolicy` — 설정 비율만큼 무작위로 뽑는 판단을 한 곳에 모았다. **난수원을 주입 가능하게** 뒀다. 고정하지 못하면 "뽑혔다/안 뽑혔다"를 테스트로 못 박을 수 없고, 그러면 **확인할 방법이 없는 코드**가 된다
  - **삽입은 트랜잭션 ② 안**이다 (D-012). 밖으로 빼면 감사율이 설정값 미달이 되어 **측정 8 의 분모가 조용히 작아진다**
  - 대상은 `AUTO_ACCEPTED` **와** `REUSED` 둘 다 (D-033). 자동 확정은 하나 틀리면 그 문의 하나가 틀리지만, **재사용은 원본 하나가 틀리면 같은 내용이 전부 틀린다**
  - **테스트 프로파일의 감사 비율은 0 이다.** 운영값 0.05 를 그대로 두면 자동 확정을 다루는 다른 테스트가 20번에 한 번씩 실패하는데, **그 실패 메시지만 봐서는 코드가 깨진 것인지 감사에 뽑힌 것인지 구분되지 않는다.** 감사가 실제로 도는지는 전용 IT 가 값을 1.0 으로 덮어써서 따로 본다
- **PR #55 (머지) — 같은 문의가 또 오면 AI 를 다시 부르지 않는다 (TRI-47 잔여 · TRI-53)**
  - **찾는 순서를 한 곳에 가뒀다 — `ClassificationReuseLookup`.** 1단 캐시 → 2단 DB 1순위(사람 확정) → 2순위(AI 자동확정) → 없으면 AI 호출.
    리포지토리는 1순위·2순위 메서드를 나란히 제공할 뿐이라 **뒤집어 불러도 컴파일된다.** 그러면 사람이 정정한 답을 두고 AI 답을 쓰게 되는데, 그건 **감사가 잡아낸 정정을 시스템이 도로 무시하는 것**이다 (D-033). 순서를 한 곳에 모으고 테스트로 고정했다
  - **트랜잭션 ②에 두 번째 입구를 냈다 — `persistReuse`** (D-063). 안쪽 네 단계(상태 전이 · 사본 갱신 · 결과 저장 · 감사 삽입)는 그대로 공유하므로 **한쪽에만 감사 삽입이 빠지는 일이 구조적으로 안 생긴다**
  - **② 커밋 후 캐시에 넣는다** (TRI-53). `putIfNotHuman` 이라 사람 답을 덮지 않고(D-036), 넣기를 커밋 후로 미루는 이유는 **②가 롤백됐는데 캐시에 값이 남으면 저장되지도 않은 판정이 재사용되기 때문**이다. 재사용 건은 **받은 값 그대로** 다시 넣는다 — 방금 만든 `REUSED` 행의 id 를 넣으면 다음 문의가 그것을 원본으로 삼아 체인이 길어진다 (D-042)
  - **캐시가 죽어도 분류는 계속되게 임시 조치**했다. `ClassificationCache` 는 Redis 장애 시 예외를 던지는데(fail-open 은 TRI-85 소관), 그대로 두면 **캐시 장애가 `verdict=FAILED` 로 기록되어 측정 2 의 사유별 분포에 「AI 문제」와 「캐시 문제」가 섞인다.** 조회 실패를 miss 로 떨어뜨려 2단 DB 가 받게 했다
  - 테스트 **207 → 249**. develop 합치기와 리뷰 반영 후 **268 통과**(실패 0 · skip 3)
- **버그 2건 — 둘 다 두 PR 이 만나는 자리에서 났다**
  - **`GET /api/stats` 를 10초 안에 두 번 부르면 500.** 첫 호출은 캐시에 담기까지 잘 되고 두 번째에 꺼내면서 실패했다. 값에 타입 이름을 적어 두는 방식(다형 타이핑)을 켜고 믿을 타입 목록을 좁혀 놨는데, 그 안의 `EnumMap`·`Long` 이 거부된 것이다.
    **좁힌 것 자체는 옳다** — 타입 이름을 그대로 믿으면 Redis 에 쓸 수 있는 사람이 그 이름을 바꿔 아무 클래스나 만들어내게 할 수 있다(CWE-502). 그래서 목록을 넓히는 대신 **반대로 갔다**: `stats:summary` 에 담기는 값은 한 종류뿐이라 **타입 이름을 아예 안 적고 타입을 지정해 꺼낸다.** 적어 두지 않으면 믿을 것도 없다. 담는 타입이 늘면 **캐시 이름을 나눠** 각각 타입을 지정한다
    - **어떻게 잡았나**: 이번 합치기로 **「감사 표본이 큐에 들어가면 통계가 갱신되는가」가 새 경로**가 됐는데 **두 PR 어느 쪽도 그 자리를 안 보고 있었다.** 테스트를 붙이자 바로 터졌다. 그 테스트는 **캐시가 살아 있다는 것부터 단언한다** — 캐시가 꺼져 있으면 evict 를 지워도 통과하는, 아무것도 못 잡는 테스트가 되기 때문이다
  - **확정 후 캐시 덮어쓰기가 실패하면 통계 비우기까지 건너뛰었다** (CodeRabbit 지적). 한 메서드가 두 가지를 하는데 앞이 예외를 던지면 뒤가 통째로 안 돌았다. 되돌릴 수단이 없어 **삼키되 `Sentry.captureException` 으로 반드시 알린다** (D-030). ⚠️ **이 자리에서 자동 캡처가 도는지는 재보지 않았다** — 커밋 후 리스너의 예외를 스프링이 어디까지 올려보내는지 확인 안 된 상태라 자동 경로를 믿지 않는다
- **PR #56 (머지) — 결정 2건 + AI 리뷰 2건 검토 (사례 21·22)**
  - **D-062** — 재사용을 끄는 스위치는 **기동 시 설정으로만** 두고 `GET /api/policies` 에 읽기 전용으로 노출한다. 런타임 변경 API 를 만들면 **측정 도중에 조건이 바뀌어 결과가 어느 조건에서 나온 것인지 사후에 구분되지 않는다**(D-028 과 같은 논리). 기본값은 `true` — 기본을 `false` 로 두면 설정을 빠뜨렸을 때 **절감이 조용히 0 이 된다**
  - **D-063** — `persistReuse` 가 `@Transactional` 네 번째 자리인 이유. 근거를 PR 본문에만 적었더니 *"PR 본문은 시간이 지나면 코드와 함께 읽히지 않는다"* 는 지적이 왔다. 맞는 말이라 **결정으로 옮겼다** — 헌법의 세 자리 원칙은 그대로 두고, **「입구가 늘어도 되는 조건」**(안쪽 로직을 공유할 것)을 기록했다
  - **AI 리뷰 2건은 방향만 받고 문장은 고쳐 넣었다**
    - ⓐ *"꺼도 `persistReuse` 는 호출된다"* 는 **사실이 아니다.** 끄면 조회를 안 하므로 재사용 판정 자체가 안 생기고 그 메서드에 도달할 길이 없다. 다만 「두 결정을 연결하라」는 방향은 맞아서, **꺼진 동안 캐시를 채우는 것은 AI 경로뿐**이라는 정확한 문장으로 D-062 ⓓ 에 넣었다
    - ⓑ *"표본이 50→38 건으로 줄어든다"* 는 **재본 적 없는 숫자다.** `dup_group` 12건은 「사람이 보기에 뜻이 같다」이고 실제 절감은 **정규화 키까지 같아야** 생기는데, D-061 이 밝혔듯 **띄어쓰기 하나로 키가 갈린다.** 헌법이 「재보지 않은 수치를 만들지 않는다」고 못박은 자리라 반영하지 않고, **그 숫자가 측정 6 에서 나온다**는 것을 적었다 — 빈칸을 채우는 대신 빈칸인 이유를 남긴다
  - 실패 사례 **21(허구 메커니즘)·22(추정치 요구)** 추가. **22 는 새 부류다** — 문서가 "아직 모른다"로 비워둔 자리를 **약점으로 읽고 그럴듯한 값을 넣으라** 하는 유형이라, 따르면 재보지 않은 값이 근거처럼 굳는다. 분류 정의에 추가했다
- **PR #58 (머지) — 측정할 때 재사용을 끄는 스위치 (TRI-90)**
  - **왜 필요한가**: 측정 1·8ⓐ-1 은 **재사용을 끈 상태**에서 재야 한다 (D-043 ⓒ). 켜져 있으면 같은 내용의 문의가 AI 를 건너뛰어 **정답과 대조할 표본이 조용히 줄고**, 그러면 「자동 확정된 건이 실제로 얼마나 틀렸나」를 못 읽는다. **그런데 끌 방법이 없었다** — D-043 이 이미 요구했는데 티켓 분해에서 빠졌던 자리다
  - **스위치를 「조회 안」에 뒀다 — 리스너가 아니라.** 끝났다고 볼 조건이 **「1단과 2단이 함께 꺼진다」** 인데, 부르는 쪽에서 끄면 **캐시 조회만 건너뛰고 DB 조회가 남는 반만 꺼진 상태**를 만들 수 있다. 찾는 순서를 `ClassificationReuseLookup` 에 가둔 것(PR #55)과 같은 이유다
    - ⚠️ **D-062 의 「영향」 항목은 이 분기를 리스너에 적어뒀다.** 결정은 **덮어쓰지 않고 그대로 두고**(결정 로그는 불변), 어긋난 이유를 PR 본문에 남겼다. 대신 리스너 javadoc 에 **「재사용은 꺼져 있을 수 있고 그 분기는 여기 없다」** 를 적어 읽는 사람이 놓치지 않게 했다
  - **기본값은 `true` 이고, 설정 블록을 통째로 안 적어도 켜진다.** `threshold`·`audit` 은 없으면 기동을 막는데 **이 값만 반대로 간다** — 저 둘은 없을 때 무엇이 맞는지 아무도 모르지만, 재사용은 평소 동작이 「켜짐」 하나뿐이고 끄는 것은 측정할 때뿐이다. `false` 가 기본이면 설정을 빠뜨렸을 때 **절감이 조용히 0 이 되고**, 그때 증상은 "AI 요금이 왜 이렇게 나오지"라서 아무도 설정 파일을 안 본다
  - **꺼졌을 때의 로그에 다른 낱말을 쓴다** (`reuse_disabled`). miss 와 같은 낱말이면 측정 6 을 읽을 때 **「찾았는데 없었다」와 「아예 안 찾았다」가 한 덩어리**가 되고, 절감이 0 인 이유가 정규화 탓인지 스위치 탓인지 사후에 구분되지 않는다
  - **리뷰 반영 2회** — ⓐ **「블록은 있는데 `enabled` 만 없을 때」를 잰다던 테스트가 실제로는 바깥 분기를 재고 있었다** (CodeRabbit). 바깥 기본값만 뒤집었더니 두 테스트가 **함께** 실패했고, 그게 같은 경로라는 증거였다 — 이 팀이 이미 이름 붙여둔 **「간접검증 오인」**(사례 13)이다. 테스트를 지우지 않고 **주장을 사실에 맞췄다** ⓑ evidence 에 **측정 조건을 어떻게 적을지** 형식을 정했다 — 기록 맨 위에 `측정 조건: classification.reuse.enabled=false` 한 줄. 아래에 묻히면 **표만 옮겨 인용될 때 조건이 떨어져 나간다**
  - **「정말 꺼졌는지」를 무엇으로 확인했나** (AI 리뷰 지적으로 추가) — 통합 테스트 3건이 각각 다른 것을 단언한다. **재현하려면 이 셋을 보면 된다** (`ClassificationReuseDisabledIT`)
    - ⓐ **DB 에 재사용할 답이 실제로 있는 상태**임을 먼저 단언한 뒤(`findLatestAutoAccepted` 가 있다), 그래도 조회 결과가 비고 **캐시는 한 번도 안 불린다**(`never()`). ⚠️ **이 순서가 핵심이다** — 빈 상태에서 껐다 켜면 결과가 양쪽 다 비어서 **스위치를 지워도 테스트가 통과한다**
    - ⓑ **꺼도 캐시에 넣기는 계속한다** (`putIfNotHuman`, `source=AI`) — D-062 ⓓ 그대로다
    - ⓒ **꺼도 판정·저장(②)은 그대로다** (`AUTO_ACCEPTED` 로 저장됨) — 스위치가 끄는 것은 **조회뿐**임을 코드로 고정한다
    - ⚠️ **라이브에서 끈 채로 돌린 기록은 아직 없다.** 그건 측정 1(TRI-77) 때 생기고, 그때 `측정 조건: classification.reuse.enabled=false` 를 evidence 맨 위에 적는다
  - 테스트 **281 → 292** 통과(실패 0 · skip 3)
- **PR #59 (머지) — 오늘 진행 기록 + 실패 사례 21~24 + D-062·D-063 을 쉬운 말로**
  - 실패 사례 **23·24 는 PR #58 리뷰에서 나왔다.** 둘 다 **새 부류는 아니다** — 23 은 「부분 관측의 일반화」(헬퍼 이름이 `given...` 이라 본문을 안 열고 "호출이 없다"고 단정), 24 는 「허구 메커니즘」(캐시 넣기의 주체를 `persistReuse` 로 지목 — **그 메서드는 실재하지만 이 경로에 없다.** 꺼진 경로라 아예 안 불린다)
  - **24 는 사례 21 과 같은 자리에서 두 번째로 나온 오해다.** 두 입구(D-063) 중 **어느 쪽이 도는지**가 헷갈리기 쉽다는 뜻이라, 코드 주석에 「이 길에 `persistReuse` 는 없다」를 경고로 남겼다

### 개인 — 김은빈 (P3)

- **PR #54 (머지)** — 검토 큐 적체 통계 + 10초 캐시(TRI-67) · **확정(③) 커밋 후 캐시를 사람 답으로 덮어쓰기(TRI-44)**
  - **TRI-44 로 D-036 의 「사람 답은 AI 답을 덮고, 그 반대는 안 된다」 중 사람 쪽이 구현됐다.** `ReviewService.confirm` 이 커밋 후 `ReviewConfirmedEvent` 를 발행하고, 리스너가 `source=HUMAN`·`confidence=null` 로 **조건 없이** 덮는다
  - `API-CONTRACT.md` **v1.6** — `GET /api/stats` 의 `backlog` 블록이 실제 응답에 나간다(대기 건수 · 사유별 건수 · 가장 오래 기다린 시간). **나머지 블록이 아직 미구현임을 함께 적었다** — 안 적으면 계약이 구현보다 앞서 보인다
  - CodeRabbit 리뷰 반영 — 캐시 역직렬화 취약점(CWE-502) 차단 · `stats:summary` 비우기를 **커밋 후로 이동**(커밋 전에 비우면 동시 요청이 옛 상태를 도로 채우는 경합이 생긴다) · `evictSummary` 실패 시 Sentry 캡처
- **진행 중** — TRI-68(`GET /api/stats` 응답 조립) · TRI-87(깊은 페이지 실행 계획 + 통째로 읽기 vs 필요한 칸만 읽기, 측정 5ⓕ)

### 개인 — 이용택 (P1)

- **PR #51·#52·#53 머지** — 8/11 작업분(1000건 파생 TRI-81 · 재사용 조회 EXPLAIN TRI-42 · 과도/과소 병합 테스트 TRI-43 · 로그 동기화와 D-061)
- **PR #57 (열림) — 캐시 규칙 번호 + 캐시가 죽어도 접수·분류 계속 (TRI-85)**
  - 규칙 번호(`v1`)를 설정값으로 빼서 **정규화 규칙을 조일 때 번호를 올리면 옛 규칙으로 만든 답이 키가 달라져 자동으로 안 잡히게** 했다. 기동 시 고정이라 `threshold`(D-028)와 같은 층위다
  - `get`·`put`·`putIfNotHuman` 이 Redis 장애에 **fail-open** — 없는 것으로 치고 2단 DB → AI 로 계속 굴러간다. **조용히 삼키지 않고 로그 + Sentry 로 표면화**(D-030)하며, **우리 쪽 직렬화 결함은 안 삼켜지게** 잡는 예외를 좁혔다
  - 시니어 리뷰 반영 — ① 값이 깨져 못 읽는 경우도 miss 로 강등(안 잡으면 분류 담당까지 올라가 **`verdict=FAILED` 로 둔갑**해 이 티켓이 막으려는 현상이 읽기 경로에서 재발한다) ② 규칙 번호 형식을 기동 시 검증(설정 한 줄 실수로 캐시 키 앞자리가 조용히 달라지는 것 차단)
  - ⚠️ **PR #55 의 임시 안전망과 겹친다** — TRI-85 가 머지되면 캐시 쪽에서 이미 miss 로 돌려주므로, **리스너에 둔 방어를 걷을지 두 사람이 정해야 한다.** 남겨두면 같은 일을 두 곳에서 하게 되고, 나중에 한쪽만 고치면 어긋난다
  - 🔁 **저녁 갱신 — PR #57 머지, 위 겹침 해소.** 김준현님 리뷰대로 리스너의 임시 방어(`fromCache`)를 **이 PR 에서 함께 걷었다**(캐시가 스스로 fail-open 하므로 죽은 코드였다). 관련 테스트는 「예외를 던진다」→「빈 결과를 준다」 가정으로 교체. 설계 근거(fail-open 비대칭·규칙 번호 기동 시 고정)는 **D-064** 로 기록. **TRI-85 완료로 전이.**

### 서로에게 넘긴 것 (내일 확인할 자리)

| 넘긴 사람 | 받는 사람 | 내용 |
| --- | --- | --- |
| 김준현 | 이용택 | **TRI-71(측정 6·11)의 선행이 풀렸다** — 재사용이 실제로 도니 8/11 의 1000건으로 바로 잴 수 있다. 단 **측정 6·11 은 켠 상태, 측정 1·8ⓐ 는 끈 상태**(D-062)다. **스위치는 같은 날 저녁 TRI-90 으로 들어왔다**(PR #58) — `classification.reuse.enabled`, 기본 `true`, 기동 시 고정. **끄면 1단·2단이 함께 꺼진다** |
| 김준현 | 이용택 | **fail-open 이 두 겹이다** (위 PR #57 항목). 머지 후 리스너 쪽 방어를 걷을지 결정 |
| 김준현 | 김은빈 | **D-062 로 `GET /api/policies`(TRI-69)에 `reuse.enabled` 가 들어간다.** `API-CONTRACT.md` §6 은 그 구현 PR 이 동반 갱신 |
| 김준현 | 김은빈 | `stats:summary` 캐시에서 다형 타이핑을 걷어냈다 — **담는 타입이 늘면 캐시 이름을 나눠야 한다.** 한 캐시에 여러 타입을 섞으면 같은 문제가 돌아온다. `ReviewConfirmedEventListener` 도 손댔다(put 이 실패해도 evict 는 돈다) |
| 김은빈 | 김준현 | ③이 캐시를 덮으므로, ②의 `putIfNotHuman` 이 **사람 답을 안 덮는지**가 이제 라이브 경로에서 실제로 걸린다 (경합 실측은 8/10 PR #42 의 300회 반복이 근거) |

#### 다음

- ~~**TRI-90(재사용 끄는 스위치)**~~ → **같은 날 저녁 PR #58 로 완료.** 이로써 **TRI-77(측정 1·8ⓐ-1·8ⓐ-2)의 선행이 전부 풀렸다** — 정답 50건 · 감사 경로 · 재사용 스위치 셋 다 있다
- **TRI-77(측정 1·8ⓐ)** — **이 프로젝트의 결론이 나오는 자리.** 잴 때 `classification.reuse.enabled=false` 로 두고, **그 조건을 evidence 맨 위에 한 줄로 적는다**(PR #58 에서 형식을 정했다)
- **TRI-66 → TRI-86** — 감사 장치 자체를 감사하는 필드(설정 비율 vs 실측 비율) → 감사 삽입을 트랜잭션 밖으로 빼본 대조 실험(측정 3 의 나머지 절반, D-045 ②). **둘은 순서가 있다** — 86 이 66 의 필드를 그대로 쓴다
- **Jira 가 실제보다 뒤처져 있다** — 내 구역 스토리 2건(`TRI-16`·`TRI-76`)은 하위가 다 끝나 **닫았다.** **`TRI-90` 은 PR #58 머지됐는데 아직 검토 중이고, `TRI-14`(스토리)는 하위 46·47·48·83·89·90 이 이제 전부 끝나 닫을 수 있다.** 남의 구역에도 있다(`TRI-44`·`TRI-67` 은 PR #54 머지됐는데 검토 중 / `TRI-85` 는 PR #57 열림인데 진행 중) — **대신 옮기지 않고 알린다**

---

## 2026-08-12(수) 저녁 ~ 08-13(목) — 결론이 나왔다 (P2 측정 4건 · PR #61·64·65·66)

> **이 프로젝트가 답하려던 질문에 숫자가 붙은 날이다.** 코드는 8/12 저녁에 닫혔고, 나머지는 재고 기록하는 일이었다.

### 순서를 하나 뒤집었다 — TRI-66 을 TRI-77 앞으로

원래 계획은 `TRI-77(측정) → TRI-66(감사율 필드) → TRI-86` 이었는데 **66 을 먼저** 했다.

```text
8ⓐ-2 의 분모 = 감사가 뽑은 건수.  50건 기준 기대값은 2건 안팎이다.
그 2건이 「5% 대로 뽑힌 것」인지 「삽입이 새서 적게 뽑힌 것」인지 가릴 수단이
측정 실행 시점에 없으면, 나중에 손으로 SQL 을 세는 수밖에 없다.
```

측정은 **실 AI 호출을 동반하는 일회성 실행**이라 한 번에 결론과 검산을 같이 뽑아야 했다. **이 판단이 곧바로 값을 했다** — 아래 ⑤ 참조.

### ① 감사 자체를 감사하는 값 (TRI-66, PR #61)

`GET /api/stats` 에 `audit` 블록. **자동 확정과 재사용을 따로** 낸다 (D-033 — 합치면 한쪽만 새도 평균에 묻힌다).

티켓에 없던 판단 3가지를 PR 본문에 남겼다.

| 무엇 | 왜 |
| --- | --- |
| 비율만 두지 않고 **분자·분모를 함께** | 반올림 뒤라 「20건 중 1건」과 「2000건 중 100건」이 구분 안 된다 |
| 모집단이 0 이면 **`null`** (`0.0` 아님) | `0.0` 은 "뽑힐 게 있었는데 안 뽑혔다"는 **누락 신호**다. 아직 아무 건도 안 들어온 상태가 같은 얼굴이면 **없는 장애를 보게 된다** (D-022 와 같은 층위) |
| `backlog` 와 달리 **캐시하지 않는다** | 10초 캐시의 비우기가 **감사에 안 뽑힌 자동 확정에는 안 걸린다**(D-042). 캐시하면 분모만 낡아 비율이 실제보다 **높게** 보인다 — **누락을 드러내라고 만든 값이 반대로 누락을 감춘다** |

### ② ★ 측정 1·8ⓐ — 이 프로젝트의 결론 (TRI-77, PR #64)

> 조건: 재사용 **끔** · `claude-sonnet-5` · 기준값 `0.8` · 감사 비율 `0.05` · 커밋 `38b2f25`

```text
8ⓐ-1 = 0.094   자동 확정 32건 중 3건 오답   ← 0 이 아니다
8ⓐ-2 = 0.333   감사가 그 3건 중 1건을 잡고 2건을 놓쳤다
측정 1 = 0.894  AI 가 답한 47건 기준
```

**틀린 3건 중 하나는 확신도 `0.95`, 하나는 기준값과 같은 `0.80` 이었다.** *"확신도가 낮으면 사람에게 넘긴다"* 만으로는 이 3건을 못 잡는다 — 2차 겹(감사)을 만든 이유가 숫자로 확인됐다.

**AI 가 확신도 `0.5` 미만을 한 번도 안 냈다.** 기준값을 0.5 로 낮추면 격리되는 건이 **0** 이 되어 1차 겹이 통째로 사라진다 — 기준값 조정 논의에 이 사실을 함께 둔다.

### ③ 틀린 3건이 같은 성격이 아니었다

리뷰 중에 발견해 커밋 하나를 더 얹었다. **두 레이블 작성자가 그 3건을 뭐라고 했는지** 대조했다.

| 부류 | 건수 | 뜻 | 줄이려면 |
| --- | --- | --- | --- |
| A | 1 | 두 사람이 AI 를 안 보고도 같은 답을 냈고 **AI 만 달랐다** | 모델·프롬프트 |
| **B** | **2** | **사람도 AI 와 똑같이 봤고** §7 경계표 재판정으로 정답이 갈렸다 | **경계표 — AI 를 바꿔도 안 줄어든다** |

**8ⓐ-1 = 0.094 는 그대로 유효하다.** 채점 기준은 최종 정답이고 운영에서도 상담원은 그 표로 판단한다. 다만 **그 숫자 안에 「AI 가 얼마나 못 맞히나」와 「경계 규칙이 직관과 얼마나 어긋나나」가 섞여 있다.**

> **`SERVICE_USAGE` 경계가 세 번째로 나왔다** — 부류 B 의 seed 34, 실제 AI 답이 시드 초안과 갈린 4건 중 3건, 그리고 `seed-label-agreement.md` 의 기존 표 보강 권고. **같은 신호가 세 번 나왔으면 표를 봐야 한다.**

### ④ 측정 8ⓑ — 감사가 잡은 정정이 재사용으로 퍼졌다 (PR #65)

재사용을 **켜고** 같은 50건을 다시 돌렸다. 8ⓐ 와 조건이 정반대라 **한 번에 못 재고 두 번 돌려야 한다.**

```text
8ⓑ = 0.062   재사용 32건 중 2건    ← 8ⓐ(0.094)보다 낮다. 재평가 조건 발동 안 함
```

**8ⓐ 에서 틀렸던 3건을 그대로 추적한 것이 이 측정의 핵심이다.**

| seed | 감사에 뽑혔나 | 8ⓑ 에서 | 결과 |
| --- | --- | --- | --- |
| 27 | ✅ 뽑힘 → 사람이 정정 | **사람 답 재사용** | **✅ 고쳐졌다** |
| 26 · 34 | ❌ 안 뽑힘 | AI 답 그대로 | ❌ 퍼졌다 |

설계한 다섯 단계가 실제로 이어졌다 — **AI 가 확신하며 틀림 → 감사가 뽑음 → 사람이 정정 → 정정이 재사용 1순위로 반영(D-033·D-036) → 전파 차단.**

뒤집으면 **8ⓐ-2 가 놓친 2건이 다음 문의의 오답으로 이어진 것**이다. 「5% 샘플링이 못 잡는 몫」이 종이 위의 비율이 아니라 **실제 피해**로 나타났다.

> **재사용은 증폭 장치가 아니라 전파 장치다.** 원본을 그대로 옮기고, 원본이 정정되면 정정도 함께 옮긴다 — 무엇을 전파할지는 감사가 정한다.

**D-043 ⓒ 도 숫자로 확인됐다** — 같은 입력 50건인데 재사용을 켜면 AI 가 답한 건이 **47 → 18**, 자동 확정이 **32 → 3** 이 된다. 이 조건으로 8ⓐ 를 재면 분모가 3건이라 1건 차이로 비율이 33%p 씩 움직인다.

### ⑤ TRI-66 이 두 번 일을 했다

**첫 번째 — 측정 1·8ⓐ 에서 감사 과다 추출을 잡았다.**

```text
설정 0.05  vs  실측 0.156   (자동 확정 32건 중 5건, 확률 약 2%)
```

세는 방법 **세 가지가 전부 5건으로 일치**해(검토 큐 · `stats` 필드 · 앱 로그) 집계 오류가 아님을 확인했다. **이 값이 없었으면 "감사가 3건 중 1건을 잡았다"만 보고 넘어갔을 것이고, 8ⓐ-2 가 낙관 편향됐다는 것도 몰랐다.**

**누적값만 봐도 틀렸다** — 전수라 이전 실행분이 섞여 `0.184` 로 나오고, **측정 전후 차이라야 `0.156`** 이다. 위 ①의 세 번째 판단(캐시 안 함)과 같은 함정이 실측에서 재현된 셈이다.

원인은 **단정하지 않았다.** 재실행하면 AI 답이 달라져 어느 쪽이 그 조건의 결과인지 애매해지므로, 그 회차를 그대로 기록했다. **8ⓑ 에서는 `0.086` 으로 설정값에 가까웠다** — 우연이었다는 방증이지만 두 번의 관측으로 단정하지 않는다.

**두 번째 — 8ⓑ 에서 검산 장치 자체의 결함을 드러냈다.**

「검토 큐 기준 3건 vs 통계 기준 0건」으로 어긋났다고 나왔는데, 파보니 **집계가 `audit.autoAccepted` 만 보고 `reused` 를 안 본** 탓이었다. **8ⓐ 에서는 재사용 판정이 0건이라 안 드러났다.** 두 블록을 합쳐 빼도록 고치고 8ⓐ 원자료로 회귀 확인했다(`0.156` 그대로).

> 장치가 **멀쩡한데 고장 신호를 내는 것**이 진짜 어긋남보다 위험하다 — 다음에 진짜로 어긋났을 때 *"또 그거겠지"* 하고 넘기게 된다.

### ⑥ 감사 삽입을 트랜잭션 밖으로 빼본 대조 실험 (TRI-86, PR #66)

D-012 의 *"측정 장치를 트랜잭션 밖에 두지 않는다"* 가 **주장으로만** 있던 것을 숫자로 바꿨다. 같은 입력 10건을 세 판으로 돌렸다.

| 판 | 큐 저장 | 모집단 | 들어간 표본 | **샌 건** | 앱이 알아챘나 |
| --- | --- | --- | --- | --- | --- |
| 안 (현행) | 정상 | 10 | 10 | **0** | — |
| **밖 · 평범** | **정상** | 10 | 0 | **10** | **아니오** ⚠️ |
| 밖 · 분리(`REQUIRES_NEW`) | 정상 | 10 | 10 | **0** | — |
| 안 (현행) | 실패 | 0 | 0 | — | **예 (예외)** |
| 밖 · 분리 | 실패 | 10 | 0 | **10** | 아니오 |

**「밖에 뒀다」는 사실만으로 새지는 않았다** — `REQUIRES_NEW` 로 확실히 분리한 판은 10건이 다 들어갔다. **D-012 를 *"밖에 두면 반드시 샌다"* 로 읽으면 틀린다.** 0 이라고 그대로 적었다.

**그런데 평범하게 옮기면 아무것도 안 터졌는데 전부 샜다.** 커밋 후에 저장 묶음을 그냥 열면 스프링이 **이미 커밋된 그 트랜잭션의 자원을 물려받아** INSERT 가 아무 데도 안 간다. 예외 0건 · 로그 0줄 · 표본 10건 소멸.

**실패했을 때 갈리는 것은 「잃는 양」이 아니라 「잃었다는 사실이 보이나」였다.** `안` 은 감사를 못 넣으면 분류 자체를 포기하지만 그 손실이 `RECEIVED` 로 남아 계기판에 잡힌다. `밖` 은 분류를 지키는 대신 **측정의 분모만 조용히 깎는다.**

### 도구를 남겼다 — 다시 돌릴 수 있게

`evidence/measurement-1-8a/` 에 **재는 것과 집계하는 것을 나눠** 뒀다.

```text
run.sh          앱에 넣고 원자료를 모은다        AI 를 50번 부른다
analyze.py      원자료로 표를 만든다             AI 를 안 부른다
compare_labels.py  틀린 건을 사람 답과 대조한다   AI 를 안 부른다
```

**집계가 틀렸을 때 AI 를 다시 50번 부르지 않기 위해서다.** 실제로 ⑤의 두 번째(검산 결함)를 고친 뒤 **원자료만으로 회귀 확인**을 했다.

- **조건이 틀리면 AI 를 부르기 전에 멈춘다** — 8ⓐ 는 재사용이 켜져 있으면, 8ⓑ(`MEASURE=8b`)는 꺼져 있으면 중단. **한쪽 조건으로 다른 쪽을 재면 분모가 0 이 되거나 표본이 줄어드는데 둘 다 눈에 잘 안 띈다**
- 원자료 파일이 측정당 61개였던 것을 **9개로** 줄였다(`details.jsonl`). **하나의 JSON 배열로는 안 합쳤다** — bash 로 배열을 만들면 중간 한 건이 실패할 때 파일 전체가 깨진 JSON 이 된다. **옛 형식도 계속 읽는다**(이미 커밋된 원자료로 재집계해야 하므로)

### 개발 중에 잡은 것

- **설계 오류 1건** — 분류 완료 대기를 `stats.stuckReceived` 로 잡았는데, 그건 **10분 넘긴 건만 세므로** 넣자마자 "다 끝났다"로 읽힌다. 넣은 건의 `status` 를 직접 확인하는 방식으로 고쳤다
- **원자료 오염 버그 1건** — 이전 실행의 `confirm-<큐id>.json` 이 남으면 큐 id 가 재사용돼 **이번에 안 뽑힌 건이 「사람이 확정한 건」으로 읽힌다.** 지우지 않고 타임스탬프를 붙여 옆으로 치우도록 고쳤다
- **주석의 근거 과장 1건** — 리포지토리 쿼리의 `reason` 등치가 격리 건을 걸러낸다고 적었는데, 실제로는 `verdict IN` 절이 이미 걸러내 **그 조건을 지워도 테스트가 안 터진다.** 방어로 남기되 **지금 무엇을 안 지키는지** 주석에 적었다
- **실패 사례 2건 추가** (25·26) — 특히 **26(프레임워크 동작을 「알고 있다」고 단정하고 기대값으로 박아 넣음)**. 하필 틀린 방향이 「안전한 쪽」이라, 그대로 통과했다면 *"D-012 는 과한 걱정이었다"* 를 evidence 에 적을 뻔했다

#### 다음

- **P2 코드는 닫혔다.** 남은 측정은 **12(검토자 불일치)** 뿐이고 그건 세 사람이 함께 한다
- **§7 경계표 보강** — 위 ③의 부류 B 2건과 `SERVICE_USAGE` 세 번째 신호. 다만 **표를 고치면 이미 잰 숫자의 기준이 바뀌므로** 결론을 먼저 남기고 별도 결정으로 다룬다
- **남의 구역 잔여** — `GET /api/stats` 나머지 블록·`/api/policies`(TRI-68·69, 김은빈)

---

## 2026-08-13 (목) — 관측이 닫혔다 (P1: 지표 배선 · 측정 6·11 실측 · 정정 시나리오)

> 스토리 **TRI-23**("개발자는 캐시 hit 비율과 AI 절감률을 다른 숫자로 본다")의 두 하위작업 **TRI-70·71** 이 닫히면서 스토리도 마감됐다. 함께 미뤄뒀던 **TRI-45**(정정 반영 시나리오)도 이 로그에 반영한다.

### 개인 — 이용택 (P1)

- **① 지표 배선 (TRI-70, PR #76)** — 계약 §8 에 이름만 있던 계기판 3개를 실제로 붙였다.
  - `triage.ai.calls`(counter, 실제 AI 호출 — 재사용은 이 지점에 도달 안 해 구조적 제외) · `triage.queue.backlog`(gauge, 10초 캐시 경유) · `triage.classification.success.rate`(gauge, FAILED 만 실패로 세는 정의 = **D-065**).
  - **`cache.gets` 계약 정정** — 1단 캐시는 커스텀 Redis 라 스프링 자동 `cache.gets` 로 안 잡힌다. 실제 hit/miss 는 이미 `triage.cache.classification.hits/misses`(TRI-68)가 세고 있어 **코드가 아니라 계약 §8 을 실제 이름에 맞췄다**(같은 값을 두 이름으로 안 내보낸다).
  - **curl 실측** — 앱 기동 후 6개 지표 모두 `/actuator/metrics` 로 조회됨(`stuck_received` 는 실 DB 잔여 7건을 실제로 읽음).
- **② 측정 6·11 실측 (TRI-71, PR #76)** — 1000건을 실제 접수→비동기 분류에 통과시켜 쟀다. **AnthropicClient 만 가짜(deep stub)로 넣어 진짜 `classify()` 가 돌게** 해서 `triage.ai.calls` 를 측정 지점으로 썼다(서비스를 목으로 갈면 카운터가 안 올라 측정 지점이 사라진다). 실 Redis + MySQL Testcontainers.
  - 실측: 실제 AI 호출 **510** / 재사용 **490**(1단 454 + 2단 DB 36) / **절감률 49.0%** / 캐시 hit 비율 **45.4%** / 동시 유입 중복 호출 손실 **60**.
  - **hit 비율(45.4%) ≤ 절감률(49.0%)** — 갈린 36건이 2단 DB 재사용이라 **D-014("캐시가 줄이는 것은 DB 조회이지 AI 호출이 아니다")를 실측으로 확인**. 절감이 나와 D-031 재평가 조항 미발동. (`evidence/measurement-6-11-actual.md`)
- **③ 정정 반영 시나리오 (TRI-45, PR #70)** — 접수→저확신 격리→상담원 확정(③)→같은 키 재사용까지 end-to-end 통합 테스트. 사람 답이 AI 답을 덮고 체인이 안 생기는 것을 못박았다.

### 개발 중에 잡은 것 — 코드리뷰 반영

- **success.rate gauge 캐시 우회 (CodeRabbit Major, PR #76 에서 수정)** — `StatsService.classificationSuccessRate()` 가 같은 클래스의 `@Cacheable classification()` 을 self-invocation 으로 불러 **프록시를 안 거쳐 스크랩마다 DB 를 쳤다.** 계산을 `Classification` 레코드의 순수 메서드로 옮기고 gauge 가 `s.classification().successRate()`(프록시 경유)로 캐시를 타게 고쳤다 — 문서와 동작이 이제 일치.

### ⚠️ 측정이 드러낸 결함 (별도 티켓 후보 · 이번 PR 범위 밖)

- **대기줄이 넘치면 분류가 유실된다.** 1000건을 한꺼번에 던지자 대기줄(50)이 넘쳐 `CallerRunsPolicy` 가 분류를 **①의 `AFTER_COMMIT` 스레드에서 인라인 실행**했고, 거기서 ②의 `@Modifying` 이 "no transaction is in progress" 로 죽어 **문의 1건이 유실**됐다(`RECEIVED` 방치). D-031(①·② 분리)×D-047(CallerRunsPolicy) 상호작용. **이 시스템이 막으려는 「조용히 유실」을 스스로 만드는 구멍**이라 `@techietaek` PAAR 카드(문의 유실 방지)와 직결된다. 소관은 P2(김준현). 지금은 `evidence/measurement-6-11-actual.md` 에 기록만 하고, 수정(예: ②를 `REQUIRES_NEW`)은 별도 티켓으로 뺀다.

### 위 결함을 이어받았다 — 비동기 유실 방지 PAAR (이용택, `@techietaek` 카드)

> 바로 위 「측정이 드러낸 결함」을 이 카드의 Problem 으로 이어받아, **발견에서 그친 것을 근본 원인 규명 + 수정 증명까지** 끌고 갔다. 운영 코드는 한 줄도 안 바꿨다(TRI-86 선례).

- **세 실패 모드를 한 자리에서 재현** (`AsyncLossPreventionIT`, 신규 · 3건). 겉은 "분류 안 됨"으로 같지만 손실 의미가 다르다 — **복구성 × 관측성** 으로 등급을 매겼다.
  - **A. AI 3회 실패** → `FAILED` 기록 + `CLASSIFY_FAILED` 큐, stuck 0 → **검토가능** (유실 0)
  - **B. ② 저장 실패** → 롤백 + `RECEIVED`, stuck 1 → 관측됨 (측정 3 인용, 재측정 아님)
  - **C. 대기줄 포화 인라인** → `no transaction is in progress`, `RECEIVED` 방치, stuck 1(뒤늦게) → **조용한 유실**
  - **C'. C + `REQUIRES_NEW`** → 정상 전이 + `LOW_CONFIDENCE` 큐, stuck 0 → **검토가능** (유실 0)
- **근본 원인은 CallerRunsPolicy 가 아니라 트랜잭션 이음새다.** 부하는 트리거일 뿐, 원인은 "완료된 `AFTER_COMMIT` 문맥에서 ②의 REQUIRED `@Modifying` 을 돌리는 것". 그래서 **부하 없이** `afterCommit()` 콜백에서 ②를 호출해 결정적으로 재현했다 — 예외 메시지가 측정 6·11 부하 실측과 정확히 일치.
- **버그는 D-031 에도 D-047 에도 없다. 둘이 만나는 이음새에 있다** — 부분의 정합성이 합성의 정합성을 보장하지 않는다.
- **수정 증명(운영 미적용)** — `REQUIRES_NEW` 트랜잭션 템플릿으로 효과만 시연(모드 C'). 제안 코드 diff·대안 3종은 **D-066**(제안·채택 대기) + `evidence/async-loss-prevention.md`. 채택은 P2(김준현) 협의 후.
- **Result gate 의 판단** — gate 세 항목(원문0·검토가능·**stuck 관측 여부**)은 **모드 A 한 입력에서 다 확인된다**(원문 그대로·`CLASSIFY_FAILED` 큐·임계 넘겨도 stuck 0). `AsyncLossPreventionIT` 모드 A 가 셋을 한 번에 assert. 배타적인 것은 gate 안이 아니라 **모드 A(검토가능·stuck 0) vs 모드 C(검토가능X·stuck 1)** 사이다.
- **한계** — 운영 미적용(답은 "해결"이 아니라 "규명·증명·채택 대기") · 「종료 중 창」은 이 수정으로도 안 닫힘(나중에 할 것 E) · 1회 실행.
- **부하 실측으로 대량 확인(`SeamLoadIT`)** — 실행기를 포화(worker 1·큐 1)시켜 200건을 흘렸더니 현행(REQUIRED) **유실 177**, `REQUIRES_NEW`(테스트 전용 빈) **유실 0**. 유실된 177건은 `receive` 예외 0 — 호출부는 아무것도 못 받는 **진짜 조용한 유실**. hey/k6 가 아니라 JUnit 통합 테스트다(결함이 HTTP 가 아니라 서비스·트랜잭션 계층 · 유실은 DB 상태로 셈 · 응답시간 아닌 유실 건수 측정이라 hey/wrk 규칙 대상 아님). ⚠️ 88.5% 는 극단 포화값이지 운영 유실률 아님(자연 상태 ≈1/1000). 상세 `evidence/async-loss-prevention.md` 「부하 측정」.
- 테스트 **339건** 통과(실패 0 · skip 5, 본인 실측). PAAR 카드 Result·체크박스 4개 반영.

**다음**: ~~**D-066 채택** — 운영에 `REQUIRES_NEW` 적용 + 애노테이션판 통합 테스트, 별도 티켓(P2) + 김준현 협의.~~
→ ✅ **2026-08-14 완료 (TRI-96 · D-068).** 애노테이션 경로로 유실 0 확인. 다만 **커넥션 풀을 넘는 동시 인라인에서는 유실이 0 이 아니라는 것**을 새로 발견했다 — 아래 8/14 절 참조.
### 개인 — 김준현 (P2) — 측정 2 + 문서 정리

- **① 측정 2 실측 (TRI-95, PR #79)** — 50건 중 **23건이 사람에게 갔다.** 사유별 몫 · 세는 방법 두 가지 검산 · 같은 신호 두 번에도 큐 1건(D-049) · 못 읽은 건이 `0` 아닌 `null`(D-022) · **값 검증 4종이 기준값 비교보다 앞인지**(D-034) 까지 한 문서에 담았다 (`evidence/measurement-2-review-queue.md`).
- **② 문서 (PR #81·#83)** — README 를 부트캠프 템플릿에서 우리 프로젝트 소개로 바꾸고, `DOMAIN-MODEL.md` 에 **엔티티 ↔ DB 컬럼 대조표**와 enum 값별 설명을 넣었다. 템플릿은 지우지 않고 접어서 보존했다.

### ⚠️ 속도 미달을 파보다 유실 버그와 만났다 (8/13 저녁)

**속도 a·b 실측(TRI-74, PR #77)이 둘 다 목표 미달로 나온 뒤 "그래서 무엇을 할 수 있나"를 정리하다,
같은 자리에서 이미 기록돼 있던 유실 버그(위 절)와 만났다.** 셋을 남긴다.

- **ⓐ 접수 코드가 느린 게 아니었다** — 응답이 **두 덩어리로 갈렸다.** 73~74% 는 1초 미만이고 **26~27% 만 6~9초대**다. 평균을 냈으면 "전체적으로 좀 느리네"로 읽고 **엉뚱한 곳을 고쳤을 것**이다. 느린 건 **분류를 떠안은 요청뿐**이다.
- **ⓑ ★ 「떠안아도 안 잃는다」가 부하에서 거짓이다** — D-045 ⑤ 의 근거는 *"느려지는 건 보이지만 사라지는 건 안 보인다"* 였는데, 위 유실 버그는 **느려지면서 동시에 사라진다.** **대기줄이 차는 그 순간에 둘 다 일어난다.**
  - ⚠️ **다만 「같은 원인」은 아니다 — 「같은 조건에서 함께 일어난다」가 맞다.** 공통인 것은 방아쇠(대기줄 포화 → 인라인 실행)이고, 그 뒤는 갈린다. **느림은 「AI 호출이 오래 걸려서」, 유실은 「그 자리에 트랜잭션이 없어서」** 다. AI 호출이 0.001초에 끝나도 유실은 그대로다.
  - **이 구분이 실무에서 중요한 이유**: `REQUIRES_NEW`(D-066)는 **유실만 고치고 속도는 그대로다.** 원인이 같았다면 한쪽을 고칠 때 다른 쪽도 고쳐져야 하는데 그렇지 않다. **둘 다 고치는 것은 DB 대기열뿐이고, 원인이 같아서가 아니라 공통 조건인 인라인 실행 자체를 없애기 때문이다.**
  - **그런데도 둘을 나란히 놓는 이유**: 속도만 보면 *"약속대로 느려졌구나, 대신 안 잃었으니 됐다"* 로 읽힌다. **속도 미달을 「받아들일 만한 대가」로 볼 근거가 사라졌다**는 것이 핵심인데, 그건 둘을 같이 놔야 보인다.
  - 그래서 개선 선택지의 **0순위를 「②를 `REQUIRES_NEW` 로」로 올렸다** — 속도와 무관하게 이게 먼저다. **사실상 한 줄**이다.
- **ⓒ TRI-94 가 유실 방지용으로만 보였는데 속도 답이기도 하다** — *"멈춘 문의를 다시 분류한다"* 를 만들려면 **「분류할 일 목록을 DB 에 둔다」**가 필요한데, 그게 곧 접수 지연의 근본 해법이다. **다만 둘은 같지 않다** — TRI-94 는 **사후 회수**(멈춘 걸 나중에 주움)라 유실은 막아도 **접수는 여전히 느리다.** 속도까지 잡으려면 주 경로 자체를 바꾸는 **사전 예방**까지 가야 한다. 이 구분을 안 하면 "TRI-94 하면 속도도 해결"로 잘못 읽힌다.

> 선택지 5개와 각각의 대가는 `evidence/api-response-time-improvement-options.md` 에 정리했고 **`DECISIONS.md` 에는 넣지 않았다** — **아직 고른 게 없다.** 무엇을 고를지는 「동시에 몇 건까지 들어오는가」가 정해져야 하는데 이번 측정은 **동시성 10 을 임의로 골라** 잰 것이다. 지금 넣으면 재보기 전에 고른 답이 결정 로그에 영구히 남는다.

**문서 동기화가 하루 늦었다** — PR #77 이 속도를 실제로 쟀는데도 `PRD.md` §9 는 **「(아직 안 쟀다)」** 로, `LIFECYCLE-COVERAGE.md` 는 **「속도 목표(a·b)는 한 번도 재지 않았다」**로 남아 있었다. 「안 만든 걸 만든 것처럼 쓰지 않는다」의 **반대 방향** — **잰 걸 안 잰 것처럼** 남는 것도 같은 문제다. 측정 PR 과 문서 갱신이 다른 사람 손에 있을 때 생기는 지연이라 기록해둔다.

---

## 2026-08-13 (목) — P3 잔여 전부 머지 (김은빈)

### 개인 — 김은빈 (P3)

- **PR #69** (TRI-68·69) — `GET /api/stats` cache 블록 + `GET /api/policies` 에 `reuse.enabled` 노출(D-062). 코드리뷰 중 `aiCallSavings.savingsRate` 계산에 **대기 중(미판정) 문의가 분모에 섞여 들어가는 버그**를 발견해 같은 PR 에서 수정
- **PR #71** (TRI-78) — 측정 12(검토자 간 일치도) 세 번째 라벨러로 재확인. 일치율 **80.0%**(참고값) — 측정 8ⓐ 인용은 그대로 TRI-76 의 90.0% 사용
- **PR #72** (TRI-87) — 검토 큐 깊은 페이지 `EXPLAIN` + `@EntityGraph` vs 프로젝션 실측(측정 5ⓕ). 프로젝션이 응답시간 56%·편차도 훨씬 작게 나와 깊은 페이지 조회는 프로젝션 채택
- **PR #77** (TRI-74) — 핵심 흐름 e2e 시나리오(투입→격리→확정) + 속도 측정 a·b 실측. **둘 다 목표 미달을 그대로 기록** — 접수 p95 6.36~8.82초(목표 0.1초), 검토목록 p95 7.59초(목표 0.2초). 실제 API 키로는 오히려 더 나쁨을 확인

### 다음

- P3 코드 범위는 이걸로 닫혔다. 남은 것은 측정 12(검토자 불일치)뿐이고 세 사람이 함께 한다

---

## 2026-08-14 (금) — 검토 항목 선점(claim, TRI-93, D-032 → D-067) + 속도 개선 방안 정리(TRI-74 후속)

### 개인 — 김은빈 (P3)

- **PR #85** (TRI-74 후속) — 동시성 1(경합 없음)로 3회 추가 실측, 경합을 없애도 목표(0.1초)를 8~31배 초과하는 것을 확인. PR #84(김준현)와 접수 API 개선 선택지 비교 내용이 겹쳐 whitejh 지적으로 정리 — 선택지 5개·최악 96초 계산·우선순위는 #84 로 넘기고, 이 PR엔 동시성 1 실측만 남김. "왜 아직 `DECISIONS.md`에 안 넣었나" 절도 "새 결정 필요"가 아니라 "D-045 재평가 조항이 이미 발동 조건(p95 > 1초)을 충족했다"는 내용으로 정정
- **PR #86** (TRI-93, D-032 → **D-067**) — 검토 항목 선점(claim). 낙관적 락(D-021, 사후 감지)에 선점(사전 예방)을 더해 D-007 「경합 성격이 다르면 수단도 다르다」를 다시 주장할 수 있는 상태로 만들었다
  - `PATCH /api/inquiry-review-queue/{id}/claim` — `isClaimAvailable` 사전 판단 + 기존 `saveAndFlush` 의 `@Version` 커밋 시점 충돌 감지. 확정(`resolve`)과 같은 배선을 재사용해 새 동시성 수단을 만들지 않았다
  - 만료 스윕(`ReviewQueueClaimSweeper`, 기본 5분) — 벌크 UPDATE, `@Version` 미적용(의도적, 경계 사건의 위험이 낮다고 판단)
  - blind 규칙(D-010)을 선점 필드까지 확장 — 값을 가리는 대신 목록에서 제외, 응답엔 `claimedByMe` boolean 만
  - 컬럼 2개(`claimed_by`·`claimed_at`) + 인덱스(V3) — `EXPLAIN` 실측으로 새 인덱스가 목록 조회가 아니라 스윕 전용임을 확인(`evidence/query-plan-review-queue.md`)
  - 동시성 재현 테스트 `ReviewClaimConcurrencyIT` — `CyclicBarrier` 로 MySQL 40회 재현, `SUCCESS` 1·`CONCURRENT_UPDATE` 1로 매번 갈리고 500 에러 0건 (실행 환경: MySQL 8.0, 격리 수준은 기본값(REPEATABLE READ)·명시적 지정 없음. 재현: `./gradlew test --tests "*.ReviewClaimConcurrencyIT"` — 별도 evidence 파일 없이 테스트 클래스 자체가 근거)
  - CodeRabbit actionable "RESOLVED 상태 항목도 선점 가능" + 테스트 커버리지 gap 2건(`SecurityConfigTest`·`ReviewQueueControllerTest`) 미반영

### 다음

- CodeRabbit 미반영 지적 처리 여부 결정 — `status == PENDING` 검사 추가할지
- 분포 편향·추정치 오차(20건→120건, `filtered` 46%→99%)를 `ReviewQueueDeepPageExplainIT` 패턴의 자동화 IT 테스트로 옮기기 (5일 범위 밖)
## 2026-08-14 (금) — 유실 수정을 운영에 붙였다 (P2: D-066 채택 · 커넥션 한계 발견)

### 개인 — 김준현 (P2) — TRI-96

**PR #87 이 「효과」를 증명했고, 이번에 「진짜 자리」로 옮겼다.** 운영 `ClassificationService` 의 ② 두
입구를 `REQUIRES_NEW` 로 바꿨다 — **애노테이션 2줄 + import 1줄**, 스키마·계약·API 전부 불변 (D-068).

- **① 애노테이션 경로를 확인했다 (D-066 재평가 조항이 요구한 것)** — 시연은 트랜잭션 템플릿·테스트 전용 빈이었으므로 **운영 클래스의 애노테이션이 프록시를 타는지**가 미확인이었다. 스프링의 `@Transactional` 은 **같은 클래스 안에서 자기 메서드를 부르면 무시**되므로 이건 재봐야 알 수 있다. 모드 C' 에서 감싸는 템플릿을 없애고 운영 빈을 그대로 불러 **유실 0** — 먹는다. 두 메서드의 호출 지점을 전수 확인해 **자기 호출이 없다**는 것도 함께 봤다. 부하도 같은 값이 나왔다 (200건 · 접수 스레드 8 · **177 → 0**).
- **② ⚠️ 커넥션이 마르면 완전히는 못 막는다 (새로 발견)** — 원본 문서가 *"더 높은 동시성에선 풀 여유를 다시 봐야 한다"* 고 한계로 적어둔 자리를 실제로 재봤다. 접수 스레드를 **24**(커넥션 풀 20 **초과**)로 올리니 `SQLTransientConnectionException` → `CannotCreateTransactionException` 으로 **유실 122~142**. `REQUIRES_NEW` 는 **인라인마다 커넥션을 하나 더** 쓴다(①의 것이 정리되기 전에 ②용을 새로 요구).
  - **그래도 채택 근거는 유지된다** — 같은 조건에서 `REQUIRED` 는 **195** 를 잃고 예외가 **0건**(완전히 조용)인데, `REQUIRES_NEW` 는 **32건이 호출부로 전파**된다. **유실의 성격이 조용한 쪽에서 보이는 쪽으로 옮겨간다** — D-066 이 노린 것이 부분적으로 작동한다.
  - **★ 이게 드러낸 별개 문제**: D-047 ⓑ 의 불변식 `max-size(8) < pool(20)` 은 **인라인 실행을 계산에 넣지 않았다.** 커넥션을 쓰는 주체가 분류 스레드(상한 8)만이 아니라 **인라인 실행하는 접수 스레드(톰캣 기본 200)** 이기도 하다. **전파 방식이 아니라 자원 상한 문제**라 `REQUIRES_NEW` 로 안 고쳐진다 — 풀 크기·톰캣 스레드 손잡이는 **속도 개선(TRI-97)의 선택지 ①② 와 같은 자리**라 거기서 함께 본다. 여기서 섞으면 "무엇이 유실 0 을 만들었나"의 검증 축이 둘이 된다.
- **③ 대조 실험의 역할을 뒤집었고, 지우지 않았다** — 운영이 `REQUIRES_NEW` 가 되면서 `SeamLoadRequiredIT` 와 `AsyncLossPreventionIT` 모드 C 의 "유실 발생" 단언이 깨지게 됐다(PR #87 이 *"채택 시 갱신/삭제한다"* 고 예고해둔 자리). **삭제하지 않고 `REQUIRED` 강제 빈으로 옮겼다** — 지우면 누가 애노테이션을 되돌려도 **아무 테스트도 실패하지 않고**, 이 결함은 대기줄이 포화돼야만 보인다. **대조 실험 자체가 회귀 방어 장치다.**
  - 모드 C 의 대조군은 `@Primary` 로 끼우지 **않았다** — 모드 A 가 운영 경로 전체(리스너 → 재시도 → `@Recover`)를 그대로 타야 하므로 빈을 하나 **더** 등록하고 모드 C 만 이름으로 집는다.
- **④ 남의 evidence 를 고치지 않았다** — 원본(`async-loss-prevention.md`)은 이용택이 잰 기록이라, 채택 실측은 **새 파일**(`async-loss-requires-new-adoption.md`)로 쓰고 원본에는 **낡아진 서술 2곳의 정정 + 링크**만 달았다. 측정과 검토를 섞으면 나중에 어디까지가 원본인지 안 보인다 (PR #84 에서 쓴 것과 같은 방식).
- **테스트 351건** 통과(실패 0 · skip 5). **이 PR 이 새로 추가한 테스트는 0건** — 기존 4건의 역할만 뒤집었다.
- **24스레드 케이스는 상시 테스트로 두지 않았다** — 커넥션 대기 타임아웃이 30초라 이 케이스만 4분을 넘긴다. `SeamLoadBase` 의 `SUBMIT_THREADS` 를 24 로 바꾸면 재현되고, 그 방법을 주석과 evidence 양쪽에 적었다.

> **결정 로그 불변 원칙을 지켰다** — D-066 의 「제안·채택 대기」 본문을 「채택」으로 고치지 않고 **D-068 을 새로 썼다.** 상태만 바꿔 적으면 **채택 당시에 무엇을 몰랐는지**(= 커넥션 한계)가 사라진다.

---

## 2026-08-15 (토, 발표일) — 발표 자료 작성 + 문서 정합성 정정 (코드 변경 0줄)

### 개인 — 김준현 (P2)

**이 날은 새로 만든 기능이 없다.** 발표 자료를 쓰고, 그 과정에서 **문서끼리 어긋난 자리 셋**을 찾아 고쳤다.

**① 발표 자료 2종을 썼다**

| 파일 | 무엇 | 청자 |
| --- | --- | --- |
| `presentation/PRESENTATION-5MIN.md` | 5분 12장 대본 — 장별 문구 + 「말로 덧붙일 한 줄」 + 예상 질문 8개 | 심사위원 · 비개발자 섞임 |
| `presentation/SCRIPT-6-FEATURES.md` | 6대 필수 기능 대본 | 백엔드 개발자 |

- **문체를 둘로 갈랐다** — 슬라이드에 올라가는 문장은 **명사형**(…함 · …음), 입으로 말하는 문장은 **존댓말**. 슬라이드는 눈으로 훑고 대사는 귀로 듣기 때문이다.
- **용어는 버리지 않고 자리를 옮겼다** — 슬라이드는 *"같은 문의는 AI에게 다시 안 물어보기"*, 발표자 노트는 *"2단 캐시 — 1단 Redis → 2단 DB"*. `CLAUDE.md` 「문서 작성 규칙」 2 를 발표 자료에 적용한 것이고, **용어를 지우면 심사에서 "뭘로 구현했나요"에 답할 근거가 사라진다.**
- **못 넘은 목표를 앞쪽에 뒀다** — 속도 a·b 미달, 감사가 놓친 2건, 커넥션 초과 시 유실. *"재봤는데 못 넘었고 원인을 추적 중"* 이 *"안 재봤다"* 보다 강하다.

**② 발표 초안이 들고 있던 옛 숫자를 전수로 맞췄다**

초안(8/13 작성)의 숫자가 이틀 만에 여섯 개 어긋나 있었다.

| | 초안 | 실제 (8/15) |
| --- | --- | --- |
| PR | 78 | **91** (머지 90 · 열린 것 0 · 닫힘 1) |
| 테스트 | 310 | **351** (실패 0 · skip 5) |
| 결정 기록 | 65 | **68** (D-068 까지) |
| 근거 문서 | 20 | **25** |
| AI 오류 사례 | 26 | **27** |
| 접수 p95 | 6.36~8.82초만 | **+ 실제 키 회차 16.94~18.77초** |

> **발표 자료는 「그날의 스냅샷」이라 특히 빨리 낡는다.** 대본 3장에 **세는 명령**(`gh pr list` · `./gradlew test` · `grep -c '^### D-' DECISIONS.md`)을 같이 적어뒀다 — 다음 발표 때 이 자리에서 또 틀리지 않게.

**③ 문서끼리 어긋난 자리 셋을 고쳤다 (`LIFECYCLE-COVERAGE.md`)**

| 어디 | 뭐라고 적혀 있었나 | 실제 | 얼마나 오래 |
| --- | --- | --- | --- |
| 매트릭스 2. 코딩 꼬리 | 「잔여: 측정 12 · 속도 a·b · e2e」 | **셋 다 8/13 에 닫힘** (PR #71 · #77) | 2일 |
| 매트릭스 3. 테스트 | 「e2e 는 여전히 health 1건만 실행」 | **핵심 흐름 포함 2건이 실제로 돔** (PR #77) | 2일 |
| 미션 대조표 코딩 | API-CONTRACT **v1.7** | **v1.10** (v1.8·v1.9·v1.10 이 안 반영) | 3일 |

> **세 칸 모두 「다른 사람이 닫은 일」이었다.** 닫은 사람은 자기 절만 갱신했고 요약 표까지는 손이 안 갔다. 특히 **e2e 는 같은 파일 182행이 이미 「미션 잔여 0」이라 적고 있어서, 한 문서 안에서 두 칸이 서로 다른 말을 하고 있었다.**
>
> **이건 사람의 실수가 아니라 구조 문제다** — 같은 사실을 여러 칸이 각자 적으면 한 칸만 고쳐진다. D-050 이 `PROJECT-BRIEF.md` 를 PRD 에서 **한 방향으로만 파생**시키기로 한 것과 같은 부류이고, `LIFECYCLE-COVERAGE.md` 는 아직 그 규칙이 적용되지 않은 자리다. **다음 기수에 물려줄 항목으로 「회고」에 적는다.**

**④ `PAAR-CARDS.md` 의 @whitejh 카드에 「처음 보는 사람용」 층을 얹었다**

카드가 이미 Result 까지 닫혀 있었지만, **읽는 사람이 백엔드 개발자라는 전제**로 쓰여 있었다. 결론(0.094 · 0.333)에 도달하려면 감사·표본·낙관 편향을 알아야 해서, **숫자를 읽는 법 자체를 카드 안에 넣었다.** 기존 본문은 **한 줄도 지우지 않고** 앞뒤로만 덧붙였다.

### 다음

- **발표 직전 숫자 재확인** — 대본 부록 C 체크리스트
- `RETROSPECTIVE.md` · `presentation/DEMO-SCRIPT.md` · `SLIDE-OUTLINE.md` 는 **아직 템플릿 그대로다.** 발표 자료는 새 파일로 썼고 이 셋은 손대지 않았다 — **채울지 지울지는 팀이 정한다**

---

## 다음 액션

| 우선순위 | 액션 | 담당 | 근거 |
| --- | --- | --- | --- |
| 1 | ~~**트랜잭션 ②(`ClassificationService.verifyAndPersist`) + 큐 사유 매핑** — TRI-51·52~~ → **PR #26 으로 완료.** 중복 실행 차단(TRI-82)까지 포함 | 김준현 | **P2 의 관문이었다.** 리스너가 결과를 넘길 대상 |
| 2 | ~~비동기 분류 리스너 + 재시도/복구 — TRI-47·54·55, 자원 설정 TRI-83~~ → **2026-08-12 로 완전히 닫혔다.** 8/10 까지는 **AI 호출 갈래만** 채워져 있었고, PR #55 가 **재사용 갈래(TRI-47 잔여·TRI-53)** 를 붙였다 | 김준현 | 계약 A 수신자. 공급 측(TRI-40·41·84)이 P1 에서 온 뒤에야 소비 측을 붙일 수 있었다 |
| 3 | ~~정규화 규칙 확정~~ → **PR #18 로 완료** (D-052·D-053). **과도/과소 병합 테스트(TRI-43)는 잔여** | 이용택 | D-031 최대 리스크. 측정 6 의 전제 |
| 4 | ~~`service/`·`api/` 착수~~ → **2026-08-06~07 로 해소.** 셋 다 코드가 올라왔다 | — | 8/4·8/5 blocker 였다 |
| 5 | ~~측정 3 (**② 롤백 + ① 생존** + `stuckReceived` 증가)~~ → **PR #39 로 완료** (`ClassificationRollbackIT` · evidence). 감사 삽입 `REQUIRES_NEW` 대조(D-045②)는 감사 경로(TRI-64·65) 착수 후 잔여 | 이용택 | **필수 체크포인트**, D-017, D-031 |
| 6 | ~~측정 7ⓑ (동시 PATCH 409 2종)~~ → **2026-08-10 완료** — `CONCURRENT_UPDATE` 40/40 재현(`evidence/concurrent-review-confirm.md`) | 김은빈 | D-021. **7ⓐ 는 D-031 으로 삭제** |
| ~~7~~ | ~~e2e 시나리오를 핵심 흐름으로 교체~~ → **2026-08-13 완료 (TRI-74, PR #77)** — 「투입 → 격리 → 확정」 + 속도 a·b 실측을 한 PR 에서. **미션 잔여가 0 이 됐다** | 이용택 | — |
| 8 | `.claude/commands/` 작성 | 전원 | 코딩 단계 유일 잔여 도구. **잔여 2건 중 1건** |
| 9 | ~~Jira MCP dry-run~~ → **2026-08-07 시연 완료** | 김준현 | 상태 전이·기한 재배치·코멘트를 MCP 로 실제 수행 (TRI-15·49·50 등). **잔여 3건 → 2건** |
| 10 | **`API-CONTRACT.md` 어긋난 항목 — 1번 닫힘, 4건 잔여** | 김준현 | 아래 표 참조. **계약은 코드 PR 에만 동반 가능** |
| 11 | ~~`GET /api/policies` 에 `classification.reuse.enabled` 노출 여부 결정~~ → **D-062 로 결정 완료**, **계약 갱신도 PR #69 로 완료**(TRI-69) | 김준현 (결정) → 김은빈 (계약) | D-043 ⓒ + D-028 논리 |
| **12** | **결정 번호 충돌을 막는 절차를 헌법에 넣기** | 전원 | **사흘 동안 세 번 났다** (D-030 / D-051 / D-057). 기록만 하고 절차를 안 고쳐서 매번 renumber 비용을 냈다. **8/12 의 D-062·D-063 은 충돌 없이 지나갔지만 그건 사람이 먼저 확인한 덕이지 장치가 생긴 게 아니다** |
| **13** | **`ENABLE_AI_REVIEW` 저장소 변수를 켤지 정하기** | 전원 (리뷰 담당 김은빈) | 8/12 16:58 부터 **AI 리뷰가 opt-in.** 안 켜면 CodeRabbit 한 겹만 남고, **D-054 의 두 도구 비교 표본도 그만큼 줄어든다** |
| ~~**14**~~ | ~~**P2 잔여 — TRI-77 / TRI-66 → TRI-86**~~ → **전부 완료.** 여기에 **측정 2(TRI-95, PR #79)** 까지 8/13 에 닫혔다 | 김준현 | — |
| ~~**15**~~ | ~~**측정 6·11 실측 (TRI-71)**~~ → **2026-08-13 완료 (PR #76)** — AI 호출 510 / 재사용 490 / 절감률 49.0% / 캐시 hit 45.4% ≤ 절감률(**D-014 실측**). **지표 배선(TRI-70)·정정 시나리오(TRI-45)도 함께 닫혀 스토리 TRI-23 마감** | 이용택 | 재사용이 실제로 도니 1000건으로 잰다. **켠 상태로 잰다** (D-062) |

| **16** | ⚠️ **부하 시 분류 유실 버그 — TRI-96 (2026-08-13 생성)** | 김준현 (P2) | **위 「측정이 드러낸 결함」이 티켓 없이 하루 남아 있다가 이 정리에서 티켓이 됐다.** 대기줄이 차면 접수 스레드가 떠안다 `no transaction is in progress` 로 죽어 **판정 행도 큐도 없이 `RECEIVED` 방치**. 재시도도 안 걸린다. **수정은 ②를 `REQUIRES_NEW` 로 — 사실상 한 줄**인데 재현 조건(부하)이 있어 테스트가 일이다. `@techietaek` PAAR 카드의 Problem 에 정확히 해당하는 반례라 **카드 주인과 코드 소관이 다르다는 것부터 합의**가 필요하다 |
| **17** | **속도 개선을 고르기 전에 「동시에 몇 건까지 들어오는가」를 정한다** (검토는 **TRI-97**) | 전원 | 속도 a·b 측정이 **동시성 10 을 임의로 골라** 쟀다(`PRD.md` 에 지정값 없음). **동시성 1(경합 없음) 재측정은 2026-08-14 완료**(TRI-74 후속, PR #85) — 경합을 없애도 목표를 8~31배 초과해 "몰리지 않으면 괜찮다"가 아님을 확인. 다만 이 값도 하루 종일 반복한 부하 잔여물 위에서 잰 값이라 완전히 빈 대기열에서의 재측정은 여전히 남아 있다. 선택지는 `evidence/api-response-time-improvement-options.md` 에 정리돼 있고 **어떤 개선안을 고를지는 아직 안 정했다** |

**「API-CONTRACT 재대조」 6건의 현재 상태** (8/5 기록의 표를 그대로 추적)

| # | 어긋난 곳 | 상태 |
| --- | --- | --- |
| 1 | §3 모델 id 가 `claude-haiku-4-5-20251001` | ✅ **v1.5 에서 `claude-sonnet-5` 로 정정** (PR #23) |
| 2 | v1.2·v1.3 변경 이력의 PR 번호 `#12` (실제는 #11) | ❌ 잔여 |
| 3 | §4 의 「결정적 역산 / 확률적 추론」 옛 용어 | ❌ 잔여 — `api/` PR 에서 |
| 4 | §7 `byConfidenceBucket` 의 *"이 프로젝트의 결론이 나오는 자리"* (D-043 으로 8ⓐ-1 로 이동) | ❌ 잔여 — `api/` PR 에서 |
| 5 | §6 에 `classification.reuse.enabled` 없음 | 🔄 **결정은 끝났다 (D-062).** 계약 갱신은 **TRI-69(`GET /api/policies`, 김은빈) 구현 PR 이 동반** — 계약은 코드 PR 에만 붙일 수 있다 |
| 6 | `ONBOARDING.md`·`LIFECYCLE-COVERAGE.md` 의 계약 버전 표기 | ✅ 해소 (계약 파일이 아니라 작업 경계에 안 걸림) |

---

## 통계 (2026-08-15 기준 — 발표일 실측)

- **총 PR**: **91 (머지 90 / 열림 0 / 닫힘 1 — #82, #83 으로 대체됨)** — 2026-08-15 `gh pr list --state all` 실측
- **개인 PR 분담** (2026-08-15 실측): **김준현 51** (머지 50 · 닫힘 1) / **이용택 25** (전부 머지) / **김은빈 14** (전부 머지) / 운영진 1
  - ✅ **열린 PR 이 0 이다.** 8/14 에 4건이 열려 있었고(#84·85·86·87) 전부 머지됐다 — **develop 이 곧 최종 상태**라 발표에서 보여주는 코드와 저장소가 어긋나지 않는다
  - ⚠️ **이전 판(67건)은 8/12 스냅샷이었다.** 총계만 갱신하고 개인별을 안 고쳐 **합이 안 맞는 상태로 하루 있었다** — CodeRabbit 이 PR #84 에서 잡았다. **총계와 분담은 같은 시점 값으로 함께 고친다**
  - **8/5 기록의 "김은빈 0" 은 8/7 에 해소됐고, 그 뒤로 6건까지 늘었다.** P3 는 endpoint 수 기준 범위가 가장 커서 가장 늦게 붙은 것이 가장 아팠던 자리인데, 착수한 PR #19 가 `config/` + `api/` 공용 예외 처리까지 한 번에 덮었다
  - PR 수는 **일의 양이 아니라 쪼갠 단위**다 — 김준현 쪽에 기획 문서 PR 이 몰려 있어 코드 기여로 읽으면 틀린다
- **AI 리뷰가 잡은 진짜 이슈**: PR #1 에서만 5회 리뷰 / 지적 21건. 그중 **설계 결함으로 evidence 에 사례화한 것 9건**. 코드 PR 로 넘어온 뒤로는 **반영 커밋이 PR 마다 1~3건씩** 붙는다
  - ⚠️ **2026-08-12 16:58 부터 `ai-review` 가 opt-in 이 됐다** — 저장소 변수 `ENABLE_AI_REVIEW=true` 전까지 실행 0건. **CodeRabbit 만 도는 상태**
- **AI hallucination·오류 기록**: **27건** (`evidence/failure-cases.md`) — **27번은 8/13 에 추가됐다**(PR #83 리뷰): *"제안은 맞는데 근거가 반대"*. 지적이 맞아서 **고치기로 한 순간 근거를 검증할 이유가 사라지는** 것이 이 부류의 함정이다
  - 부류별: 설계 결함 5 / 허구 메커니즘 **4** / 내부 불일치 2 / 부분 관측의 일반화 **2** / 명세 불완전 2 / 논리 자기모순 1 / 낡은 지식 1 / 잘못된 인과 1 / 계약 누락 1 / 형식 준수 시늉 1 / 과소 구분 1 / 간접검증 오인 1 / 검증값 오산 1 / 검사 지점 오배치 1 / **추정치 요구 1** / **쉬운 말로 바꾸다 뜻을 좁힘 1** / **미검증 기대값을 단언에 박음 1**
  - **26번(미검증 기대값)은 8/13 에 생긴 새 부류이고 측정 코드에서 특히 위험하다** — *"밖에 둬도 정상 경로에서는 안 샌다"* 를 실행해보지 않고 테스트 단언에 미리 박았는데 실측은 정반대(10건 전부 소멸)였다. **하필 틀린 방향이 「안전한 쪽」이라** 그대로 통과했다면 *"D-012 는 과한 걱정이었다"* 를 evidence 에 적었을 것이다. **재보지 않은 수치를 만들지 않는다는 규칙이 문서뿐 아니라 테스트 단언에도 적용된다**는 뜻이다
  - **20번은 AI 가 아니라 파이프라인이 틀린 유일한 사례**다 (각 PR 은 통과시키면서 합친 결과를 아무도 안 봄)
  - **22번(추정치 요구)은 8/12 에 생긴 새 부류다** — 문서가 "아직 모른다"로 비워둔 자리를 약점으로 읽고 값을 채우라 한다. 따르면 **재보지 않은 값이 근거처럼 굳는다**
  - **8/12 하루에 4건(21~24)이 나왔고, 그중 셋이 「캐시 넣기가 어느 길로 가는가」에서 틀렸다** — **21·24 는 같은 자리**다(아예 안 불리는 `persistReuse` 를 지목). 23 은 원인이 다르다(헬퍼 본문을 안 열었다). **두 입구 구조(D-063)가 봇에게 반복해서 헷갈렸다면 사람에게도 그렇다**는 신호로 읽고 코드 주석에 경고를 남겼다 — D-063 재평가 조항의 관찰 자료가 된다
- **E2E 시나리오**: ✅ **2026-08-13 확장 완료**(TRI-74, PR #77) — health 1건 + `test.skip` 1건에서 **핵심 흐름(문의 접수 → 검토 목록 → 확정 → 통계)** 으로. **미션 잔여 ⓑ 가 닫혔다.** 현재 `tests/e2e/api.spec.ts` 에 **실행되는 시나리오 2건**(`test.skip` 없음). 단위·통합 테스트는 **351건**(2026-08-14 `./gradlew test` 통과분, 실패 0 · skip 5) — 8/7 의 88건에서 **88 → 222 → 292 → 310 → 325 → 339 → 351** 로 세 사람 몫이 함께 늘었다
  - ⚠️ **8/13 부터 GitHub Actions 가 안 돈다** — *"recent account payments have failed or your spending limit needs to be increased"*. **작업이 시작조차 안 되므로 로그도 없다.** 코드 문제가 아니고, PR #61·#64 까지는 `SUCCESS` 였다. 결제·한도를 풀기 전까지 `e2e` 체크는 계속 빨간불이다
- **측정 실행 횟수**: 실 AI 호출을 동반한 측정 **2회**(8ⓐ 50건 · 8ⓑ 50건) + 도구 점검 3회(LIMIT 2~3건). **원자료를 커밋해 뒀으므로 집계는 AI 없이 몇 번이든 다시 돌린다**
- **API 계약 변경**: v0.1 → **v1.10** (18회). v1.4 공용 예외 처리(TRI-25~30) / v1.5 값 검증 4종(TRI-49·50) / v1.6 적체 통계(TRI-67) / v1.7 감사율 필드(TRI-66) / v1.8 `audit` 블록 중복 구현 통합(TRI-66·68) / v1.9 관측 지표 3종 Actuator 배선 + `cache.gets` 이름 정정(TRI-70) / **v1.10 검토 항목 선점 endpoint 신설(TRI-93, D-032)**
  - **v1.10 만 endpoint 가 늘어난 변경이다** — 나머지는 기존 응답의 필드·표기 정정이다. **계약을 늘리는 변경과 고치는 변경을 같은 번호 체계로 세면 「18회나 바뀌었다」로 잘못 읽힌다**
- **의사결정 기록**: D-001 ~ **D-068** (68건). **D-042 까지는 설계·계약 결정이었고, D-051 이후는 「코드를 짜다가 정해야 했던 것」** 이다
  - D-051 생각하기 끄기 / D-052·D-053 정규화 키 / D-054 CodeRabbit / D-055·D-056 권한·테스트 격리 / D-057 Lombok / D-058 클래스 이름 / D-059 패키지 경계 / D-060 확신도의 대안 / D-061 띄어쓰기 과소 병합 유보 / D-062 재사용 스위치 · D-063 트랜잭션 ②의 두 번째 입구 / D-064 캐시 fail-open / D-065 분류 성공률의 정의 / **D-066 유실 수정 제안 → D-068 채택** / D-067 선점 구현 완료
  - ⚠️ **D-067 은 한 번 번호가 겹쳤다** — 같은 날 두 사람이 D-067 을 잡아서, 나중에 쓴 쪽을 **D-068 로 옮겼다**(커밋 `d8f55e3`). **번호를 당기지 않고 미는 쪽으로 해결한 것**은 이미 인용된 번호가 어긋나지 않게 하기 위해서다

### 이 팀에서 실제로 효과가 컸던 것

1. **AI 코드리뷰 반복** — 회차를 거듭할수록 문서 대조로는 못 잡는 층위로 내려갔다 (구조 → 원자성 → 논리 → 실행 가능성 → 검증 가능성). 한 번으로 끝냈다면 4·5차에서 나온 실행 불가 결함이 코드에 들어갔다
2. **결정 로그를 불변으로 운영** — 기존 항목을 고치지 않고 후속 항목으로 무효화(D-004→D-014, D-007→D-016, D-002→D-020). 덮어썼다면 "왜 틀렸었는지"가 사라졌다
3. **훅으로 편집 시점을 막기** — 되돌리는 비용이 가장 싼 지점이다. 단 정적으로 확실한 것만
4. **판별 기준을 헌법에 올려둔 것 (D-027)** — 도메인 전환 요구가 왔을 때 **탈락 사실과 그 대가를 계산할 수 있었다.** 기준이 없었다면 "골격은 그대로니까 도메인만 바꾸면 된다"로 넘어갔을 것이고, 그러면 그룹핑에 매달린 자산 6건이 사라지는 걸 나중에야 알았다

### 효과가 작았던 것

- **Jira MCP** — ~~등록만 하고 미시연~~ → **2026-08-07 에 실제로 썼다.** 이슈 88건(TRI-1~88)은 그 전에 이미 분해돼 있었고, 이날 MCP 로 **상태 전이 · 기한 재배치 · 진행 코멘트**를 돌렸다. 다만 여전히 **읽고 쓰는 도구**일 뿐 판단은 사람이 한다 — "AI 가 이슈를 관리해준다"는 아니다
  - 이날 확인된 실효: 하위 작업 기한을 옮기면 **부모 스토리·에픽 기한이 자동으로 안 따라온다.** 사람이 하위 최댓값을 보고 맞춰야 하고, 안 맞추면 보드가 "끝난 것처럼" 보인다
- **Sentry MCP** — 실 트래픽 0. 다만 **도메인 전환으로 역할 중복은 해소됐다** — 이전에는 이 시스템 자체가 에러 수집 파이프라인이라 Sentry 와 겹쳤지만, 지금은 이쪽이 **CS 문의**를 다루고 Sentry 는 **앱 자체의 장애**를 본다. 경계가 분명해져 시연 시나리오를 잡기 쉬워졌다

## 보강 항목 (로드맵 Phase 3)

- `PRD.md` §8 「나중에 할 것」 표 A~H 참조 — 문의 묶기 / 종류별 기준값 / 기준값 화면 변경 / JWT / 멈춘 문의 자동 재분류 / AI 장애 시 대기 후 재시도 / 감사 비율 런타임 변경 / **검토 항목 선점**
- **H(검토 항목 선점)만 성격이 다르다** — 나머지는 "여유가 없어서 미룬 것"이고 H 는 **D-031 이 비운 동시성 자리를 메우려고 후보에서 끌어올린 것**이다 (D-032). `service/`·`api/` 가 예정보다 빨리 끝나면 가장 먼저 붙인다
- **프로세스 구멍 1건 — 결정 번호에 잠금이 없다. 사흘 동안 세 번 났다.** 3명이 병렬로 PR 을 열면 `D-NNN` 이 선착순 자원이 되는데, `DECISIONS.md` 꼬리의 다음 번호 마커는 **자기 브랜치에서만 최신**이라 충돌을 못 막는다.

  | 일자 | 충돌 | 간격 | 어느 쪽이 비켰나 |
  | --- | --- | --- | --- |
  | 08-05 | D-030 — Sentry(PR #9) ↔ 도메인 전환 | — | 도메인 전환 계열 **4건을 통째로** (인용 146건 치환) |
  | 08-06 | D-051 — CodeRabbit ↔ AI 생각하기 끄기 | 5분 | CodeRabbit → D-054 |
  | 08-07 | D-057 — 클래스 개명 ↔ Lombok | 7분 | 개명 → D-058 |

  8/5 에 *"다음에 같은 일이 생기면 비용이 더 크다"* 고 적고 **후보 해법까지 써뒀는데 아무도 안 넣었다.** 두 번째·세 번째는 인용이 적어 renumber 가 쌌지만, 그건 운이지 절차가 아니다.
  → **후보**: 결정 신설 전 `origin/develop` 의 `DECISIONS.md` 를 먼저 확인하는 것을 훅으로 강제하거나, 번호를 PR 번호에 종속시키기. **「기록만 하고 절차를 안 고치면 같은 일이 계속 난다」는 것을 이 로그가 스스로 3회 증명했다**
- **가장 약한 곳은 검증 층이다.** AI 리뷰의 검출력이 **문서·설계 층에서만 입증**됐고 런타임 결함에 대해서는 아직 미검증이다 (`evidence/failure-cases.md` 「미검출 위험이 남은 영역」 9항목)

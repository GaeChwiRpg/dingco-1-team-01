# DECISIONS — 의사결정 로그

> 헌법 변경/기술 선택은 모두 여기에 새 항목으로 추가. 기존 항목은 수정하지 않고 후속 항목으로 추가.
> 예외: `상태` 필드는 갱신 가능 (보류 → 채택 등). 그 외 본문은 불변.
>
> 📖 모르는 단어는 [`GLOSSARY.md`](./GLOSSARY.md) 참조. **처음 읽는다면 아래 「한눈에 보기」만 훑어도 됩니다.**

## 한눈에 보기

> 57개를 다 읽을 필요는 없습니다. **굵게 표시된 5개가 이 시스템의 뼈대**이고, 나머지는 그 결과이거나 세부 사항입니다.
> **항목을 추가하면 이 숫자도 같이 고칩니다** — 표가 낡으면 인덱스가 아니라 오답이 됩니다.
> 「정정」 표시는 **앞선 결정이 틀렸다는 걸 나중에 알고 고친 것**입니다 — 원래 항목을 지우지 않고 새 항목으로 덮는 방식이라, 왜 틀렸었는지가 그대로 남습니다.
> **⛔ 표시는 D-031(도메인 전환)으로 폐기된 것**입니다. 지우지 않는 이유도 같습니다 — 무엇을 포기하고 이 도메인으로 왔는지가 남아야 합니다.

| # | 쉽게 말하면                                                                                   | 비고 |
| --- |------------------------------------------------------------------------------------------| --- |
| D-001 | **주제를 정했다** — AI 가 곁다리 기능이 아니라 주제 자체인 것을 고름. "AI 를 썼다"가 아니라 "AI 를 못 믿는 설계를 했다"           | 주제는 D-031 으로 교체, **고른 이유는 승계** |
| D-002 | 라이프사이클 5단계를 3명이 나눠 맡음 (1명은 2단계 겸임)                                                       | → D-020 에서 확정 |
| D-003 | 기술 스택 — Spring Boot 3.3 / Java 21 / MySQL 8 / Redis                                      | AI 부분은 D-024 로 교체 |
| D-004 | ~~같은 에러 1000번 = 판정 1번. 판정 단위를 "에러 1건"에서 "에러 그룹"으로 올림~~                                   | ⛔ **폐기** — 문의는 못 묶음 (D-031) |
| D-005 | **자동 통과한 것도 20건 중 1건은 몰래 뽑아 사람이 다시 봄.** 안 그러면 AI 가 자신 있게 틀린 걸 영원히 못 찾음                   | 이 프로젝트의 차별점 |
| D-006 | ~~카테고리마다 합격 점수를 다르게 — 틀렸을 때 손해가 큰 쪽을 더 엄격하게~~                                            | ⛔ **폐기** — 스코프 밖 (D-031) |
| D-007 | **동시성 문제를 한 방법으로 통일하지 않고 3가지로 나눔** — 상황마다 성격이 달라서                                        | 일부 D-016 정정 · **3개 중 2개가 D-031 으로 소멸** |
| D-008 | 이번 주에 만들 범위를 자르고 나머지는 다음으로 미룸                                                            | |
| D-009 | 문서만 바꾸는 PR 을 어디까지 허용할지 정리                                                                | |
| D-010 | **검토자에게 "이게 감사 표본"임을 들키지 않게** 점수·기준·사유를 모두 가림. 사유만 가려서는 뺄셈으로 역산됨                         | |
| D-011 | 목록 조회를 빠르게 하려고 분류 결과를 문의 테이블에도 복사해둠                                                      | 원본과 어긋날 위험이 대가 |
| D-012 | 감사 표본을 큐에 넣는 작업도 같은 트랜잭션에 포함 — 빠지면 측정값이 **조용히** 틀어짐                                      | |
| D-013 | ~~정렬 기준을 파라미터로 나눔. 기간 필터 + 횟수 정렬을 동시에 하면 인덱스로 못 덮음~~                                     | ⛔ **폐기** — 해당 endpoint 소멸 (D-031) |
| D-014 | **캐시 적중률과 AI 절감률은 다른 지표다.** 캐시에 없어도 DB 에 같은 게 있으면 AI 를 안 부름                              | **정정** — D-004 의 등식이 틀렸음 · 근거는 D-031 이 교체 |
| D-015 | 코딩을 3명이 나누는 기준 — 트랜잭션 경계로 P1/P2/P3 분할                                                    | P1 내용물은 D-031 이 재배정 |
| D-016 | ~~재시도는 트랜잭션을 끝낸 뒤 밖에서 해야 한다. 안에서 하면 무조건 실패함~~                                            | ⛔ **폐기** — 충돌할 제약이 없어짐 (D-031). **정정 이력으로서의 값어치는 남음** |
| D-017 | 롤백으로 방치된 건을 지표로 드러냄. 고치지는 못해도 **보이게는 만듦**                                                | D-031 의 US-9 재정의로 **다시 중요해짐** |
| D-018 | ~~인덱스는 조회 이득만 보지 말고 쓰기가 느려지는 비용도 재고 결정~~                                                 | ⛔ **폐기** — 대상 컬럼 소멸 (D-031). 논거는 유효 |
| D-019 | 감사 표본을 알아내는 방법을 "확실한 계산"과 "짐작" 두 종류로 구분                                                  | |
| D-020 | 5단계 책임자를 실명으로 확정                                                                         | **정정** — D-002 의 "미확정" 대체 |
| D-021 | 중복 확정 시 409 를 낼 때, **"이미 끝난 것"과 "동시에 눌린 것"을 코드로 구분**                                     | D-031 이후 **유일하게 남은 동시성 장치** |
| D-022 | **AI 응답이 깨지면 점수를 0 으로 쓰지 않고 "실패"로 남김.** 0 을 쓰면 "AI 가 0 이라 한 것"과 섞임                       | |
| D-023 | 스키마를 Flyway 로 관리하고, 실험용 인덱스는 V2 파일로 분리                                                   | 앞부분 유지 / **V2 분리만 폐기** (D-031) |
| D-024 | AI 는 Anthropic 으로 확정                                                                     | **정정** — D-003 의 "OpenAI" 대체 |
| D-025 | 엔티티는 `new` 대신 정적 팩토리로만 생성 — 잘못된 조합을 **컴파일러가 막게**                                         | |
| D-026 | Docker Desktop 버전을 4.44.2 이하로 고정 (최신 버전에서 통합 테스트가 전부 깨짐)                                 | 로컬 환경 |
| D-027 | 도메인을 바꾸지 않기로 함 + **어떤 도메인이라야 하는지 기준 3개를 명문화**                                            | 결론은 D-031 이 뒤집음 / **기준 3개는 존속** |
| D-028 | 감사 비율을 **실행 중에는 못 바꾸게** 함 — 측정 도중 바뀌면 그 측정을 믿을 수 없음                                      | 감사 비율분 유지 / 정책 모드분만 소멸 |
| D-029 | ~~정책 수정 API 를 "없으면 만들고 있으면 고치는" 방식으로~~                                                   | ⛔ **폐기** — 해당 endpoint 소멸 (D-031) |
| D-030 | 예외를 catch 하고도 아무것도 안 하면 Sentry 가 영구히 모른다 — `Sentry.captureException()` 명시 호출 규칙을 헌법에 못박음 | |
| D-031 | **도메인을 CS 문의 분류로 바꾸면서 "묶기"(그룹핑)를 포기했다.** 대신 AI 호출만 재사용하는 2단 경로를 만듦                       | 무엇을 잃었는지까지 기록 |
| D-032 | 상담원이 검토 항목을 열면 **"내가 처리 중"으로 잡아두는** 기능. D-031 이 비운 동시성 자리를 메움                            | 5일 범위 밖 — 여유 생기면 우선 착수 |
| D-033 | **사람이 확정한 답도 재사용한다.** 같은 내용 100건이면 사람이 보는 건 1번. 대신 재사용 건도 감사한다                           | **정정** — D-031 이 "판정 단위 축소"를 통째로 포기한 것이 과했음 |
| D-034 | AI 가 **형식은 맞는데 이상한 값**(확신도 1.5 같은)을 줬을 때를 정의. 잘라 넣지 않고 실패로 본다                            | 자동 확정되면서 측정에서 사라지는 구멍이었음 |
| D-035 | 새로 합류한 사람이 먼저 읽을 안내 문서를 만들고, 브랜치 이름 규칙을 함께 고정                                                     | `ONBOARDING.md` |
| D-036 | **상담원이 답을 고치면 저장해둔 값도 같이 바꾼다.** 사람 답은 AI 답을 덮고, AI 답은 사람 답을 못 덮는다                                | 안 고치면 정정한 답이 무시됨 |
| D-037 | 지난 답을 DB 에서 찾을 때 "사람 답 먼저, 없으면 AI 답"을 **쿼리 두 번**으로 나눠 친다                                          | 한 번에 하면 사람 답을 지나침 |
| D-038 | 문의 목록에서 **누구 것을 볼지를 요청이 정하지 못하게** 함 — 서버가 정한다                                                     | 남의 문의를 넘겨다볼 구멍 차단 |
| D-039 | 확신도가 **비어 있는 경우가 언제인지**를 계약에 못박음                                                                  | 스키마 변경 없음 |
| D-040 | 개인정보를 가린 본문을 **저장하지 않고 보여줄 때마다 만든다**                                                              | 규칙을 세게 바꿔도 옛 데이터가 안 남음 |
| D-041 | 지난 답을 찾을 때의 정렬 기준이 "판정한 시각"이 아니라 **"문의가 들어온 시각"** 임을 인정하고 그대로 둠                                   | 덮으면 사람끼리 갈리는 게 안 보임 |
| D-042 | 재사용한 답이 저장해둔 값에 남길 수 있는 **우회로 두 개**를 막음                                                            | 코드·스키마 변경 없음 |
| D-043 | **결론이 되는 오분류율 측정을 둘로 쪼갬** — 감사로 뽑히는 표본이 2건뿐이라 비율을 낼 수 없었음                                         | R(측정)을 실제로 읽을 수 있게 |
| D-044 | **만드는 순서를 결론 숫자에서 거꾸로 정함** — 기능 목록 순서로 만들면 마지막 날까지 잴 게 없음                                         | 설계 변경 없음, 순서만 |
| D-045 | 필수 기능 5개에 **잘 안 될 때 어떻게 되는지**를 하나씩 붙임. 애노테이션 한 줄이면 "썼다"는 되지만 캐시가 죽거나 대기줄이 차면 무너짐               | 권한 의존성 누락도 함께 확정 |
| D-046 | **정답 문의 50건을 코드보다 먼저 만들고, 정답은 두 사람이 따로 붙임.** AI 가 쓴 문장으로 AI 를 평가하면 결과가 좋게 나옴                     | 0단계 — 지금 시작 |
| D-047 | 뒤에서 도는 **분류 담당**의 스레드 수·DB 연결 수·종료 처리·예외 처리를 한 묶음으로 정함. **배포할 때마다 처리 중이던 문의가 버려지고 있었음**       | 설정이 전부 기본값이었음 |
| D-048 | 캐시의 구멍 둘 — **사람 답을 안 덮는다는 판단이 원자적이지 않았고**, 메모리 상한이 없어 키가 무한히 쌓임                                | 한쪽은 Lua 한 개로 막음 |
| D-049 | **같은 문의를 두 번 분류하지 못하게** 조건부 UPDATE 로 막음. 두 번 들어가면 상담원이 같은 걸 두 번 보고 오분류율이 희석됨                 | D-031 에서 잃었던 수단이 돌아옴 |
| D-050 | **한 장 요약본(`PROJECT-BRIEF.md`)은 팀 밖에 보여주는 용도.** 내용은 PRD 에서 한 방향으로만 흘러가고, 정해진 시점에만 고침                | 같은 값을 두 곳에 적으면 한쪽이 낡음 |
| D-051 | **AI 에게 「생각하기」를 시키지 않는다.** 정확해지면 이 프로젝트가 재려는 「자신 있게 틀리는」 표본이 귀해짐                                  | D-024 가 모델 티어를 고른 것과 같은 논리 |
| D-052 | 정규화 키는 정규화·마스킹 문자열의 **SHA-256 hex(64자)**. 절단하면 서로 다른 문의가 같은 키가 될 수 있음                                   | `VARCHAR(64)` 에 정확히 맞음 · 과도 병합 방지 |
| D-053 | 정규화 키의 두 병합 결함 정정 — **키 전용 토큰**(토큰이 자연어와 충돌하는 과도 병합) + **유니코드 문장부호·공백 접기**(전각이 안 접혀 생기는 과소 병합)  | AI 리뷰(Claude·CodeRabbit) 실측 반영 |
| D-054 | 리뷰 단계에 **CodeRabbit 을 두 번째 AI 리뷰 도구로 병행 도입** — `ai-review.yml` 이 못 잡는 라인 단위·커밋 단위 지점 보강           | |
| D-055 | 권한은 통과했는데 컨트롤러가 없는 요청이 **404 대신 401/500 으로 가려짐** — 서블릿 재디스패치·MVC 예외 처리기 두 계층 모두에서 막아야 했음 | 도커 실증 검증으로 발견 |
| D-056 | 테스트 전용 더미 컨트롤러가 **통합 테스트 컨텍스트에 실제 빈으로 섞여 들어감** — `@Profile("!test")` 로 격리 | TRI-25~30 진행 중 재현 |
| D-057 | 분류 담당 클래스 이름을 `AiClassifyWorker` → **`service/event/InquiryReceivedEventListener`** 로. 이름에 "무엇을 하나" 대신 **"누가 부르나"** 를 담음 | 코드 0줄일 때 바꿈 · 용어집에 3중 대응 추가 |

**뼈대 5개만 읽는다면**: D-001(왜 이 주제) → D-005(왜 감사하는가) → D-010(왜 가리는가) → D-027(어떤 도메인이라야 하는가) → D-031(왜 이 도메인이고, 무엇을 포기했는가).

## 형식

```markdown
### D-NNN. (한 줄 결정)

- **일자**: YYYY-MM-DD
- **상태**: 채택 / 보류 / 폐기
- **배경**: 무엇이 문제였나? (Problem)
- **선택지**: 본 결정에서 고려한 옵션 2~3개 + 각각의 trade-off (Options)
- **결정**: 최종 선택 + 이유 (Decision)
- **영향**: 어떤 코드/문서/프로세스에 영향 가는가
- **재평가**: 언제 / 어떤 신호 보면 다시 검토하는가
```

> ⚠️ **새 항목을 추가할 때 위 「한눈에 보기」 표에도 한 줄을 추가한다.** 표가 낡으면 인덱스가 아니라 오답이 된다.
> 요약은 **전문 용어 없이** 쓴다 — 그 표의 목적이 "처음 보는 사람이 전체를 훑는 것"이기 때문이다.

## 의사결정

### D-001. 자율 주제 — AI 에러 분류 검증 파이프라인 선정

- **일자**: 2026-07-30
- **상태**: 채택 — **주제는 D-031 으로 대체** (에러 분류 → CS 문의 분류). "AI 가 곁다리 기능이 아니라 주제 자체이고, AI 를 못 믿는 설계를 한다"는 **선정 이유는 그대로 승계**된다
- **배경**: Week 9 5일 안에 6 공통 필수 기능 + 라이프사이클 5 단계를 모두 시연해야 함. 추천 5개 프로젝트(운영 티켓 / 커머스 클레임 / 채용 파이프라인 / 콘텐츠 검수 / 의료 예약)는 도메인이 안전하지만 **AI 보조 기능이 "요약 붙이기" 수준에 머무르기 쉽다** — 6개 필수 기능 중 AI 항목만 깊이가 얕아지는 문제.
- **선택지**:
  1. 추천 1번 운영 티켓 시스템 — 도메인 단순, 매핑 자연스러움. 단 AI 기능이 부수적
  2. 커머스 주문 후처리 — 트랜잭션 풍부하지만 결제 연동이 시간 잡아먹음
  3. **자율 주제 — AI 에러 분류 검증 파이프라인** ([NAVER D2 광고SDK 에러 모니터링](https://d2.naver.com/helloworld/8319114) 기반). AI 가 부수 기능이 아니라 **시스템의 주제 자체**. 대신 도메인을 새로 설계해야 함
- **결정**: 3번. 난이도 조건(6 필수 기능 매핑 가능)을 만족하면 자율 주제가 허용되고, "AI 결과를 검증하는 설계"는 트랜잭션·비동기·캐시가 **자연스럽게 필요해지는** 주제라 억지 매핑이 없다. 이력서/면접에서도 "AI 를 썼다"가 아니라 "AI 를 못 믿는 설계를 했다"로 말할 수 있다.
- **영향**: `PRD.md` 전면, `CLAUDE.md` 도메인 컨텍스트, `API-CONTRACT.md` (템플릿 ticket 예시 폐기), `src/` 패키지 구조
- **재평가**: Day 3 시점에 AI 분류 정확도가 너무 낮아 검증 흐름 시연이 불가하면 카테고리 수를 10종 → 5종으로 축소

### D-002. 라이프사이클 5 단계 책임자 분담

- **일자**: 2026-07-30
- **상태**: **채택** — 확정 내역은 후속 항목 **D-020** 참조 (본 항목의 `결정` 문장은 D-020 으로 대체됨)
- **배경**: 5 단계를 1명이 다 못함. 팀은 3명(@agbink, @whitejh, @techietaek)이고 단계는 5개라 2명이 2단계씩 겸해야 한다. (템플릿은 4명 기준으로 쓰여 있어 그대로 쓸 수 없음)
- **선택지**:
  1. 3명 × (2 + 2 + 1) 분담 — 코딩은 전원 공동
  2. 단계별 순환 — 학습 폭은 넓지만 산출물 책임이 흐려짐
- **결정**: **미확정.** 1번 형태를 제안하되 누가 어느 단계를 맡을지는 팀 미팅에서 확정한다. 확정 시 이 항목의 `상태` 를 채택으로 바꾸고, `CLAUDE.md` 책임자 표 + `LIFECYCLE-COVERAGE.md` 매트릭스 + `PRD.md` §6 담당자 열을 동시에 채운다.
- **영향**: `CLAUDE.md` 책임자 표, `LIFECYCLE-COVERAGE.md`, `PRD.md` §6
- **재평가**: 누가 명백히 막힘 발생 시 swap

### D-003. 기술 스택 — Spring Boot 3.3 + Java 21 + JPA + MySQL 8 + Redis

- **일자**: 2026-07-30
- **상태**: 채택 — AI 프로바이더는 D-024 로 대체 (OpenAI → Anthropic)
- **배경**: 부트캠프 학습 자산이 Spring Boot 3.3 / Java 21 기준. 5일 안에 완주해야 하므로 학습 비용이 드는 선택은 배제.
- **선택지**: Kotlin / Spring WebFlux / Quarkus 검토 후 모두 기각 — WebFlux 는 `@Async` + `@Retryable` 실습 의도와 충돌하고, 나머지는 학습 비용 대비 이득 없음.
- **결정**: Spring Boot 3.3 + Java 21 + Spring Data JPA + MySQL 8 + Redis(캐시) + Docker Compose. AI 는 OpenAI API. 인증은 Phase 2 헤더 stub → Phase 3 JWT.
- **영향**: `build.gradle`, `application.yml`, `docker-compose.yml`, 패키지 구조
- **재평가**: Phase 3 진입 시 Reactive / Native image 검토 가능

### D-004. 에러 그룹핑(fingerprint) 도입 — 상태 소유 주체를 ErrorGroup 으로 이동

- **일자**: 2026-07-30
- **상태**: **폐기** (2026-08-05, D-031) — 도메인이 CS 문의로 바뀌면서 그룹핑 자체가 사라졌다. 문의는 개인 건이라 묶으면 개별 처리가 불가능하다(D-027 기준 1 탈락). 판정 단위는 **문의 1건**이고, AI 호출 절감은 그룹핑이 아니라 **정규화 키 기반 2단 절감 경로**가 맡는다 — D-031
- **배경**: 초안 설계는 **에러 1건 = AI 호출 1회 = 검토 큐 1건**이었다. 같은 NPE 가 1000번 발생하면 AI 를 1000번 호출하고 검토자가 같은 것을 1000번 확정해야 한다 — 실제 에러 모니터링에서 성립할 수 없는 가정이고, 원 사례(NAVER D2)에도 그룹핑이 있다.
- **선택지**:
  1. 그룹핑 없음 — 구현 단순, 위 문제 그대로
  2. 메시지 문자열 완전 일치로 그룹핑 — 구현 최단. 동적값(ID·타임스탬프) 때문에 거의 병합되지 않음
  3. **스택트레이스 정규화 fingerprint** (예외 클래스 + 상위 3프레임 + 값 마스킹) — 실효성 있으나 정규화 품질이 리스크
- **결정**: 3번. AI 호출은 **신규 fingerprint 에만** 발생. 부수 효과로 캐시(`fingerprint → 분류 결과`)의 명분이 "억지로 붙인 캐시"에서 "없으면 AI 비용이 30배 드는 캐시"로 바뀌고, hit rate 가 그대로 비용 절감률 지표가 된다.
- **영향**:
  - ERD 3 → 5 테이블 (`error_group`, `classification_policy` 추가)
  - **상태(`status`)의 소유 주체가 `errors` → `error_group` 으로 올라감.** 개별 에러는 상태 없는 append-only 로그가 된다 (같은 에러 1000번 = 판정 1번)
  - `CLAUDE.md` 불변 규칙 1번, `PRD.md` §4-0/§4-1, 신규 동시성 지점(D-007)
- **재평가**: 투입 건수 대비 그룹 수를 측정해 **과소 병합**(절감률 95% 미달) 또는 **과도 병합**(서로 다른 에러가 한 그룹 → 오분류가 그룹 전체로 전파) 징후가 보이면 정규화 규칙 조정. 과도 병합이 더 위험하므로 그룹마다 `sample_message` 를 남겨 검토자가 감지할 수 있게 한다.

### D-005. 감사 샘플링 도입 — 자동 승인 건 5% 무작위 검토 (blind)

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: 초안 설계에는 치명적 맹점이 있었다. 정답 레이블은 검토자가 확정한 건에서만 나오는데, 검토 큐에 들어가는 건 **신뢰도 낮은 건뿐**이다. 즉 `AUTO_ACCEPTED` 로 통과한 건은 아무도 보지 않아 **AI 가 자신 있게 틀린 경우(high confidence + wrong)를 영구히 발견할 수 없다.** 이 상태의 시스템은 "AI 결과를 검증한다"고 주장하면서 실제로는 **AI 가 스스로 신고한 신뢰도를 무조건 신뢰**하고 있다 — 프로젝트 주제 자체에 대한 반례.
- **선택지**:
  1. 맹점 인정하고 사전 실험(50건)으로만 측정 — 운영 중 측정 불가
  2. 자동 승인 건 전수 검토 — 자동화 의미 소멸
  3. **자동 승인 건 중 설정 비율(기본 5%)을 무작위 추출해 검토 큐 삽입** — 표본 검사. 적체가 소폭 늘어나는 대가로 운영 중 상시 측정 가능
- **결정**: 3번. `review_queue.reason = AUDIT_SAMPLE` 로 구분하되 **검토자에게는 사유를 노출하지 않는다(blind)** — 감사 건임을 알면 평소보다 신중히 봐서 감사 결과가 실제 운영 정확도보다 낙관적으로 편향된다. 그룹 상태는 `CLASSIFIED` 를 유지하고, 사람이 카테고리를 정정하면 `category ≠ final_category` 로 오분류 1건이 기록된다.
- **영향**:
  - `review_queue.reason` enum 에 `AUDIT_SAMPLE` 추가
  - `classification_result` 에 `category`(AI 제안)와 `final_category`(사람 확정)를 **둘 다 보존** — 덮어쓰면 오분류 증거가 사라진다 (`CLAUDE.md` 불변 규칙 2번)
  - `GET /api/review-queue` 응답에서 `reason` 제거 (`API-CONTRACT.md` §4), `GET /api/stats` 에만 노출
  - 상태 다이어그램에 `CLASSIFIED → CLASSIFIED` 자기 전이 추가
- **재평가**: 자동 승인 건이 적으면 5% 표본이 통계적으로 부족하다(하루 100건 → 5건). 비율을 ADMIN 이 조정 가능하게 두고, 표본 부족이 관측되면 상향. 반대로 적체 압박이 크면 하향.

### D-006. 카테고리별 차등 신뢰도 임계값

- **일자**: 2026-07-30
- **상태**: **폐기** (2026-08-05, D-031) — 팀 협의에서 카테고리별 개별 임계값 정책 테이블을 Phase 2 스코프 밖으로 뺐다. 임계값은 `classification.threshold` 단일 설정값이 되고 `classification_policy` 테이블과 측정 9(대조군 비교)가 함께 사라진다
- **배경**: 초안은 전역 단일 임계값 0.7 이었고 그 근거가 없었다. 게다가 `AUTH`(보안 인접) 오분류와 `NULL_REFERENCE` 오분류의 비용이 같을 수 없다.
- **선택지**:
  1. 전역 단일값 유지 — 단순. 근거 없음
  2. **카테고리별 임계값 테이블** — 오분류 비용을 설계에 반영. 테이블 1개 + 조회 1회로 저비용
  3. 감사 데이터 기반 자동 조정 — 이상적이나 데이터 누적이 선행돼야 함
- **결정**: 2번. 오분류 비용이 큰 쪽을 높게 배정 (`AUTH` 0.9 / `DB_CONNECTION`·`OUT_OF_MEMORY` 0.85 / … / `NULL_REFERENCE`·`VALIDATION` 0.6). 미등록 카테고리는 **0.9 fallback — 보수적, 즉 격리 쪽으로 실패**한다. 3번은 D-005 감사 데이터가 쌓인 뒤 Phase 3 후속 항목으로.
- **영향**: `classification_policy` 테이블, `PolicyService`, `GET·PATCH /api/policies` (ADMIN 전용 → 권한 요구사항이 "임계값 설정 권한"으로 자연스러워짐)
- **재평가**: D-005 감사 데이터로 신뢰도 구간별 실제 정확도가 나오면 그 수치를 근거로 재배정하고 후속 항목으로 기록. 임계값 변경은 **소급 적용하지 않는다** (이미 판정된 그룹 재분류는 Phase 3)

### D-007. 동시성 제어를 3분할 — 원자적 UPDATE / UNIQUE+재조회 / 낙관적 락

- **일자**: 2026-07-30
- **상태**: 채택 — **3분할 중 2개가 D-031 으로 폐기**. 원자적 UPDATE(`occurrence_count`)와 UNIQUE+재시도(D-016)는 대상 컬럼·제약이 사라져 소멸하고 **낙관적 락만 남는다**. "경합 성격이 다르면 수단도 다르다"는 원칙은 유지
- **배경**: D-004 그룹핑 도입으로 동시성 지점이 3개로 늘었고, 성격이 서로 다르다. 하나의 수단(예: 비관적 락)으로 통일하면 최고 빈도 경로가 직렬화된다.
- **선택지**: 전부 비관적 락 / 전부 낙관적 락 / **지점별 분할**
- **결정**: 지점별 분할.
  | 지점 | 수단 | 이유 |
  | --- | --- | --- |
  | `occurrence_count` 증가 (최고 빈도) | JPQL 원자적 UPDATE | 락 없이 DB 원자성 사용. 비관적 락을 걸면 수신 경로 전체가 직렬화됨 |
  | 신규 그룹 동시 생성 | `fingerprint` UNIQUE + 위반 예외 캐치 후 재조회 | 선-조회 후-삽입만으로는 못 막는다. 락이 아니라 제약 조건을 신뢰 근거로 삼음 |
  | 큐 항목 중복 확정 | 낙관적 락 `@Version` → 409 | 두 검토자가 같은 항목을 집는 빈도가 낮음. 비관적 락은 과잉 |
- **영향**: `CLAUDE.md` 락·동시성 전략 표, `ErrorGroup.version` 불필요/`ReviewQueueItem.version` 필요, `PATCH /api/review-queue/{id}` 409 응답
- **재평가**: 동시 `POST` 20 스레드 테스트에서 중복 그룹이 1건이라도 생기면 UNIQUE 전략 재검토. 검토자 수가 늘어 409 가 빈발하면 claim(선점) 모델로 전환 (Phase 3 항목 E)

### D-008. Phase 2 범위 확정 — 확장 항목 D~H 는 Phase 3 이월

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: D-004~D-006 으로 도메인 복잡도가 올라가면서 5일 일정 초과 위험이 생겼다. 무엇을 지금 하고 무엇을 미룰지 문서로 고정해야 스코프 크립을 막을 수 있다.
- **선택지**: 전부 Phase 2 / 핵심만 Phase 2 / 그룹핑도 Phase 3 로 미룸
- **결정**: **Phase 2 = 그룹핑(D-004) + 감사 샘플링(D-005) + 차등 임계값(D-006) 까지.** 아래는 Phase 3 로 이월하되 `PRD.md` §7 에 항목으로 명시해 "빠뜨린 것이 아니라 순서상 뒤"임을 남긴다.
  - D. 캘리브레이션 리포트 (신뢰도 구간별 실제 정확도 → 임계값 자동 재조정 제안)
  - E. 검토 항목 claim(선점) + TTL 반납, 낙관 vs 비관 락 비교 실측
  - F. 검토 우선순위 스코어링 (`occurrence_count × 심각도 × (1 - confidence)`)
  - G. AI 실패 유형 구분(일시적/영구적) + 백오프 재분류 + circuit breaker
  - H. 이중 모델 불일치 격리 (신뢰도 과신을 잡는 두 번째 축)
  - JWT 본격 통합, Actuator 대시보드 연동, 멀티테넌시
- **영향**: `PRD.md` §7 Phase 3 표, `LIFECYCLE-COVERAGE.md` Phase 진행 상태
- **재평가**: Day 3 종료 시점에 진도가 예정보다 늦으면 D-006(차등 임계값)을 전역 단일값으로 되돌려 Phase 3 로 넘긴다 — 비용이 가장 낮은 항목이라 회수도 가장 쉽다. **D-005 감사 샘플링은 어떤 경우에도 유지** (프로젝트 주제의 핵심)

### D-009. 문서 단독 PR 허용 범위를 좁힘 — 헌법 문구 정정

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: `CLAUDE.md` 작업 경계에 *"루트 문서(`PRD.md`, `API-CONTRACT.md`, `DECISIONS.md` 등)는 코드 PR 에 동반 변경만 허용. 단독 리팩터링 금지"* 라고 썼는데, 이 규칙이 **기획 단계 산출물 작성 자체를 차단**했다. 실제로 D-001~D-008 을 담은 첫 문서 커밋이 자기 헌법 위반이 되는 자기모순이 발생.
- **선택지**:
  1. 규칙 유지 + 이번만 예외 — 예외가 선례가 되어 규칙이 무력화됨
  2. 규칙 삭제 — API 계약이 구현과 몰래 어긋나는 것을 막을 장치가 사라짐
  3. **적용 대상을 `API-CONTRACT.md` 로 좁힘** — 원래 의도만 남김
- **결정**: 3번. 규칙의 원래 의도는 "**계약과 구현의 괴리 방지**" 였고 그건 `API-CONTRACT.md` 에만 해당한다. `PRD.md` / `DECISIONS.md` 는 코드보다 먼저 쓰이는 문서이므로 단독 PR 을 허용한다.
- **영향**: `CLAUDE.md` 작업 경계 2줄
- **재평가**: `PRD.md` 가 구현과 어긋난 채 방치되는 사례가 관측되면 기획 문서에도 동반 변경 의무를 부과

### D-010. blind 누설 차단 — confidence·threshold·category 필터 제거, 앵커링 한계는 수용

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 코드리뷰가 D-005 blind 규칙의 구멍을 지적했다. `GET /api/review-queue` 에서 `reason` 은 가렸지만 `confidence` 와 `threshold` 를 함께 반환하고 있었다. **격리 사유는 3종뿐이고 `AUDIT_SAMPLE` 은 정의상 `confidence >= threshold`** 이므로, 검토자는 두 값을 비교하는 것만으로 감사 표본을 100% 식별한다. `reason` 을 가린 것이 무의미했다.
  추가로, 리뷰가 지적하지 않은 더 깊은 문제도 함께 발견했다 — AI 제안 카테고리를 보여주는 것 자체가 **앵커링 편향**을 만들어 우리가 수집하는 정답 레이블을 오염시킨다. 감사의 목적은 *독립적인* 사람 판단인데 그 전제가 깨진다.
- **선택지**:
  1. `confidence` / `threshold` 만 제거 — 식별 누설은 닫히지만 앵커링은 남음. 저비용
  2. `suggestedCategory` 까지 제거해 완전 blind — 앵커링 제거. 단 `CLASSIFY_FAILED`(제안 없음)가 역으로 구별되고, 검토자가 매번 원문만 보고 분류해야 해 생산성이 크게 떨어짐
  3. 2단계 공개 — 사람이 먼저 분류 제출 → 그 후 AI 제안 공개. 이론상 최선이나 5일 안에 UI/API 2단계는 무리
- **결정**: **1번 + 한계 명시.** `confidence`·`threshold` 응답 필드와 `category` 요청 필터를 제거한다 (`category` 필터도 AI 제안을 노출하므로). `suggestedCategory` 는 남기고, 대신 **측정된 오분류율을 하한값(lower bound)으로 해석**한다고 문서에 못박는다 — 실제 오분류율은 이보다 높다. 3번은 Phase 3 항목 I 로 이월.
  "편향을 없앴다"보다 **"편향의 방향과 성격을 알고 측정했다"** 가 5일 범위에서 더 정직하고, 면접 답변으로도 낫다.
- **영향**:
  - `API-CONTRACT.md` v0.1 → v0.2 — §4 파라미터·응답 필드, §4 한계 주석
  - `CLAUDE.md` blind 규칙 확장 + **"새 응답 필드 추가 시 감사 표본 역산 가능성을 먼저 확인"** 규칙 신설
  - `PRD.md` US-8(카테고리·사유 필터 제거), §4-4 흐름, §5 비기능(blind 무결성), §7 리스크(앵커링), §8 측정 8·10, 성공 기준 3번
  - 부수 효과: 검토 큐의 `category` 필터가 사라져 `classification_result` 조인이 불필요해지고 `(status, created_at)` 인덱스가 정확해짐 (D-011 참조)
- **재평가**: 감사 표본에서 `matched=true` 비율이 비정상적으로 높으면(예: 95%+) 앵커링이 심한 신호. 그때 Phase 3 항목 I 를 앞당긴다.

### D-011. ErrorGroup 에 current_category / current_confidence 역정규화

- **일자**: 2026-07-30
- **상태**: 채택 — **적용 대상만 `ErrorGroup` → `Inquiry` 로 이동** (D-031). 역정규화의 목적(목록 조회 조인 제거)과 제약(판정 확정 트랜잭션 안에서만 갱신 = 불변 규칙 3)은 그대로다
- **배경**: PR #1 AI 코드리뷰가 `CLAUDE.md` 인덱스 표와 실제 필터 컬럼의 불일치를 지적했다. 확인해보니 두 곳이 어긋나 있었다.
  ① `GET /api/error-groups` 는 `category` 필터와 `last_seen_at` 범위를 받고 응답에 `category`·`confidence` 를 포함하는데, **`error_group` 테이블에는 그 컬럼이 없다** (`classification_result` 소유). 조인이 필요한데 명시하지 않았다.
  ② `(status, occurrence_count DESC)` 인덱스는 `last_seen_at` 범위를 커버하지 못한다.
- **선택지**:
  1. 조인 유지 + `classification_result` 에 인덱스 추가 — 정규화 유지. 목록 조회마다 그룹당 조인, 필터+정렬 인덱스 커버가 어려움
  2. **`error_group` 에 `current_category` / `current_confidence` 역정규화** — 조인 제거 + `(status, current_category, occurrence_count DESC)` 로 필터·정렬 동시 커버. 대가는 갱신 일관성 책임
  3. 카테고리 필터 기능 삭제 — 요구사항 후퇴
- **결정**: 2번. `error_group` 은 **분류의 단위**이므로 현재 카테고리를 자기 속성으로 갖는 것이 도메인상 자연스럽다. 일관성은 규칙으로 묶는다 — **판정이 확정되는 트랜잭션(②③) 안에서만 갱신** (`CLAUDE.md` 불변 규칙 4번). 원본(`classification_result`)은 그대로 이력으로 남으므로 감사 대조는 영향받지 않는다.
  `last_seen_at` 범위 + `occurrence_count` 정렬 동시 사용은 **B-tree 로 커버 불가**임을 인덱스 표에 명시하고, 측정 5번에서 EXPLAIN 으로 확인한다. 숨기지 않고 예상 계획을 미리 적어두는 쪽을 택했다.
- **영향**: `CLAUDE.md` 도메인 모델 + 불변 규칙 4번 + 인덱스 표(예상 EXPLAIN 열 추가), `PRD.md` §4-0, `API-CONTRACT.md` §2 출처 주석
- **재평가**: 역정규화 컬럼과 원본이 어긋난 사례가 1건이라도 관측되면 조인 방식(1번)으로 되돌린다. 검증은 정합성 확인 쿼리로 주기 실행.

### D-012. 감사 표본 삽입을 분류 트랜잭션에 포함 + 감사율 자체를 검증 가능하게

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 리뷰 2차가 §4-2 의 트랜잭션 분리를 지적했다. 초안은 *"감사는 부차적이니 삽입이 실패해도 자동 승인을 되돌릴 필요 없다"* 는 이유로 감사 표본 큐 삽입을 **별도 트랜잭션 + 실패 로깅**으로 뒀다. 그런데 표본이 누락되면 실제 감사율이 설정값 5% 미달이 되고, **§8 측정 8(자동 승인 건 오분류율)의 분모가 조용히 줄어든다.** 측정 무결성이 이 프로젝트의 주제인데 측정 장치 자체를 best-effort 로 둔 셈이다.
- **선택지**:
  1. 별도 트랜잭션 유지 + 삽입 실패 카운터 노출 — 자동 승인 경로가 감사 실패에 영향받지 않음. 대신 누락분을 사후 보정할 방법이 없음
  2. **분류 트랜잭션(②)에 포함** — 감사율이 정확히 보장됨. 삽입 실패 시 분류째 롤백되어 AI 호출 1회가 낭비됨
  3. 감사를 비동기 후처리로 완전 분리 — 결합도는 최소지만 누락 가능성이 가장 큼
- **결정**: 2번. *"부차적이니 분리한다"* 는 직관은 **외부 의존성**에 적용되는 것이고, 여기 두 쓰기는 같은 DB·같은 커넥션이라 분리해서 얻는 격리 이득이 없다. 삽입 실패 시 그룹은 `NEW` 로 롤백되어 다음 재분류에서 다시 표본 추첨 대상이 되므로 통계적으로도 왜곡되지 않는다.
  추가로 **감사 장치 자체를 감사**한다 — `GET /api/stats` 의 `audit` 에 `eligibleTotal`(모집단) · `actualSampleRate`(실측) · `configuredSampleRate`(설정) 를 함께 노출해, 설정값과 실측값의 괴리가 즉시 드러나게 한다. 어느 방식을 택하든 이 검증 필드는 필요하다.
- **영향**:
  - `PRD.md` §4-2 시퀀스(감사 삽입을 같은 `[TX]` 로) + 트랜잭션 원자성 설명
  - `CLAUDE.md` `@Transactional` 규칙에 **"측정 장치를 트랜잭션 밖에 두지 않는다"** 신설, 트랜잭션 ② 정의에 감사 삽입 포함 명시
  - `API-CONTRACT.md` v0.2 → v0.3 — `GET /api/stats` audit 필드 3개 추가
- **재평가**: 감사 삽입 실패로 인한 롤백이 빈발해 AI 호출 낭비가 유의미해지면 1번으로 전환하고 누락 카운터를 노출한다. 판단 근거는 `actualSampleRate` 와 `configuredSampleRate` 의 괴리 폭.

### D-013. GET /api/error-groups 에 sort 파라미터 도입 — 기간 필터 시 filesort 회피

- **일자**: 2026-07-30
- **상태**: **폐기** (2026-08-05, D-031) — `GET /api/error-groups` endpoint 자체가 사라졌다. 후속 `GET /api/inquiries` 는 `occurrence_count` 에 해당하는 컬럼이 없어 정렬 축이 `received_at` 하나뿐이므로 sort 파라미터가 필요 없다. **다만 "범위 필터 + 다른 컬럼 정렬은 한 B-tree 로 못 덮는다"는 본문의 판단 자체는 여전히 옳다**
- **배경**: PR #1 AI 리뷰 2차 지적. `from`/`to`(`last_seen_at` 범위) 필터와 `occurrence_count DESC` 정렬을 함께 쓰면 한 B-tree 로 커버되지 않아 filesort 가 발생한다. D-011 에서 이 한계를 문서에 적어두기는 했지만, **회피 수단을 제공하지 않았다.**
- **선택지**:
  1. 현상 유지 — 한계만 문서화. 기간 조회 성능이 나쁨
  2. **`sort` 파라미터 도입** (`occurrenceCount` 기본 \| `lastSeenAt`) + `(status, last_seen_at)` 인덱스 추가 — 조회 패턴별로 커버 인덱스를 고를 수 있음
  3. 기간 필터 제거 — 요구사항 후퇴
- **결정**: 2번. 기간 필터를 쓰는 조회는 `sort=lastSeenAt` 으로 **범위 + 정렬을 한 인덱스로 커버**한다. 어느 쪽이 실제로 유리한지는 측정 5번 EXPLAIN 결과로 확정하며, 인덱스 표에 조회 패턴별 권장 조합을 미리 적어둔다.
- **영향**: `API-CONTRACT.md` §2 `sort` 파라미터 + 권장 조합 표, `CLAUDE.md` 인덱스 표 3행(`(status, last_seen_at)` 추가)
- **재평가**: 측정 5번에서 `sort=lastSeenAt` 이 예상만큼 이득이 없으면 인덱스를 걷어낸다 (쓰기 비용만 남으므로).

### D-014. 캐시 hit rate 와 AI 절감률을 분리 — D-004 의 등식 정정

- **일자**: 2026-07-30
- **상태**: 채택 — **결론은 유지되고 근거만 바뀐다** (D-031). "캐시 miss 여도 DB 에 있으면 AI 를 안 부른다"는 문장에서 *DB 에 있는 것*이 **그룹 → 같은 정규화 키의 이전 분류 결과**로 대체된다. `hit rate ≤ 절감률` 부등식과 두 지표의 분리 노출은 그대로다. D-033 로 재사용 대상에 **사람이 확정한 답**이 추가되면서 부등식의 여유폭은 더 커진다
- **배경**: PR #1 AI 리뷰 3차가 D-004 에 쓴 *"hit rate 가 그대로 비용 절감률 지표가 된다"* 는 등식이 틀렸음을 지적했다. 확인 결과 맞다. 수신 경로는 이렇게 동작한다.
  ```text
  캐시 hit  → AI 호출 없음
  캐시 miss → DB SELECT
              ├ 그룹 존재  → AI 호출 없음   ← 여기가 등식을 깨뜨린다
              └ 그룹 없음  → AI 호출
  ```
  **AI 호출을 막는 것은 "그룹이 이미 존재한다"는 사실이고, 캐시는 그 사실을 DB 에 묻지 않아도 알게 해줄 뿐이다.** TTL 만료·재기동·eviction 후에는 miss 지만 AI 는 호출되지 않으므로 `hit rate < AI 절감률` 이 정상이다. 두 값을 같다고 쓴 것은 캐시의 효과를 과대평가한 것이고, 더 나쁘게는 측정 6(절감률)을 캐시 메트릭으로 대체할 수 있다고 오독할 여지를 남겼다.
- **선택지**:
  1. 등식 유지 + 오차 무시 — 반복 유입 시나리오에서는 거의 같으니 실용상 문제없다는 주장. 그러나 측정 정의가 부정확해짐
  2. **두 지표를 완전 분리** — 절감률은 AI 호출 카운터로만, hit rate 는 캐시 메트릭으로만. 격차 자체를 별도 관측 대상으로
  3. 캐시를 걷어내고 매번 DB 조회 — 캐시 요구사항(6 공통 필수 기능) 미충족
- **결정**: 2번. 측정 6 은 **`1 - (AI 호출/투입)`** 으로만 정의하고, 캐시 hit rate 는 측정 11 로 독립시킨다. 두 값의 **격차 = "캐시 miss 지만 그룹은 존재" 비율**이라는 해석까지 문서에 남긴다.
  캐시의 정당화 근거도 정직하게 교체한다 — "없으면 AI 비용이 30배" 가 아니라 **"수신 경로는 시스템 최고 QPS 지점이고, fingerprint→그룹 매핑은 생성 후 거의 불변이라 변경 빈도 << 조회 빈도 조건에 가장 잘 맞는다"**. 근거가 약해진 것이 아니라 대상이 AI 비용에서 DB 부하로 바뀐 것이다.
  D-004 본문은 형식 규칙상 수정하지 않으며, 그 항목의 해당 문장은 **본 항목으로 무효화**된다.
- **영향**:
  - `PRD.md` §4-1 설계 의도 2, §5 비기능 2행, §6 캐시 셀, §8 측정 6 정의 + 측정 11 신설
  - `CLAUDE.md` §6 캐시 셀, 캐시 전략 전면, 모듈 간 **계약 C**(캐시 값 구조) 신설
  - `API-CONTRACT.md` v0.3 → v0.4 — `GET /api/stats` 의 `cacheHitRate` 를 `cache` 객체로 분리
- **재평가**: 측정 11 에서 격차가 5%p 미만으로 작으면 캐시의 실효가 낮다는 뜻이므로(그룹 존재 판정이 이미 대부분 캐시에 있음) TTL·크기 설정을 재검토한다. 반대로 격차가 크면 캐시 크기가 작아 eviction 이 잦은 신호다.

### D-015. 코딩 단계 작업 패키지 P1/P2/P3 배정

- **일자**: 2026-07-30
- **상태**: 채택 — **P1 의 내용물이 D-031 으로 재배정**. 그룹핑이 사라져 P1 이 "접수 CRUD" 하나로 얇아지므로 **정규화 규칙 + 2단 절감 경로 + 캐시 레이어**를 P1 소유로 옮긴다. 트랜잭션 경계로 나눈다는 분할 기준(①②③)은 그대로
- **배경**: D-002 로 라이프사이클 5단계 책임자는 정해졌지만, **코딩 단계는 "팀 전원" 이라 코드 소유자가 비어 있었다.** PR #1 AI 리뷰가 3회 연속으로 이를 지적했다 — 계약 A·B·C 를 고정해 "미정이어도 병렬 가능"하게 만들었어도, 배정이 없으면 실제로는 아무도 시작하지 않는다.
- **선택지**:
  1. 기능(6 공통 필수) 단위로 쪼개기 — 기능이 여러 계층에 걸쳐 파일 충돌이 잦음
  2. **트랜잭션 경계를 분할선으로** — ①②③ 이 그대로 P1/P2/P3 가 되고, 경계가 곧 계약이라 인터페이스가 자연스럽게 정의됨
  3. 계층(api/service/domain) 단위 — 한 기능을 완성하려면 3명이 모두 움직여야 해 병렬성이 없음
- **결정**: 2번 + 배정.
  | 패키지 | 담당 | 근거 |
  | --- | --- | --- |
  | P1 수신·그룹핑 | 이용택 | 테스트 단계 겸임 — 동시 유입 중복 방지(측정 7)·fingerprint 정규화 단위 테스트가 본인 산출물과 직결. 코드 범위가 가장 작아 2단계 겸임 부담 완화 |
  | P2 분류·검증 (+ `SecurityConfig`) | 김준현 | 기획 담당으로 D-005·D-010·D-012 설계 의도를 가장 잘 안다. 판단이 몰린 패키지 |
  | P3 검토·관측 | 김은빈 | 리뷰 담당 — 관측·통계와 시너지. blind 규칙(D-010) 준수를 조회 API 코드에서 직접 통제 |
  `SecurityConfig` 는 전 계층에 걸치지만 단독 파일이라 충돌 위험이 낮아 김준현이 P2 착수 전 선작업한다.
- **영향**: `PRD.md` §6 담당자 열, `CLAUDE.md` 모듈 간 계약 헤더, `LIFECYCLE-COVERAGE.md` 작업 패키지 섹션 신설
- **재평가**: P1 이 가장 먼저 끝나면 이용택이 P3 의 조회 API 를 지원한다 (P3 범위가 endpoint 수 기준 가장 큼). 특정 패키지가 이틀 이상 막히면 재배정.

### D-016. UNIQUE 충돌 재시도를 트랜잭션 밖으로 — D-007 의 구현 오류 정정

- **일자**: 2026-07-30
- **상태**: **폐기** (2026-08-05, D-031) — `fingerprint` UNIQUE 제약이 사라져 충돌할 대상이 없다. 문의는 접수마다 새 행을 INSERT 하므로 동시 첫 유입 race 자체가 성립하지 않는다. **"제약 위반 예외를 같은 트랜잭션 안에서 캐치하면 rollback-only 마킹 탓에 재조회가 실패한다"는 본문의 Hibernate 지식은 폐기하지 않는다** — 향후 어떤 UNIQUE 제약을 도입하든 그대로 적용된다
- **배경**: PR #1 AI 리뷰 4차가 D-007 락 전략의 실행 불가능성을 지적했다. *"`fingerprint` UNIQUE + 위반 예외 캐치 후 재조회"* 라고만 썼는데, **제약 위반 예외를 같은 트랜잭션 안에서 캐치하면 재조회가 반드시 실패한다.** Hibernate 는 제약 위반 시 세션을 오염된 것으로 간주하고 트랜잭션을 rollback-only 로 마킹하므로, 이어지는 모든 DB 접근이 `UnexpectedRollbackException` 으로 끝난다. **전략 자체는 맞았으나 경계를 명시하지 않아 그대로 구현하면 동작하지 않는 상태**였다.
- **선택지**:
  1. 삽입만 `REQUIRES_NEW` 로 분리 — 내부 TX 가 롤백돼도 외부 TX 는 살아남아 재조회 가능. 단 별도 커넥션을 점유해 최고 빈도 경로에서 커넥션 풀 압박
  2. **예외를 트랜잭션 밖으로 전파시킨 뒤 새 트랜잭션에서 재시도** — 롤백이 완전히 끝난 상태에서 재시도하므로 세션 오염 문제가 없다. 재시도 시엔 다른 스레드가 커밋한 그룹이 보여 "기존 그룹 존재" 경로로 성공
  3. MySQL `INSERT ... ON DUPLICATE KEY UPDATE` — 예외 자체가 발생하지 않음. 1 round trip 으로 가장 효율적이나 이식성이 낮고, 관찰할 race 자체가 사라진다
- **결정**: 2번. `ingest` 를 **비트랜잭션 메서드**로 두고 그 안에서 트랜잭션 메서드를 호출하며, `@Retryable(DataIntegrityViolationException.class, maxAttempts=2)` 를 바깥에 붙인다. 같은 클래스 내부 호출은 프록시를 타지 않으므로 **빈을 분리**한다.
  3번은 Phase 3 성능 개선 후보로 남긴다 — Phase 2 는 동시 첫 유입 race 를 **측정 7 로 관찰하는 것이 학습 목표**이고, upsert 를 쓰면 관찰 대상이 사라진다.
- **영향**: `CLAUDE.md` 락 전략 2행 + 트랜잭션 경계 다이어그램 신설, `PRD.md` §4-1 시퀀스 Note 와 설계 의도 3
- **재평가**: 측정 7(20 스레드 동시 `POST`)에서 재시도 2회로도 실패하는 케이스가 나오면 `maxAttempts` 상향 또는 3번(upsert)으로 전환

### D-017. 판정 롤백으로 방치된 그룹을 관측 가능하게 — stuckNew 지표

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 리뷰 4차 지적. D-012 에서 *"삽입이 실패하면 분류째로 롤백되어 그룹이 `NEW` 로 남고, 다음 재분류에서 다시 표본 추첨 대상이 된다"* 고 썼는데 **"다음 재분류"가 존재하지 않는다.** `ErrorGroupCreatedEvent` 는 그룹 생성 시 1회만 발행되고 이미 소비됐으며, 자동 재분류는 Phase 3(항목 G)다. 결과적으로 그룹은 **`NEW` 로 영구 방치**되어 분류도 안 되고 큐에도 없다 — 이 시스템이 막으려는 "조용히 유실된 에러"를 **스스로 만드는 구멍**이다.
- **선택지**:
  1. 재분류 스케줄러를 Phase 2 에 앞당김 — 구멍은 막히지만 Phase 3 항목 G(백오프·circuit breaker)와 중복 설계가 되고 5일 일정 압박
  2. **관찰만 — `stuckNew` 지표 노출** — 고치지는 못해도 조용하지 않게 만든다
  3. 판정 실패 시 이벤트 재발행 — 무한 재시도 위험, 실패 원인이 영구적이면 폭주
- **결정**: 2번. `GET /api/stats` 의 `classification.stuckNew`(생성 후 10분 이상 `NEW`) + Actuator gauge `triage.groups.stuck_new` 를 노출한다. **이 값이 0 이 아니면 분류 파이프라인이 조용히 실패 중이라는 신호**다.
  재분류 스윕은 Phase 3 항목 G 로 이월한다 — 재시도 소진·일시적 장애 복구와 **같은 문제**이므로 따로 만들지 않고 함께 처리하는 것이 맞다.
  이 프로젝트의 성공 기준 1번이 *"조용히 틀리지 않고 관찰 가능하게 틀리는가"* 이므로, Phase 2 에서 "구멍을 남기되 계기판에 띄운다"는 선택은 주제와 일관된다.
- **영향**: `API-CONTRACT.md` v0.4 → v0.5 (`stats.classification.stuckNew`, Actuator gauge), `PRD.md` §4-2 복구 경로 문단 + §7 리스크 표 1행. **측정 3(롤백 검증) 절차에 "stuckNew 가 실제로 증가하는지 확인"을 포함**
- **재평가**: 정상 운영에서 `stuckNew` 가 지속적으로 0 이 아니면 Phase 3 를 기다리지 말고 재분류 스윕을 앞당긴다

### D-018. 인덱스 조합을 측정으로 확정 — category + 기간 + lastSeenAt

- **일자**: 2026-07-30
- **상태**: **폐기** (2026-08-05, D-031) — A/B 의 대상이던 `occurrence_count` 컬럼이 사라졌다. 이 결정의 핵심 논거는 "**인덱스 컬럼의 갱신 빈도를 함께 본다**"였는데, 새 도메인의 `inquiries` 는 INSERT 위주이고 UPDATE 는 판정 시 1회뿐이라 조회 이득과 쓰기 비용의 트레이드오프가 성립하지 않는다. 측정 5ⓔ(쓰기 p95 A/B)도 함께 삭제한다. **논거 자체는 유효하므로 갱신 빈도가 높은 컬럼을 인덱스에 넣게 되면 다시 발동한다**
- **배경**: PR #1 AI 리뷰 4차 지적. D-013 에서 `sort=lastSeenAt` + `(status, last_seen_at)` 로 filesort 를 피한다고 했는데, **`category` 필터를 동시에 쓰면 그 인덱스로는 `current_category` 가 커버되지 않아** 추가 필터링이 남는다. 조회 패턴 조합을 전부 따져보지 않고 단일 케이스만 보고 결론지었다.
- **선택지**:
  1. `(status, current_category, last_seen_at)` 인덱스 추가 — `category` 는 등치라 선행에 두면 뒤의 범위+정렬까지 커버 가능. 단 `error_group` 인덱스가 3개로 늘어 쓰기 비용 증가 (수신 경로는 최고 QPS 지점이라 쓰기 비용에 민감)
  2. 현상 유지 + 한계 문서화
  3. `category` 필터를 `error_group` 조회에서 제거 — 요구사항 후퇴
- **결정**: **측정으로 확정한다.** 1번을 후보로 인덱스 표에 올리되 `측정 5 로 이득 확인 후 확정` 으로 표기하고, 측정 5 의 `EXPLAIN` 케이스를 4개로 확장한다 (ⓐ 검토 큐 / ⓑ occurrenceCount+category / ⓒ lastSeenAt+기간 / **ⓓ lastSeenAt+기간+category**). 쓰기 비용과 조회 이득의 균형은 추정으로 정할 수 없고 실측이 필요하다 — `CLAUDE.md` AI 검증 규칙상 예상 계획은 evidence 가 될 수 없다.
- **영향**: `CLAUDE.md` 인덱스 표 1행 추가, `PRD.md` 측정 5 를 4개 케이스로 확장
- **재평가**: ⓓ 에서 3컬럼 인덱스의 이득이 미미하면 추가하지 않고 filesort 를 수용한다 (쓰기 비용만 남으므로)

### D-019. blind 한계를 결정적 역산 / 확률적 추론으로 구분

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 리뷰 4차가 *"`sampleMessage` 로 감사 표본을 역산할 여지가 없는지 D-010 기준으로 점검"* 을 요청했다. 점검 결과 **제거할 수 없다** — 검토자가 에러 원문을 못 읽으면 분류 작업 자체가 성립하지 않는다. 그러나 숙련된 검토자는 *"이건 딱 봐도 명확한데 왜 내 큐에 있지"* 로 감사 표본을 짐작할 수 있고, `occurrenceCount` 도 약한 신호가 된다.
- **선택지**:
  1. `sampleMessage` 제거 — 검토 불가능. 기각
  2. 감사 표본에만 다른 메시지 제공 — 조작이며 검토 품질을 해침
  3. **누설을 두 종류로 구분해 각각 다르게 다룬다**
- **결정**: 3번. **결정적 역산**(필드 조합으로 100% 식별 — D-010 의 `confidence >= threshold` 같은 것)은 **결함이며 0건이어야 한다.** **확률적 추론**(원문 난이도·발생 횟수로 짐작)은 **제거 불가능하므로 목록화하고 감수한다.** 측정 10(blind 무결성)의 판정 기준도 이에 맞춰 둘로 나눈다.
  이 구분이 없으면 "완벽한 blind" 라는 달성 불가능한 목표를 세우거나, 반대로 D-010 같은 실제 결함을 "어차피 완벽할 수 없다"며 넘기게 된다.
- **영향**: `API-CONTRACT.md` §4 한계 3줄, `PRD.md` 측정 10 판정 기준
- **재평가**: 감사 표본의 `matched` 비율이 비감사 표본보다 유의하게 높으면 확률적 추론이 실제로 작동하고 있다는 신호 → Phase 3 항목 I(2단계 공개)를 앞당긴다

### D-020. 라이프사이클 5 단계 책임자 확정 — D-002 의 "미확정" 대체

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 리뷰 5차가 D-002 의 자기모순을 지적했다. `상태` 는 **채택**으로 바꿨는데 본문 `결정` 필드는 여전히 *"**미확정.** 팀 미팅에서 확정한다"* 로 남아 있어 서로 충돌한다. 확정 내역을 `상태` 필드 괄호 안에 욱여넣은 것이 원인 — 본문 불변 규칙을 지키려다 오히려 문서를 모순 상태로 만들었다.
- **선택지**:
  1. D-002 본문의 `결정` 을 직접 수정 — 형식 규칙(본문 불변) 위반. 결정 이력이 사라져 "언제 무엇이 바뀌었나"를 추적 못 함
  2. **후속 항목으로 확정 사실을 기록** — 형식 규칙 준수. D-002 는 "미확정이었던 시점의 기록"으로 그대로 보존
  3. 상태 필드 괄호 유지 — 현 상태. 모순 지속
- **결정**: 2번. D-002 의 `상태` 를 `채택 — 확정 내역은 D-020 참조` 로 줄이고, 확정 내역은 여기에 기록한다.

  | 단계 | 책임자 |
  | --- | --- |
  | 기획 | 김준현 |
  | 코딩 | 팀 전원 |
  | 테스트 | 이용택 |
  | 리뷰 | 김은빈 |
  | 배포·운영 | 이용택 |

  인원 3명 / 단계 5개이므로 이용택이 테스트 + 배포·운영을 겸한다. 코딩 단계의 코드 범위 분할은 D-015(P1/P2/P3).
  **교훈**: `상태` 필드만 갱신 가능하다는 규칙은 "보류 → 채택" 같은 **한 단어 전환**을 위한 것이다. 내용이 있는 결정은 괄호에 넣지 말고 후속 항목으로 만든다.
- **영향**: `DECISIONS.md` D-002 상태 1줄, `LIFECYCLE-COVERAGE.md` 매트릭스(기획 상태를 D-001~D-020 으로, 리뷰 수령 횟수 갱신)
- **재평가**: 누가 명백히 막히면 swap (D-002 재평가 조건 승계)

### D-021. 409 의 두 원인을 code 로 분리 — 상태 검사와 @Version 은 서로 다른 창을 막는다

- **일자**: 2026-07-30
- **상태**: 채택
- **배경**: PR #1 AI 리뷰 5차 지적. `PATCH /api/review-queue/{id}` 의 409 를 *"낙관적 락 충돌 (이미 확정된 큐 항목 재확정)"* 이라고 **한 줄로 뭉뚱그렸는데**, 이 문장 안에 성격이 다른 두 시나리오가 섞여 있다. "이미 확정된 항목"은 조회 시점 **상태 검사**로 걸리는 것이고, "낙관적 락 충돌"은 **커밋 시점 `@Version`** 으로 걸리는 것이다. 검출 시점도 필요한 장치도 다르다.
- **선택지**:
  1. 현상 유지 — 클라이언트는 재시도 여부를 판단할 수 없고, 동시성 테스트에서 **락이 실제로 동작했는지 검증 불가**
  2. **`code` 로 2종 분리** — `ALREADY_RESOLVED` / `CONCURRENT_UPDATE`
  3. HTTP 상태를 다르게 (409 / 412) — 둘 다 "확정 불가"라는 같은 결과이므로 과분리
- **결정**: 2번. 두 장치가 **각각 다른 창을 막는다**는 것이 핵심이다.
  - **상태 검사만으로는 부족** — 두 검토자가 동시에 `PENDING` 을 읽으면 **둘 다 검사를 통과**한다 (check-then-act 경합). 이 창을 막는 것이 `@Version` 이고, 그래서 D-007 이 낙관적 락을 택했다
  - **`@Version` 만으로도 부족** — 시간 차를 두고 온 요청이 `CONCURRENT_UPDATE` 로 보고되어 **경합이 없었는데 경합이라고 말하게** 된다. 운영 지표가 오염된다

  측정 7 에 큐 동시 확정 케이스를 추가하고 두 코드의 발생 비율을 기록한다. **`CONCURRENT_UPDATE` 가 0 이면 경합 창이 재현되지 않은 것이므로 테스트가 무의미하다는 신호** — 이 판정 기준이 없으면 "409 나왔으니 통과"로 잘못 결론 낸다.
- **영향**: `API-CONTRACT.md` v0.5 → v0.6 (공통 오류 표 + §5 원인 표 신설), `CLAUDE.md` 락 전략 3행, `PRD.md` US-10 + 측정 7
- **재평가**: 검토자 수가 늘어 `CONCURRENT_UPDATE` 가 빈발하면 claim(선점) 모델로 전환 (Phase 3 항목 E)

### D-022. 파싱 실패는 `confidence 0` 이 아니라 `FAILED` 다 — 판정 의미와 저장 값의 분리

- **일자**: 2026-07-31
- **상태**: 채택
- **배경**: 코드 착수 직전 문서 정합 점검에서 발견. 파싱 실패 처리를 두고 문서 3곳이 서로 다른 전제를 깔고 있다.
  - `CLAUDE.md` 「AI 호출 규칙」: *"파싱 실패 = `confidence 0` 으로 간주해 무조건 격리"*
  - `PRD.md` §시퀀스: 재시도 소진 시 `classification_result(FAILED)` + `review_queue(CLASSIFY_FAILED)`
  - 계약 B: *"`CLASSIFY_FAILED` 는 `classification_result.category = null`"*

  **충돌 지점**: `confidence 0` 을 그대로 저장하면 파싱 실패 건은 `0 < threshold` 이므로 `LOW_CONFIDENCE` 로 흘러간다. 그런데 파싱이 깨졌으니 `category` 도 null 이다. 즉 계약 B 가 구분자로 쓰는 `category = null` 이 `CLASSIFY_FAILED` 를 더 이상 특정하지 못한다. 게다가 파싱 실패가 `@Retryable` 재시도 대상인지도 어디에도 없다 — D-008 이 일시적/영구적 실패 구분을 Phase 3(항목 G)으로 이월하면서 생긴 틈이다.
  세 담당자의 코드가 전부 여기에 걸린다: 측정 2의 사유별 분모(P2), 측정 8의 감사 모집단(P2), `suggestedCategory` 표시 로직(P3), `@Recover` 경로(P2).
- **선택지**:
  1. `confidence = 0` 을 실제로 저장하고 `LOW_CONFIDENCE` 로 처리 — 구현은 가장 단순하나 **측정 8의 신뢰도 구간 집계가 오염된다.** 최하위 구간에 "AI 가 스스로 0 에 가깝다고 신고한 건"과 "응답이 깨져서 신뢰도라는 게 애초에 없는 건"이 섞인다. 오분류율을 신뢰도의 함수로 보겠다는 이 프로젝트의 핵심 측정이 무너짐
  2. `category = null` 대신 새 컬럼(`parse_failed` 플래그)을 추가 — `verdict = FAILED` 가 이미 그 정보를 담고 있어 중복
  3. **판정 의미와 저장 값을 분리한다** — "`confidence 0` 으로 간주"는 *격리 쪽으로 실패하라*는 **판정 방향 지시**이지 컬럼에 쓸 값이 아니다
- **결정**: 3번. 확정 규격은 다음과 같다.

  ```text
  파싱 실패 → @Retryable 재시도 대상에 포함한다
    ├ 재시도 중 성공 : 정상 경로
    └ 3회 소진      : @Recover →
                       classification_result(verdict=FAILED,
                                              category=null, confidence=null)
                       review_queue(reason=CLASSIFY_FAILED)
                       error_group.status=UNCLASSIFIED

  confidence 컬럼에 0 을 쓰지 않는다. nullable 이다.
  ```

  **파싱 실패를 재시도 대상에 넣는 이유**: LLM 응답 형식 흔들림은 재현 가능한 영구 실패가 아니라 확률적 사건이다. 재시도로 회수되는 건이 실제로 있다면 큐 적체가 줄고, 없다면 측정 2에서 `CLASSIFY_FAILED` 건수로 드러난다. 어느 쪽이든 관찰된다. (429/5xx 와의 구분은 D-008 대로 Phase 3 이월 — **재시도 여부는 같고 백오프 전략만 다르므로 Phase 2 에서 구분하지 않아도 정확도가 떨어지지 않는다.**)

  **큐 사유 판별 기준을 `reason` 자체로 고정한다.** `category = null` 은 결과일 뿐 판별식이 아니다.

  | reason | 성립 조건 |
  | --- | --- |
  | `LOW_CONFIDENCE` | `verdict = NEEDS_REVIEW` — `category != null` **AND** `confidence < threshold` |
  | `CLASSIFY_FAILED` | `verdict = FAILED` — `category`, `confidence` 모두 null |
  | `AUDIT_SAMPLE` | `verdict = AUTO_ACCEPTED` — `confidence >= threshold` |

  **blind 규칙과의 관계 (D-010/D-019)**: `CLASSIFY_FAILED` 는 `suggestedCategory: null` 로 응답하므로 검토자에게 구별된다. 이는 blind 위반이 아니다 — 가려야 하는 것은 **감사 표본**이고, 감사 표본은 정의상 `AUTO_ACCEPTED` 라 `suggestedCategory` 가 **항상 존재**한다. null 여부로 드러나는 건 `CLASSIFY_FAILED` 쪽이며, 그것은 감사 표본이 아님을 알려줄 뿐 어느 것이 감사 표본인지는 알려주지 않는다. D-019 분류로는 **결정적 역산 아님**.
- **영향**: `CLAUDE.md` 「AI 호출 규칙」 1줄 + 계약 B 구분자 문구, `PRD.md` §기술 리스크 1행, `API-CONTRACT.md` `confidence` nullable 명시, `evidence/failure-cases.md` 미검증 항목 2줄
- **재평가**: 측정 2에서 `CLASSIFY_FAILED` 가 전체의 10% 를 넘으면 재시도로 회수되지 않는 구조적 실패이므로 프롬프트/파서를 고친다. 반대로 0 건이면 재시도가 실제로 회수하고 있는지(재시도 횟수 로그)를 확인한다 — 0 건이 "문제 없음"과 "측정 안 됨" 둘 다일 수 있기 때문

### D-023. 스키마는 Flyway 로 관리하고 후보 인덱스를 V2 로 분리한다 — `ddl-auto` 로는 D-018 을 측정할 수 없다

- **일자**: 2026-07-31
- **상태**: 채택 — **앞부분(Flyway + `ddl-auto: validate`)은 유지, 뒷부분(V2 후보 인덱스 분리)만 폐기** (D-031). 근거인 D-018 이 폐기돼 `V2__candidate_index.sql` 과 `spring.flyway.target` 을 삭제한다. "엔티티와 DDL 이 어긋나면 부팅이 실패해야 한다"는 판단은 새 도메인에서도 그대로 적용된다
- **배경**: D-003 이 스택만 정하고 스키마 관리 방식을 정하지 않았다. 기본값대로 `ddl-auto=update` 를 쓰면 **측정 5ⓔ 를 실행할 방법이 없다.** D-018 은 "`occurrence_count` 를 포함한 인덱스가 최고 QPS 쓰기 경로에 비용을 얹는지"를 인덱스 유무별 `POST /api/errors` 지연으로 확인하라고 요구하는데, `ddl-auto` 에는 인덱스를 붙였다 떼는 수단이 없다. 결국 조회 이득만 보고 인덱스를 추가하게 되는데, 그것이 D-018 이 "이 프로젝트에서 가장 하기 쉬운 실수"라고 지목한 바로 그 행동이다.
  루트 패키지명도 미정이라 3명이 서로 다른 패키지로 짜면 Day 3 통합에서 전부 이동해야 한다.
- **선택지**:
  1. `ddl-auto=update` — 착수는 빠르나 측정 5ⓔ 불가. 엔티티 변경이 운영 스키마에 조용히 반영되는 것도 이 프로젝트 주제("조용히 틀리지 않기")와 정면으로 어긋남
  2. Flyway 단일 마이그레이션 — 스키마는 재현 가능하나 인덱스 A/B 를 하려면 매번 파일을 고쳐야 하고, 그러면 "무엇을 켜고 잰 수치인지"가 이력에 안 남음
  3. **Flyway + 확정/후보 인덱스 분리**
- **결정**: 3번.

  ```text
  V1__init_schema.sql      5테이블 + 확정 인덱스 + classification_policy 10행 seed
  V2__candidate_index.sql  후보 인덱스만 (D-018 의 ⚠️ 표시 2개)
                             (status, current_category, occurrence_count DESC)
                             (status, current_category, last_seen_at)
  ```

  `spring.jpa.hibernate.ddl-auto=validate` 로 고정한다 — 엔티티와 DDL 이 어긋나면 **부팅이 실패**해야 한다. 측정 5ⓔ 는 V2 적용 전/후로 `POST /api/errors` p95 를 비교하고, 양쪽 수치를 `evidence/` 에 함께 남긴다. **후보 인덱스는 측정으로 이득이 확인되기 전까지 V1 으로 승격하지 않는다.**

  부수 확정 3건 (baseline PR 에서 함께 고정):
  - 루트 패키지: `com.dingco.triage` — 하위는 `api` / `service` / `domain` (CLAUDE.md 3계층)
  - `stuckNew` 임계값을 `monitoring.stuck-new-threshold` property 로 분리. 기본 10분이나 **테스트에서 초 단위로 낮출 수 없으면 D-017 을 검증할 방법이 없다**
  - `classification.policy.mode = PER_CATEGORY | GLOBAL` + `global-threshold` — 측정 9(카테고리별 임계값의 효과)의 대조군이자, D-008 이 Day 3 에 막혔을 때의 탈출 경로
- **영향**: `build.gradle`(flyway 의존성), `application.yml`, `src/main/resources/db/migration/`, `PRD.md` 측정 5, `CLAUDE.md` 인덱스 표 ⚠️ 2행
- **재평가**: 측정 5ⓔ 에서 쓰기 지연 증가가 유의하지 않으면(p95 차이가 측정 노이즈 이내) V2 를 V1 으로 승격한다. 유의하면 D-013 대로 `sort=lastSeenAt` 유도를 유지하고 후보 인덱스를 폐기한다

### D-024. AI 프로바이더를 Anthropic 으로 확정 — D-003 의 "OpenAI API" 대체

- **일자**: 2026-08-03
- **상태**: 채택
- **배경**: 프로바이더가 문서 간에 어긋난 채로 남아 있었다. D-003 과 `PRD.md`(시퀀스 다이어그램·보안·리스크 3곳)는 **OpenAI**, `API-CONTRACT.md` §3 의 응답 예시는 `"model": "claude-sonnet-5"`. baseline PR 은 이 충돌을 알고도 선점을 피해 `build.gradle` 에 AI SDK 의존성을 넣지 않고 주석으로만 남겼다. `TRI-14`(US-4) 는 착수 전 확정을 선결 과제로 명시하고 있고, 확정 없이는 SDK 의존성·모델 ID·API key 이름·JSON 강제 방식이 모두 갈린다.
- **선택지**:
  1. **OpenAI** — D-003·PRD 다수결. `API-CONTRACT.md` §3 만 고치면 됨
  2. **Anthropic** — `API-CONTRACT.md` 의 기존 값과 일치. `.github/workflows/ai-review.yml` 이 이미 `ANTHROPIC_API_KEY` 시크릿을 쓰고 있어 키 확보 경로가 하나 줄어든다. 대신 `PRD.md` 3곳 정정 필요
  3. 프로바이더 추상화 계층을 두고 둘 다 지원 — Phase 2 범위 초과. 5일 안에 완주해야 하는 D-003 의 전제와 어긋난다
- **결정**: 2번 Anthropic.

  | 항목 | 확정값 |
  | --- | --- |
  | SDK | `com.anthropic:anthropic-java:2.34.0` |
  | 모델 | `claude-sonnet-5` — `GET /v1/models` 실물 조회로 존재 확인 (2026-08-03) |
  | API key | `ANTHROPIC_API_KEY` 환경변수. SDK 가 이 이름을 자동으로 읽어 `AnthropicOkHttpClient.fromEnv()` 로 끝난다 |
  | JSON 강제 | **프롬프트만.** structured outputs 를 쓰지 않는다 |

  **모델을 `claude-sonnet-5` 로 둔 이유**: 에러 분류는 10종 enum 중 하나를 고르는 단순 분류 작업이라 이 티어로 충분하다. 상위 모델은 분류 정확도가 올라가는 대신 **측정 8 이 재려는 "AI 가 자신 있게 틀리는 빈도"의 표본이 귀해진다** — 오분류율을 신뢰도의 함수로 관찰하는 것이 목표인 이상, 표본이 나오는 티어를 택한다.

  **모델 ID 의 근거를 `API-CONTRACT.md` §3 에서 실물 조회로 바꾼 이유**: 원래 근거 사슬이 순환하고 있었다 — §3 의 값 자체가 AI 가 생성한 예시였는데, D-024 가 그것을 "기존 값"이라며 채택 근거로 인용하고, 다시 `.env.example`·테스트·workflow 로 퍼졌다. **어디에도 실물 확인이 없었다.** `GET /v1/models` 한 번으로 닫았고 값은 맞았지만, 맞은 것과 확인한 것은 다르다 (`evidence/failure-cases.md` 「미검출 위험」 참조).

  **structured outputs 를 쓰지 않는 이유 (중요)**: Anthropic SDK 는 JSON 스키마를 API 에 넘겨 응답 형식을 **보장**받는 기능을 제공한다. 이를 쓰면 enum 밖 카테고리도 깨진 JSON 도 API 레벨에서 차단되어 구현이 단순해지지만, **`CLASSIFY_FAILED` 가 거의 발생하지 않는다.** 그러면 D-022 가 규정한 `@Retryable(3)` → `@Recover` → `verdict=FAILED` 경로는 사실상 죽은 코드가 되고, 측정 2 의 판정 기준(`CLASSIFY_FAILED` 10% 초과 시 프롬프트/파서 수정)도 분모가 0 이라 의미를 잃는다.

  이 프로젝트의 주제는 **AI 가 실패하는 방식을 수치로 관찰하는 것**이다. 실패를 관찰하겠다고 선언한 시스템이 실패를 발생시키지 않는 장치를 먼저 켜면 앞뒤가 맞지 않는다. 편의를 포기하고 관찰 가능성을 택한다 — D-022 와 같은 판단이다.
- **영향**:
  - `build.gradle` — SDK 의존성 1행 추가 (baseline 의 보류 주석 제거)
  - `.env` / `.env.example` — `AI_API_KEY` → `ANTHROPIC_API_KEY` 로 이름 변경 (SDK 자동 인식)
  - `application.yml` — 모델 ID·타임아웃 설정 키
  - `PRD.md` — "OpenAI API" 3곳 (§시퀀스 다이어그램 / 보안 / 리스크)
  - `DECISIONS.md` — D-003 `상태` 필드 1줄 (본문 불변, D-020 선례)
  - `CLAUDE.md` 「작업 경계」 — 비밀 정보 예시의 "OpenAI API key" 1곳. 규칙의 실질은 그대로이고 프로바이더 이름만 따라간다
  - `service/AiClassificationService` — `TRI-14` 구현 시점
- **재평가**:
  - 신뢰도 0.8 이상 구간의 분류 일치율이 90% 미달이면(E2 완료 기준) 모델을 `claude-opus-5` 로 올리는 것을 검토한다. 단 **모델을 바꾸면 측정 8 의 수치는 그 모델에 한정된 결론**이 되므로 evidence 에 모델 ID 를 함께 기록한다
  - 측정 2 에서 `CLASSIFY_FAILED` 가 10% 를 넘고 재시도로 회수되지 않으면(D-022 재평가 조건) 프롬프트를 먼저 고치고, 그래도 안 되면 structured outputs 도입을 후속 항목으로 재검토한다
  - AI API 장기 장애 시 전건이 수동 처리 대상이 되는 한계는 Phase 2 에서 명시만 한다. 백오프 재분류·circuit breaker 는 Phase 3 항목 G

### D-025. 엔티티 생성은 정적 팩토리로만 — baseline 이 "생성 시점 불변식"까지 고정한다

- **일자**: 2026-08-03
- **상태**: 채택
- **배경**: PR #3 AI 리뷰 지적. baseline 엔티티 5개에 `protected` 무인자 생성자만 있어 **애플리케이션 코드에서 생성이 아예 불가능**하다. 즉 P1/P2/P3 담당자가 각 패키지의 첫 줄에 할 일이 "생성 수단 만들기"이고, 그 자리에서 setter 가 나온다. 그러면 불변 규칙 2(`category` 덮어쓰기 금지)와 4(`current_*` 는 ②③ 안에서만)를 지킬 자리가 사라진다.

  더 큰 문제는 **생성 시점의 불변식이 한 패키지 소유가 아니라는 점**이다. `ErrorGroup` 의 `current_* = null` 은 계약 C 이고, `ClassificationResult` 의 `verdict` × `(category, confidence)` null 조합은 D-022 이자 계약 B 의 `reason` 판별식이다. 세 사람이 각자 만들면 계약이 세 벌로 갈라진다.
- **선택지**:
  1. 지금처럼 두고 각 담당자가 필요할 때 만든다 — 계약이 코드에 없으므로 어긋나도 컴파일도 테스트도 통과한다. 어긋난 사실은 측정 단계에서야 드러난다
  2. `EntityFactory` 같은 별도 클래스로 분리 — 그 클래스가 필드에 접근하려면 생성자를 package-private 이상으로 열어야 한다. **생성 경로를 좁히려는 목적 자체를 되돌린다**
  3. **엔티티 안에 정적 팩토리**
- **결정**: 3번. baseline 이 제공하는 범위를 "필드·매핑" → **"필드·매핑 + 생성 팩토리"** 로 넓힌다. 상태 **전이** 메서드는 종전대로 각 트랜잭션 소유자가 추가한다 — 생성은 전이가 아니므로 D-015 의 P1/P2/P3 분담과 충돌하지 않는다.

  | 엔티티 | 팩토리 | 고정하는 계약 |
  | --- | --- | --- |
  | `ErrorGroup` | `create(fingerprint, sampleMessage, seenAt)` | 계약 C — `status=NEW`, `occurrenceCount=1`, `firstSeenAt == lastSeenAt`, `current_* = null` |
  | `ErrorEvent` | `of(group, rawMessage, stackTrace, source, occurredAt)` | 불변 규칙 1 — 파라미터에 상태가 없다. `stackTrace` 만 nullable |
  | `ClassificationResult` | `autoAccepted(...)` / `needsReview(...)` / `failed(...)` | D-022 — `failed(...)` 에 `confidence` 파라미터가 **없다** |
  | `ReviewQueueItem` | `from(result)` | 계약 B — 파라미터에 `reason` 이 **없다** |
  | `ClassificationPolicy` | 없음 | seed 10행이 `V1` 에 있어 애플리케이션이 생성하지 않는다. 필요한 건 전이 메서드 `updateThreshold(...)` (P3) |

  **핵심은 두 팩토리에서 파라미터를 뺀 것이다.** `failed(...)` 에 `confidence` 자리가 없으면 D-022 가 금지한 `confidence = 0` 이 **문법적으로 불가능**해진다. `from(result)` 에 `reason` 자리가 없으면 D-022 가 금지한 "`category == null` 로 reason 판별"이 애초에 불가능해지고, 내부 `switch` 가 exhaustive 라 `Verdict` 에 값이 늘면 **컴파일 에러로 터진다.** 문서가 아니라 컴파일러가 계약을 지킨다.

  **부수 확정 2건:**
  - **`ErrorGroup.firstSeenAt` / `lastSeenAt` 은 서버 수신 시각**이다. 클라이언트가 보고한 `occurredAt` 은 `ErrorEvent` 에만 보고값 그대로 남긴다 — 클라이언트 시계는 신뢰할 수 없고, 이 값이 D-017 `stuckNew` 와 목록 기간 필터의 기준이라 조작 가능한 값을 쓰면 두 지표가 함께 오염된다
  - **엔티티는 `Instant.now()` 를 직접 부르지 않는다.** 시각은 항상 파라미터로 받는다. `stuck-new-threshold` 를 property 로 뺀 것과 같은 이유로(D-023), 테스트에서 시각을 조작할 수 없으면 D-017 을 검증할 방법이 없다
  - fingerprint 정규화는 팩토리 밖 별도 컴포넌트(`service/FingerprintGenerator`, P1) 다. 팩토리는 **계산된 값**을 받는다 — 정규화 규칙이 바뀌면 그룹핑 결과 전체가 바뀌므로 DB 없이 단독 테스트가 가능해야 한다
- **영향**:
  - `domain/ErrorGroup.java`, `ErrorEvent.java`, `ClassificationResult.java`, `ReviewQueueItem.java` — 팩토리 추가 + 클래스 javadoc 의 "필드·매핑까지만" 문구 정정
  - `src/test/java/.../domain/DomainFactoryTest.java` 신설 — 컨텍스트 없는 순수 단위 테스트
  - Lombok 은 도입하지 않는다. `@Setter`/`@Data`/`@Builder` 가 위 세 계약을 모두 우회시키고, `@Getter` 만 쓸 거라면 이미 손으로 쓴 getter 와 차이가 없다. 이벤트·캐시값·DTO 는 Java 21 `record` 를 기본으로 한다
- **재평가**: P1/P2/P3 가 전이 메서드를 붙이는 시점에 팩토리 시그니처가 부족하면 여기 표를 갱신한다. 단 **파라미터를 늘리는 방향만 허용하고, `reason`·`confidence` 를 다시 파라미터로 여는 변경은 D-022/계약 B 재논의를 동반해야 한다**

### D-026. 로컬 개발 환경 — Docker Desktop 을 4.44.2(build 202017) 이하로 고정

- **일자**: 2026-08-04
- **상태**: 채택
- **배경**: `.claude/hooks/` push 검증 훅을 만들며 `./gradlew test` 를 처음 실 Docker 환경에서 돌려보니 `BaselineSmokeTest`·`CandidateIndexMigrationTest` 등 Testcontainers 기반 통합 테스트 7건이 전부 `"Could not find a valid Docker environment"` 로 실패했다 (`DomainFactoryTest` 순수 단위 테스트 10건은 영향 없음). 원인을 좁혀보니 Spring Boot 3.3.13 BOM 이 고정하는 **Testcontainers 1.19.8** 의 docker-java 가 Docker 환경 감지(probe) 단계에서 API 버전을 **1.24 로 하드코딩 요청**하는데, 이 머신의 Docker Desktop(4.6x~4.7x대, Engine 29.x)은 데몬 `MinAPIVersion` 이 **1.40** 으로 올라가 있어 이 요청 자체를 거부한다 (`curl` 로 `/v1.24/_ping` 을 직접 찔러 `"client version 1.24 is too old. Minimum supported API version is 1.40"` 응답을 실측 확인).
  개인 머신 문제가 아니다 — 팀원 중 한 명(Docker Desktop 구버전, `MinAPIVersion=1.24`)은 이미 통과하고 있었고, 최신 버전을 쓰는 사람만 실패했다. 즉 **Docker Desktop 을 최신으로 유지하는 것 자체가 P1/P2/P3 전원의 로컬 통합 테스트를 막는 요인**이다.
- **선택지**:
  1. **Testcontainers 를 2.x 로 업그레이드** — 업스트림에 fix 존재(`testcontainers/testcontainers-java#11212`, PR #11216, `2.0.2` 부터 포함, 근거: GitHub 이슈·PR 본문 직접 확인). 그러나 2.x 는 `org.testcontainers:mysql`·`org.testcontainers:junit-jupiter` 좌표가 Maven Central 에서 사라졌다(`1.21.3` 이 마지막 발행 — 직접 `maven-metadata.xml` 조회로 확인). 단순 버전 번호 교체가 아니라 모듈 재구조화를 처음부터 조사해야 하고, 커뮤니티에서도 `2.0.2` 에서까지 문제가 남았다는 보고가 있어 불확실성이 크다. 실제 시도 결과 `compileTestJava` 단계에서 좌표 미해결로 실패(`evidence/` 대상)
  2. **Docker Desktop 을 `MinAPIVersion=1.24` 인 버전으로 고정** — 실측으로 `4.44.2`(build `202017`, Engine `28.3.2`)에서 통과 확인. 즉시 해결되지만 자동 업데이트를 꺼야 유지되고, 팀 전원이 같은 상한선을 지켜야 한다
  3. 로컬 통합 테스트는 포기하고 CI(`e2e.yml`)만 신뢰 — `e2e.yml` 은 Testcontainers 가 아니라 실제 `docker compose` 스택을 쓰므로 이 버그의 영향을 받지 않는다. 다만 로컬 개발 루프에서 통합 테스트 없이 작업해야 해 회귀를 늦게 발견한다
- **결정**: 2번. 5일 일정 안에 메이저 버전 마이그레이션까지 검증할 여유가 없다는 점이 D-008 의 판단과 같다. Docker Desktop 버전 고정은 되돌리기 쉬운 선택이고(자동 업데이트 체크박스 하나), 1번(Testcontainers 2.x)은 재평가 조건이 성립하면 후속 항목으로 넘긴다. 3번은 로컬 개발 경험을 포기하는 것이라 최후 수단으로만 둔다.
- **영향**:
  - 팀원 전원 로컬 환경 — Docker Desktop **`4.44.2`(build `202017`) 이하**로 설치 + Settings → General 에서 자동 업데이트 체크 해제 필요
  - `.claude/hooks/handlers/verify-before-push.sh`(push 전 `./gradlew test` 검증)가 이 환경 조건에 실질적으로 의존한다 — 조건이 깨지면 이 훅이 코드와 무관하게 push 를 막는다
  - `README.md` 또는 `MONITORING.md` 에 로컬 환경 요구사항으로 명시 필요(본 PR 범위 밖 — 후속 PR)
- **재평가**: Testcontainers 2.x 의 `mysql`/`junit-jupiter` 2.x 좌표가 확인되거나 커뮤니티에서 `2.0.2` 이후 패치의 안정성 보고가 쌓이면 1번으로 전환한다. 반대로 Docker Desktop 자동 업데이트로 이 문제가 팀 내 재발하는 사례가 관측되면 3번(CI 전용 검증)으로 축소한다.

### D-027. 도메인 재검토 결과 현행 유지 — 도메인 판별 기준 3개 명문화 + 측정 12 신설

- **일자**: 2026-08-04
- **상태**: 채택 — **결론("현행 유지")은 D-031 으로 뒤집혔으나 판별 기준 3개는 헌법에 남는다.** D-031 은 기준을 폐기한 것이 아니라 **기준 1 에서 탈락함을 인정하고 그룹핑을 포기하는 쪽**을 택했다. 기준 1 의 적용 조건은 D-031 이 정정한다. 측정 12(검토자 간 일치도)는 그대로 유지
- **배경**: 팀 내에서 도메인 교체 제안이 나왔다 — "에러 분류" 대신 **핀테크 이상거래 알림 유형 분류**로 바꾸고 핵심 로직(임계값 검증·트랜잭션·비동기·재시도)은 그대로 두자는 것. 실제로 설계 자산의 대부분은 도메인 중립이다: `classification_result` · `review_queue` · `classification_policy` 세 테이블과 D-005~D-026 의 결정 22건이 "분류 대상이 무엇이든" 성립하고, 도메인에 묶인 결정은 **D-004(fingerprint 산출 규칙) 하나뿐**이다. 코드도 P1/P2/P3 의 `service/`·`api/` 가 미착수라 교체 비용이 가장 싼 시점이었다.
  그런데 검토해보니 도메인은 자유롭게 갈아끼울 수 없었다. **그룹핑이 이 시스템의 척추이고**(D-004), 그룹핑이 성립하려면 도메인이 조건을 만족해야 한다. 제안된 도메인은 그 조건을 만족하지 않았을 뿐 아니라, **제안자가 그 도메인을 고른 이유 자체가 조건과 충돌**했다 — "미탐 비용이 오탐보다 훨씬 크므로 재현율을 우선한다"는 설계 의도는 곧 **개별 건을 놓치면 안 된다**는 뜻이고, 이는 "같은 것 1000건 = 판정 1번"인 그룹핑을 금지한다. 거래 1000건은 서로 다른 사람의 다른 돈이라 묶는 순간 그 안의 진짜 도용 1건이 사라진다.
  이 검토에서 **판별 기준이 문서 어디에도 없다는 것**이 드러났다. D-001 은 주제를 고른 이유만 적었고 "어떤 도메인이라야 이 설계가 성립하는가"는 적지 않았다. 기준이 없으면 같은 논의가 팀원마다 반복된다.
- **선택지**:
  1. **핀테크 이상거래 알림 분류** — 판정 대상이 개별 거래라 묶음 내 동질성이 없다. 정답 레이블에 FDS 도메인 전문성이 필요한데 팀에 없어 AI 생성 합성 라벨을 쓰게 되고, 그러면 측정 1 이 "AI 가 자기가 만든 문제를 채점"하는 자기참조가 되어 `CLAUDE.md` AI 검증 규칙과 정면 충돌한다. 실거래 데이터를 외부 AI 로 보내는 마스킹 요구사항도 새로 생긴다
  2. **콘텐츠 신고 유형 분류** — 묶음 동질성은 최상이다(신고 1000건이 문자 그대로 같은 콘텐츠 하나를 가리킨다). 그러나 정답이 단일하지 않다 — 모더레이션의 검토자 간 일치도는 업계 통념상 70~80% 대이고, 그러면 측정 8 의 `category ≠ final_category` 가 **"AI 가 틀린 것"인지 "사람이 다르게 본 것"인지 분리되지 않는다.** 카테고리 상호배타성도 깨진다(한 콘텐츠가 `스팸`이면서 `사기·피싱`)
  3. **결제 실패 코드 트리아지** — 묶음 동질성·QPS 는 통과하나 **PG 응답코드가 이미 분류다.** `51 = 잔액 부족`을 룩업 테이블 한 장으로 풀 수 있어 AI 의 존재 이유가 없고, fingerprint 가 PG 코드면 그룹 수가 수십 개로 고정돼 측정 6(절감률)이 측정이 아니라 산수가 된다. AI 명분을 살리려 코드 미부여 실패로 범위를 좁히면 데이터가 희소해져 QPS 장점이 사라지고 현행 도메인과 같아진다
  4. **도메인 비특정(범용 분류 파이프라인)** — 성공 기준 1(관찰 가능하게 틀리는가)만 달성 가능하다. 2·3 은 수치를 요구하고 수치는 정답 레이블에서 나오므로 도메인 없이 성립하지 않는다. 측정 1·6·8·9 가 무력화되고, 카테고리를 모르면 차등 임계값의 근거가 사라져 **D-006 이 폐기되고 근거 없는 전역 0.7 로 회귀**한다
  5. **현행(에러 분류) 유지** — 문서·스키마·코드 변경 0
- **결정**: 5번. 아래 **판별 기준 3개**로 후보를 평가한 결과 현행 도메인이 1위였다. 도메인을 바꾸지 않기로 한 것이 아니라, **기준을 세워 평가했더니 바꿀 이유가 없었던 것**이다.

  | 기준 | 왜 필요한가 | 현행 | ① 이상거래 | ② 콘텐츠 신고 | ③ 결제 실패 |
  | --- | --- | --- | --- | --- | --- |
  | **1. 묶음 동질성** — 묶인 N건이 같은 판정을 받아야 하는가 | D-004 의 전제. 안 맞으면 그룹핑이 정보를 파괴한다 | 통과 (같은 스택트레이스 = 같은 원인) | **탈락** | 통과 | 통과 |
  | **2. 정답 단일성** — 팀이 정답을 확정할 수 있고 그 정답이 유일한가 | 측정 1·8·9 의 전제. 안 맞으면 오분류율에 검토자 불일치가 섞인다 | 통과 (`NullPointerException` → `NULL_REFERENCE`) | **탈락** | **탈락** | 통과 |
  | **3. AI 존재 이유** — 규칙·룩업으로 풀리지 않는가 | D-001 의 전제. 안 맞으면 AI 가 부수 기능으로 강등된다 | 통과 (스택트레이스는 비정형 텍스트) | 통과 | 통과 | **탈락** |

  기준 2 를 새로 세운 것이 이번 검토의 소득이다. 기존에는 "팀이 라벨을 붙일 수 있는가"만 봤는데, **붙일 수 있다는 것과 그 라벨이 유일하다는 것은 다르다.** 후자가 아니면 성공 기준 3(편향의 방향과 크기)이 성립하지 않는다 — 편향원을 분리할 수 없기 때문이다. 현행 도메인은 정답이 단일해서 편향원이 **앵커링 하나**로 좁혀지고, D-010 의 "측정된 오분류율은 하한값" 이라는 문장이 깨끗하게 성립한다.
- **영향**:
  - **도메인·스키마·코드 변경 없음.** `PRD.md` §1, `V1__init_schema.sql`, 엔티티 5종(`src/main/` Java 19파일) 전부 그대로. `CLAUDE.md` 「도메인 컨텍스트」도 **도메인 서술 자체는 손대지 않고 판별 기준 3개만 덧붙인다**
  - `PRD.md` §8 에 **측정 12(검토자 간 일치도) 신설** — 같은 표본을 2명이 독립 분류해 일치도를 재고, 측정 8 의 오분류율을 그 값으로 보정한다. 후보 ②를 탈락시킨 근거가 "정답이 흔들리면 측정 8 이 오염된다"였는데, **그 위험은 정도만 다를 뿐 현행 도메인에도 있다**(`TIMEOUT` 과 `EXTERNAL_API` 의 경계 등). 남의 도메인을 탈락시킨 기준을 자기 도메인에는 적용하지 않는 것은 일관성이 없다
  - `PRD.md` §8 측정 8 의 해석 규칙 보강 — 오분류율은 **앵커링(D-010)** 과 **검토자 불일치(측정 12)** 두 편향원을 분리해 보고한다
  - D-006 의 임계값 배정 근거 보강 — 현재는 "오분류 비용이 같을 수 없다"까지만 말하고 **어느 방향으로** 비대칭인지를 말하지 않는다. `DB_CONNECTION` → `VALIDATION` 오분류는 장애 대응을 늦추지만(고비용) 반대 방향은 검토자가 한 번 더 볼 뿐(저비용)이다. 이 방향성이 카테고리별 임계값의 실제 근거다. 후보 ①의 "미탐 비용 > 오탐 비용" 프레임에서 건진 유일한 소득
  - `LIFECYCLE-COVERAGE.md` 측정 책임자 표에 12 추가
  - `CLAUDE.md` 「도메인 컨텍스트」에 **판별 기준 3개를 명시** — **이후 도메인 변경 제안은 이 기준 통과를 먼저 보이고**, 못 넘으면 새 `DECISIONS.md` 항목 없이 반려한다. 기준이 헌법에 없으면 강제력이 없어 매 제안마다 같은 논의가 반복되므로, D-027 기록만으로 두지 않고 헌법에 올린다
- **재평가**: 측정 12 의 검토자 간 일치도가 낮게 나오면 **기준 2(정답 단일성)를 현행 도메인이 스스로 만족하지 못한다는 뜻**이므로, 카테고리 10종의 경계 정의를 조이거나 D-001 재평가 조항(카테고리 10종 → 5종 축소)을 앞당겨 발동한다. 반대로 일치도가 충분히 높으면 측정 8 을 보정 없이 그대로 읽어도 된다는 근거가 된다.

### D-028. 감사 비율·정책 모드는 Phase 2 에서 런타임 API 를 두지 않는다 — 설정 고정 + 조회만 노출

- **일자**: 2026-08-04
- **상태**: 채택 — **감사 비율 부분은 그대로 유효**하고, `policy.mode` / `globalThreshold` 부분만 D-031 으로 소멸한다(D-006 폐기로 대조군 자체가 없어짐). "측정 조건을 실행 중에 바꿀 수 있게 만들면 그 측정의 분모를 신뢰할 수 없다"는 핵심 논거는 감사 비율에 계속 적용된다
- **배경**: 문서 정합 점검에서 **계약에 없는 기능이 PRD 에만 있는 상태**가 드러났다. `PRD.md` §2 관리자 페르소나는 *"카테고리별 임계값 정책 + **감사 비율 설정** + 전체 통계"* 라고 쓰고 §7 리스크 표도 *"감사 비율을 설정 가능하게 (ADMIN)"* 로 대응을 적었는데, `API-CONTRACT.md` 의 8 endpoint 어디에도 감사 비율을 바꾸는 자리가 없다. 실제 값은 `application.yml` 의 `classification.audit.sample-rate` 와 `classification.policy.mode` / `global-threshold` 에만 있다.
  이 상태의 실질적 해악은 문서 품질이 아니라 **소유자 부재**다. 계약에 없으면 P2(감사 샘플링)와 P3(정책 API) 중 누가 만들어야 하는지가 정해지지 않고, 그래서 아무도 만들지 않은 채 PRD 의 문장만 남는다 — D-015 가 코딩 단계에서 막으려던 바로 그 구멍이다.
- **선택지**:
  1. **`PATCH /api/settings/audit-sample-rate` 신설** — PRD 문장 그대로 구현. 그러나 감사 비율을 런타임에 바꾸면 **측정 8 의 모집단이 측정 도중 바뀐다.** `actualSampleRate` 와 `configuredSampleRate` 를 대조해 표본 누락을 잡는 장치(D-012)가, 설정값이 중간에 움직이면 "누락된 것"과 "설정을 바꾼 것"을 구분하지 못한다
  2. **설정 파일로 고정 + `GET /api/policies` 에 읽기로만 노출** — 변경은 재기동을 동반하므로 설정 변경 경계와 측정 경계가 일치한다. 측정 9(`policy.mode` 대조군)도 이미 재기동 방식이라 절차가 하나로 통일된다
  3. PRD 에서 해당 문장 삭제 — 정합은 맞지만 감사 비율이 **어디서 정해지는 값인지**가 어디에도 안 남는다. `configuredSampleRate` 필드의 출처가 사라진다
- **결정**: 2번. 이 프로젝트에서 감사 비율은 **운영 파라미터가 아니라 측정 조건**이다. 측정 조건을 실행 중에 바꿀 수 있게 만들면 그 측정의 분모를 신뢰할 수 없다 — 감사 장치 자체를 감사하겠다는 D-012 의 목적과 정면으로 어긋난다.
  삭제(3번)하지 않고 조회로 남기는 이유는, `GET /api/stats` 의 `configuredSampleRate` 가 이미 설정값을 응답에 내보내고 있어서다. 값은 나가는데 그 값을 어디서 정하는지가 계약에 없는 상태가 더 나쁘다. `policy.mode` / `globalThreshold` 도 같은 이유로 함께 노출한다 — 측정 9 의 두 처리군을 구분하는 유일한 근거인데, 응답만 보고는 어느 모드로 처리된 결과인지 알 수 없었다.
- **영향**:
  - `API-CONTRACT.md` v0.7 → v0.8 — §6 `GET /api/policies` 응답에 `mode` · `globalThreshold` · `audit.sampleRate` 추가 + **Phase 2 는 읽기 전용**임을 명시
  - `PRD.md` §2 관리자 행("감사 비율 설정" → 확인) · §7 리스크 표(감사 표본 부족 행의 대응) · §7 Phase 3 표에 **항목 J** 신설
  - 코드 소유: `GET /api/policies` 는 P3(김은빈). 설정 값 자체는 P2(김준현)가 읽어 쓴다
- **재평가**: 측정 9 를 재기동 없이 반복해야 할 만큼 자주 돌리게 되거나, 운영에서 감사 비율을 조정할 실제 필요가 생기면 1번으로 전환한다. 그때는 **변경 이력(누가·언제·이전 값)을 함께 남겨** `actualSampleRate` 괴리의 원인을 구분할 수 있게 하는 것이 조건이다.

### D-029. `PATCH /api/policies/{category}` 를 upsert 로 — 404 는 fallback 규칙과 모순이다

- **일자**: 2026-08-04
- **상태**: **폐기** (2026-08-05, D-031) — 카테고리별 임계값(D-006)이 폐기되면서 `PATCH /api/policies/{category}` endpoint 와 `classification_policy` 테이블이 함께 사라졌다. 고칠 모순 자체가 없어진 것이지 판단이 틀렸던 것은 아니다
- **배경**: `API-CONTRACT.md` §7 은 오류로 **404 (미정의 카테고리)** 를 적었는데, `CLAUDE.md` 도메인 모델과 `V1__init_schema.sql` 주석은 **"미등록 카테고리는 기본값 0.9 fallback"** 이라고 적었다. 두 문장이 같이 성립할 수 없다 — fallback 규칙은 `classification_policy` 에 **행이 없는 상태를 정상으로 인정**하는데, 404 는 그 정상 상태의 카테고리에 임계값을 **영원히 설정할 수 없게** 만든다. 정책을 조정하려면 행이 있어야 하고, 행을 만들려면 정책을 조정해야 하는 순환이다.
  V1 이 10종을 모두 seed 해서 지금은 드러나지 않는다. 그러나 D-001 재평가 조항(카테고리 10종 → 5종 축소 검토)이나 enum 확장이 발동하는 순간 바로 걸린다. **지금 안 터지는 이유가 "설계가 맞아서"가 아니라 "seed 가 가려주고 있어서"인 종류의 결함**이다.
- **선택지**:
  1. 404 유지 + `POST /api/policies` 신설 — endpoint 가 늘고, 클라이언트가 "먼저 만들지 고칠지"를 알아야 한다. 값을 정하는 행위는 하나인데 호출이 둘로 갈린다
  2. **upsert** — 행이 없으면 만들고 있으면 갱신. 클라이언트는 원하는 상태만 말한다
  3. V1 seed 를 신뢰하고 404 유지 — 문서 모순은 남고, seed 밖 카테고리가 생기면 재발
- **결정**: 2번. `ErrorCategory` enum 에 **없는** 값은 400(잘못된 요청)이고, enum 에 **있으나 행이 없는** 것은 오류가 아니라 fallback 이 적용 중인 정상 상태이므로 생성한다. 응답의 `previousThreshold` 에는 그때 실제로 적용되던 값 — 즉 `defaultThreshold` — 를 담아, "무엇을 무엇으로 바꿨는지"가 행 유무와 무관하게 일관되게 읽히도록 한다.
  이 endpoint 에서 404 는 제거한다. 남겨두면 `ErrorCategory` 오타(→400)와 정책 미등록(→정상)이 같은 코드로 보고돼, 클라이언트가 어느 쪽인지 구분할 수 없다.
- **영향**:
  - `API-CONTRACT.md` v0.7 → v0.8 — §7 오류 표에서 404 제거 + upsert 동작·`previousThreshold` 규칙 명시. 공통 오류 표 404 행에 "정책 갱신은 예외" 주석
  - `evidence/failure-cases.md` 사례 15
  - 코드 소유: P3(김은빈)
- **재평가**: 카테고리 enum 이 고정되고 seed 가 항상 전수를 보장한다는 것이 운영으로 확인되면 upsert 분기를 걷어내도 된다. 다만 그때도 404 로 되돌리지는 않는다 — 되돌리면 같은 모순이 복원된다.

### D-030. 예외 처리 규칙을 CLAUDE.md 에 못박음 — Sentry 캡처 사각지대 방지

- **일자**: 2026-08-04
- **상태**: 채택
- **배경**: PR #9 AI 코드리뷰가 지적 — `SENTRY-GUIDE.md` 2-3 이 실측으로 확인한 사각지대(catch 해서 로그만 찍고 `Sentry.captureException()` 을 안 부르면 자동 캡처(`UncaughtExceptionHandler`/Spring 필터 체인)도 수동 캡처도 아니라서 `Envelope sent successfully` 가 0번 — 영구히 전송 안 됨)를, `service/` 코드를 P1/P2/P3 가 각자 작성할 때 서로 다르게 판단할 위험이 있다. 판단 기준은 이미 `SENTRY-GUIDE.md` 2-4 표에 있지만 참조 문서라 강제력이 없다 — `CLAUDE.md` 는 매 prompt 에 자동 포함되는 팀 헌법이라 여기 있어야 실제 작업 시점에 놓치지 않는다.
- **선택지**:
  1. 현행 유지 — `SENTRY-GUIDE.md` 참조 문서로만 둔다. 지금 당장 이 규칙을 어길 코드가 없어 시급하지 않다는 반론이 가능하나, `service/` 착수 시점에 놓칠 위험은 그대로 남는다
  2. **`CLAUDE.md` 코딩 규칙에 한 줄 규칙 + `SENTRY-GUIDE.md` 링크만 추가** — 판단 기준 표 전체를 복제하지 않아 버전 번호 3중 기록(`MONITORING.md`/`SENTRY-GUIDE.md`/`build.gradle`) 같은 드리프트를 반복하지 않는다
  3. `API-CONTRACT.md` 에 추가 — 기각. 그 문서는 endpoint 계약 전용이고 예외 처리는 내부 구현 규칙이라 성격이 다르다
- **결정**: 2번. 이 프로젝트의 `CLAUDE.md`/`DECISIONS.md` 자체가 baseline 코드보다 먼저 쓰인 선례(D-001~D-021)가 있어, 코드가 아직 없다는 것이 규칙을 미리 못박지 않을 이유는 아니다. 다만 강제력의 실체를 정확히 이해해야 한다 — 지금 있는 훅(`verify-before-push.sh`)은 `./gradlew test` 통과 여부만 보고 **Java 코드에서 catch 블록에 Sentry 호출이 있는지 정적 분석하지 않는다.** 그러므로 이 규칙의 실질적 강제력은 "hook 이 자동 차단"이 아니라 "매 prompt 컨텍스트에 규칙이 있어 놓치기 어려워진다"는 수준이다 — `CLAUDE.md` 서두의 "위반 시 hook 이 차단"이라는 문구를 이 규칙에 그대로 적용해 과신하지 않는다.
- **영향**: `CLAUDE.md` 「코딩 규칙」에 신설 서브섹션 1개(3줄), `SENTRY-GUIDE.md` 와 중복 없이 링크만
- **재평가**: P1/P2/P3 가 실제 `service/` 코드에서 이 규칙이 애매한 케이스를 만나면 사례를 `SENTRY-GUIDE.md` 에 추가하고 이 규칙 문구도 재검토한다. Java 정적 분석 기반 훅(예: catch 블록 검출)이 실제로 필요해지면 별도 항목으로 검토한다.

### D-031. 도메인을 CS 문의 분류로 전환 — 기준 1 탈락을 인정하고 그룹핑을 포기한다

- **일자**: 2026-08-05
- **상태**: 채택
- **배경**: 팀 협의로 도메인을 **CS 문의 자동 분류**로 바꾸기로 했다. 설계 골격(비동기 분류 → 재시도 → 신뢰도 검증 → 격리 → 사람 확정 → 관측)은 그대로 두고 판정 대상만 에러 → 문의로 옮기는 안이다.
  문제는 **바로 전날 채택한 D-027 과 정면으로 부딪힌다**는 점이다. D-027 은 판별 기준 3개를 헌법에 올리고 "교체 제안은 기준 통과를 먼저 보인다. 못 넘으면 새 항목 없이 반려한다"고 못박았는데, CS 문의를 그 기준에 넣으면 **기준 1(묶음 동질성)에서 탈락**한다. 문의는 개인 건이라 N건을 묶으면 각자의 주문·각자의 사정이 사라진다 — D-027 이 후보 ①(이상거래)을 탈락시킨 바로 그 사유다. 실제로 팀이 가져온 스코프도 `topic_key` 그룹핑을 **명시적 제외**로 빼두었다. 팀은 이미 묶을 수 없다는 것을 알고 있었던 셈이다.
  그러면 이 전환의 본질은 도메인 교체가 아니다. **"그룹핑을 포기하고 그 자리를 무엇으로 메울 것인가"** 다. 그룹핑은 D-004 가 "시스템의 척추"라고 부른 것이고, 여기에 캐시 지점(계약 C)·AI 절감률(측정 6)·인덱스 A/B(D-018)·UNIQUE 재시도(D-016)·원자적 UPDATE(D-007)가 전부 매달려 있다. 척추를 빼면 **6 공통 필수 기능의 「캐시」가 정당화를 잃는다** — 남는 것이 `stats:summary` TTL 10s 하나뿐이라 캐시가 형식 요건으로 전락한다.
- **선택지**:
  1. **현행 도메인 유지 (D-027 재확인)** — 문서·코드 변경 0. 그러나 팀이 이미 합의한 결정을 문서 한 줄로 반려하는 것이고, D-027 의 기준은 *"이 설계가 성립하는 도메인을 고르라"* 는 뜻이지 *"도메인을 영원히 바꾸지 말라"* 는 뜻이 아니었다
  2. **전환 + 그룹핑 유지(`topic_key`)** — 기존 자산을 최대한 보존한다. 그러나 기준 1 위반을 그대로 안고 가는 것이라 **묶인 문의 중 개별 처리가 필요한 건이 조용히 사라진다.** 이 시스템의 주제("조용히 틀리지 않기")와 정면으로 어긋나고, 팀이 스코프에서 뺀 것을 되살리는 일이기도 하다
  3. **전환 + 그룹핑 포기 + 정규화 키 2단 절감 경로** — 판정 단위는 문의 1건으로 두되, **AI 호출만** 같은 정규화 키의 이전 결과로 재사용한다. 캐시(1단) → DB 인덱스 조회(2단) → AI 호출 순
  4. **전환 + 캐시만 (DB fallback 없음)** — 구현이 가장 싸다. 그러나 Redis 재시작 시 절감이 0 으로 리셋되고, 무엇보다 `hit rate` 와 `절감률`이 같은 값이 되어 **D-014 가 무의미해진다.** D-014 는 이 팀이 스스로 틀린 것을 찾아 정정한 항목이라 그 구조를 잃는 대가가 크다
- **결정**: **3번.** 기준 1 탈락은 부정하지 않고 인정한다. 대신 **탈락 사유였던 "묶으면 개별 건이 사라진다"를 발생시키지 않는 형태로만 재사용**한다 — 문의 행도 판정 행도 문의마다 1개씩 그대로 생기고, 각 문의는 각자 상태를 갖고 각자 확정된다. **재사용하는 것은 AI 호출뿐이다.**

  이것이 그룹핑과 다른 지점을 분명히 해둔다. 그룹핑은 *판정의 단위*를 바꾸는 것이고(N건 = 판정 1번), 2단 절감 경로는 *비용의 단위*만 바꾼다(N건 = 판정 N번 + AI 호출 1번). 기준 1 이 금지한 것은 앞쪽이다.

  ```text
  POST /api/inquiries → 문의 저장 후 즉시 응답 (AI 대기 없음)
       ↓ AFTER_COMMIT: InquiryReceivedEvent
  @Async 워커
    ├ 1단 캐시 조회 (normalized_key)        → hit  : AI 호출 없이 결과 재사용
    ├ 2단 DB 조회 (normalized_key 인덱스)   → hit  : AI 호출 없이 재사용 + 캐시 put
    └ miss                                        : AI 호출 → @Retryable(3) → @Recover
  ```

  2단을 두는 이유가 4번을 탈락시킨 이유다. **캐시 miss 여도 DB 에 같은 키가 있으면 AI 를 부르지 않으므로 `hit rate ≤ 절감률` 부등식이 성립하고, D-014 가 근거만 바뀐 채 그대로 살아난다.** 캐시가 줄이는 것은 DB 조회이지 AI 호출이 아니라는 문장이 새 도메인에서도 참이다.

  1번(현행 유지)을 택하지 않은 이유는, D-027 의 목적이 **기준 없이 반복되는 논의를 끝내는 것**이었지 도메인을 동결하는 것이 아니었기 때문이다. 기준을 세운 값어치는 이번처럼 **탈락 사실과 그 대가를 정확히 계산할 수 있다는 데** 있다. 실제로 이 결정은 기준 덕분에 "그룹핑을 포기한다"는 진짜 쟁점을 처음부터 볼 수 있었다.

  **D-027 기준 1 의 적용 조건을 여기서 정정한다.** 기준 1 은 *"그룹핑을 하는 설계"* 의 전제이지 모든 도메인의 관문이 아니다. 그룹핑을 포기하면 기준 1 은 적용 대상이 아니고, 대신 **그룹핑에 매달렸던 자산을 무엇으로 대체하는지 보이는 것**이 새 관문이 된다. 기준 2(정답 단일성)와 3(AI 존재 이유)은 그대로 관문으로 남으며 CS 문의는 둘 다 통과한다 — 카테고리 경계를 상호배타로 정의할 수 있고(기준 2, 단 `PRD.md` §7 에 경계를 1줄씩 명시하는 것이 조건), 자연어 문의는 룩업 테이블로 풀리지 않는다(기준 3).
- **영향**:
  - **폐기 6건** — D-004(그룹핑) · D-006(카테고리별 임계값, 팀 스코프 밖) · D-013(sort 파라미터) · D-016(UNIQUE 재시도) · D-018(인덱스 A/B) · D-029(정책 upsert)
  - **부분 개정 8건** — D-001(주제) · D-007(3분할 → 낙관적 락만) · D-011(대상이 `Inquiry` 로) · D-014(근거만 교체) · D-015(P1 재배정) · D-023(V2 분리분만 폐기) · D-027(결론 뒤집힘, 기준은 존속) · D-028(정책 모드분만 소멸)
  - **스키마 5테이블 → 3테이블** — `inquiries` / `inquiry_classification_result` / `inquiry_review_queue`. `error_group` · `errors` · `classification_policy` 삭제. `V2__candidate_index.sql` 과 `spring.flyway.target` 삭제
  - **계약 A/B/C 재정의** — A 는 `InquiryReceivedEvent(inquiryId, normalizedKey, content)` 로 바뀌고 **"신규 그룹만 발행" 조건이 사라져 전건 발행**이 된다. C 는 `classification:byNormalizedKey`
  - **동시성 자산이 3분할 → 1개로 축소된다.** 원자적 UPDATE 와 UNIQUE 재시도가 함께 사라지고 낙관적 락(D-021 의 409 2종 포함)만 남는다. 이건 이번 전환이 치르는 **가장 큰 학습 자산의 손실**이라 축소 사실을 감추지 않고 기록한다
  - **감사 샘플링은 유지한다.** 팀 User Story 8개에는 없지만 D-005 의 두 겹 검증이 이 프로젝트의 차별점이자 성공 기준 2·3 의 근거다. `PRD.md` 에 **US-10** 으로 추가하고 blind 규칙(D-010) · 감사 표본 트랜잭션 포함(D-012) · 역산 구분(D-019)을 그대로 승계한다
  - **측정 12개 → 11개** — 통째로 사라지는 것은 **측정 9(임계값 대조군)** 하나이고, **5ⓔ(쓰기 p95)** 와 **7ⓐ(동시 그룹 생성)** 는 상위 번호가 남는 하위 항목 삭제다. 측정 3 은 아래 AC 재정의 반영. **번호는 당기지 않는다** — 당기면 `evidence/` 와 커밋 이력의 참조가 전부 어긋난다. `PRD.md` §9 와 `LIFECYCLE-COVERAGE.md` 에서 1:1 유지
  - **US-9 의 AC 를 재정의한다.** 팀 원안은 *"큐 삽입이 실패하면 `inquiries` 원문 저장까지 롤백되는지 확인"* 인데, US-2 가 이미 *"`@Async` 로 문의 저장 트랜잭션과 분리"* 라고 했으므로 접수는 그 시점에 커밋이 끝나 있다. **원안대로면 고객이 이미 받은 접수 확인이 거짓말이 된다.** 따라서 AC 는 **"롤백 경계가 트랜잭션 ② 에서 끊기는지"** 로 바꾼다 — 큐 삽입 실패 시 `inquiry_classification_result` 는 롤백되고 `inquiries` 는 **생존**해야 한다. 그 결과 문의가 `RECEIVED` 로 방치되므로 **D-017 의 stuck 지표가 여기서 다시 필요해진다**
  - `CLAUDE.md` 「도메인 컨텍스트」 전면 교체 + 기준 1 적용 조건 정정. `PRD.md` · `API-CONTRACT.md`(v0.8 → v1.0) · `GLOSSARY.md` · `LIFECYCLE-COVERAGE.md` 갱신
  - 패키지명 `com.dingco.triage` 는 유지한다 (triage = 선별·분류로 CS 문의에도 그대로 맞는다). rename 은 import·테스트·빌드 설정 연쇄 수정만 낳고 얻는 것이 없다
- **재평가**: **정규화 키의 hit rate 가 이 결정의 급소다.** CS 문의는 자연어라 완전일치가 드물어, 정규화가 약하면 측정 6·11 이 0 에 수렴하고 그러면 「캐시」 필수 기능의 정당화가 4번 안(캐시만 두기)보다도 나빠진다 — 2단 경로를 만들어놓고 둘 다 안 걸리는 상태이기 때문이다. 측정 6 에서 절감률이 유의미하게 나오지 않으면 정규화 규칙을 조이고(주문번호·날짜·금액 마스킹 강화), 그래도 안 되면 **캐시 지점을 검토 큐 목록 조회로 옮기는 것**을 후속 항목으로 검토한다. 반대로 정규화를 너무 조이면 서로 다른 문의가 한 키로 병합돼(과도 병합) 잘못된 분류가 재사용되므로, 과도/과소 병합을 **양쪽 다** 테스트 케이스로 만들고 과도 병합을 더 위험한 것으로 다룬다.

### D-032. 검토 항목 선점(claim)을 동시성 소재로 승격 — D-031 이 비운 자리를 메운다

- **일자**: 2026-08-05
- **상태**: 채택 (**착수는 Phase 3 최우선**, 5일 범위 밖)
- **배경**: D-031 으로 동시성 장치가 3개에서 1개로 줄었다. 원자적 UPDATE(`occurrence_count`)와 UNIQUE 충돌 재시도(D-016)는 대상 컬럼·제약이 함께 사라졌고 낙관적 락(D-021)만 남았다.
  먼저 분명히 해둘 것은 **이것이 미션 통과를 위협하지는 않는다**는 점이다. 6 공통 필수 기능은 권한·트랜잭션·검색·캐시·비동기·AI 여섯이고 **동시성은 그중에 없다.** 이 팀이 스스로 추가한 학습 목표다.
  그러나 D-007 이 세운 원칙 — *"경합 성격이 다르면 수단도 다르다. 하나로 통일하지 말 것"* — 은 지점이 하나만 남으면 **주장할 수 없는 말**이 된다. 비교 대상이 없으면 "왜 이 수단을 골랐나"를 보일 자리가 사라진다.
  동시에 도메인 쪽에도 실제 공백이 있다. 지금 설계는 **두 상담원이 같은 항목을 동시에 붙들고 각자 분류한 뒤, 확정 순간에야 한 명이 409 를 받는다.** 늦게 누른 쪽의 작업은 통째로 버려진다. 낙관적 락은 데이터 정합성은 지키지만 **사람의 시간은 지켜주지 않는다.**
- **선택지**:
  1. **검토 항목 선점(claim)** — 상담원이 항목을 열면 "처리 중"으로 잡아두고 일정 시간 뒤 자동으로 풀린다. 충돌을 **사후에 잡는** 낙관적 락과 달리 **사전에 막는다**. 다만 잡아둔 채 자리를 뜨면 항목이 묶이므로 만료 처리가 반드시 필요하고, 만료 시간을 짧게 잡으면 선점의 의미가 없고 길게 잡으면 큐가 막힌다 — 그 트레이드오프 자체가 학습 소재다
  2. **같은 정규화 키 동시 유입 시 AI 중복 호출 억제** — 캐시에 "분류 중" 표시를 두어 조율한다(락이 아니다). 그러나 **D-031 이 이 중복을 의도적으로 수용하기로 했다.** 되살리려면 그 결정을 뒤집어야 하고, 접수 경로에 조율 장치를 넣는 순간 "그룹핑으로 되돌아가는 길"이라는 D-031 의 경계가 흐려진다
  3. **고객 중복 제출 방지(멱등키)** — 접수 버튼 연타 대응. 흔하고 유용하지만 경합 구조가 단순해 학습 밀도가 낮고, 이미 있는 낙관적 락과 성격이 겹치지 않을 뿐 대비되지도 않는다
  4. **아무것도 하지 않음** — 6 공통 필수에 동시성이 없으므로 미션상 문제없다. 다만 발표·면접에서 "동시성을 어떻게 다뤘나"에 답할 거리가 1개로 남는다
- **결정**: **1번.** 세 가지가 겹쳤기 때문이다.

  | 근거 | 내용 |
  | --- | --- |
  | 도메인이 실제로 요구한다 | 두 상담원이 같은 문의를 동시에 붙들면 한 명의 작업이 통째로 낭비된다. 억지로 만든 학습용 기능이 아니다 |
  | 낙관적 락과 **성격이 대비된다** | "충돌을 사후에 감지" vs "충돌을 사전에 예방". D-007 의 원칙을 다시 주장할 수 있게 된다 |
  | 이미 후보에 있었다 | `INTEGRATION-LOG.md` 의 Phase 3 목록에 "검토 항목 claim" 으로 들어 있던 항목을 끌어올린 것이지 새로 만든 것이 아니다 |

  **2번을 택하지 않은 이유**가 중요하다. AI 중복 호출을 막으면 절감률이 올라가므로 이득처럼 보이지만, D-031 은 그 중복을 **"막으려 들면 접수 경로가 느려지고 그룹핑으로 되돌아간다"** 는 이유로 수용했다. 하루 만에 뒤집으면 그 판단이 무엇에 근거했는지가 흐려진다. 중복분은 계속 **측정 6 에서 세어 기록**하고, 수치가 예상보다 크게 나오면 그때 근거를 갖고 재론한다.

  **착수 시점은 5일 범위 밖이다.** 지금 `service/`·`api/` 가 0줄이고 핵심 흐름(분류 → 격리 → 확정)도 미착수다. 동시성 소재를 늘리자고 핵심을 미루면 본말이 뒤집힌다. **`service/`·`api/` 가 예정보다 빨리 끝났을 때 가장 먼저 붙이는 항목**으로 둔다.
- **영향**:
  - `PRD.md` §8 「나중에 할 것」 에 **항목 H** 신설 — 다른 항목과 달리 **"여유가 생기면 우선 착수"** 로 표시한다
  - `CLAUDE.md` 「락·동시성 전략」 표의 1행짜리 각주에 이 항목을 참조로 건다 — 표가 1행인 것이 설계가 단순해서가 아님을 읽는 사람이 알 수 있게
  - 구현 시 필요한 것: 큐 항목에 선점자·선점 시각 컬럼, 만료 스윕, `GET /api/inquiry-review-queue` 의 선점 상태 노출. **선점 정보가 blind 를 깨지 않는지 먼저 확인한다** (D-010) — 누가 무엇을 오래 붙들고 있는지가 감사 표본 추론 단서가 될 수 있다
  - 코드 소유: P3(김은빈). 트랜잭션 ③ 의 소유자와 같아야 확정 경로와 선점 경로가 어긋나지 않는다
- **재평가**: 측정 7ⓑ 에서 `CONCURRENT_UPDATE` 가 실제로 관측되면 "동시에 같은 항목을 집는 일이 실제로 일어난다"는 근거가 되므로 우선순위를 올린다. 반대로 0 이면 경합 자체가 드물다는 뜻이니 선점의 실익도 낮다 — 그때는 4번(하지 않음)으로 되돌리고 그 사실을 근거로 적는다.

### D-033. 사람이 확정한 답도 재사용한다 — 그리고 재사용 건을 감사 대상에 넣는다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: D-031 의 2단 절감 경로는 **AI 가 낸 답만** 재사용한다. 사람이 확정한 답(`final_category`)은 재사용하지 않는다. 그 결과 이런 구멍이 생긴다.

  ```text
  문의 A "환불해주세요"  → AI 확신도 0.5 → 검토 큐 → 상담원이 RETURN_REFUND 확정
  문의 B (같은 내용)     → 2단 경로가 A 의 AI 결과(0.5)를 재사용 → 또 검토 큐 ❗
  문의 C, D, E ...       → 계속 검토 큐
  ```

  **저확신 문의는 사람이 아무리 확정해도 같은 내용이 계속 큐에 쌓인다.** AI 호출은 아꼈는데 사람 노동은 하나도 아끼지 못했다. 검토 큐 적체가 이 시스템의 관측 대상(측정 2)인데, 적체를 만드는 구조적 원인을 설계가 스스로 갖고 있는 셈이다.

  이 문제는 D-031 이 그룹핑을 포기하면서 **ⓑ 판정 단위 축소**를 "CS 문의에서는 해서는 안 되는 것"으로 분류하고 대체를 검토하지 않은 데서 왔다. 그런데 다시 보니 그룹핑이 줄여준 것은 두 가지였다 — **답변 노동**과 **분류 노동**이다. 답변은 문의마다 개별로 해야 하니 줄일 수 없지만, **분류는 같은 내용이면 같은 답이므로 줄일 수 있다.** D-031 은 둘을 하나로 묶어 판단했다.
- **선택지**:
  1. **현행 유지** — AI 답만 재사용. 구현 변경 0. 그러나 같은 내용의 저확신 문의가 무한히 큐에 쌓이고, 상담원이 같은 판단을 반복한다
  2. **사람 확정을 재사용하되 그냥 자동 확정** — 검토 부담은 사라진다. 그러나 **사람이 틀리게 확정했을 때 그게 조용히 퍼진다.** 게다가 "사람이 확정했다"는 사실이 신뢰의 근거가 되어 아무도 의심하지 않는다. 이 프로젝트가 AI 에 대해 하지 않기로 한 일을 사람에 대해 하는 셈이다
  3. **사람 확정을 재사용하고, 재사용 건도 감사 대상에 넣는다** — 검토 부담을 줄이면서 전파되는 오류를 잡을 장치를 함께 둔다
  4. 검토 큐에서 같은 키를 묶어 보여주고 일괄 확정 — 효과는 비슷하나 **blind 를 깬다.** 같은 키가 여러 건 쌓여 있다는 사실 자체가 "저확신이 반복됨 = 감사 표본 아님"을 시사하는 단서가 된다 (D-019 의 확률적 추론을 **일부러 늘리는** 것이라 D-010 과 정면으로 어긋난다)
- **결정**: **3번.** 2번이 아닌 이유가 이 항목의 핵심이다 — **이 시스템의 주제는 "AI 를 믿지 않는다"가 아니라 "자동으로 확정된 것을 믿지 않는다"** 이다. 확정한 주체가 사람이어도 그 결과가 **자동으로 전파**되는 순간 같은 검증이 필요하다.

  **① 조회 우선순위** — 같은 정규화 키의 지난 결과를 찾을 때

  | 순위 | 대상 | 이유 |
  | --- | --- | --- |
  | 1 | 사람이 확정한 것 (`final_category IS NOT NULL`) | 사람 답이 AI 답보다 신뢰도가 높다 |
  | 2 | AI 가 자동 확정한 것 (`verdict = AUTO_ACCEPTED`) | 기존 동작 |
  | — | **재사용 결과(`REUSED`)는 참조하지 않는다** | 아래 체인 금지 |

  **재사용을 다시 재사용하지 않는다.** A(원본) → B(재사용) → C 가 B 를 참조하면 체인이 길어지고, 원본 하나가 틀렸을 때 어디까지 퍼졌는지 추적할 수 없다. 재사용의 출처는 **항상 원본**이어야 한다.

  **② `verdict` 에 `REUSED` 추가** — 4종이 된다

  ```text
  AUTO_ACCEPTED  AI 호출, confidence >= threshold
  NEEDS_REVIEW   AI 호출, confidence < threshold
  FAILED         AI 호출 실패. category·confidence 모두 null
  REUSED         AI 를 부르지 않고 같은 키의 원본 결과를 재사용
                   └ 사람 확정을 재사용 → confidence 는 null (사람은 확신도를 매기지 않는다)
                   └ AI 답을 재사용     → confidence 는 원본 값
                   └ model 에 원본 결과 id 를 남긴다 (추적 경로)
  ```

  **이는 D-022 를 부분 개정한다.** D-022 는 *"`confidence` 가 null 인 것은 `FAILED` 일 때만"* 이라고 못박았는데, 사람 확정을 재사용한 건도 null 이 된다. **1 이나 원본 AI 값을 채우지 않는다** — 1 은 거짓말이고(사람도 틀린다), 원본 AI 값은 사람이 뒤집은 값이라 의미가 없다. D-022 의 취지("모르는 값에 숫자를 채우지 않는다")는 오히려 여기서 그대로 지켜진다.

  **③ 재사용 건은 자동 확정한다** (`status = CLASSIFIED`). 조회 우선순위상 원본은 이미 사람이 확정했거나 AI 가 자동 확정한 것이므로 다시 사람에게 보낼 이유가 없다.

  **④ 감사를 확대한다** — `AUTO_ACCEPTED` 5% **+ `REUSED` 5%**

  재사용 건에 감사가 **더** 필요하다. 자동 확정 건은 AI 답 하나가 틀리면 그 문의 하나가 틀리지만, 재사용은 **원본 하나가 틀리면 같은 키의 모든 후속 문의가 틀린다.** 피해 범위가 다르다.

  **⑤ 측정 8 을 ⓐ/ⓑ 로 나눈다** (번호는 그대로 — 당기면 다른 문서 참조가 어긋난다)

  | | 모집단 | 무엇을 보나 |
  | --- | --- | --- |
  | 8ⓐ | `verdict = AUTO_ACCEPTED` | AI 가 자신 있게 틀린 비율 (기존 측정 8) |
  | 8ⓑ | `verdict = REUSED` | 재사용된 답이 틀린 비율 (신설) |

  **둘을 합치면 안 된다.** 8ⓐ 는 "AI 답 vs 사람 답" 비교이고, 8ⓑ 는 재사용 건이라 **비교할 AI 답이 없다.** 합치면 이 프로젝트의 결론인 8ⓐ 가 오염된다.
- **영향**:
  - `CLAUDE.md` — 「캐시 전략」에 조회 우선순위와 체인 금지 명시, 도메인 모델의 `verdict` 4종, 감사 대상 확대
  - `PRD.md` — §5-4(캐시) 조회 순서, §5-6(AI 보조) 감사 대상, §6 데이터, §9 측정 8 분해
  - `API-CONTRACT.md` — `verdict` 에 `REUSED` 추가, `GET /api/stats` 의 `audit` 을 자동확정/재사용으로 분리, `GET /api/inquiries/{id}` 의 `model` 필드 설명
  - **계약 B 는 바뀌지 않는다.** `REUSED` 는 검토 큐 삽입 사유가 아니다 — 감사로 뽑힐 때만 큐에 들어가고 그때 `reason` 은 기존대로 `AUDIT_SAMPLE` 이다. `verdict → reason` 매핑에 `REUSED → AUDIT_SAMPLE` 이 추가될 뿐이라 판별식은 그대로 `verdict` 다
  - 코드 소유: 조회·재사용 판정은 P1(이용택, 2단 경로 소유), 감사 확대는 P2(김준현)
  - **`Verdict` enum 에 값이 늘면 `InquiryReviewQueueItem.from(...)` 의 switch 가 컴파일 에러로 터진다** (D-025). 계약 B 표를 함께 갱신하라는 신호이므로 의도된 동작이다
- **재평가**: 측정 8ⓑ 가 8ⓐ 보다 유의미하게 높으면 **재사용 자체가 오류를 증폭하고 있다는 뜻**이므로, 사람 확정 재사용을 끄고 AI 답 재사용만 남기는 쪽으로 되돌린다. 반대로 8ⓑ 가 충분히 낮으면 감사 비율을 5% 아래로 내려 검토 부담을 더 줄일 근거가 된다. 또한 측정 2 에서 **같은 키의 반복 유입이 큐에 쌓이는 비율**을 함께 보고, 이 결정 이전 대비 얼마나 줄었는지를 기록한다.

### D-034. AI 응답의 값 검증 — 형식은 맞는데 값이 이상한 경우를 정의한다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: `CLAUDE.md` 는 *"프롬프트로 `{"category": ..., "confidence": 0.0~1.0}` JSON 강제"* 와 *"파싱 실패는 무조건 격리"* 만 정해두었다. 그런데 그 사이에 **정의되지 않은 구간**이 있다 — **JSON 은 멀쩡히 파싱되고 값만 이상한 경우**다.

  | AI 응답 | 현재 일어나는 일 |
  | --- | --- |
  | `{"confidence": 1.5}` | `DECIMAL(4,3)` 이라 저장은 된다 → 임계값 0.8 과 비교해 **자동 확정** → 그런데 측정 8ⓐ 의 신뢰도 구간(0.8~0.9 / 0.9~1.0) **어디에도 안 들어가 조용히 누락** |
  | `{"confidence": -0.3}` | 동작 미정의. 저장될 수도, 예외가 날 수도 있다 |
  | `{"category": "REFUND_XYZ"}` | enum 에 없는 값. 동작 미정의 |
  | `{"category": "..."}` (confidence 필드 없음) | 동작 미정의 |
  | `{"confidence": "high"}` | 파싱 실패 → `FAILED` (**이것만 정의돼 있다**) |

  **1.5 케이스가 가장 나쁘다. 자동 확정되면서 동시에 측정에서 사라진다.** 이 프로젝트가 막으려는 "조용히 틀리는" 유형을 설계가 스스로 갖고 있는 것이고, 하필 결론이 나오는 자리(측정 8ⓐ)를 오염시킨다.

  실제로 일어날 수 있는 일이다. LLM 은 형식을 지키면서도 값의 의미를 놓치는 경우가 있고, 특히 재시도 중에 앞선 응답을 흉내 내다 범위를 벗어나기도 한다.
- **선택지**:
  1. **범위 안으로 잘라 넣기(clamp)** — `1.5 → 1.0`, `-0.3 → 0.0`. 저장은 항상 성공하고 구현이 가장 싸다. 그러나 **D-022 가 금지한 것과 같은 종류의 조작이다.** `1.0` 으로 잘라 넣으면 측정 8ⓐ 의 최상위 구간에 "AI 가 1.0 이라 신고한 건"과 "1.5 를 잘라낸 건"이 섞인다. D-022 가 `confidence 0` 을 거부한 논리가 그대로 적용된다
  2. **`FAILED` 로 처리** — 파싱 실패와 같은 취급. 값이 이상하면 그 응답 전체를 신뢰할 수 없다고 보고 재시도하고, 3회 소진 시 사람에게 넘긴다
  3. **`verdict` 에 `INVALID_RESPONSE` 신설** — 원인이 명확히 구분된다. 그러나 `verdict` 가 5종이 되고 계약 B 의 매핑도 함께 늘어난다. D-033 로 이미 4종이 된 직후라 부담이 크다
  4. **그대로 저장** — 현행. 위 표의 문제가 그대로 남는다
- **결정**: **2번.** 값이 범위를 벗어났다는 것은 **AI 가 요청한 형식을 이해하지 못했다는 신호**이고, 그러면 `category` 쪽 답도 믿을 근거가 없다. 필드 하나만 이상하다고 나머지를 신뢰하는 것은 근거 없는 낙관이다.

  **검증 항목과 순서** — 파싱 직후, **임계값 비교 전에** 한다. 순서가 뒤바뀌면 1.5 가 이미 자동 확정을 통과한 뒤가 된다.

  | 검사 | 위반 시 |
  | --- | --- |
  | `confidence` 필드 존재 | `FAILED` |
  | `0.0 <= confidence <= 1.0` | `FAILED` |
  | `category` 필드 존재 | `FAILED` |
  | `category` 가 `InquiryCategory` 10종에 있음 | `FAILED` |

  **잘라 넣지 않는다 (1번을 버린 이유).** `FAILED` 는 `category`·`confidence` 를 **둘 다 null** 로 남기므로(D-022) 이상한 값이 측정에 흘러들 경로가 아예 없다. **모르는 값에 숫자를 채우지 않는다**는 D-022 의 원칙이 여기서도 그대로다.

  **3번을 버린 이유**는 원인 구분을 포기해서가 아니라 **저장 위치를 바꿨기 때문**이다. 실패 원인은 `raw_response` 에 원문이 남아 사후 추적이 가능하고, 추가로 **구조화 로그**에 사유(`PARSE_ERROR` / `OUT_OF_RANGE` / `UNKNOWN_CATEGORY` / `MISSING_FIELD` / `API_ERROR`)를 남긴다. 컬럼과 enum 을 늘리지 않고도 측정 4(재시도 동작)에서 분포를 읽을 수 있다.

  **재시도는 그대로 한다.** LLM 응답은 비결정적이라 같은 프롬프트에 다시 물으면 정상 값이 올 수 있다. 재시도 없이 바로 큐로 보내면 회수 가능한 건까지 사람에게 넘어간다 (D-022 의 재시도 논리와 같다).
- **영향**:
  - `CLAUDE.md` 「AI 호출 규칙」 — 검증 항목 4개와 "잘라 넣지 않는다" 명시
  - `PRD.md` §5-6 — "답이 깨져서 못 읽으면" 을 "**답을 못 읽거나 값이 이상하면**" 으로
  - `API-CONTRACT.md` — `verdict = FAILED` 의 정의에 값 검증 실패 포함
  - **스키마 변경 없음.** 컬럼도 enum 도 늘지 않는다 — 5일 범위를 지키기 위한 선택이다
  - 코드 소유: P2(김준현). AI 응답 파싱과 임계값 비교가 모두 P2 안에 있다
  - 단위 테스트 필수: `1.5` · `-0.3` · 필드 누락 · enum 밖 카테고리 4케이스. **`0.0` 과 `1.0` 은 정상**이라는 경계 케이스도 함께 (부등호를 `<` 로 잘못 쓰면 정상 값이 실패로 간다)
- **재평가**: 구조화 로그에서 `OUT_OF_RANGE` 나 `UNKNOWN_CATEGORY` 가 유의미하게 잦으면 두 가지를 검토한다 — ⓐ 프롬프트에 카테고리 목록과 확신도 범위를 더 명확히 넣는다 ⓑ 그래도 반복되면 그때 `verdict` 분리(3번)나 실패 사유 컬럼 신설을 재론한다. 반대로 0 에 가까우면 이 검증은 방어 장치로만 남는다.

### D-035. 신규 팀원 온보딩 문서(`ONBOARDING.md`) 신설 — 이슈 등록 경로와 브랜치 이름을 함께 고정

- **일자**: 2026-08-05
- **상태**: 채택 — 단 **결정 3(브랜치 이름)은 세 담당자 확인 후 확정**. 전원이 매일 지켜야 하는 규칙이라 혼자 못박지 않는다
- **배경**: 세 자리가 동시에 비어 있었다.

  1. **로컬을 띄우는 방법이 어디에도 정리돼 있지 않다.** 특히 D-026 이 정한 **Docker Desktop `4.44.2` 이하 고정**은 그 항목 「영향」에서 *"`README.md` 또는 `MONITORING.md` 에 로컬 환경 요구사항으로 명시 필요(본 PR 범위 밖 — 후속 PR)"* 라고 숙제로 남겼는데 **아직 이행되지 않았다.** 이걸 모르는 사람이 합류하면 `./gradlew test` 가 **코드와 무관하게 전부 실패**하고, 원인이 Docker 버전이라는 걸 스스로 알아낼 방법이 없다. `README.md` 는 부트캠프에서 받은 안내 템플릿 그대로라 우리 팀 안내가 아니다
  2. **이슈가 Jira 에만 있어 코드와 이어지지 않는다.** 기획 도구는 Jira 로 확정돼 있고(D-020) `TRI-` 키로 돌아가는데, 브랜치·PR·리뷰는 전부 GitHub 에서 일어난다. `Closes #N` 으로 이슈가 닫히는 것도 GitHub 쪽뿐이다
  3. **브랜치 이름 규칙이 없다.** `CLAUDE.md` 에 항목 자체가 없어서 지금까지 만든 브랜치가 `feat/…` `docs/…` `chore/…` 로 제각각이고 이슈 번호가 들어간 적이 한 번도 없다. 이름만 보고 어떤 작업인지 알 수 없다

  덧붙여 — 이 문서의 초안으로 **다른 프로젝트에서 쓰던 온보딩 문서를 가져왔는데, 레포와 대조해보니 사실이 거의 다 어긋나 있었다.** DB 포트·이름(`3307/ecommerce` → 실제 `3306/triage_db`), push 전 검증 훅의 실체(gradle 태스크가 아니라 `.claude/hooks/handlers/verify-before-push.sh`), `DECISIONS.md` D-004·D-006·D-007 오인용(각각 그룹핑·차등 임계값·동시성 3분할이며 앞의 둘은 폐기됐다), PR base(`main` → `develop`), 그리고 *"`API-CONTRACT.md` 는 아직 프로젝트와 안 맞으니 참고하지 말고 PR 에서 직접 채워라"*(실제로는 **v1.1 로 갱신 완료**).

  **마지막 것이 가장 위험했다** — 그대로 뒀다면 신규 팀원이 완성된 계약을 무시하고 새로 쓰는 일이 벌어진다. 문서는 코드와 달리 CI 가 틀렸다고 알려주지 않는다. **레포와 한 줄씩 대조하는 것 말고 검증 수단이 없다.**

- **선택지**:
  1. **`README.md` 를 우리 팀 안내로 고쳐 쓴다** — 새 파일이 안 늘어난다. 그러나 이 레포의 `README.md` 는 부트캠프 제출 템플릿이라 구조를 유지해야 하고, **채점자가 보는 문서와 팀이 매일 보는 문서가 한 곳에 섞인다**
  2. **`CLAUDE.md` 에 온보딩 절을 추가한다** — 규칙이 한 곳에 모인다. 그러나 `CLAUDE.md` 는 **모든 프롬프트에 자동으로 포함된다.** 로컬 셋업 순서나 Docker 버전처럼 *사람이 한 번 읽으면 되는 정보*를 넣으면 매 프롬프트마다 실려 나간다. 헌법이 길어지는 것 자체도 위반 판정을 흐린다
  3. **`ONBOARDING.md` 를 새로 만든다** — 읽는 사람과 목적이 분명히 갈린다. 대신 문서가 하나 늘고, 규칙이 `CLAUDE.md` 와 중복되면 한쪽이 낡는다
  4. **안 만든다** — 팀이 3명이고 이미 전원 셋업을 마쳤다. 그러나 D-026 숙제가 계속 남고, 발표·평가 때 **"이 프로젝트를 처음 받은 사람이 띄울 수 있는가"** 에 답할 수 없다
- **결정**: **3번.** 여기에 세부 셋을 함께 정한다.

  **결정 1 — 문서 사이의 서열을 먼저 박는다.** `ONBOARDING.md` 는 **길잡이**이고, 규칙이 서로 다르면 **`CLAUDE.md` 가 이긴다.** 이 한 줄을 문서 서두에 둔다.

  그래서 초안에 있던 **`CLAUDE.md` 밖 규칙은 넣지 않았다** — 패키지 `ports/in`·`client`·`common`, DTO 를 `XXXCommand`/`XXXResult` 로, Service 는 `@SpringBootTest` 만 쓰고 Mockito 금지, 의존 방향 명시. 헌법에 없는 규칙을 길잡이 문서가 몰래 만드는 셈이고, 무엇보다 **`service/`·`api/` 는 아직 코드가 한 줄도 없어 규칙을 정할 근거가 없다.** 필요해지면 그때 별도 항목으로 정한다.

  **결정 2 — 이슈는 Jira 가 기준(SoT), GitHub 은 미러.**

  | | Jira (`TRI-…`) | GitHub 이슈 (`#N`) |
  | --- | --- | --- |
  | 요구사항·완료 조건·상태 | **여기에만 쓴다** | 안 쓴다 — 제목 + Jira 링크 + 두 줄 요약 |
  | 역할 | 기준 | 코드·PR 을 잇는 손잡이 |

  **요구사항을 양쪽에 복사하지 않는다.** 같은 내용이 두 곳에 있으면 한쪽이 반드시 낡고, 그때 어느 쪽이 맞는지 아무도 모른다. GitHub 을 없애지 않는 이유는 브랜치·PR·리뷰가 전부 거기 있고 `Closes #N` 이 GitHub 에서만 동작하기 때문이다.

  **이 방식의 대가는 숨기지 않는다** — PR 이 머지돼도 **Jira 상태는 따라 닫히지 않아 손으로 옮겨야 한다.** 이중 등록의 비용이 여기 한 군데로 모이게 한 것이지, 비용이 없는 게 아니다.

  **결정 3 — 브랜치 이름을 정한다.** 형식 표는 `ONBOARDING.md` 4장에 둔다(여기 옮겨 적으면 그 순간 두 곳이 된다). 이 항목이 정하는 것은 둘뿐이다.
  - **이슈 번호 자리에는 Jira 키를 쓴다.** 이슈가 두 곳에 있으니 안 정하면 사람마다 갈리고 규칙이 무의미해진다. 기준이 Jira 이므로 브랜치도 Jira 키다. GitHub 번호는 커밋의 `Closes #12` 로 잇는다
  - **소급 적용하지 않는다.** 지금 있는 브랜치는 이름을 바꾸지 않는다 — 바꾸면 열려 있는 PR 과의 연결이 끊긴다. **앞으로 만드는 것부터** 적용한다
- **영향**:
  - `ONBOARDING.md` 신설 — 문서 지도 / 지금 코드가 어디까지 왔나 / 로컬 셋업 / 프롬프트 원칙 / 작업 등록·시작 / 커밋·push / PR / 알려진 갭
  - **D-026 이 남긴 미이행 숙제를 여기서 해소한다.** 다만 그 항목이 지목한 `README.md`·`MONITORING.md` 가 아니라 `ONBOARDING.md` 에 적었다
  - `CLAUDE.md` 는 헤더에 포인터 한 줄만 추가했다. **규칙은 건드리지 않았으므로 헌법 변경이 아니다**
  - `README.md` 맨 위에 "아래는 부트캠프 템플릿이고 실제 안내는 `ONBOARDING.md`" 한 줄
  - GitHub 이슈 템플릿 파일명을 한글에서 ASCII 로 바꿨다 (`feature` / `chore_fix` / `maintenance_issue`). **내용 변경 없음**
  - **GitHub 이슈보다 Jira 이슈를 먼저 만들어야 한다.** 순서가 뒤집히면 미러에 링크할 대상이 없다
  - 문서 담당자만이 아니라 **전원**이 지켜야 하는 규칙이 둘 늘었다(결정 2·3)
  - 초안의 사실 오류는 **AI 가 지어낸 것이 아니라 다른 프로젝트 문서를 그대로 가져온 것**이므로 `evidence/failure-cases.md`(AI hallucination 기록) 대상은 아니다. 대신 **문서를 도입할 때 레포와 대조한다**는 절차를 남긴다
- **재평가**:
  - **GitHub 미러가 방치되면**(만들어만 놓고 아무도 안 닫음) 미러를 버리고 Jira 만 남긴다. 판단 시점은 Phase 2 종료 회고(`RETROSPECTIVE.md`)
  - 반대로 **손으로 두 번 만드는 게 번거롭다는 불만이 나오면** GitHub Actions 로 동기화를 검토한다. 5일 범위 밖이다
  - `ONBOARDING.md` 와 `CLAUDE.md` 에 **같은 규칙이 두 번 적히기 시작하면** 그때 한쪽으로 몰아넣는다. 이 문서가 낡는 방식은 대개 그것이다

### D-036. 계약 C 개정 — 캐시 값에 판정 출처를 담고, 사람이 뒤집으면 캐시를 덮어쓴다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: D-033 이 2단 DB 조회에 **1순위(사람 확정 우선)** 를 넣었는데, **계약 C 의 캐시는 그 개정을 받지 못했다.** 값 구조는 `{category, confidence, model}` 그대로이고 `evict: 없음` 그대로다. 구멍이 셋이다.

  **ⓐ 사람이 뒤집어도 캐시는 옛 답을 계속 재사용한다.** 1단에서 hit 이 나면 **2단 조회가 아예 실행되지 않으므로**, 1순위 규칙이 실행될 기회조차 없다.

  ```text
  문의 A → AI 자동확정 DELIVERY(0.85) → 캐시에 DELIVERY 올라감
  감사 표본으로 뽑힘 → 상담원이 RETURN_REFUND 로 정정 (오분류 1건 집계)
  문의 B (같은 키) → 1단 캐시 hit → DELIVERY 재사용 ❗
  문의 C, D, E ...  → 계속 DELIVERY ❗ (TTL 이 끝날 때까지)
  ```

  **D-033 이 막으려던 것과 같은 모양의 구멍이고, 이번에는 틀린 답이 퍼지는 쪽이라 더 나쁘다.** 감사로 오분류를 잡아냈는데 그 정정이 재사용 경로에 반영되지 않는다 — 감사가 잡은 것을 시스템이 도로 무시하는 셈이다.

  **ⓑ 캐시 값만 봐서는 사람 답인지 AI 답인지 알 수 없다.** `confidence == null` 이면 사람 답이라고 추론할 수는 있으나, 그건 D-033 의 부수 효과일 뿐 **계약이 보장하는 표현이 아니다.** 계약에 없는 규칙에 기대는 구현은 다음 개정에서 조용히 깨진다.

  **ⓒ `put` 시점이 두 갈래로 읽힌다.** *"판정 확정 트랜잭션(②) 커밋 후. 2단 hit 시에도 put"* 은 2단 hit 이 ② 와 별개 경로처럼 보이게 한다. **롤백된 판정이 캐시에 남으면 안 된다**는 규칙과 맞물리는 자리라 한 줄로 확정해야 한다.
- **선택지**:
  1. **그대로 두고 TTL 에 맡긴다** — 구현 0. 그러나 *"틀린 답이 언제 사라지나"* 가 메모리 상한용으로 정한 설정값에 좌우된다. TTL 을 줄이면 절감이 죽고 늘리면 옛 답이 오래 산다. **두 목적이 한 손잡이에 묶인다**
  2. **③ 커밋 후 evict** — 다음 요청이 2단으로 내려가 1순위를 다시 찾으므로 정확하다. 다만 이미 손에 쥔 답을 버리고 조회를 한 번 더 하며, evict 와 다음 put 사이가 비어 절감이 잠깐 끊긴다
  3. **③ 커밋 후 사람 답으로 덮어쓰기(put) + 값에 출처를 담는다** — 정정이 즉시 재사용 경로에 반영되고, 캐시 단에서도 1순위/2순위가 구분된다
- **결정**: **3번.**

  **① 값 구조** — `model` 을 `sourceResultId` 로 바꾸고 `source` 를 넣는다

  ```text
  key   : normalized_key (String)
  value : { category, confidence, source, sourceResultId }
    └ source         : HUMAN | AI
    └ sourceResultId : 원본 InquiryClassificationResult.id
                         → REUSED 행의 model 컬럼에 그대로 쓴다 (추적 경로, D-033)
    └ confidence     : source=AI 면 원본 값 / source=HUMAN 이면 null (D-033)
  ```

  **`model`(모델명)을 뺀 이유**: 재사용 행의 `model` 에는 모델명이 아니라 **원본 결과 id** 가 들어간다(D-033). 캐시가 모델명을 들고 있어도 쓸 자리가 없고, 원본 id 를 따라가면 모델명은 언제든 읽을 수 있다. **값 하나로 두 가지를 가리키게 두면 계약이 흐려진다.**

  `inquiryId` 를 담지 않는다는 기존 금지는 그대로다 — 담으면 "이 문의의 판정"으로 오해돼 그룹핑처럼 쓰인다(D-031).

  **② `put` 시점 — 판정 행이 저장되는 트랜잭션의 `AFTER_COMMIT`, 그 두 자리뿐** (ⓒ 확정)

  | 자리 | 무엇을 올리나 | `source` |
  | --- | --- | --- |
  | ② 커밋 후 | `AUTO_ACCEPTED` · `REUSED` | `AI` (원본이 사람 답이면 `HUMAN` 을 그대로 물려받는다) |
  | ③ 커밋 후 | 사람이 확정한 `final_category` | `HUMAN` |
  | — | `NEEDS_REVIEW` · `FAILED` 는 올리지 않는다 | 판정이 확정되지 않았다 |

  **"2단 hit 시에도 put" 은 별도 경로가 아니었다.** 2단 hit 이면 `REUSED` 행을 저장하고, 그 저장이 곧 트랜잭션 ② 다. 따라서 put 은 언제나 ②(또는 ③) 커밋 후 하나로 모인다. 문장이 두 갈래로 읽힌 것은 D-033 이전 — 재사용에 판정 행을 남기지 않던 시절 — 의 잔재다.

  **③ 덮어쓰기 방향은 한 방향이다**

  > **사람 답은 AI 답을 덮는다. AI 답은 사람 답을 덮지 않는다.**

  ③ 의 put 은 조건 없이 덮는다(항상 승격이다). ② 의 put 은 기존 값이 `source=HUMAN` 이면 덮지 않는다.

  **완벽한 원자성은 목표가 아니다.** 워커가 AI 를 부르는 사이에 사람이 확정하면 검사와 쓰기 사이에 창이 남는다(D-031 이 수용한 같은 키 동시 유입과 같은 창이다). 이 경합의 결과는 **옛 AI 답이 잠시 더 재사용되는 것**이고, 재사용 건은 감사 대상이며(D-033) 다음 사람 확정이 다시 덮는다. 이걸 막으려고 키 단위 직렬화를 넣으면 D-031 이 금지한 길로 되돌아간다.

  **④ `evict` 는 여전히 없다.** TTL 은 메모리 상한 목적으로만 쓴다. 정확성은 evict 가 아니라 **덮어쓰기**가 지킨다 — 비워두는 동안 절감이 끊기는 구간을 만들지 않기 위해서다.
- **영향**:
  - `CLAUDE.md` — 계약 C 블록 전면 교체, 「캐시 전략」에 덮어쓰기 방향 명시, 「`@Transactional` 위치」의 캐시 규칙 줄에 ③ 추가
  - `PRD.md` §5-4 — "사람이 정정하면 캐시도 같이 바뀐다" 한 줄
  - **3자 합의가 필요한 계약 변경이다.** 캐시 레이어는 P1(이용택), ② put 은 P2(김준현), **③ put 은 P3(김은빈)** — ③ 이 캐시를 건드리는 것은 이번이 처음이다
  - 측정 11 에 확인 케이스 추가: **③ 확정 직후 같은 키로 들어온 문의가 사람 답을 받는지.** hit rate 숫자만 보면 이 구멍은 안 보인다 — ⓐ 상황에서도 hit 은 정상적으로 난다
- **재평가**: 같은 키를 여러 상담원이 서로 다르게 확정하면 캐시 값이 확정할 때마다 흔들린다. 측정 12(검토자 간 일치도)가 낮게 나오면 **"마지막에 확정한 사람이 이긴다"** 는 지금 규칙이 타당한지 다시 본다. 일치도가 높으면 누가 확정하든 같은 답이므로 이 규칙은 그대로 둔다.

### D-037. 2단 DB 조회는 쿼리를 두 번 친다 — 우선순위를 정렬식으로 표현하지 않는다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: D-033 은 조회 **우선순위**(1순위 사람 확정 → 2순위 AI 자동확정)만 정하고 **쿼리를 몇 번 치는지**는 정하지 않았다. P1 착수 전에 정하지 않으면 구현이 갈린다. 그리고 확인해보니 문제가 하나 더 있었다.

  **이 조회는 조인이다.** `normalized_key` 는 `inquiries` 에 있고 `final_category` · `verdict` 는 `inquiry_classification_result` 에 있다. 그런데 `CLAUDE.md` 인덱스 표는 *"`normalized_key` 로 직전 분류 결과 조회 → `(normalized_key, created_at DESC)` → `ref`, 정렬까지 커버"* 라고 **단일 테이블 조회처럼** 적혀 있다. 그 인덱스가 커버하는 것은 조인의 **한쪽(구동 테이블)뿐**이다.

  **가장 나쁜 구현은 `ORDER BY created_at DESC LIMIT 1` 로 최신 한 건만 뽑는 것**이다. 최신 행이 AI 자동확정이면 그걸 집고, 그 뒤에 있는 사람 확정 행을 지나친다 — **1순위 규칙이 조용히 무력화되고 D-033 이 되돌려진다.**
- **선택지**:
  1. **한 쿼리 + 정렬식** — `ORDER BY (final_category IS NOT NULL) DESC, created_at DESC LIMIT 1`. 왕복 1회지만 **이 정렬은 어떤 인덱스로도 커버되지 않아 filesort 가 확정**이다. 같은 키의 행이 쌓일수록 정렬 비용이 늘어난다
  2. **쿼리 두 번** — 1순위를 먼저 치고, 없으면 2순위를 친다
  3. **함수 인덱스·생성 컬럼 신설** — `final_category IS NOT NULL` 을 인덱스에 태운다. 그러나 **이득을 측정으로 확인하기 전에 쓰기 비용을 얹지 않는다**(D-018 이 남긴 논거)는 현행 판단과 정면으로 어긋난다
- **결정**: **2번 — 쿼리 두 번.**

  ```sql
  -- 1순위: 사람이 확정한 답
  SELECT r.* FROM inquiries i
    JOIN inquiry_classification_result r ON r.inquiry_id = i.id
   WHERE i.normalized_key = ? AND r.final_category IS NOT NULL
   ORDER BY i.created_at DESC LIMIT 1;

  -- 위가 비었을 때만: AI 가 자동 확정한 답
  SELECT r.* FROM inquiries i
    JOIN inquiry_classification_result r ON r.inquiry_id = i.id
   WHERE i.normalized_key = ? AND r.verdict = 'AUTO_ACCEPTED'
   ORDER BY i.created_at DESC LIMIT 1;
  ```

  **왕복이 늘 두 번이 되는 것은 아니다.** 사람 확정이 있는 키는 1순위에서 끝나고, 두 번째가 나가는 것은 **1순위가 빈 경우뿐**이다. 그리고 그 경우는 곧 2단 자체가 miss 이거나 AI 답만 있는 키라, 어차피 AI 호출(수백 ms)이 뒤따르는 경로다. **조회 한 번은 그 옆에서 무시할 만한 비용이고, filesort 는 데이터가 쌓일수록 커진다.**

  **1순위에서 `verdict = REUSED` 를 제외하지 않는다.** D-033 의 체인 금지가 막는 것은 *"AI 답을 재사용한 것을 또 재사용하는 것"* 이다. `REUSED` 행에 `final_category` 가 채워졌다는 것은 **그 건이 감사로 뽑혀 사람이 직접 다시 판단했다**는 뜻이므로, 참조하는 값은 재사용된 답이 아니라 **사람이 새로 매긴 답**이다. 체인이 아니라 새 원본이다.

  > 여기서 제외하면 **감사가 잡아낸 정정이 재사용 경로에 못 들어간다.** 재사용 건을 감사 대상에 넣기로 한 D-033 ④ 의 목적이 통째로 사라진다.

  2순위는 `verdict = 'AUTO_ACCEPTED'` 등치 조건이라 `REUSED` 가 자연히 빠진다 — 체인 금지는 이 한 줄로 지켜진다.
- **영향**:
  - `CLAUDE.md` 인덱스 표 — 해당 두 행을 **조인 형태로 정정**하고 쿼리가 2번임을 명시. `idx_icr_inquiry_created (inquiry_id, created_at DESC)` 가 조인의 안쪽을 받는다
  - 코드 소유: P1(이용택). 2단 경로 소유자다
  - **스키마 변경 없음.** 인덱스를 새로 붙이지 않는다 (3번을 버린 이유)
  - 측정 5ⓓ 가 이 두 쿼리의 `EXPLAIN` 을 본다 — 위 예상은 **AI 추정이므로 evidence 가 아니다** (헌법 「AI 검증 규칙」)
- **재평가**: 측정 5ⓓ 에서 ⓐ 구동 테이블이 `inquiries` 가 아니거나 ⓑ `Using filesort` 가 뜨거나 ⓒ 훑는 `rows` 가 키당 두 자릿수를 넘으면, 그때 3번(함수 인덱스)을 **측정값을 근거로** 다시 올린다. 키당 판정 행이 한 자릿수면 인덱스는 낭비다.

### D-038. `GET /api/inquiries` 에 `customerId` 파라미터를 두지 않는다 — 조회 범위는 서버가 강제한다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: `API-CONTRACT.md` §2 의 오류란에 *"403 (`ROLE_CUSTOMER` 가 `customerId` 필터 없이 전체 조회 시도)"* 라고 적혀 있는데, **바로 위 쿼리 파라미터 표에 `customerId` 가 없다.** 계약 안에서 서로 어긋난다.

  문서 흠이 아니라 **구현이 갈리는 자리**다. P1 은 파라미터가 있다고 읽고 "안 주면 403", P3 는 없다고 읽고 "서버가 알아서 필터" 로 만들 수 있다. 둘 다 계약을 지켰다고 주장할 수 있고, **E2E 는 둘 중 한쪽에서만 통과한다.**
- **선택지**:
  1. **파라미터를 추가한다** — 고객이 `customerId=본인` 을 직접 넣고, 서버가 `X-User-Id` 와 대조해 다르면 403. 상담원은 특정 고객으로 좁힐 수 있다는 이득이 딸려온다
  2. **파라미터를 두지 않고 서버가 강제 필터한다** — `ROLE_CUSTOMER` 면 `X-User-Id` 로 무조건 좁힌다
- **결정**: **2번.**

  **1번을 버린 이유는 실패 방향이다.** 파라미터로 두면 "안 넣으면 전체 조회"가 기본 동작이 되고, **검사를 빠뜨렸을 때 남의 문의가 보이는 쪽으로 실패한다.** 서버가 강제하면 검사를 빠뜨릴 자리 자체가 없다 — 필터를 안 걸면 아무것도 안 보이는 쪽으로 실패한다. **권한은 클라이언트가 스스로 신고하는 값에 걸지 않는다.**

  §2 의 403 은 **사라진다.** `ROLE_CUSTOMER` 의 목록 조회는 거부 대상이 아니라 **범위가 좁혀진 채 200** 이다. 상세 조회(§3)의 403(남의 문의)은 그대로다 — 그쪽은 id 를 직접 지목하므로 거부할 대상이 실재한다.

  **상담원이 특정 고객으로 좁히는 기능은 넣지 않는다.** 지금 필요하다는 근거가 없고, 넣으면 위 실패 방향 논의를 `ROLE_AGENT` 에 대해 다시 해야 한다. 필요해지면 그때 `PRD.md` 「나중에 할 것」에서 꺼낸다.
- **영향**:
  - `API-CONTRACT.md` §2 — 오류란에서 403 삭제, 파라미터 표 아래에 **"범위는 서버가 강제한다"** 명시. 버전 v1.2
  - `SecurityConfig` 만으로는 부족하다 — 역할 검사는 endpoint 접근까지만 막는다. **범위 좁히기는 `service/` 에서 걸어야 한다**
  - 코드 소유: P1(이용택, `GET /api/inquiries`)
- **재평가**: 없음. 되돌릴 조건이 있는 결정이 아니다 — 계약 내부 모순을 한쪽으로 닫는 것이다.

### D-039. `confidence = null` 의 표기 규칙을 계약에 고정한다 — 스키마 변경 없음

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: D-033 으로 `confidence = null` 이 **두 가지**를 뜻하게 됐다 — `FAILED`(AI 가 답을 못 냈다)와 `REUSED`(사람 확정을 재사용해 애초에 확신도가 없다). 판별식은 `verdict` 로 이미 정리됐지만(계약 B), **응답 계층과 역정규화 사본에는 이 개정이 안 닿았다.**

  | 빈 자리 | 구현자가 채울 수 있는 잘못된 답 |
  | --- | --- |
  | 목록·상세 응답의 `confidence` 가 null 일 때 | `0` 이나 `"-"` 로 치환 → **D-022 가 막은 오염이 응답 계층에서 되살아난다** |
  | `inquiries.current_confidence` (역정규화 사본, D-011) | 원본이 null 인데 `0` 을 복사 → 측정 8ⓐ 의 최하위 구간이 오염된다 |

  **`0` 은 특히 나쁘다.** 클라이언트가 `confidence < threshold` 같은 비교를 하면 null 은 대개 비교에서 빠지지만 `0` 은 **가장 낮은 신뢰도로 참여**한다. 조용히 틀리는 쪽이다.
- **결정**: 세 가지를 못박는다.

  **① 응답은 `null` 을 그대로 내보낸다.** 치환도 반올림도 하지 않는다.

  **② 필드 생략과 `null` 은 다른 뜻이다.** 이 계약에서 이미 둘 다 쓰이고 있으므로 혼동하면 안 된다.

  | 표현 | 뜻 |
  | --- | --- |
  | 필드 자체가 없음 | **권한이 없어 주지 않는다** (`ROLE_CUSTOMER` 에게 `confidence` 를 생략하는 경우) |
  | 필드가 있고 값이 `null` | **값이 존재하지 않는다** (`FAILED` 이거나 사람 확정을 재사용한 `REUSED`) |

  **③ 역정규화 사본도 null 을 그대로 복사한다.** `current_confidence` 는 원본의 사본이지 요약이 아니다. 단 `current_category` 는 `REUSED` 여도 채워진다 — 재사용 건에는 카테고리가 있다. **`current_category` 가 null 인 것은 미판정뿐**이다.

  **스키마는 바뀌지 않는다.** `V2__domain_switch.sql` 의 `current_confidence` 는 이미 `DECIMAL(4,3) NULL` 이다. **V2 는 적용됐으므로 DDL 주석조차 고치지 않는다** (D-023 forward-only) — 주석 한 줄을 위해 checksum 을 깨는 것은 이 원칙이 막으려는 바로 그 일이다. 설명은 헌법과 계약 문서가 진다.
- **영향**:
  - `API-CONTRACT.md` §2 · §3 — `confidence: null` 의 두 의미와 생략/`null` 구분
  - `CLAUDE.md` 도메인 모델 — `current_confidence` 의 null 규칙
  - **코드는 이미 맞다.** `InquiryReviewQueueItem.from(...)` 의 `switch` 는 `verdict` 로만 판별하고 `Verdict` 4종을 exhaustive 하게 다루며, `DomainFactoryTest.reasonIsDerivedFromVerdict` 가 4종 전부를 케이스로 갖고 있다. **`confidence` 로 판별하는 코드는 없다** (AI 리뷰 질의에 대한 확인 결과)
  - 테스트 1건 추가: `reusedFromHuman` 이 `confidence` 를 null 로 남기는지 **직접** 단언한다. 지금은 계약 B 매핑 케이스의 부산물로만 지나간다
- **재평가**: 없음. 이미 정해진 규칙(D-022·D-033)이 닿지 않은 자리를 메우는 항목이다.

### D-040. 마스킹본을 저장하지 않고 조회할 때 계산한다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: 계약은 *"`content` 는 마스킹된 본문을 반환한다. 원문은 저장하되 응답으로 되돌려주지 않는다"* 라고만 정했다. **어디서 마스킹하는지가 없다.** 목록 조회는 최대 100건을 한 번에 내보내므로, 매번 다시 계산하는지 컬럼에 저장해두는지에 따라 스키마와 비용이 달라진다. P1 이 정하지 않으면 나중에 스키마를 바꿔야 한다.
- **선택지**:
  1. **`masked_content` 컬럼 신설** — 조회 비용 0. 대신 `V3` 마이그레이션과 쓰기 비용이 붙는다
  2. **조회할 때 계산** — 스키마 변경 없음. 조회할 때마다 정규식이 돈다
  3. **원문을 저장하지 않고 마스킹본만 저장** — 가장 안전해 보이지만, **상담원이 답변하려면 원문이 필요하다.** 마스킹은 노출 통제이지 폐기가 아니다
- **결정**: **2번.**

  **3번을 먼저 버린다.** 주문번호를 가려버리면 그 주문을 조회할 수 없다 — 이 시스템은 문의를 분류하는 것이지 폐기하는 것이 아니다.

  **1번을 버린 이유가 이 항목의 핵심이다.** 마스킹 강도는 **측정으로 확정하기로 예정돼 있다**(D-031 재평가 조항). 즉 규칙이 바뀌는 것이 이미 계획된 일이다. 컬럼에 저장하면 **규칙을 조여도 과거 행은 옛 마스킹 그대로 남고**, 고치려면 백필이 필요하다. 백필을 빠뜨리면 **이미 저장된 개인정보가 계속 응답으로 나간다.** 조회할 때 계산하면 규칙을 조이는 즉시 과거분까지 적용된다 — **실패 방향이 안전한 쪽이다.**

  **비용은 재보기 전에는 모른다.** 정규식 4종 × 최대 100행이고 DB 왕복 하나보다 쌀 것으로 보지만, 그건 추정이라 evidence 가 아니다. **측정 b(목록 p95 < 200ms)에 이 비용이 포함**되며, 목표를 못 넘으면 그때 1번을 실측을 근거로 다시 올린다.

  **구현 위치는 `service/` 이고, AI 전송 전 마스킹과 같은 구현을 공유한다.** 두 벌로 두면 한쪽만 조여져서, 화면에는 가려지는데 AI 프롬프트에는 남는(또는 그 반대) 상태가 된다.
- **영향**:
  - `API-CONTRACT.md` §1 · §2 · §3 — 마스킹 시점 명시
  - `PRD.md` §5-4 — "가리는 건 저장해두지 않고 보여줄 때마다 한다" 한 줄
  - **스키마 변경 없음.** 코드 소유: P1(이용택, 정규화·마스킹 소유자)
- **재평가**: 측정 b 가 목표(200ms)를 못 넘고 **원인이 마스킹으로 지목되면** 1번(컬럼 저장)을 올린다. 그때는 마스킹 규칙 변경 시 백필을 함께 설계해야 한다.

### D-041. 2단 조회의 정렬 축은 `inquiries.created_at` — 판정 시각의 근사임을 인정한다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: AI 리뷰가 *"D-037 쿼리는 `created_at` 인데 계약의 정렬 축은 `received_at` 이라 어긋난다"* 고 지적했다. **확인 결과 어긋난 것은 아니다** — 둘은 서로 다른 쿼리다.

  | 쿼리 | 정렬 축 | 인덱스 |
  | --- | --- | --- |
  | `GET /api/inquiries` 목록 | `received_at` | `idx_inquiries_status_received` |
  | 2단 절감 경로 조회 | `created_at` | `idx_inquiries_key_created` |

  다만 **들여다보니 다른 문제가 있었다.** 정렬 축이 `inquiries` 쪽이라 이 쿼리가 집는 것은 *"가장 최근 판정"* 이 아니라 **"가장 최근 문의의 판정"** 이다. 늦게 확정된 옛 문의의 사람 답이 있으면 지나친다.
- **선택지**:
  1. **`r.created_at`(판정 행 시각)으로 정렬** — 의도에 정확하다. 그러나 정렬 축이 조인 안쪽으로 가서 **filesort** 가 된다. D-037 이 쿼리를 두 번 치기로 하면서까지 피한 바로 그 비용이다
  2. **`i.created_at` 유지 + 근사임을 명시** — 인덱스 정렬을 그대로 쓴다
  3. **인덱스 신설** — 측정 전에 인덱스를 늘리지 않는다는 현행 판단과 어긋난다 (D-037 이 같은 이유로 버렸다)
- **결정**: **2번.**

  **어느 사람 답을 집든 같은 답이어야 정상이다.** 같은 정규화 키의 사람 확정 답이 서로 다르다면 그건 정렬 축 문제가 아니라 **검토자 불일치**이고, 그것은 **측정 12 가 재는 대상**이다. 정렬 순서를 바꿔 덮을 문제가 아니다 — 덮으면 불일치가 있다는 사실 자체가 안 보이게 된다.

  **D-036 의 캐시 규칙("마지막에 확정한 사람이 이긴다")과 순서가 갈릴 수 있음을 인정한다.** 캐시는 확정 시각 순, DB 는 문의 접수 순이다. 같은 키의 사람 답이 하나뿐이면 두 경로가 같은 값을 준다. 둘 이상이면 갈릴 수 있고, **그 조건은 D-036 재평가 조항의 발동 조건과 정확히 같다.**

  `received_at` 이 아닌 이유는 인덱스가 `created_at` 으로 잡혀 있기 때문이다(`V2`). 두 값은 접수 트랜잭션 ① 안에서 거의 같이 찍히고, 축을 바꾸려면 **`V3` 인덱스가 필요**하다.
- **영향**: `CLAUDE.md` 인덱스 표 각주 한 줄. 측정 5ⓓ 에서 이 정렬이 실제로 인덱스로 커버되는지 함께 본다. **스키마 변경 없음**
- **재평가**: 측정 12 에서 검토자 간 일치도가 낮게 나오면 D-036 과 **함께** 재론한다. 둘은 같은 원인을 공유하므로 따로 고치면 두 경로가 더 어긋난다.

### D-042. 재사용 건이 캐시에 남기는 두 우회로 — 출처 체인과 통계 evict

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: AI 리뷰가 캐시 쪽 우회로 둘을 물었다.

  **ⓐ 체인 금지가 캐시를 우회할 수 있나.** 2단 hit 으로 `REUSED` 행을 만들고 그 결과를 캐시에 put 할 때, `sourceResultId` 에 **방금 만든 `REUSED` 행의 id** 를 넣으면 다음 재사용이 재사용을 가리킨다. 보장 지점이 조회 쪽인지 put 쪽인지 계약에 없었다.

  **ⓑ `stats:summary` 의 evict 조건이 재사용 확정을 안 덮는다.** evict 는 *"큐 삽입/확정 시"* 인데 **재사용 건은 자동 확정이라 큐를 거치지 않는다**(D-033 ③). `audit.reused` 블록이 갱신 없이 남는다.
- **결정**:

  **ⓐ 보장 지점은 조회 쪽이다. `sourceResultId` 는 항상 원본을 가리킨다.**

  2순위 조회가 `verdict = 'AUTO_ACCEPTED'` **등치 조건**이라 `REUSED` 행은 애초에 조회 결과로 나오지 않고, 1순위가 돌려주는 것은 사람이 매긴 답이라 체인이 아니다(D-037). 따라서 **캐시 put 은 조회가 돌려준 값을 그대로 옮길 뿐 스스로 판단하지 않는다.**

  > **put 쪽에 검사를 또 두지 않는 이유**: 같은 규칙을 두 곳에서 지키면 한쪽이 낡는다. 조회가 원본만 돌려주는 한 put 은 옳고, 조회가 틀리면 put 의 검사도 무엇이 원본인지 모른다.

  코드는 이미 이 모양이다 — `reusedFromHuman(…, sourceResultId)` / `reusedFromAi(…, sourceResultId)` 가 원본 id 를 **인자로 받는다.** 호출자가 조회 결과를 그대로 넘기면 체인이 생길 자리가 없다.

  **ⓑ evict 지점을 늘리지 않는다.**

  재사용 확정은 **접수 경로마다 일어나므로 빈도가 높다.** 여기에 `allEntries=true` evict 를 걸면 캐시가 사실상 상시 비어 있게 되고, **「변경 빈도 << 조회 빈도인 지점만」이라는 캐시 전략의 전제 자체가 무너진다.** TTL 10s 가 이미 지연 상한이고, 감사 통계가 10초 늦어도 읽는 사람의 판단은 바뀌지 않는다.

  **큐 삽입/확정에 evict 가 붙어 있는 이유는 빈도가 아니라 관측자다** — 그쪽은 **사람이 방금 한 행동의 결과를 화면에서 확인하는 경로**라, 10초 지연이 "내가 확정했는데 숫자가 안 변한다"로 보인다. 재사용 자동 확정에는 그 자리에서 결과를 기다리는 사람이 없다.
- **영향**: `CLAUDE.md` 계약 C 와 「캐시 전략」에 각 한 줄. **코드·스키마 변경 없음**
- **재평가**: 측정 11 에서 `stats:summary` 의 지연이 실제로 관측을 방해하면(예: 감사율이 설정값에 수렴하는지 보는 데 10초가 걸려 판단이 어려우면) TTL 을 줄인다. **evict 를 늘리는 쪽이 아니라 TTL 쪽을 만진다** — 손잡이가 하나여야 어느 것이 원인인지 안다.

### D-043. 측정 8ⓐ 를 둘로 쪼갠다 — 표본이 2건이면 결론이 아니라 일화다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: 멘토 피드백은 **PAAR 을 북극성으로 삼으라**는 것이었다. 그 기준으로 현재 상태를 보면 P·A 는 과잉이고 **R 이 0** 이다. 그런데 R 을 채우려고 측정 계획을 다시 읽어보니, **코드를 다 짜도 숫자가 안 나오는 구멍이 셋** 있었다.

  **ⓐ 감사 표본이 너무 작다.** 측정 1 의 입력은 50건이고, 그중 자동 확정이 40건쯤이면 감사 5% 는 **2건**이다. 2건으로 오분류율을 말할 수 없다. 1000건을 넣어도 사람이 검토해야 할 감사 표본이 수십 건이라 5일 안에 소화가 어렵다. **측정 8ⓐ 는 이 프로젝트의 결론인데, 지금 설계로는 결론 자리에 표본 2건이 온다.**

  **ⓑ 측정 6 이 자작극이 된다.** "같은 문의를 섞어 1000건"의 중복 비율을 우리가 정하면 절감률도 우리가 정하는 것이다. 절감률 단일 수치는 정규화 규칙의 성능이 아니라 **입력 데이터의 성질**을 재는 숫자가 된다.

  **ⓒ 절감 경로가 켜져 있으면 측정 1 의 표본이 줄어든다.** 재사용이 동작하면 같은 내용의 문의는 AI 를 부르지 않는다. seed 50건 중 중복분만큼 **AI 응답 표본이 조용히 사라진다.** 측정 1 과 8ⓐ 는 "AI 가 얼마나 맞나"를 보는 것인데 AI 를 안 부른 건이 섞이면 분모가 무엇인지 모르게 된다.
- **선택지**:
  1. **입력을 키운다** — 자동 확정이 2000건 나오도록 seed 를 늘린다. 감사 5% 로 100건이 나오지만 **사람이 100건을 검토**해야 하고, 5일 범위에서 불가능하다
  2. **측정용으로 감사 비율을 100% 로 올린다** — 자동 확정 전수가 큐에 들어간다. 사람 노동은 1번과 같고, 게다가 **운영값 5% 에서의 결과가 아니게 된다**
  3. **8ⓐ 를 둘로 쪼갠다** — seed 에 정답 레이블이 이미 붙어 있으므로, 자동 확정 **전수를 레이블과 대조**하면 사람 검토 없이 진짜 오분류율이 나온다. 5% 감사는 "그중 몇 건을 집어냈나"로 따로 평가한다
- **결정**: **3번.** 셋 다 반영한다.

  **ⓐ 측정 8ⓐ → 8ⓐ-1 / 8ⓐ-2**

  | | 무엇 | 표본 | 사람 노동 |
  | --- | --- | --- | --- |
  | 8ⓐ-1 | 자동 확정 건의 **실제** 오분류율 (확신도 구간별) | 자동 확정 **전수** | 없음 (레이블 대조) |
  | 8ⓐ-2 | 5% 감사가 그 오분류를 몇 건 집어냈나 | 감사로 뽑힌 건 | 그 건수만큼만 |

  **쪼개는 쪽이 결론이 더 강하다.** 원래 8ⓐ 는 "오분류율이 N% 였다" 하나였는데, 쪼개면 **"실제 오분류율은 N% 이고 5% 감사는 그중 M% 를 잡았다"** 가 된다. 뒤쪽이 곧 **이 설계의 한계 진술**이고, §11 성공 기준 3번("실제보다 낮게 나온다는 것까지 설명")에 직접 답하는 값이다.

  > **8ⓐ-1 에는 앵커링이 없다.** 사람이 AI 답을 보고 판단하는 게 아니라 미리 붙여둔 레이블과 대조하는 것이라, D-010 이 남긴 "측정값은 하한" 단서가 여기엔 붙지 않는다. **8ⓐ-1 과 8ⓐ-2 의 차이가 앵커링과 샘플링 손실을 합친 크기**다.

  **ⓑ 측정 6 은 두 값으로 보고한다.** 절감률 단일 수치를 쓰지 않는다.

  - **잡은 비율** = AI 를 안 부른 건수 ÷ 사람이 보기에 중복인 건수 (과소 병합의 반대면)
  - **잘못 묶은 비율** = 서로 다른 문의인데 같은 키가 된 건수 ÷ 전체 (과도 병합)
  - 입력 1000건의 **정답 중복 비율을 먼저 세어 적어둔다.** 이 값을 안 적으면 위 두 비율의 분모가 없다

  **ⓒ 재사용 경로에 기동 시 스위치를 둔다.** `classification.reuse.enabled` (기본 `true`). 측정 1 과 8ⓐ-1 은 `false` 로 잰다.

  > **런타임 변경은 여전히 금지다 (D-028 승계).** 기동 시 고정이고 실행 중에는 못 바꾼다. 측정 도중 바뀌면 그 측정을 믿을 수 없다는 이유는 감사 비율과 똑같이 적용된다.
  >
  > **스위치를 두는 것이 절감 경로를 뒤로 미루는 것과 같지 않다.** §8 의 2단계는 여전히 만든다 — 다만 측정 1·8ⓐ-1 을 잴 때만 끈다.
- **영향**: `PRD.md` §9 측정표(8ⓐ 분할·6번 재정의·조건 컬럼 추가), §1-1 R 칸, §11 성공 기준. `application.yml` 에 `classification.reuse.enabled` 1개. **스키마 변경 없음**, 코드 영향은 분류 담당의 분기 하나
- **재평가**: 8ⓐ-1 의 오분류율이 **0** 이면 (자동 확정 구간에서 AI 가 하나도 안 틀리면) 감사 경로가 잡아낼 것이 없어 8ⓐ-2 를 못 읽는다. 그때는 기준값을 0.8 에서 낮춰(예: 0.6) 자동 확정 구간을 넓힌 뒤 다시 잰다 — **기준값을 낮추는 것은 오분류를 만들려는 게 아니라, 「기준값이 낮으면 자동 확정이 위험해진다」를 숫자로 보이는 것**이다

### D-044. 구현 순서를 결론에서 역산한다 — 기능 목록 순서로 만들지 않는다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: `PRD.md` §8 「5일 동안 만들 것」은 **목록**이지 순서가 아니었다. 목록은 위에서부터 만들게 되는데, 그 순서(접수 → 정규화 → 재사용 → 분류 → 감사 → 검토)로 가면 **마지막 날까지 잴 수 있는 숫자가 하나도 없다.** 절감 경로가 3번째에 있어서 결론(8ⓐ)까지 도달하기 전에 이틀을 쓴다.

  더 나쁜 것은 D-043 ⓒ 다 — **절감 경로를 먼저 만들면 그게 측정 1 의 표본을 갉아먹는다.** 순서를 잘못 잡으면 늦어지는 게 아니라 **측정이 오염된다.**
- **선택지**:
  1. **기능 목록 순서** — 문서에 적힌 대로. 담당자별로 자기 것부터 시작하면 자연스럽지만, 셋이 각자 끝나야 처음으로 무언가 돌아간다
  2. **계층 순서** (`domain/` 전부 → `service/` 전부 → `api/` 전부) — 마지막 날에 처음 돌아본다. 최악
  3. **결론에서 역산한 수직 관통** — 8ⓐ 숫자가 나오는 사슬만 먼저 잇고, 나머지는 그 뒤에 붙인다
- **결정**: **3번.** §8 을 1·2·3단계로 나눠 다시 썼다.

  ```text
  결론 8ⓐ ← ③ 확정 ← ② 감사·큐 삽입 ← ② 기준값 비교 ← AI 호출 ← ① 접수
  ```

  이 사슬에 들어간 것이 **1단계**이고, 절감 경로(2단계)와 관측·성능(3단계)은 그 뒤다.

  **P1/P2/P3 분담(D-015·D-031)은 바꾸지 않는다.** 바뀌는 것은 **각자 무엇부터 손대는가**다 — 셋이 각자 자기 패키지를 끝까지 만드는 대신, 사슬 한 줄이 관통할 때까지 1단계 항목을 먼저 끝낸다. 계약 A/B/C 가 이미 고정돼 있어 병렬은 그대로 가능하다.

  > **2단계를 뒤로 미루는 것이 D-031 의 「2단 절감 경로」를 약화시키지 않는다.** 그건 그룹핑을 대체하는 자산이고 여전히 만든다. 다만 **결론을 만드는 장치가 아니라 결론의 표본을 줄이는 장치**라, 결론이 먼저 나온 뒤에 붙이는 것이 순서상 맞다.
- **영향**: `PRD.md` §8 전면(1·2·3단계 + 역산 그림), §10 「지금 상태」에 PAAR 진행 칸. `LIFECYCLE-COVERAGE.md`·`INTEGRATION-LOG.md` 의 진행 순서. **설계·스키마 변경 없음**
- **재평가**: 1단계가 Day 3 까지 관통되지 않으면 **2단계를 5일 범위 밖으로 뺀다.** 절감 경로 없이도 §11 의 성공 기준 셋은 전부 답할 수 있고, 8ⓑ 와 11 만 못 내게 된다 — 못 낸 이유를 「나중에 할 것」에 적는다

### D-045. 필수 기능 5개에 실패 모드를 하나씩 붙인다 — "썼다"와 "동작한다"의 차이

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안** — 단 1단계 관통 **이후**)
- **배경**: 6 공통 필수 기능은 애노테이션 한 줄로도 체크가 된다. `@Cacheable` 한 줄이면 「캐시 적용 포인트 1개 이상」을 만족하고, `@PreAuthorize` 한 줄이면 「권한 또는 역할 구분」을 만족한다. 그런데 그렇게 만든 것은 **잘 될 때만 잘 된다.**

  구현 매핑을 코드 수준으로 내려 적으면서(`PRD.md` §5-0) 각 기능이 **정상 경로만 정의돼 있다**는 것이 드러났다. 이 프로젝트의 주제가 *"조용히 틀리지 않는다"* 인데 **필수 기능 다섯 개가 각자 조용히 틀리는 구멍을 하나씩 갖고 있다.**

  구멍을 열거하면 이렇다.

  | 기능 | 정의 안 된 실패 |
  | --- | --- |
  | 권한 | 소유권 검사를 한 군데서 빠뜨리면 남의 문의가 나간다. 검사는 사람이 기억해서 지키는 구조다 |
  | 트랜잭션 | D-012 의 "감사 삽입을 밖에 두면 안 된다"가 **주장으로만** 있다. 얼마나 새는지 잰 적이 없다 |
  | 검색 | 앞 몇 페이지만 재고 "인덱스로 빠르다"고 말하게 된다. 오프셋이 깊어지면 인덱스가 있어도 느리다 |
  | 캐시 | ⓐ 정규화 규칙을 조이기로 이미 정해뒀는데(D-031 재평가) **옛 키가 캐시에 그대로 남는다** ⓑ Redis 가 죽으면 접수가 통째로 실패한다 |
  | 비동기 | `@Async` 기본 실행기는 스레드를 무한 생성한다. 풀을 지정하면 **가득 찼을 때 정책**을 정해야 하는데 정해진 바 없다 |
- **선택지**:
  1. **그대로 둔다** — 5일 범위를 지키는 데 유리하다. 그러나 위 표의 캐시 ⓐ 는 **재평가 조항이 발동하는 순간 곧바로 오작동**하고, 비동기 쪽은 **문의가 조용히 버려지는** 경로다. 프로젝트 주제와 정면으로 어긋난다
  2. **전부 제대로 만든다** — 캐시 스탬피드 방지, 커서 페이징 전환, 권한을 정책 엔진으로 등. 5일 범위 밖이고 1단계 관통을 밀어낸다
  3. **기능마다 가장 값싸고 가장 잘 드러나는 실패 하나씩만** 고른다
- **결정**: **3번.** 다섯 개를 아래로 확정한다. 전부 코드 몇 줄이거나 실험이고, 전부 기존 측정에 얹힌다 — **새 측정 번호를 만들지 않는다.**

  | | 기능 | 붙이는 것 | 얹히는 측정 |
  | --- | --- | --- | --- |
  | 1 | 권한 | **소유자 인자 없이 문의를 조회하는 리포지토리 메서드를 두지 않는다.** 검사를 잊는 것이 컴파일 단계에서 드러나게 한다 | 코드 검색 (US-13 과 함께) |
  | 2 | 트랜잭션 | **감사 큐 삽입을 `REQUIRES_NEW` 로 뺀 대조 실험** — 표본이 몇 건 새는지 센다 | 측정 3 |
  | 3 | 검색 | **깊은 오프셋의 `EXPLAIN`** — 10만 건에서 1페이지 vs 500페이지 | 측정 5 (케이스 추가) |
  | 4 | 캐시 | ⓐ **키 앞에 정규화 규칙 버전** (`v1:…`) ⓑ **Redis 장애 시 요청을 실패시키지 않는다**(`CacheErrorHandler`) | 측정 11 |
  | 5 | 비동기 | **스레드풀 명시 + 포화 정책 확정** — 버리지 않고 **호출한 쪽이 대신 처리**한다 | 속도 a (대기줄 상태 함께 기록) |

  **1번이 D-025 와 같은 방식이다.** 정적 팩토리로 잘못된 엔티티 조합을 컴파일러가 막게 했듯, 소유자 없는 조회 메서드를 **아예 만들지 않으면** 검사를 빠뜨릴 자리가 없다. D-038 이 "조회 범위는 서버가 강제한다"고 정한 것의 구현 형태다.

  **5번의 대가를 감춘다면 이 결정은 무의미하다.** 호출한 쪽이 대신 처리하면 AI 가 밀릴 때 **접수 API 가 같이 느려진다** — 속도 목표 a(0.1초)를 못 넘길 수 있다. 그래도 이쪽을 고르는 이유는 **느려지는 것은 지표에 보이고 사라지는 것은 안 보이기 때문**이다. 목표를 못 넘기면 그 사실을 그대로 적고, 풀 크기를 조정한 결과를 함께 남긴다.

  **2·3 은 코드가 아니라 실험이다.** 잴 대상이 먼저 있어야 하므로 1단계 관통 이후에만 성립한다.
- **영향**: `PRD.md` §5-0(구현 매핑 신설) · §5-7(본 결정) · 측정 3·5·11·a 의 기록 항목. `build.gradle` 에 **`spring-boot-starter-security` 와 AI 호출 라이브러리 추가** (아래 참조). **스키마 변경 없음**

  > **의존성 누락을 함께 확정한다.** `build.gradle` 에 `spring-boot-starter-security` 가 없다 — 필수 기능 1번의 의존성이 통째로 빠져 있어 `SecurityConfig` 를 쓰는 순간 컴파일이 막힌다. AI SDK 는 *"프로바이더가 문서 간에 어긋나 있어 baseline 이 선점하지 않는다"* 는 주석과 함께 의도적으로 비어 있었으나, **D-024 로 Anthropic 이 확정됐으므로 보류 사유가 소멸했다.** 둘 다 1단계 착수 전에 넣는다 (P2 김준현).
- **재평가**: 5번(포화 정책)으로 속도 a 가 목표를 크게 벗어나면 — 예컨대 p95 가 1초를 넘으면 — **버리지 않는다는 원칙은 유지하되 풀 크기를 키운다.** 그래도 안 되면 접수와 분류 사이에 저장 단계를 두는 설계를 Phase 3 로 올린다. **정책을 "버림"으로 되돌리지는 않는다** — 그건 이 프로젝트가 막으려는 실패를 스스로 만드는 것이다

### D-046. 정답 문의 50건을 코드보다 먼저 만든다 — 그리고 정답은 두 사람이 따로 붙인다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**, **0단계**)
- **배경**: D-043 ⓐ 로 측정 8ⓐ-1 을 "자동 확정 전수를 정답 레이블과 대조"로 바꿨다. 그런데 이 설계는 **정답 레이블이 붙은 seed 50건이 이미 있다**는 전제 위에 서 있고, 그 50건은 코드가 아니라 **사람이 손으로 만드는 것**이다.

  현재 일정은 `LIFECYCLE-COVERAGE.md` 기준 **측정 1·8ⓐ-1 이 Day 4** 다. 즉 Day 4 에 코드 완성과 정답 데이터 작성이 동시에 걸린다. 그리고 seed 는 **코드를 한 줄도 안 쓰고 지금 시작할 수 있는 유일한 작업**이라, 여기 두는 것은 순수한 낭비다.

  더 중요한 문제가 둘 더 있었다.

  **ⓐ AI 로 문의 본문을 만들면 측정이 오염된다.** 같은 계열 모델이 만든 문장은 그 모델이 분류하기 쉬운 쪽으로 치우친다. 오분류율이 실제보다 낮게 나오고 **그게 이 프로젝트의 결론이 된다.** `CLAUDE.md` 「AI 검증 규칙」이 *"AI 가 만든 추정 수치는 evidence 로 쓰지 않는다"* 고 한 것과 같은 종류의 오염이다.

  **ⓑ 쉬운 문의만 50건이면 8ⓐ-2 를 못 읽는다.** 경계가 없는 문의만 넣으면 AI 가 거의 다 맞히고, **측정 8ⓐ-1 이 0 이 되면 감사 경로가 잡아낼 것이 없어 8ⓐ-2 가 성립하지 않는다.** D-043 재평가 조항이 이 상황을 다루지만, 애초에 seed 구성으로 막는 편이 낫다.
- **선택지**:
  1. **일정대로 Day 4** — 코드와 데이터가 같은 날 겹친다. 어느 하나가 늦으면 둘 다 못 낸다
  2. **AI 로 50건을 한 번에 생성** — 30분이면 끝난다. 그러나 ⓐ 로 결론이 오염되고, 그 오염은 **결과가 좋게 나오는 방향**이라 알아채기 어렵다
  3. **0단계로 앞당기고, 문장 출처와 레이블 절차를 규칙으로 고정**
- **결정**: **3번.** 네 가지를 함께 고정한다.

  **ⓐ 시점 — 0단계.** 코드 착수 전에 시작해 **Day 1 종료 시 확정**한다. 1000건(측정 6용)은 Day 2 에 50건에서 **파생**시킨다 — 새로 쓰면 "사람이 보기에 중복인 비율"(D-043 ⓑ 의 분모)을 다시 세어야 한다.

  **ⓑ 문장 출처를 나눈다.**

  | 무엇 | 누가 |
  | --- | --- |
  | 쉬운 문의 초안 | AI 허용. 단 **사람이 문장을 다시 쓴다** |
  | **경계에 걸리는 문의** | **사람이 직접 쓴다** — 결론이 갈리는 자리다 |
  | **정답 레이블** | **무조건 사람.** 예외 없음 |

  AI 로 초안을 만들었으면 **`evidence/` 에 그 사실을 적는다.** 안 적으면 나중에 "왜 이렇게 잘 나왔나"에 답할 수 없다.

  **ⓒ 구성 요건 3개** — §7 경계표의 헷갈리는 경계 **9개(`ETC` 제외)를 각각 최소 1건** / 개인정보처럼 보이는 문자열(주문번호·연락처·금액·날짜)을 섞어 **마스킹 테스트를 겸함** / `ETC` 5건 중 **"판단이 어려워서 고른 것" 0건**(있으면 §7 표부터 고친다).

  **ⓓ 정답은 두 사람이 독립으로 붙인다.**

  ```text
  1. 작성자가 본문 50건 + 정답 초안
  2. 다른 1명이 본문만 보고 정답을 따로 붙인다 (초안 비공개)
  3. 갈린 건은 §7 경계표로 판정
  4. 표로도 안 갈리면 → §7 표를 고친다. 정답을 억지로 맞추지 않는다
  ```

  > **이 절차의 부산물이 측정 12 의 예비값이다.** 50건에 대한 2인 일치율이 Day 1 에 나온다. 낮으면 **Day 5 이전에 §7 을 고칠 시간이 생긴다** — Day 5 에 처음 알면 고칠 시간이 없고, D-027 기준 2(정답 단일성)를 현행 도메인이 만족하지 못한다는 결론만 남는다.
  >
  > **4번이 핵심이다.** 두 사람이 갈렸는데 정답을 임의로 정하면, 측정 8ⓐ-1 의 "틀림"이 **AI 가 틀린 것인지 정답이 자의적인 것인지** 구분되지 않는다.
- **영향**: `PRD.md` §8 에 **0단계 신설**. `src/test/resources/seed/inquiries-50.csv` 신규. `evidence/` 에 작성 절차·문장 출처·2인 일치율 기록. `LIFECYCLE-COVERAGE.md` 일정. **코드·스키마 변경 없음**
- **재평가**: 2인 일치율이 **90% 미만**이면 §7 경계표를 조인 뒤 재라벨한다. 조여도 안 오르면 **D-001 재평가 조항(카테고리 10종 → 5종 축소)을 앞당겨 발동**한다 — 카테고리가 사람에게도 안 갈라지면 AI 에게 물어보는 것 자체가 성립하지 않는다

### D-047. 비동기 실행 환경을 한 묶음으로 확정한다 — 스레드풀만 정하고 나머지를 비워두면 유실 경로가 남는다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: D-045 ⑤ 가 *"스레드풀을 명시하고 포화 정책을 정한다"* 까지 결정했다. 그런데 `application.yml` 을 실제로 열어보니 **관련 설정이 하나도 없다** — 커넥션 풀도, 실행기도, 종료 처리도 전부 스프링 기본값이다. 풀 하나만 정하고 나머지를 두면 **구멍이 옆으로 옮겨갈 뿐**이라 다섯 개를 한 묶음으로 본다.

  | | 지금 상태 | 무슨 일이 생기나 |
  | --- | --- | --- |
  | ⓐ | `@Async` 실행기 미지정 | 기본 실행기가 요청마다 스레드를 새로 만든다. D-045 ⑤ 가 이미 다룸 |
  | ⓑ | HikariCP 미설정 = **커넥션 10개** | 분류 담당의 스레드가 트랜잭션 ②로 커넥션을 전부 점유하면 **접수 API(①)가 커넥션을 못 얻어 대기한다.** 속도 a 가 깨지는데 **원인이 AI 가 아니라 커넥션**이라 오진하기 쉽다 |
  | ⓒ | 종료 대기 설정 없음 | **배포·재시작 때마다 처리 중이던 문의가 버려진다.** "조용히 사라지는 문의가 없게 한다"는 목표의 **가장 흔한 위반 경로**가 정작 배포다 |
  | ⓓ | `AsyncUncaughtExceptionHandler` 없음 | `@Recover` 가 잡는 것은 `@Retryable` 이 지정한 예외뿐이다. 그 밖의 예외(NPE 등)는 반환값을 안 쓰는 `@Async` 메서드에서 **로그 한 줄 남고 끝난다.** D-030 이 막으려던 "Sentry 가 영구히 모르는" 사각지대의 비동기 판 |
  | ⓔ | 재시도 백오프 값 미정 | 백오프 동안 **분류 담당의 스레드가 묶인다.** AI 가 죽어 있으면 모든 스레드가 대기 상태가 되어 **정상 건까지 멈춘다** |
- **선택지**:
  1. **필요할 때 하나씩** — 각각은 설정 몇 줄이라 그때그때 붙여도 된다. 그러나 ⓑ·ⓔ 는 **서로의 값에 의존**해서 따로 정하면 반드시 어긋나고, ⓒ 는 **증상이 배포 때만 나타나서** 마지막까지 발견되지 않는다
  2. **한 묶음으로 확정** — 다섯이 사실상 하나의 문제(분류 담당이 자원을 어떻게 쓰고 어떻게 끝나는가)다
- **결정**: **2번.** 다섯을 함께 정한다. **숫자는 초기값이고 측정 a 에서 조정한다 — 재보지 않은 값을 근거로 쓰지 않는다.**

  | | 확정 |
  | --- | --- |
  | ⓐ | `ThreadPoolTaskExecutor` 빈 `classifyExecutor` 를 이름으로 지정. `@Async("classifyExecutor")` |
  | ⓑ | **불변 관계: 분류 담당의 최대 스레드 수 < 커넥션 풀 크기.** 숫자가 아니라 **관계**를 고정한다 — 풀을 조정할 때 한쪽만 만지면 이 관계가 깨진다 |
  | ⓒ | 종료 시 진행 중 작업을 기다린다 (`setWaitForTasksToCompleteOnShutdown(true)` + `spring.lifecycle.timeout-per-shutdown-phase`) |
  | ⓓ | `AsyncUncaughtExceptionHandler` 를 등록해 `Sentry.captureException` 으로 넘긴다 (D-030) |
  | ⓔ | 백오프는 **풀 크기와 함께** 정하고, 측정 4 에서 재시도 중 스레드 점유 시간을 함께 기록한다 |

  **ⓑ 를 숫자가 아니라 관계로 적은 이유**: "스레드 8 / 커넥션 20" 같은 숫자만 적어두면, 나중에 누가 스레드를 늘릴 때 커넥션을 같이 봐야 한다는 것을 모른다. **관계를 적으면 한쪽을 만질 때 다른 쪽이 따라온다.**

  **ⓒ 가 이 결정에서 가장 값싸고 가장 값지다.** 설정 두 줄인데, 없으면 **배포할 때마다 유실이 생기고 그 유실은 `stuckReceived` 에만 보이며 아무도 고치지 않는다.**
- **영향**: `application.yml`(HikariCP·`spring.lifecycle`), `config/AsyncConfig` 신규(실행기 빈 + 예외 핸들러), `config/RetryConfig`(백오프). `PRD.md` §5-5. 측정 a 에 **접수 지연의 원인이 대기줄인지 커넥션인지 구분해 기록**. **스키마 변경 없음**
- **재평가**: 측정 a 에서 접수 p95 가 목표를 넘고 **원인이 커넥션 고갈로 확인되면** 분류 담당의 스레드 풀을 줄인다(커넥션을 늘리지 않는다 — DB 쪽 부하를 먼저 본다). 원인이 대기줄 포화면 D-045 ⑤ 재평가 조항으로 넘긴다

### D-048. 캐시의 두 구멍 — 덮어쓰기 판단의 원자성과 메모리 상한

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: 계약 C(D-036)는 *"②의 put 은 기존 값이 사람 답(`source=HUMAN`)이면 덮지 않는다"* 로 **덮어쓰기 방향**을 정했다. 그런데 이 문장을 코드로 옮기면 **읽고 → 판단하고 → 쓰는 세 단계**가 된다.

  ```text
  ② 담당 : 캐시 읽음 → 비어 있음 (AI 답을 넣어도 되겠다)
  ③ 상담원:                        사람 답을 put        ← 이 틈에
  ② 담당 :                                      AI 답을 put   ← 사람 답을 덮는다
  ```

  **D-036 이 막으려던 상황이 그대로 재현된다** — 사람이 정정한 답이 무시되고 옛 AI 답이 계속 재사용된다.

  > `CLAUDE.md` 가 *"완벽한 원자성은 목표가 아니다"* 라고 이미 적어뒀지만, **그 문장이 다루는 창은 「분류 담당이 AI 를 부르는 사이」다.** 위는 **put 안쪽의 read-then-write 창**이라 다른 문제이고, 앞의 창과 달리 **한 줄로 막힌다.**

  두 번째 구멍은 메모리다. **`docker-compose.yml` 에 `maxmemory` 도 `maxmemory-policy` 도 없다.** 계약 C 는 *"evict 없음. TTL 은 메모리 상한 목적으로만"* 이라 했는데 **TTL 값 자체가 정해진 적이 없다.** 1단 캐시는 evict 가 없으므로 정규화 키가 **무한히 쌓인다.**
- **선택지**:
  - **ⓐ 원자성**: (1) 그대로 두고 창을 한계로 적는다 — 공짜지만 D-036 의 목적이 확률적으로 깨진다 (2) `WATCH`/`MULTI` + 재시도 루프 — 재시도 코드가 붙는다 (3) **Lua 스크립트 1개** — Redis 가 원자 실행을 보장하고 코드가 몇 줄이다
  - **ⓑ 메모리**: (1) 무제한 — Redis 가 죽는다 (2) 모든 키에 TTL — 1단 캐시에 TTL 을 걸면 **절감률이 TTL 에 따라 흔들려 측정 6 을 해석할 수 없다** (3) **`maxmemory` + `allkeys-lru`** — 상한을 넘을 때만 오래 안 쓴 키부터 밀어낸다
- **결정**: **ⓐ 는 3번(Lua), ⓑ 는 3번(LRU).**

  **ⓐ `SET NX` 로는 안 된다.** 기존 값이 AI 답이면 **덮어야** 하고 사람 답이면 **안 덮어야** 하므로, "없을 때만 쓰기"가 아니라 **"기존 값을 보고 결정"** 이다. 조건이 값에 달려 있으니 읽기와 쓰기가 한 번에 일어나야 한다 — Lua 가 그 자리다.

  **③(사람 확정)의 put 은 조건 없이 덮으므로 그냥 `SET` 이면 된다** (D-036). 원자화가 필요한 것은 ②쪽 하나뿐이다.

  **ⓑ TTL 대신 LRU 인 이유**: TTL 을 걸면 **캐시가 비는 시점이 시계에 따라 달라져** 측정 6(AI 호출 절감)과 11(적중률)이 실행할 때마다 다른 값을 낸다. LRU 는 **메모리가 부족할 때만** 밀어내므로 상한을 넉넉히 잡으면 측정 중에는 아무것도 안 밀려난다. 정확성은 어차피 evict 가 아니라 ③의 덮어쓰기가 지킨다(계약 C).

  > ⚠️ **`allkeys-lru` 는 `stats:summary` 도 밀어낼 수 있다.** 그쪽은 TTL 10s 라 밀려나도 손해가 없다.
  >
  > ⚠️ **상한이 낮으면 분류 캐시가 밀려나 절감률이 떨어진다.** 그래서 **측정 11 에 "키 개수와 메모리 사용량"을 함께 기록**한다 — 절감률이 낮게 나왔을 때 그게 정규화 탓인지 메모리 탓인지 구분할 수 있어야 한다.
- **영향**: `service/ClassificationCache`(② 경로를 Lua 로), `docker-compose.yml`(`maxmemory` + `maxmemory-policy`), `CLAUDE.md` 「캐시 전략」에 한 줄, 측정 11 기록 항목. **스키마 변경 없음**
- **재평가**: 측정 11 에서 키가 밀려난 흔적(`evicted_keys` > 0)이 보이면 **상한을 올린다.** 그래도 밀려나면 정규화 키가 예상보다 훨씬 잘게 쪼개졌다는 뜻이므로 **D-031 재평가(정규화 강도)를 발동**한다 — 캐시 크기 문제가 아니라 키 설계 문제다

### D-049. 트랜잭션 ②를 조건부 UPDATE 로 한 번만 실행되게 한다 — 잃었던 원자적 UPDATE 가 여기서 돌아온다

- **일자**: 2026-08-05
- **상태**: 채택 (5일 범위 **안**)
- **배경**: 계약 A 는 *"접수 전건에 이벤트를 발행한다"* 이고, 트랜잭션 ②에는 **같은 문의에 두 번 실행되는 것을 막는 장치가 없다.** 두 번 실행되면 이렇게 된다.

  | 무엇이 두 개가 되나 | 결과 |
  | --- | --- |
  | 분류 결과 행 | 스키마상 여러 행이 허용되므로 저장은 된다. 그러나 `current_*` 역정규화가 **나중 것으로 덮인다** |
  | **검토 큐 항목** | **상담원이 같은 문의를 두 번 본다.** 그리고 감사 표본이면 **측정 8ⓐ 의 분모가 부풀어 오분류율이 희석된다** |

  지금 당장 이벤트가 두 번 올 경로는 좁다. 그러나 **D-045 ⑤(포화 시 호출한 쪽이 대신 처리)**, 분류 담당이 다시 뜨는 경우, 수동 재처리, 그리고 「나중에 할 것」 E(멈춘 문의 재분류)를 붙이는 순간 **반드시 발생한다.** E 는 정의상 *"이미 이벤트가 한 번 나갔던 문의를 다시 태우는 일"* 이다.

  **D-012 가 지키려던 것이 여기서 반대 방향으로 깨진다.** D-012 는 "감사 표본이 **빠지면** 분모가 준다"를 막았는데, 이건 "감사 표본이 **겹치면** 분모가 는다"이다. 둘 다 측정 8ⓐ 를 조용히 틀리게 만든다.
- **선택지**:
  1. **`(inquiry_id, reason)` UNIQUE 제약** — DB 가 보장한다. 그러나 **D-016 의 교훈**이 그대로 적용된다: 제약 위반을 같은 트랜잭션 안에서 잡아 재조회하면 `UnexpectedRollbackException` 으로 실패한다. 재시도를 트랜잭션 밖에 둬야 해서 구조가 커진다
  2. **② 진입부에서 `inquiry.status != RECEIVED` 면 반환** — 싸다. 그러나 **동시에 두 이벤트가 들어오면 둘 다 `RECEIVED` 를 읽는다** (check-then-act). D-021 이 이미 같은 문제를 지적했다 — *"상태 검사만으로는 동시에 읽은 경합을 못 막는다"*
  3. **조건부 UPDATE** — `UPDATE inquiries SET status = ? WHERE id = ? AND status = 'RECEIVED'` 를 먼저 치고, **갱신된 행이 0이면 이미 처리된 것**으로 보고 반환한다
- **결정**: **3번.**

  읽기와 판단과 쓰기가 **DB 한 문장 안에서 원자적으로 일어난다.** 별도 락도, 새 제약도, 재시도 루프도 필요 없다.

  > **이건 D-031 에서 잃었던 자산이 돌아온 것이다.** `CLAUDE.md` 의 락·동시성 표는 *"1행뿐인 것은 설계가 단순해서가 아니라 D-031 에서 자산을 잃었기 때문"* 이라고 적어뒀고, 사라진 것 중 하나가 **원자적 UPDATE**(`occurrence_count`)였다. 대상 컬럼이 없어져 수단이 함께 소멸했는데, **같은 수단이 다른 자리에서 필요해졌다.** 표의 2행째가 D-032(선점)보다 먼저 이 결정으로 채워진다.
  >
  > **1번을 안 쓰는 이유를 D-016 이 이미 적어뒀다.** 새 UNIQUE 제약을 들일 때마다 재시도를 트랜잭션 밖으로 빼는 구조가 따라온다. 얻는 것(중복 방지)이 같은데 비용이 훨씬 크다.
- **영향**: `service/ClassificationService.verifyAndPersist` 진입부, `domain/repository/InquiryRepository` 에 조건부 UPDATE 1개. `CLAUDE.md` 락·동시성 표에 **2행 추가**. 측정 2·8ⓐ 의 분모 신뢰성. **스키마 변경 없음**
- **재평가**: 「나중에 할 것」 E(멈춘 문의 재분류)를 도입하면 **`RECEIVED` 에서 `RECEIVED` 로 다시 태우는 경로**가 생겨 이 조건식이 그대로는 안 먹는다. 그때 재분류 전용 상태나 시도 횟수를 함께 설계한다 — **E 를 붙이는 사람이 이 항목을 먼저 읽는다**

### D-050. `PROJECT-BRIEF.md` 는 외부용 요약이고 PRD 에서 한 방향으로만 파생된다

- **일자**: 2026-08-05
- **상태**: 채택
- **배경**: 프로젝트 전체를 한 장에 보여주는 문서(`PROJECT-BRIEF.md`)를 신설했다. 발표·이력서·포트폴리오처럼 **읽는 사람이 팀 밖에 있는 자리**에서 쓰려는 것이다.

  문제는 **PRD 와 내용이 상당 부분 겹친다**는 점이다. 이 팀은 같은 값을 두 곳에 적어 한쪽이 낡는 실패를 이미 여러 번 겪었다.

  | 낡았던 곳 | 무엇이 |
  | --- | --- |
  | `GLOSSARY.md` 「캐시 hit rate」 | *"AI 절감을 만드는 건 그룹핑"* — **그룹핑은 D-031 로 폐기됐는데** 용어집이 아직 절감의 원인으로 설명하고 있었다 |
  | `DECISIONS.md` 「한눈에 보기」 | D-035~D-042 **8건이 표에 없었다.** 표 자체가 *"낡으면 인덱스가 아니라 오답"* 이라고 경고하고 있었는데도 |
  | Sentry 버전 (D-030 이 지적) | `MONITORING.md` / `SENTRY-GUIDE.md` / `build.gradle` **3중 기록** |

  **규칙 없이 두면 브리프도 같은 길을 간다.** 게다가 브리프는 외부용이라 **낡은 채로 발표에 쓰이면 그 자리에서 틀린 말을 하게 된다.**
- **선택지**:
  1. **PRD 의 PAAR 요약(§1-1)과 일정(§8)을 브리프로 옮기고 PRD 에서는 링크만** — 중복이 0 이 된다. 그러나 **PRD 는 팀이 매일 보는 문서라 자족적이어야 한다.** 일정을 보려고 다른 파일을 열게 만들면 안 본다
  2. **브리프를 외부용으로 고정하고, 중복은 인정하되 흐르는 방향을 한쪽으로 묶는다**
  3. 브리프를 만들지 않는다 — 발표 때마다 PRD 를 요약하는 일이 반복된다
- **결정**: **2번.** 세 가지를 고정한다.

  **ⓐ 역할이 다르다.**

  | | 읽는 사람 | 성격 |
  | --- | --- | --- |
  | `PRD.md` · `DECISIONS.md` · `CLAUDE.md` | **팀 안** | 매일 본다. 자족적이어야 한다 |
  | `PROJECT-BRIEF.md` | **팀 밖** (발표·이력서·포트폴리오) | 한 번에 훑는다. 요약이어도 된다 |

  **ⓑ 흐름은 한 방향이다 — 브리프만 고치는 일은 없다.**

  ```text
  새 사실  →  PRD / DECISIONS 에 먼저  →  브리프를 다시 뽑는다
                                    ✗ 반대 방향 없음
  ```

  **브리프에 어떤 사실을 처음 쓰지 않는다.** 브리프를 고치고 싶어지면 그건 PRD 나 DECISIONS 가 낡았다는 신호다.

  **ⓒ 갱신 시점을 못박는다 — 수시로 고치지 않는다.**

  | 언제 | 무엇이 바뀌나 |
  | --- | --- |
  | Week 9 종료 (Day 5) | 측정값이 처음 채워진다. 「지금 상태」와 Result 절 |
  | Week 10 발표 전 | 최종 수치 + 못 낸 측정과 그 이유 |
  | 그 사이 | **고치지 않는다.** PRD 만 고친다 |

  > **브리프 머리에 "요약본이고, 값이 어긋나면 PRD/DECISIONS 가 맞다"를 명시한다.** 낡는 것을 완전히 막을 수는 없으므로, **어느 쪽이 맞는지를 읽는 사람이 알 수 있게** 하는 것이 현실적인 방어다.
- **영향**: `PROJECT-BRIEF.md` 머리에 역할·갱신 규칙 블록. `ONBOARDING.md` 문서 지도. `PRD.md` §1-1 에 역방향 링크 1줄. **코드·스키마 변경 없음**
- **재평가**: Week 10 발표 후 브리프가 실제로 낡아 있었다면 **원인이 ⓑ 위반인지 ⓒ 위반인지 구분해 적는다.** ⓑ 위반(브리프에 먼저 쓴 것)이면 규칙이 안 지켜진 것이고, ⓒ 위반(갱신 시점을 놓친 것)이면 시점 정의가 틀린 것이라 대응이 다르다

### D-051. AI 에게 「생각하기」를 시키지 않는다 — D-024 의 확정값 표에 없던 항목

- **일자**: 2026-08-06
- **상태**: 채택
- **배경**: `AiClassificationService` 를 만들면서 요청에 한 줄을 더 넣어야 했다. 요즘 모델은 답하기 전에 **혼자 생각하는 단계**를 켜고 끌 수 있는데, D-024 의 확정값 표(SDK·모델·키 이름·JSON 강제 방식)에는 이 항목이 없다. 기본값에 맡기면 **아무도 고른 적 없는 설정으로 측정 8ⓐ 가 나온다.**

  D-024 는 모델 티어를 고르면서 이미 같은 종류의 판단을 했다 — *"상위 모델은 분류 정확도가 올라가는 대신 측정 8 이 재려는 표본이 귀해진다"*. 생각하기도 정확도를 올리는 장치라 **같은 저울에 올려야 한다.**
- **선택지**:
  1. **켠다** — 분류가 정확해진다. 대신 D-024 가 모델을 낮춰 확보한 표본을 다시 깎는다
  2. **끈다**
  3. 켠 것과 끈 것을 둘 다 재서 비교한다 — 5일 범위 밖이다. 정답 50건을 두 번 돌려야 하고, 그 비교 자체가 별도 측정이 된다
- **결정**: **2번, 끈다.** 이유는 두 가지이고 **두 번째가 결정적이다.**

  | | 이유 |
  | --- | --- |
  | 1 | 답이 `{"category": ..., "confidence": ...}` **한 줄**이라 길게 생각할 것이 없다. 생각한 만큼 토큰과 시간을 쓰고, 그 시간 동안 분류 담당 스레드가 묶인다 (D-047) |
  | 2 | **생각을 시키면 이 프로젝트가 재려는 표본이 사라진다.** 측정 8ⓐ 는 *"AI 가 확신하면서 틀린 비율"* 인데, 정확해질수록 그 사례가 귀해진다 |

  **2번을 다시 풀어 쓰면**: 이 시스템은 AI 를 잘 쓰는 법을 보여주는 것이 목적이 아니라, **AI 가 실패하는 방식을 수치로 관찰하는 것**이 목적이다. 실패를 관찰하겠다고 선언한 시스템이 실패를 줄이는 장치를 먼저 켜면 앞뒤가 맞지 않는다. structured outputs 를 쓰지 않기로 한 D-024 의 판단과 **정확히 같은 논리**다.

  > ⚠️ **이건 「분류를 잘하게 만들지 않겠다」는 뜻이 아니다.** 정확도를 올리는 다른 수단(프롬프트의 경계 규칙, 모델 교체)은 열려 있다. 다만 **정확도를 올리는 선택은 표본을 깎는 대가와 함께 기록한다**는 것이 D-024 부터 이어지는 이 팀의 방식이다.
- **영향**:
  - `service/AiClassificationService` — 요청에 「생각하기 끔」을 명시. 기본값에 맡기지 않는다
  - **측정 결과를 `evidence/` 에 적을 때 이 설정을 모델 id 와 함께 적는다.** 안 적으면 8ⓐ 의 숫자가 어느 조건에서 나온 것인지 사후에 알 수 없다 (D-024 의 「모델 id 를 함께 기록한다」와 같은 이유)
  - `DECISIONS.md` 인덱스 표의 결정 개수 50 → 51
- **재평가**:
  - 측정 1 의 정답 일치율이 **너무 높아 8ⓐ-1 이 0 으로 나오면** 표본이 없어 결론을 못 낸다. 그때는 생각하기가 아니라 **정답 50건의 경계 난이도**를 먼저 의심한다 — 경계 9건이 실제로 어려운지 `evidence/` 로 확인한 뒤에 다른 손을 댄다
  - 반대로 **`CLASSIFY_FAILED` 가 10% 를 넘고 재시도로 회수되지 않으면** (D-022·D-024 와 같은 조건) 프롬프트를 먼저 고치고, 그래도 안 되면 그때 생각하기를 켜는 것을 실측 근거와 함께 다시 올린다

### D-052. 정규화 키는 정규화·마스킹 문자열의 SHA-256 hex — 절단이 아니라 해시

- **일자**: 2026-08-06
- **상태**: 채택 (5일 범위 **안**)
- **배경**: `NormalizedKeyGenerator`(TRI-39) 를 만들면서 결정이 하나 비어 있었다. `normalized_key` 컬럼은 `V2` 에서 `VARCHAR(64) NOT NULL` 로 정해졌는데, **정규화·마스킹을 거친 문자열을 그 64자에 어떻게 담는지**가 어느 문서에도 없다. 문의 본문은 최대 2000자라 정규화한 뒤에도 64자를 쉽게 넘는다.
- **선택지**:
  1. **정규화 문자열을 그대로 저장하고 64자 초과 시 절단** — 키가 사람 눈에 읽혀 디버깅이 쉽다. 대신 앞 64자가 같고 뒤가 다른 두 문의가 **같은 키로 병합**된다(과도 병합). 과도 병합은 이 시스템에서 가장 위험한 실패다 — 틀린 분류가 조용히 재사용된다
  2. **정규화·마스킹 문자열의 SHA-256 hex(64자)** — 길이에 무관하게 항상 64자라 컬럼에 정확히 맞고, 내용이 조금이라도 다르면 키가 완전히 달라져 절단발 과도 병합이 없다. 대신 키만 봐선 내용을 알 수 없다
- **결정**: **2번, 해시.**

  절단(1번)을 버리는 이유가 이 항목의 핵심이다. `PRD.md` §5-4 와 TRI-39 는 정규화가 잘못되는 두 방향 중 **과도 병합(서로 다른 문의가 같은 키)** 을 "더 위험하다"고 못박았다 — 틀린 분류가 재사용되며 조용히 퍼지기 때문이다. 절단은 정확히 그 실패를 **길이 상한이라는 우연한 이유로** 만들어낸다. 해시는 그 경로를 없앤다.

  **키가 안 읽히는 대가는 받아들인다.** 키는 조회용 식별자일 뿐 사람이 읽을 대상이 아니고(계약 C 도 캐시 값에 `inquiryId`·모델명을 담지 않는다 — 오해를 막으려고), 디버깅이 필요하면 원문을 대조한다.

  **해시는 정규화·마스킹을 거친 문자열에 건다 — 원문이 아니다.** 그래야 대소문자·연속 공백·문장부호·개인정보 값만 다른 문의가 같은 키로 병합된다(과소 병합 방지). 마스킹을 정규화 파이프라인에 넣는 이유가 이것이다.
- **영향**:
  - `service/NormalizedKeyGenerator` — 소문자화 → 마스킹 → 문장부호·공백 정리 → SHA-256 hex. 코드 소유: P1(이용택)
  - **스키마 변경 없음.** `normalized_key VARCHAR(64)` 가 64자 hex 를 그대로 담는다
  - `Inquiry.receive(...)` 의 `normalizedKey` 인자가 이 값을 받는다 (TRI-31)
- **재평가**: 측정 6(AI 절감)에서 절감이 기대보다 낮고 원인이 **키가 너무 안 겹치는 것**(과소 병합)으로 지목되면, 그건 해시 vs 절단의 문제가 아니라 **마스킹·정규화 강도**의 문제다(D-031 재평가 조항). 해시는 유지하고 정규화 규칙을 조인다.

### D-053. 정규화 키의 두 병합 결함 정정 — 키 전용 토큰 + 유니코드 접기

- **일자**: 2026-08-06
- **상태**: 채택 (5일 범위 **안**)
- **배경**: PR #18(마스킹·정규화 키)에 AI 리뷰 둘이 붙었고, 둘 다 `NormalizedKeyGenerator`의 문장부호 정리 단계를 **서로 반대 방향의 병합 결함**으로 지적했다. 둘 다 Java 실측으로 사실 확인됨.
  - **과도 병합 (Claude)**: 표시용 토큰 `[주문번호]`가 정리 단계에서 대괄호를 잃고 `주문번호`가 되어, 고객이 "주문번호"라는 낱말만 쓴 문의와 **실제 주문번호가 든 문의가 같은 키**가 된다. 과도 병합은 틀린 분류가 조용히 재사용되는 **가장 위험한 방향**이다.
  - **과소 병합 (CodeRabbit)**: 정리 정규식 `[\p{Punct}\s]+`가 ASCII만 잡아 전각 문장부호(`！` U+FF01)·전각 공백(U+3000)이 남는다. 전각으로 쓴 문의와 반각으로 쓴 같은 문의가 **다른 키**가 된다.
- **결정**: 둘 다 정정한다.
  1. **키 전용 토큰 도입.** `ContentMasker`가 표시/AI 경로에는 `[주문번호]`(사람이 읽어야 함)를, 키 경로에는 자연어와 겹치지 않는 영문 센티넬(`ordertoken` 등)을 쓴다. **탐지 규칙·순서는 한 벌로 공유**하고 치환 토큰만 다르다 — "무엇이 개인정보인가"는 여전히 한 곳에서 정의된다.
  2. **유니코드 범주로 접기.** 정리 정규식을 `[\p{P}\p{Z}\s]+`로 바꿔 전각 문장부호·유니코드 공백까지 접는다.
- **D-040 과의 관계 (중요)**: D-040 이 막는 것은 **두 출력 경로**(AI 전송·화면)가 다른 마스킹을 쓰는 것이다. 정규화 키는 밖으로 나가지 않는 **세 번째 내부 용도**(해시 입력)라, 키 경로가 다른 토큰을 써도 D-040 을 깨지 않는다. 탐지 로직을 공유하므로 "한쪽만 조여지는" 위험도 없다.
- **영향**:
  - `service/ContentMasker` — 탐지 규칙을 `RULES` 리스트 한 벌로 모으고 `mask`(표시)·`maskForKey`(키) 두 진입점이 공유. 코드 소유: P1(이용택)
  - `service/NormalizedKeyGenerator` — 정리 정규식 유니코드화 + `maskForKey` 사용. `generate(null)` 은 명확한 메시지로 거부
  - `NormalizedKeyGeneratorTest` — 회귀 3건 추가(토큰 충돌 / 전각 문장부호·공백 / null). **스키마 변경 없음**
  - D-052 는 유지된다 — 이 항목은 해시 이전 단계(정규화)를 고친 것이다
- **재평가**: 마스킹 강도 전반은 여전히 측정 6 결과를 보고 조인다(D-031 재평가 조항). 이 정정은 그 조정 전에 드러난 **명백한 병합 결함**이라 먼저 고쳤다.

> **정정 (같은 PR, CodeRabbit 재리뷰 반영) — 영문 센티넬은 불충분, 예약 마커로 「토큰 위조 불가」를 보장.**
>
> 위 결정 1의 "영문 센티넬(`ordertoken` 등)"은 충돌을 **없앤 게 아니라 옮긴 것**이었다. 고객이 `ordertoken`이라고 그대로 치면 실제 주문번호 문의와 여전히 같은 키가 된다(실측):
> - 수정 전: `"환불해주세요 20260801-773412"` 와 `"환불해주세요 ordertoken"` → 둘 다 `f0e167583460…` (**충돌**)
>
> **예약 마커 U+E000(Private Use Area)로 키 토큰을 감싸고, `maskForKey`가 입력에서 그 문자를 먼저 제거한다.** 그러면 마커로 감싼 키 토큰은 이 마스킹만 만들 수 있어, 어떤 자연어 입력도 그 토큰을 **재현(위조)할 수 없다.** 마커는 정리 정규식 `[\p{P}\p{Z}\s]+`의 어느 범주도 아니라 해시 입력까지 살아남는다. 실측(전체 해시·재현법은 `evidence/normalized-key-forgery-check.md`):
> - 주문번호 `…773412` vs `…119203` → 둘 다 `e09d926b58db…` (**정상 병합 유지**)
> - 낱말 `ordertoken` → `f0e167583460…` (**충돌 닫힘**)
> - 마커를 직접 붙여넣은 위조 입력 → 실제 주문번호와 **다른 키** (**위조 불가**)
>
> **「단사(injective)」라고 쓰지 않는다 (CodeRabbit 지적, 정정).** 입력에서 마커를 지우는 것은 손실이라, `"문의"` 와 (사용자가 못 치는) `"문<U+E000>의"` 는 같은 키가 된다 — 함수 전체로는 단사가 아니다. **필요하고 성립하는 보증은 「생성 토큰을 자연어로 위조할 수 없다」이지 「단사」가 아니다.** 이 손실은 수용한다 — 실제 문의엔 U+E000 이 안 들어와 현실 트래픽의 과도 병합을 만들지 않는다. 엄밀 단사가 필요하면 마커 이스케이프(2배화)를 쓰면 되지만, 현실에 없는 입력만 막자고 복잡도를 얹지 않는다(D-018 계보).
>
> **이 정정이 닫는 것과 못 닫는 것 (경계 명시):**
> - ✅ **닫힘**: 키 토큰을 자연어 입력으로 위조해 생기는 과도 병합. 구조적(확률 아님)으로 불가능해졌다.
> - ❌ **별개(측정 6 소관)**: 마스킹이 뜻까지 지워 생기는 과도 병합 — 예를 들어 금액이 구별점인데 둘 다 `[금액]`으로 병합되는 경우. 이건 토큰 버그가 아니라 마스킹 강도 문제라 측정 6에서 조인다(D-031 재평가).
> - ⚪ **수용(비현실 입력)**: 입력에 U+E000 이 든 경우의 정보 손실. 실제 문의엔 없다.
> - ⚪ **무시 가능**: 서로 다른 정규화 문자열의 SHA-256 해시 충돌(2⁻²⁵⁶).
>
> **영향 추가**: `ContentMasker` 에 `KEY_MARK`(U+E000) 도입, `maskForKey` 가 입력의 마커를 선제거. 위조 가드는 소문자화가 끼지 않는 `maskForKey` 수준에서 검사한다(`ContentMaskerTest.maskForKeyStripsReservedMarkerFromInput`) — `generate` 수준에서는 대소문자 차이가 strip 결함을 가려 회귀를 못 잡기 때문이다. `NormalizedKeyGeneratorTest` 회귀(낱말 충돌 / 전각 문장부호·공백 / 대문자 접힘 / null). 스키마 변경 없음.

### D-054. 리뷰 단계에 CodeRabbit 을 두 번째 AI 리뷰 도구로 추가 — `ai-review.yml` 이 놓치는 지점 보강

- **일자**: 2026-08-05
- **상태**: 채택
- **배경**: 리뷰 단계는 `ai-review.yml`(Claude, GitHub Actions 커스텀 스크립트)로 이미 동작 중이다(PR #1~#11, `evidence/failure-cases.md` 16건 기록). 다만 이 워크플로우는 구조적 한계가 있다 — (1) diff 전체를 텍스트로 모델에 보내 PR 전체에 **댓글 1개**로만 응답하고 라인 단위 인라인 코멘트가 없다, (2) LLM 추론에만 의존해 실제 정적분석 도구(린터류) 없이 코드를 본다, (3) push 마다 diff 를 처음부터 다시 보내 **커밋 단위 증분 리뷰가 없다**, (4) 댓글에 대화형으로 후속 질문·수정 요청이 불가능하다(1회성 서술). CodeRabbit 은 이 네 가지를 상호 보완한다 — 라인별 인라인 코멘트(커밋 가능한 suggested diff), 정적분석 도구 병행, 커밋 단위 증분 리뷰, 댓글 스레드 대화, 경로별 커스텀 규칙(`path_instructions`)까지 제공한다.
- **선택지**:
  1. `ai-review.yml` 유지, 추가 도구 없음 — 중복 코멘트 노이즈가 없다. 다만 위 네 가지 갭은 그대로 남는다
  2. `ai-review.yml` 을 CodeRabbit 으로 **교체** — 부트캠프 채점 기준(요구사항 충족도/구조·가독성/기술 포인트/검증 근거/설명력)에 맞춘 커스텀 프롬프트와 팀 소유 Anthropic 키 통제를 포기해야 한다
  3. **둘 다 병행** — PR 마다 리뷰 코멘트가 2개씩 달리는 노이즈가 생기지만, 두 도구가 잡는 지점이 겹치지 않는다
- **결정**: 3번. `ai-review.yml` 은 설계·계약 수준 결함(D-010, D-012, D-016, D-021 등 실제 사례)에 강했고, CodeRabbit 은 라인 단위 실수(정적분석형 결함)와 커밋 단위 대화형 수정에 강점이 있어 겹치는 영역보다 서로 못 잡는 영역이 더 크다고 판단했다.
- **영향**:
  - `.coderabbit.yaml` 추가 — 리뷰 언어 한국어, `domain/`·`api/`·`service/` 경로별로 CLAUDE.md 핵심 불변 규칙(setter 금지, blind 규칙 D-010, `@Transactional` 위치) 체크 지시 포함
  - `path_instructions` 는 D-031 도메인 전환 이후 기준(`Inquiry`/`InquiryClassificationResult`/`InquiryReviewQueueItem`, `GET /api/inquiry-review-queue`)으로 작성했다 — 최초 초안이 전환 이전 용어(`ErrorGroup`/`ClassificationResult`/`GET /api/review-queue`)를 썼던 것을 같은 날 바로잡았다
  - GitHub App 설치 **완료** — 리포지토리 access 는 이 레포로 한정(조직 전체 아님). 설치 이후 PR 부터 `.coderabbit.yaml` 기준으로 리뷰가 실제 동작한다
  - `CLAUDE.md` 「라이프사이클 단계 책임자」 표의 리뷰 도구 칸에 CodeRabbit 추가
  - `path_instructions` 의 `service/` 항목에 D-034(AI 응답 검증 순서·clamp 금지)·D-036(캐시 덮어쓰기 방향)·D-030(Sentry 캡처) 체크 지시 추가 — `service/` 착수 전에 세부 규칙을 먼저 걸어둔다
  - `reviews.profile: "chill"` (`assertive` 대신) 선택 — 이미 `ai-review.yml` 이 설계·계약 수준 결함을 훑고 있어, CodeRabbit 까지 `assertive` 로 두면 PR 마다 코멘트가 나노 단위 지적으로 늘어나 "코멘트 2개" 노이즈가 더 커진다. `chill` 로 유의미한 지적만 남겨 두 도구의 병행 비용을 억제한다
  - **검증 예정**: `service/`·`api/` PR 이 열려 CodeRabbit 이 실제 코멘트를 달면 해당 PR 링크·코멘트 캡처를 `evidence/` 에 추가하고 여기 링크를 남긴다 — 지금까지는 "설치 완료·병행 동작 중"이 설정 완료 사실일 뿐, 실제 코멘트 결과물로 검증되지는 않았다
- **재평가**: 도메인이 다시 바뀌거나 엔티티·엔드포인트 이름이 리네이밍되면 `path_instructions` 도 함께 갱신한다. 실제 PR에서 CodeRabbit 이 `ai-review.yml` 과 중복되는 지적만 반복하고 고유 가치가 관측되지 않으면 도구를 하나로 축소한다. **판단은 위 중복/고유 집계가 나온 뒤에 내린다** — 지금은 가설 단계다.

### D-055. 권한은 통과했는데 컨트롤러가 없는 요청이 404 대신 401/500 으로 가려짐 — 계층마다 따로 막아야 했다

- **일자**: 2026-08-06
- **상태**: 채택
- **배경**: TRI-25/26(권한 매핑) 완료 조건을 도커에 직접 띄워 확인하던 중, 권한은 맞게 통과했는데 아직 안 만든 API(`/api/stats` 등)에 요청을 보내면 **404 가 아니라 401** 이 나오는 걸 발견했다. 원인은 서블릿 컨테이너가 "핸들러 없음"을 처리하려고 내부적으로 `/error` 로 요청을 다시 보내는데(재디스패치), `/error` 가 `SecurityConfig` 매핑표에 없어서 `anyRequest().authenticated()` 에 걸려 익명 처리되고 401 이 나갔다. 이걸 고치자(`/error` permitAll), 이번엔 같은 상황이 **500** 으로 다시 가려졌다 — `GlobalExceptionHandler`(TRI-29)를 추가하고 나니, Spring 6.1 이 핸들러 없음을 던지는 `NoResourceFoundException` 을 공용 예외 처리기의 catch-all(`Exception.class`)이 그대로 삼켰기 때문이다.
- **선택지**:
  1. 그때그때 발견되는 대로 개별 처리 — 계층이 늘어날 때마다 따로 막는다
  2. "핸들러 없음"이 어느 계층에서 나든 최종적으로 404 로 수렴하도록 **두 지점 모두** 명시적으로 처리한다: ⓐ `SecurityConfig` 에 `/error` permitAll ⓑ `GlobalExceptionHandler` 에 `NoResourceFoundException` → 404
- **결정**: 2번. 서블릿 레벨 재디스패치와 MVC 예외 처리기, **두 계층이 독립적으로** "핸들러 없음"을 가릴 수 있다는 걸 같은 날 두 번 실제로 겪었다. 한쪽만 고치면 다른 쪽에서 재발한다.
- **영향**:
  - `config/SecurityConfig.java` — `/error` permitAll 추가
  - `api/GlobalExceptionHandler.java` — `NoResourceFoundException` 을 `EntityNotFoundException`·`NoSuchElementException` 과 함께 404 로 매핑
  - `config/SecurityErrorDispatchTest.java` 신설 — `@WebMvcTest` 슬라이스는 이 재디스패치를 재현하지 않는다(확인됨). `@SpringBootTest(webEnvironment=RANDOM_PORT)` 로 실제 서블릿 컨테이너를 띄워야 잡힌다
- **재평가**: 요청 처리 파이프라인에 새 계층(API 게이트웨이, 추가 필터 등)이 생기면 "핸들러 없음"이 거기서도 404 로 나가는지 같은 방식(실제 컨테이너 기동 + 실측)으로 재확인한다.

### D-056. 테스트 전용 더미 컨트롤러가 통합 테스트 컨텍스트에 실제 빈으로 섞여 들어감 — `@Profile("!test")` 로 격리

- **일자**: 2026-08-06
- **상태**: 채택
- **배경**: `@WebMvcTest` 슬라이스 테스트(`SecurityConfigTest`·`SecurityAccessTest`·`GlobalExceptionHandlerTest`)는 아직 없는 진짜 컨트롤러 대신 테스트 전용 더미 컨트롤러(`ProbeController`·`BoomController`)를 쓴다. 이 더미들이 메인 애플리케이션과 같은 패키지(`com.dingco.triage.*`) 아래 테스트 소스에 있는데, 전체 컨텍스트를 띄우는 `@SpringBootTest`(예: D-055 의 `SecurityErrorDispatchTest`)는 기본 컴포넌트 스캔으로 이 패키지 전체를 훑는다. 그 결과 더미 컨트롤러가 **실제 빈으로 등록**되어 "컨트롤러가 없어야 할 경로"가 더미의 응답(200 `"ok"`)을 돌려주고, D-055 를 검증하려던 통합 테스트가 실제 앱과 다른 결과를 냈다(재현 확인).
- **선택지**:
  1. 통합 테스트가 있는 한 더미 컨트롤러를 아예 안 만든다 — `@WebMvcTest` 슬라이스도 다른 방식(예: `standaloneSetup`)으로 다시 써야 해서 기존 테스트 다수가 영향받는다
  2. 더미 컨트롤러에 `@Profile("!test")` 를 붙인다 — `@ActiveProfiles("test")` 를 쓰는 통합 테스트에서는 빠지고, profile 을 안 켜는 `@WebMvcTest` 슬라이스에서는 그대로 산다
- **결정**: 2번. 한 줄 애너테이션으로 기존 슬라이스 테스트를 하나도 안 건드리고 격리된다.
- **영향**:
  - `config/ProbeController.java`, `api/BoomController.java` 에 `@Profile("!test")` 추가
  - 두 더미 모두 **최상위 클래스**로 둔다 — 테스트 클래스 안 중첩 클래스로 두면 `RequestMappingHandlerMapping` 이 등록 자체를 못 찾는 별개 문제가 있다(같은 세션에서 두 번 재현). 다음에 더미 컨트롤러를 또 만들 때 같은 시행착오를 반복하지 않도록 여기 남긴다
- **재평가**: P1/P2 가 진짜 컨트롤러를 만들면 두 더미 파일은 삭제 대상이다. 그 전까지 새 슬라이스 테스트를 추가할 때마다 이 패턴(최상위 클래스 + `@Profile("!test")`)을 따른다.

### D-057. 분류 담당 클래스는 `AiClassifyWorker` 가 아니라 `service/event/InquiryReceivedEventListener` — 이름에 "누가 부르나"를 담는다

- **일자**: 2026-08-07
- **상태**: 채택
- **배경**: `service/` 안에 `Worker` 로 끝나는 파일이 하나만 있어 계층이 눈에 안 들어온다는 지적에서 시작했다. 처음 나온 안은 **`Service` 로 통일**이었는데, 그러면 같은 패키지에 `AiClassificationService`(AI 를 실제로 부르는 것) · `AiClassifyService`(새 이름) · `ClassificationService`(트랜잭션 ②) **셋이 한 글자 차이로 늘어선다.** 자동완성에서 잘못 고르면 **비동기 진입점을 직접 호출**하게 되고, 그러면 트랜잭션 ①과 ②가 한 덩어리로 붙어 D-031 이 금지한 상태(② 실패 시 고객이 받은 접수 확인이 롤백됨)가 된다.
- **선택지**:
  1. `AiClassifyWorker` 유지 — 비용 0. 대신 계약 A 의 짝(`InquiryReceivedEvent` ↔ 받는 쪽)이 이름으로 안 이어지고, `Service` 3형제와의 구분이 접미사 하나에만 걸린다
  2. `AiClassifyService` 로 통일 — **위의 3중 충돌.** 기각
  3. `service/event/InquiryReceivedEventListener` 로 이동·개명 — 발행자(P1)와 수신자(P2)의 이름이 대칭이 되고, `Listener` 라는 이름 자체가 "직접 부르는 것이 아니다"를 말한다. 이벤트와 리스너가 한 디렉토리에 모인다
  4. 절충 `service/event/AiClassifyListener` — 패키지는 옮기되 "AI 분류"를 이름에 남김
- **결정**: 3번. **접미사 규약의 목적은 "이름만 보고 어떻게 쓰는 물건인지 알게 하는 것"이고, 여기서 정작 중요한 정보는 "무엇을 하나"가 아니라 "누가 부르나"다.** 이 클래스를 잘못 쓰는 방법은 하나뿐인데(직접 호출), 그 하나를 이름이 막아준다. `Controller` 가 HTTP 진입점이라 `service/` 밖에 있는 것과 같은 층위다.
  - 4번을 안 고른 이유: 계약 A 대칭이 이 변경의 가장 큰 이득인데 절충안은 그걸 절반만 얻는다. 잃는 정보("AI 분류를 한다")는 **클래스 주석으로 메운다** — 이름이 못 담는 것을 주석이 담는 것은 정상적인 분업이고, 이름이 담을 수 있는 자리를 주석에 미루는 것과는 다르다.
- **영향**:
  - **코드 수정 없음.** TRI-47 이 미착수라 이 클래스는 아직 0줄이다. **바꾸려면 지금이 유일하게 싼 시점**이고, 짜고 난 뒤면 코드 + 테스트까지 따라온다
  - **TRI-47 착수 조건**: 클래스 최상단 javadoc 에 ⓐ 이게 `InquiryReceivedEvent` 를 받는 자리라는 것 ⓑ **하는 일은 AI 분류 호출 + 트랜잭션 ② 위임**이라는 것 ⓒ **직접 호출 금지 — 부르면 ①②가 한 트랜잭션으로 붙는다(D-031)** 세 가지를 적는다. 이름이 잃은 정보를 여기서 되찾는다
  - 문서 7곳: `CLAUDE.md` 6 공통 필수 기능 매핑 · `PRD.md` 5-0 표와 5-5 본문 · `GLOSSARY.md` 「분류 담당」 항목 2곳 · `.claude/agents/` 2개 파일
  - Jira **TRI-47** 제목
  - **`DECISIONS.md` 684행·965행의 「워커」 표기는 고치지 않는다** (본문 불변 규칙). 대신 `GLOSSARY.md` 에 **「분류 담당」 = 옛 기록의 「워커」 = 지금의 「리스너」** 3중 대응 표를 넣었다. 이 겹이 늘어난 것이 이번 변경의 대가다
- **재평가**: **같은 `InquiryReceivedEvent` 를 받는 리스너가 둘 이상 생기면 이 이름으로는 구분이 안 된다.** 그때는 하는 일을 이름에 되돌린다(예: `InquiryClassifyListener`). 「나중에 할 것」 E(멈춘 문의 자동 재분류)가 붙을 때가 가장 유력한 시점이다.

<!-- 다음 결정 추가 시 D-058 부터 -->
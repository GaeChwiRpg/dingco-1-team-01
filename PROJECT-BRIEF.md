# AI 문의 분류 검증 파이프라인: 자동으로 확정된 결과를 다시 의심하는 시스템

> AI가 **확신하면서 틀린 분류**는 어떻게 찾아낼 것인가?
> 확신도가 낮은 것을 거르는 것만으로는 이 실패를 절대 못 잡는다. 그래서 **자동으로 통과한 것 중 일부를 몰래 뽑아 사람이 다시 본다.**

**한 줄 소개** — 고객이 남긴 문의를 AI가 자동 분류하되, ① AI가 확신하지 못한 건은 사람에게 넘기고 ② **확신한 건도 20건에 1건을 뽑아 사람이 다시 확인한다.** 그리고 AI 답과 사람 답이 다른 비율을 숫자로 낸다.

---

### 이 문서를 읽는 법 · 고치는 법 (D-050)

**이 문서는 팀 밖에 보여주려고 만든 한 장 요약이다.** 발표·이력서·포트폴리오에서 쓴다. 팀이 매일 보는 문서는 따로 있다.

| 궁금한 것 | 어디를 보나 |
| --- | --- |
| 무엇을 만드는지 (자세히) | [`PRD.md`](./PRD.md) |
| 왜 그렇게 정했는지 | [`DECISIONS.md`](./DECISIONS.md) |
| API 규격 | [`API-CONTRACT.md`](./API-CONTRACT.md) |
| 코딩 규칙 | [`CLAUDE.md`](./CLAUDE.md) |
| 모르는 단어 | [`GLOSSARY.md`](./GLOSSARY.md) |

> ⚠️ **값이 어긋나면 위 문서들이 맞다.** 이건 요약본이다.

**고칠 때 지킬 것 두 가지**

1. **이 문서를 먼저 고치지 않는다.** 새로 정한 것은 `PRD.md` 나 `DECISIONS.md` 에 먼저 쓰고, 그다음 여기를 다시 뽑는다. **여기를 고치고 싶어졌다면 그건 저쪽이 낡았다는 신호다**
2. **정해진 때만 고친다** — ⓐ Week 9 종료(측정값이 처음 채워질 때) ⓑ Week 10 발표 전. 그 사이에는 손대지 않는다

> **성능·정확도 수치는 아직 하나도 없다.** 코드가 `service/`·`api/` 미착수 상태이고, 아래 「Result 측정 계획」은 **무엇을 어떻게 잴지 정해둔 것**이다. AI가 추정한 숫자는 쓰지 않는다.

---

## 1. 왜 이 프로젝트인가

### 문제

AI에게 분류를 시키면 답은 준다. 그런데 **그 답이 맞는지 아무도 확인하지 않는다.** 문제는 두 겹이다.

| | 실패 | 확신도 필터로 잡히나 |
| --- | --- | --- |
| 1 | AI가 자신 없어 하는 답이 그대로 저장된다 | ✅ 잡힌다 |
| 2 | **AI가 확신하면서 틀린다** | ❌ **절대 안 잡힌다** — 확신도가 높으니까 |

2번이 이 프로젝트가 붙잡은 문제다. AI가 스스로 매긴 확신도는 **시험 본 사람이 자기 점수를 매기는 것**과 같아서, 그 값이 맞는다는 보장이 어디에도 없다.

### 왜 이 주제인가 — AI가 곁가지가 아니라 주제 자체다

추천 주제 5개(운영 티켓 / 커머스 클레임 / 채용 파이프라인 / 콘텐츠 검수 / 의료 예약)는 도메인이 안전하지만 **AI 기능이 "요약 붙이기" 수준에 머무르기 쉽다.** 6개 필수 기능 중 AI 항목만 깊이가 얕아진다.

이 주제는 반대다. **"AI 결과를 검증한다"가 주제이므로 트랜잭션·비동기·캐시가 자연스럽게 필요해진다.** 억지 매핑이 없다.

이력서에서도 **"AI를 썼다"가 아니라 "AI를 못 믿는 설계를 했다"** 로 말할 수 있다. (D-001)

### 왜 CS 문의 도메인인가 — 기준을 세우고 그 기준으로 골랐다

도메인 교체 논의가 반복되는 것을 막으려고 **판별 기준 3개**를 먼저 못박았다 (D-027).

| | 기준 | CS 문의 |
| --- | --- | --- |
| 1 | **묶음 동질성** — N건을 묶어도 개별 건이 사라지지 않는가 | ❌ **탈락** |
| 2 | **정답 단일성** — 팀이 확정한 정답이 유일한가 | ✅ (§7 경계표로 상호배타 정의) |
| 3 | **AI 존재 이유** — 규칙·룩업으로 풀리지 않는가 | ✅ (자연어 문의) |

**기준 1에서 탈락한 것을 인정하고 「묶기(그룹핑)」를 포기했다** (D-031). 문의는 각자 다른 주문·다른 사정이라 묶으면 개별 건이 묶음 판정에 덮이는데, 그건 **사람이 잡아낼 기회를 시스템이 없애는 것**이라 주제와 정면으로 어긋난다.

기준을 세운 값어치가 여기 있다 — **탈락 사실과 그 대가를 정확히 계산할 수 있었다.** 잃은 것은 감추지 않고 기록했다.

| 잃은 것 | 상태 |
| --- | --- |
| 동시성 수단 3개 → 1개 | 원자적 UPDATE·UNIQUE 충돌 재시도 소멸. **가장 큰 학습 자산 손실**<br>단 원자적 UPDATE는 D-049에서 **다른 자리에 다시 필요해져 돌아왔다** |
| 인덱스 쓰기비용 A/B 실험 | 대상 컬럼 소멸로 폐기. 논거만 남음 |
| 「캐시」 필수 기능의 정당화 | **2단 절감 경로**가 대신 떠받친다 — 아직 검증 안 됨(측정 6) |

### 이 프로젝트의 차별점

레퍼런스 설계는 **"확신도 낮으면 검토 큐"** 까지다. 여기서 한 칸 더 갔다.

```text
1차 방어  확신도 < 0.8   → 사람에게       ← 여기까지는 누구나 만든다
2차 방어  자동 확정된 것 중 5%를 몰래 뽑아 사람이 다시 봄   ← 이 프로젝트의 차별점
         + 재사용해서 확정된 것도 5%
```

**"AI를 믿지 않는다"가 아니라 "자동으로 확정된 것을 믿지 않는다"** 가 정확한 주제다. 사람이 확정한 답이라도 그것이 다른 문의로 자동 전파되는 순간 같은 검증이 필요하다 — 오히려 *"사람이 정했다"* 는 사실이 신뢰의 근거가 되어 아무도 의심하지 않기 때문에 더 위험하다. (D-005, D-033)

### 커리큘럼 연결

Spring API · JPA 조회 · DB 인덱스 · 트랜잭션·동시성 · 비동기·재시도 · AI 검증 · 배포·관측

---

## 2. 2주 MVP 범위

### 만드는 것

**0단계 — 코드보다 먼저 (사람 손으로)**

- 정답이 붙은 문의 **50건** (10종 × 5건). §7 경계표의 헷갈리는 경계 **9개를 각각 최소 1건**
- **정답 레이블은 무조건 사람이 붙인다.** AI가 쓴 문장으로 AI를 평가하면 결과가 실제보다 좋게 나온다
- **두 사람이 독립으로 정답을 붙이고 일치율을 잰다** — 이 값이 측정 12의 예비값이다 (D-046)

**1단계 — 결론이 나오는 최소 경로 (최우선)**

| | 만들 것 | 담당 |
| --- | --- | --- |
| 1 | 정규화 키 + 개인정보 가리기 (DB 없이 단독 테스트되는 조각) | 이용택 (P1) |
| 2 | `POST /api/inquiries` + 트랜잭션 ① + 접수 이벤트 발행 | 이용택 (P1) |
| 3 | 역할 3개 차단 + 오류 응답을 한 곳에서 만드는 자리 | 김준현 (P2) |
| 4 | AI 호출(Anthropic) + **응답 값 검증 4가지** + 재시도 3회 + 실패 시 큐 | 김준현 (P2) |
| 5 | 트랜잭션 ② — 기준값 비교 + 상태 전이 + **감사 5% 표본** + 큐 삽입 | 김준현 (P2) |
| 6 | `GET /api/inquiry-review-queue`(가린 채) + `PATCH`(트랜잭션 ③) + 동시 확정 409 2종 | 김은빈 (P3) |
| 7 | `GET /api/stats` — **여기서 결론 숫자가 나온다** | 김은빈 (P3) |

**2단계 — AI 호출 아끼기 (1단계 관통 후)**

- 1단 캐시(정규화 키) → 2단 DB 조회(**쿼리 두 번**) → 없으면 AI 호출
- **사람이 확정한 답을 먼저 찾는다** (1순위). 없을 때만 AI 자동 확정 답 (2순위)
- **재사용해서 만든 답은 다시 재사용하지 않는다** — 원본이 틀렸을 때 몇 건이 퍼졌는지 한 번에 못 세게 된다
- 사람이 정정하면 저장해둔 값도 덮어쓴다 (사람 답 → AI 답 한 방향)

**3단계 — 보이게 만들기와 속도**

- Actuator 지표: 분류 성공률, 밀린 검토 건수, **멈춘 문의 수(`stuckReceived`)**
- 인덱스 적용 전후 `EXPLAIN`, 목록 조회 추가 쿼리 제거, 10만 건에서 응답 시간

**필수 기능마다 붙이는 실패 대응 5개 (D-045)**

| 기능 | 붙이는 것 |
| --- | --- |
| 권한 | 소유자 인자 없이 문의를 조회하는 함수를 **아예 만들지 않는다** |
| 트랜잭션 | 감사 삽입을 밖으로 빼본 **대조 실험** — 표본이 몇 건 새는지 센다 |
| 검색 | **깊은 페이지**의 `EXPLAIN` (1페이지 vs 500페이지) |
| 캐시 | 키에 규칙 버전 붙이기 + **캐시 서버가 죽어도 요청이 실패하지 않게** |
| 비동기 | 스레드풀 명시 + **대기줄이 찼을 때 버리지 않는다** |

### 데이터베이스 표 3개

```text
inquiries                     고객이 남긴 문의. 분류의 단위이고 상태를 여기서 관리
inquiry_classification_result AI가 낸 답 + 사람이 고친 답. 둘 다 남긴다 (덮어쓰기 금지)
inquiry_review_queue          사람이 봐야 할 목록
```

### API

| | 누가 쓰나 |
| --- | --- |
| `POST /api/inquiries` | 고객 — AI를 안 기다리고 즉시 접수 확인 |
| `GET /api/inquiries` · `/{id}` | 고객(자기 것만) / 상담원 / 매니저 |
| `GET /api/inquiry-review-queue` | 상담원 — **오래된 순**, 감사 여부는 가림 |
| `PATCH /api/inquiry-review-queue/{id}` | 상담원 — 최종 분류 확정 |
| `GET /api/policies` | 매니저 — 읽기 전용 |
| `GET /api/stats` | 매니저 — **오분류율은 여기서만 본다** |

### 안 만드는 것 — 두 종류다

**① 안 하기로 정한 것 (시간이 생겨도 안 한다)**

| 내용 | 왜 |
| --- | --- |
| **같은 문제의 문의를 묶어서 한 번에 처리** | **이 도메인에서는 성립하지 않는다** (위 기준 1). 되살리려면 "묶어도 개별 건이 안 사라진다"를 먼저 보여야 한다 |
| 기준값을 실행 중에 바꾸기 | 바꾸면 **"이 결과가 어느 기준으로 나온 건지" 사후에 알 수 없다** |
| 감사 비율을 실행 중에 바꾸기 | 감사 표본의 모집단이 측정 도중 바뀐다 |

> 뒤 두 개는 **일부러 안 만든 기능**이다. 만들 줄 몰라서가 아니라, 만들면 이 프로젝트의 결론을 못 믿게 되기 때문이다.

**② 시간이 없어 미룬 것 (여유가 생기면)**

검토 항목 선점(가장 먼저) · 멈춘 문의 자동 재분류 · 종류별 기준값 · 진짜 로그인(JWT) · AI 장애 시 대기 후 재시도

---

## 3. 10일 실행안

> ⚠️ **원 템플릿은 10일을 전부 구현에 쓰지만, 우리 팀은 다르다.** Week 9(Day 1~5)가 구현·측정이고 Week 10(Day 6~10)은 통합·발표·이력서다. 그래서 Day 6~10을 구현으로 채우지 않았다.
> **Day 0은 지금이다** — 코드 없이 시작할 수 있는 유일한 작업이라 앞으로 당겼다.

### Week 9 — 만들고 잰다

| | 하는 일 | 산출물 |
| --- | --- | --- |
| **Day 0** | **정답 문의 50건 초안** (경계 9종 포함) + `build.gradle`에 Security·AI 라이브러리 추가 | `seed/inquiries-50.csv` |
| **Day 1** | 두 번째 사람이 정답 독립 부여 → 갈린 건 §7로 조정 → **2인 일치율 기록**<br>정규화 키 + 개인정보 가리기 (단위 테스트) · 역할 3개 차단 + 공용 오류 처리 | 정답 50건 확정 · **측정 12 예비값** |
| **Day 2** | 접수 API + 트랜잭션 ① + 이벤트 발행 · 큐 조회/확정 API + 동시 확정 차단<br>1000건 파생(측정 6용) | **측정 7ⓑ** |
| **Day 3** | AI 호출 + 값 검증 4가지 + 재시도/복구 · 트랜잭션 ② + 감사 표본<br>**필수 체크포인트: 롤백 경계** | **측정 3 · 4** |
| **Day 4** | 통계 API · 지표 노출 · 인덱스 `EXPLAIN` · 2단 절감 경로 | **측정 1 · 2 · 5 · 6 · 8ⓐ-1 · 8ⓑ · 11 · 속도 a·b** |
| **Day 5** | 감사 표본을 상담원이 실제 확정 · blind 재점검 · 검토자 일치도 | **측정 8ⓐ-2 · 10 · 12** |

### Week 10 — 통합하고 말한다

| | 하는 일 |
| --- | --- |
| Day 6 | 못 낸 측정 보충, 값이 이상한 것 원인 규명 (**숫자를 고치지 않고 원인을 적는다**) |
| Day 7 | E2E 시나리오 확장 (지금은 health 1건만 동작), AI 리뷰 지적 반영 |
| Day 8 | 측정 결과를 `evidence/`에 정리 — **조건까지 같이 적어 재현 가능하게** |
| Day 9 | 발표 자료 + 데모 시나리오. 아키텍처 그림 |
| Day 10 | 이력서 문장화, 면접 답변 5장 흐름 |

> **Day 3의 롤백 검증을 필수 체크포인트로 못박은 이유**: 트랜잭션 ①과 ②의 경계가 잘못 잡히면 **고객이 이미 받은 접수 확인이 거짓말이 된다.** 늦게 발견하면 구조를 다시 짜야 한다.

---

## 4. 필수 역량 매핑

| 태그 | 이 프로젝트에서 |
| --- | --- |
| **spring-api** | 문의 접수 / 목록·상세 / 검토 큐 조회·확정 / 통계 — 총 7개 endpoint. **응답 필드를 추가할 때마다 "이 값만 보고 감사 표본을 알아낼 수 있나"를 먼저 확인**한다 |
| **jpa-query** | 표 3개 연관 매핑 + 상태 기반 조회. **목록에서 항목마다 쿼리가 더 나가는 문제(N+1)** 를 없애고, 그것과 **필요한 칸만 뽑는 방식**을 나란히 재본다 |
| **database-index** | `(status, created_at)` 등 5개 인덱스. `EXPLAIN` **5 케이스** — 큐 / 문의 목록 / 재사용 조회 2개 / **깊은 페이지**. `final_category IS NOT NULL`은 인덱스로 안 걸린다는 것까지 확인한다 |
| **transaction-concurrency** | 트랜잭션은 **딱 세 곳**. ①과 ②는 반드시 분리(②가 실패해도 ①은 살아남아야 한다). 동시 확정은 **상태 검사 + 낙관적 락 둘 다** — 하나만으로는 서로 다른 창을 못 막는다. 중복 실행은 **조건부 UPDATE** |
| **async-retry** | 접수 커밋 후 이벤트 → 다른 스레드에서 AI 호출 → 3회 재시도 → 소진 시 큐. **스레드 수·DB 연결 수·종료 대기·예외 전달을 한 묶음으로 정한다** — 정하지 않으면 배포할 때마다 처리 중이던 문의가 버려진다 |
| **ai-verification** | **이 프로젝트의 중심.** 확신도 임계값 비교 + **응답 값 검증 4가지** + **감사 5% 샘플링** + blind. 이상한 값을 잘라 넣지 않고 비워두는 것까지 |
| **delivery-observability** | Actuator 지표(분류 성공률 / 밀린 건수 / **멈춘 문의 수**), 구조화 로그(실패 사유 5종), Sentry, Docker Compose |

> 6 공통 필수 기능(권한·트랜잭션·검색·캐시·비동기·AI)이 코드 어디에 붙는지는 [`PRD.md` §5-0](./PRD.md) 표에 파일 단위로 있다.

---

## 5. PAAR 시작안

### Problem

AI 분류 결과를 무조건 신뢰하면 잘못된 분류가 그대로 운영 데이터가 된다. 그런데 **"확신도가 낮으면 사람에게"** 라는 규칙으로는 **AI가 확신하면서 틀린 건을 절대 못 잡는다.**

그 비율을 어떻게 숫자로 낼 것인가?

### Analyze

**대안**

| | 방식 | 확신 없이 틀림 | **확신하며 틀림** | 대가 |
| --- | --- | --- | --- | --- |
| A | AI 답을 무조건 저장 | ❌ | ❌ | 없음 (가장 단순) |
| B | 확신도 낮으면 그냥 버림 | △ | ❌ | 버린 문의가 사라짐 |
| C | 확신도 낮으면 사람 검토 큐로 | ✅ | ❌ | 큐 + 확정 API + 트랜잭션 설계 |
| **D** | **C + 자동 확정 건 5%를 몰래 뽑아 재검토** | ✅ | **✅** | C 전부 + 감사 샘플링 + 가리기 설계 |

**판단 기준**

1. 틀린 분류가 운영 데이터에 그대로 남지 않는가
2. **AI가 확신하면서 틀린 비율을 숫자로 낼 수 있는가** ← A·B·C가 전부 여기서 탈락
3. 밀린 검토 건수를 눈으로 볼 수 있는가
4. AI 호출이 실패해도 문의가 조용히 사라지지 않는가

**옵션 C가 아니라 D여야 하는 이유를 한 문장으로**: C까지만 만들면 *"AI가 확신하면서 틀린 비율이 얼마인가"* 를 물었을 때 **모른다고 답해야 한다.** 그 숫자가 이 프로젝트의 결과물이다.

### Action

**옵션 D를 채택.** 구체적으로 정한 것은 여덟 가지다.

| | 정한 것 | 왜 |
| --- | --- | --- |
| 1 | 확신도 기준값 **하나**(0.8)로 자동 확정 / 검토 큐를 가른다 | 종류마다 다르게 하면 측정 결과를 해석할 수 없다 |
| 2 | **자동 확정 5% + 재사용 확정 5%** 를 무작위로 뽑아 큐에 넣는다 | 문제 2번을 잡는 유일한 장치 |
| 3 | 큐 응답에서 **사유·확신도·기준값을 모두 가린다** | 사유만 가리면 뺄셈 한 번으로 감사 표본이 전부 드러난다 |
| 4 | 접수(①)와 분류 결과 저장(②)을 **다른 트랜잭션**으로 | ②가 실패해도 고객이 받은 접수 확인이 거짓말이 되면 안 된다 |
| 5 | AI 응답이 깨지거나 값이 이상하면 **확신도를 비워둔다** | 0이나 1.0을 채우면 구간별 집계가 오염된다 |
| 6 | AI 호출은 **다른 스레드**에서, 3회 재시도, 소진 시 큐 | 고객을 기다리게 하지 않으면서 문의를 잃지도 않는다 |
| 7 | 같은 내용은 AI를 다시 부르지 않되 **판정은 문의마다 따로** | 재사용하는 것은 **AI 호출뿐**이다. 판정 단위를 묶으면 개별 건이 사라진다 |
| 8 | 감사 표본 큐 삽입을 **② 안에** 넣는다 | 밖에 두면 조용히 빠지고, 그러면 결론의 분모가 줄어든다 |

### Result 측정 계획

> 실제 결과는 구현 후 같은 조건으로 측정해 기록한다. **아직 하나도 재지 않았다.**

**결론이 되는 세 숫자**

| | 무엇 | 어떻게 읽나 |
| --- | --- | --- |
| **8ⓐ-1** | 자동 확정 건의 **실제** 오분류율 (확신도 구간별). 자동 확정 **전수**를 정답 레이블과 대조 | 0이 아니면 **옵션 C로는 못 잡는 실패가 실재한다**는 증거 |
| **8ⓐ-2** | 5% 감사가 그 오분류를 **몇 건 집어냈나** | 8ⓐ-1과의 차이가 곧 **샘플링의 검출 한계** |
| **12** | 상담원 2명이 같은 20건을 따로 분류했을 때 일치율 | 오분류율 중 **"사람끼리도 갈리는 몫"** 을 떼어낸다 |

> **8ⓐ를 둘로 쪼갠 이유**: 50건을 넣으면 자동 확정이 40건쯤이고 그 5%는 **2건**이다. 2건으로는 비율을 말할 수 없다. seed에 정답이 이미 붙어 있으니 전수 대조로 진짜 값을 내고, 감사는 **"그중 몇 건을 잡았나"** 로 따로 평가한다. 쪼개는 쪽이 결론이 더 강해진다 (D-043).

**나머지 측정**

| | 무엇 | 조건 |
| --- | --- | --- |
| 1 | AI 분류 일치율 (확신도 구간별) | 10종 × 5건 = 50건. **재사용 끈 상태** |
| 2 | 검토 큐 적체율 (사유별) + **신호 두 번 와도 큐 1건인지** | 위 50건 |
| 3 | **롤백 경계** — ②는 되돌아가고 ①은 살아남는지 + `stuckReceived` 증가 | 큐 삽입 강제 실패 |
| 4 | 재시도 횟수·간격 + 최종 큐 삽입 | AI 호출에 오류 주입 |
| 5 | `EXPLAIN` 5 케이스 (큐 / 문의 목록 / 재사용 조회 2개 / **깊은 페이지**) | 인덱스 전후 |
| 6 | **정규화 키의 중복 포착률 · 과도 병합률** | 1000건. **입력의 정답 중복 비율을 먼저 세어 적는다** |
| 7ⓑ | 동시 확정 시 **409가 두 종류로 갈리는지** | 동시 요청 / 시간차 요청 |
| 8ⓑ | 재사용해서 확정된 건의 오분류율 | ⓐ와 **섞지 않는다** (비교할 AI 답이 없다) |
| 10 | 큐 응답만 보고 감사 표본을 알아낼 수 있는지 | 응답 필드 하나씩 점검 |
| 11 | 캐시 적중률 + 정정 직후 반영 + **밀려난 키 개수·메모리** | 재사용 켠 상태 |
| a·b | 접수 p95 0.1초 / 큐 조회 p95 0.2초(10만 건) | 본인 `hey` 실측만 |

> **측정 6을 절감률 하나로 적지 않는 이유**: 1000건의 중복 비율을 우리가 정하면 절감률도 우리가 정하는 것이 된다. **정규화가 진짜 중복을 얼마나 잡았나(포착률)** 와 **다른 문의를 같은 키로 묶은 비율(과도 병합)** 두 값으로 나눠 적는다.

---

## 6. 포트폴리오 증거

### 코드로 남기는 것

- 확신도 임계값 검증 + **응답 값 검증 4가지**(필드 없음 / 범위 벗어남 / 종류 없음 / 10종 밖) 단위 테스트
- **정규화 키의 과도 병합·과소 병합 양방향 테스트** — 과도 병합을 더 위험한 것으로 다룬 흔적
- 롤백 경계 통합 테스트 — **②는 되돌아가고 ①은 살아남는** 것을 한 테스트에서 확인
- 동시 확정 테스트 — 409 **두 종류가 각각 나오는지** (한 종류만 나오면 무효)
- 개인정보 가리는 함수 단위 테스트 (**AI 전송 전과 화면 표시가 같은 구현을 쓰는지**까지)

### 숫자로 남기는 것

- **정답 50건 비교 표** — 확신도 구간별 일치율 + **2인 독립 레이블 일치율**
- **오분류율 표** — 8ⓐ-1(실제) / 8ⓐ-2(감사가 잡은 몫) / 8ⓑ(재사용 건). **셋을 섞지 않는다**
- `EXPLAIN` 5 케이스 전후 비교 — `type` · `rows` · 따로 정렬하는지
- 재시도 로그(횟수·간격) + 소진 후 큐 삽입 기록
- AI 호출 절감 — **포착률과 과도 병합률 두 값** + 입력의 정답 중복 비율
- Actuator 지표 화면 (분류 성공률 / 밀린 건수 / 멈춘 문의 수)

### 판단으로 남기는 것 — **여기가 이 프로젝트의 진짜 차별점이다**

| | 무엇 | 어디에 |
| --- | --- | --- |
| **결정 로그 49건** | 기존 항목을 **고치지 않고** 후속 항목으로 덮는다. **왜 틀렸었는지가 그대로 남는다** | `DECISIONS.md` |
| **틀린 것을 스스로 찾아 정정한 이력** | D-004→D-014(캐시=절감률 등식이 틀렸음) · D-002→D-020 · D-027→D-031(자기가 만든 기준에 자기 도메인이 탈락) | 같은 곳 |
| **도메인 전환의 대가 계산** | 무엇을 포기했고 무엇을 잃었는지. **폐기된 항목도 지우지 않았다** | D-031 |
| **AI가 틀린 사례 16건** | AI 리뷰가 틀린 경우 1건 포함 | `evidence/failure-cases.md` |
| **측정값의 한계 진술** | 앵커링 · 샘플링 검출 한계 · 검토자 불일치 — **세 편향원을 분리해 보고** | `PRD.md` §11 |

> **성공 기준 3번이 이 프로젝트에서 가장 어렵다** — *"그 숫자가 실제보다 낮게 나온다는 것까지 설명할 수 있는가."* **"측정했다"가 아니라 "측정값의 한계까지 안다"** 를 요구하기 때문이다.

### 라이프사이클 5단계 흔적

| 단계 | 책임자 | 산출물 |
| --- | --- | --- |
| 기획 | 김준현 | `PRD.md`, `DECISIONS.md` |
| 코딩 | 팀 전원 | `CLAUDE.md`(헌법), `API-CONTRACT.md`, Hooks, 서브에이전트 5종 |
| 테스트 | 이용택 | `tests/e2e/`, Testcontainers |
| 리뷰 | 김은빈 | `.github/workflows/ai-review.yml`, `evidence/failure-cases.md` |
| 배포·운영 | 이용택 | `docker-compose.yml`, `MONITORING.md`, `SENTRY-GUIDE.md` |

---

## 7. 위험과 범위 조절

### 이 설계의 급소

| 위험 | 어떻게 대응하나 |
| --- | --- |
| **정규화 키가 거의 안 겹칠 수 있다** — 고객이 쓴 문장은 제각각이라 똑같기 어렵다 | **가장 큰 위험.** 측정 6에서 절약이 안 나오면 가리는 범위를 넓힌다. 그래도 안 되면 **캐시 지점을 다른 곳으로 옮기는 것**을 재검토 (D-031 재평가) |
| 반대로 너무 많이 가리면 다른 문의가 같은 키가 된다 | 양방향을 다 테스트한다. **이쪽이 더 위험하다** — 틀린 분류가 조용히 퍼진다 |
| **8ⓐ-1이 0이 나오면 감사 경로를 평가할 수 없다** | 자동 확정 구간에서 AI가 하나도 안 틀리면 감사가 잡을 것이 없다. **기준값을 0.8→0.6으로 낮춰 다시 잰다** — 오분류를 만들려는 게 아니라 *"기준값이 낮으면 자동 확정이 위험해진다"* 를 숫자로 보이는 것 |
| **사람이 틀리게 확정한 답이 같은 내용 전부로 퍼진다** | 재사용 확정 건도 감사 5%에 넣는다. **8ⓑ가 8ⓐ보다 높으면 재사용이 오류를 키우는 것**이므로 사람 확정 재사용을 끈다 |
| 문의 종류 경계가 애매해 사람마다 다르게 분류 | §7 경계표 + **"원인이 아니라 조치"** 규칙. 측정 12로 확인하고 안 맞으면 표를 고친다. **90% 미만이면 카테고리 10종 → 5종 축소를 앞당긴다** |

### 만들면서 생기는 위험

| 위험 | 어떻게 대응하나 |
| --- | --- |
| **서버를 껐다 켤 때 분류 중이던 문의가 사라짐** | **가장 흔한 유실 경로가 배포다.** 정상 종료는 하던 일을 기다리게 한다. **갑자기 죽는 경우는 못 막는다** — `stuckReceived`로만 보이고, 없애려면 「멈춘 문의 재분류」가 필요하다 |
| 같은 문의에 분류 신호가 두 번 와서 큐에 2건 | 상담원이 같은 걸 두 번 보고 **감사 건수가 부풀어 오분류율이 낮게 나온다.** 조건부 UPDATE로 막는다 |
| 저장해둔 답이 쌓여 메모리가 참 | 상한을 두고 넘칠 때만 오래된 것부터 밀어낸다. **밀려난 개수를 측정에 함께 적는다** — 절약이 안 나왔을 때 원인을 갈라야 하니까 |
| AI가 오래 죽어 있으면 전부 사람이 처리 | 한계로 적어둔다. **재시도 대기가 분류 담당의 스레드를 묶어 멀쩡한 문의까지 밀리므로** 대기 시간과 스레드 수를 같이 정한다 |
| 문의 본문에 개인정보가 들어온다 | AI로 보내기 전에 가린다. **가린 본문을 저장하지 않고 보여줄 때마다 만든다** — 저장하면 규칙을 조여도 과거 데이터가 옛 기준으로 남는다 |
| 통합 테스트가 Docker 버전을 탄다 | 팀 전원 Docker Desktop 4.44.2 이하 고정 |

### 범위를 줄여야 할 때 어디부터 자르나

| 순서 | 자르는 것 | 잃는 것 |
| --- | --- | --- |
| 1 | **2단계(AI 호출 아끼기)를 5일 범위 밖으로** | 측정 6·8ⓑ·11만 못 낸다. **성공 기준 셋은 전부 답할 수 있다** |
| 2 | 3단계의 속도 측정(10만 건) | 측정 b. 인덱스 `EXPLAIN`은 남긴다 |
| 3 | 필수 기능 보강 5개 중 실험 성격 2개 | 트랜잭션 대조 실험, 깊은 페이지 |
| — | **1단계는 자르지 않는다** | 자르면 결론 숫자가 하나도 안 나온다 |

**1단계가 Day 3까지 관통되지 않으면 1번을 발동한다.** 못 낸 이유는 그대로 적는다 — **못 낸 것을 안 적으면 그게 이 프로젝트가 막으려는 실패다.**

---

## 지금 상태

**아직 기능 코드가 없다.** 표 3개와 그에 맞는 객체(엔티티), 잘못된 값으로는 객체를 만들 수 없게 막는 코드까지 되어 있다.

| | 상태 |
| --- | --- |
| DB 표 3개 + 마이그레이션 · 엔티티 · enum · 저장소 인터페이스 | 있음 |
| 접수 / 분류 / 검토 기능 코드 (`service/`, `api/`) | **없음** |
| 측정 결과 | **없음** |
| 정답 문의 50건 | **뼈대만** (`src/test/resources/seed/`) |

PAAR로 보면 **P와 A는 충분하고, Action이 거의 없고, Result가 0이다.** 그래서 지금부터 설계 논쟁에 쓰는 시간을 줄이고 1단계 관통에 투입한다.

---

## 출처

- 원안: [NAVER D2 · SaaS 대체하기: AI와 함께한 광고SDK 에러 모니터링 시스템 구축기](https://d2.naver.com/helloworld/8319114)
- **주제는 에러 분류 → CS 문의 분류로 바꿨지만 검증 구조는 그대로 가져왔다** (D-031)
